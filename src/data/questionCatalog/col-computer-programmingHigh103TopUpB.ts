import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe col-computer-programming top-up'

export const colComputerProgrammingHigh103TopUpBQuestions: Question[] = makeQuestionBank('High', [
  // ---- Making webpages interactive (HTML/JS) ----
  {
    id: 7915500,
    chapter: 'Making webpages interactive (HTML/JS)',
    title: 'Selecting an element by id',
    prompt: 'In the browser, which JavaScript call returns the single page element whose id attribute equals "score"?',
    correct: 'document.getElementById("score")',
    wrong: [
      miss('document.getElementsByTagName("score")', 'getElementsByTagName matches tag names like "p" or "div" and returns a collection, not a single element by id.', 'You want the one element with a specific id, not every element of a tag type.'),
      miss('document.getClass("score")', 'There is no getClass method; class-based selection uses getElementsByClassName.', 'Recall the exact DOM method names for id versus class.'),
      miss('document.querySelector("score")', 'querySelector("score") would look for a <score> tag; to match an id it needs the "#score" selector.', 'CSS selectors mark ids with a leading hash.'),
    ],
    lesson: 'The Document Object Model (DOM) lets JavaScript find and change page elements. getElementById takes an id string and returns the one matching element (or null). Ids are meant to be unique on a page, which is why this method returns a single element rather than a list.',
    source,
    generated: true,
  },
  {
    id: 7915501,
    chapter: 'Making webpages interactive (HTML/JS)',
    title: 'Changing visible text',
    prompt: 'You have an element stored in the variable box. Which assignment changes the text the user actually sees inside it to the word "Done"?',
    correct: 'box.textContent = "Done"',
    wrong: [
      miss('box.value = "Done"', 'The value property applies to form controls like input fields, not to the visible text of a general element such as a div or paragraph.', 'Think about whether the element is a form input or ordinary content.'),
      miss('box.text("Done")', 'text is not a standard DOM property or method on a plain element; that style resembles jQuery, not the native DOM.', 'Recall the native property that holds an element\'s text.'),
      miss('box.id = "Done"', 'Setting id renames the element for selection purposes; it does not change what the user reads on screen.', 'Distinguish an element\'s identifier from its content.'),
    ],
    lesson: 'textContent reads or writes the plain text inside an element. Assigning to it replaces the element\'s text content immediately in the page. The value property is for form fields like <input>, while innerHTML is used when you intend to insert HTML markup rather than plain text.',
    source,
    generated: true,
  },
  {
    id: 7915502,
    chapter: 'Making webpages interactive (HTML/JS)',
    title: 'Responding to a click',
    prompt: 'Which line makes the function start() run every time the user clicks the element stored in btn?',
    correct: 'btn.addEventListener("click", start)',
    wrong: [
      miss('btn.addEventListener("click", start())', 'Writing start() calls the function immediately and passes its return value, so it runs once now instead of on each click.', 'Pass the function itself, not the result of calling it.'),
      miss('btn.click = start', 'Assigning to a property named click does not register a click handler; the correct property would be onclick.', 'Recall the exact event-handler property name or use addEventListener.'),
      miss('btn.addEvent("click", start)', 'addEvent is not a standard DOM method; the standard method is addEventListener.', 'Use the standard method name for attaching event handlers.'),
    ],
    lesson: 'addEventListener(eventType, handler) registers a callback that the browser runs when the event happens. You pass the function by name (start) without parentheses so the browser can call it later; adding parentheses would invoke it right away. This lets a page respond to user actions like clicks, key presses, and mouse movement.',
    source,
    generated: true,
  },
  {
    id: 7915503,
    chapter: 'Making webpages interactive (HTML/JS)',
    title: 'innerHTML versus textContent',
    prompt: 'If you set an element\'s innerHTML to the string "<b>Hi</b>", what does the user see?',
    correct: 'The word Hi in bold, because the markup is interpreted as HTML',
    wrong: [
      miss('The literal text <b>Hi</b> shown exactly as typed', 'That is what textContent would display; innerHTML parses the string as HTML rather than showing the tags literally.', 'Recall which property treats the string as markup.'),
      miss('Nothing, because innerHTML only reads content and cannot be assigned', 'innerHTML is read/write; assigning to it replaces the element\'s HTML content.', 'innerHTML can both get and set markup.'),
      miss('An error, because tags are not allowed inside strings', 'Strings can contain any characters, including angle brackets; the browser simply parses them as HTML when set via innerHTML.', 'A string holding tags is perfectly valid text.'),
    ],
    lesson: 'innerHTML treats its string as HTML markup, so tags inside it become real elements. textContent treats the same string as plain text and shows the tags literally. Because innerHTML interprets markup, inserting untrusted user input through it can create security risks, so textContent is safer for plain text.',
    source,
    generated: true,
  },
  {
    id: 7915504,
    chapter: 'Making webpages interactive (HTML/JS)',
    title: 'Reading input from a form field',
    prompt: 'A user typed into an <input> element stored in nameField. Which expression gives you the text they entered?',
    correct: 'nameField.value',
    wrong: [
      miss('nameField.textContent', 'For form controls the typed text lives in the value property, not textContent, which holds an element\'s child text nodes.', 'Form inputs store user-entered data in a dedicated property.'),
      miss('nameField.innerHTML', 'innerHTML returns the markup between an element\'s tags; an <input> is empty between its tags, so this does not hold the typed text.', 'An input has no content between tags to read.'),
      miss('nameField.input', 'There is no input property that returns the typed string; input is the name of an event, not a value holder.', 'Recall the property name that holds a control\'s current data.'),
    ],
    lesson: 'Form controls such as text inputs, checkboxes, and select menus expose their current data through the value property. Reading nameField.value gives the string currently in the box, and assigning to it changes what the box shows. This is how interactive pages collect data from users.',
    source,
    generated: true,
  },

  // ---- The DOM and jQuery ----
  {
    id: 7915505,
    chapter: 'The DOM and jQuery',
    title: 'What jQuery is',
    prompt: 'In the Khan Academy course, what is jQuery best described as?',
    correct: 'A JavaScript library that simplifies selecting and manipulating DOM elements',
    wrong: [
      miss('A separate programming language that replaces JavaScript', 'jQuery is written in JavaScript and runs as JavaScript; it does not replace the language.', 'A library is code you load, not a new language.'),
      miss('A markup language used instead of HTML', 'jQuery does not define page structure; HTML still does that. jQuery manipulates the structure with code.', 'Markup languages and code libraries do different jobs.'),
      miss('A database system for storing webpage data', 'jQuery does not store data; database querying in this course is done with SQL.', 'Think about which tool selects and changes page elements.'),
    ],
    lesson: 'jQuery is a JavaScript library: reusable code you include so common DOM tasks take less typing. With it, selecting elements, handling events, and animating become shorter and work consistently across browsers. Because it is just JavaScript, anything jQuery does can also be written in plain JavaScript.',
    source,
    generated: true,
  },
  {
    id: 7915506,
    chapter: 'The DOM and jQuery',
    title: 'jQuery selector syntax',
    prompt: 'Which jQuery expression selects every element that belongs to the CSS class "card"?',
    correct: '$(".card")',
    wrong: [
      miss('$("#card")', 'A leading hash selects an element by id, not by class; ids are unique while classes can be shared.', 'Recall which symbol marks a class in CSS selectors.'),
      miss('$("card")', 'Without a symbol, "card" matches the tag name <card>, which is not a class match.', 'Plain text in a selector targets a tag, not a class.'),
      miss('$(class="card")', 'That is not valid selector syntax; jQuery takes a CSS-selector string in quotes, using a dot for classes.', 'jQuery uses the same selector notation as CSS.'),
    ],
    lesson: 'jQuery uses CSS-selector syntax inside $(). A dot marks a class (".card"), a hash marks an id ("#card"), and a bare word marks a tag name ("p"). Because classes can be applied to many elements, a class selector typically returns a set of elements that you can then style or modify together.',
    source,
    generated: true,
  },
  {
    id: 7915507,
    chapter: 'The DOM and jQuery',
    title: 'Why libraries are useful',
    prompt: 'What is the main practical benefit of using a library like jQuery instead of writing every DOM operation from scratch?',
    correct: 'It packages tested, reusable code so you write less and avoid reinventing common solutions',
    wrong: [
      miss('It makes the webpage load instantly with no download cost', 'Including a library is itself an extra file the browser must load; libraries do not eliminate download cost.', 'Adding code does not make pages weigh nothing.'),
      miss('It lets the page run without any JavaScript engine', 'jQuery is JavaScript, so it still needs the browser\'s JavaScript engine to run.', 'A JS library cannot remove the need for JS.'),
      miss('It guarantees the program will have no bugs', 'Libraries reduce some boilerplate errors but cannot guarantee your own logic is bug-free.', 'Reusing code helps, but you still write the logic.'),
    ],
    lesson: 'A library is a collection of pre-written, tested functions you can reuse. The benefit is leverage: instead of solving common problems again, you call code others have already debugged, so you write less and focus on your app\'s unique logic. The trade-off is the extra code your page must download and learn to use.',
    source,
    generated: true,
  },

  // ---- Querying databases with SQL ----
  {
    id: 7915508,
    chapter: 'Querying databases with SQL',
    title: 'Selecting specific columns',
    prompt: 'Which SQL statement returns only the name and age columns from a table called students?',
    correct: 'SELECT name, age FROM students;',
    wrong: [
      miss('SELECT students FROM name, age;', 'The table name belongs after FROM and the column names belong after SELECT; this reverses them.', 'Remember the order: SELECT columns FROM table.'),
      miss('GET name, age FROM students;', 'SQL uses the keyword SELECT to retrieve data, not GET.', 'Recall the standard SQL keyword for reading rows.'),
      miss('SELECT * FROM students;', 'The asterisk returns every column, but the question asks only for name and age.', 'List the specific columns you want, not all of them.'),
    ],
    lesson: 'A SELECT statement names the columns you want, then FROM names the table. Listing specific columns (SELECT name, age) returns just those fields, while SELECT * returns all columns. Statements typically end with a semicolon. Choosing only the columns you need keeps results focused and efficient.',
    source,
    generated: true,
  },
  {
    id: 7915509,
    chapter: 'Querying databases with SQL',
    title: 'Filtering rows with WHERE',
    prompt: 'Which clause restricts a query so it returns only rows where the price column is greater than 20?',
    correct: 'WHERE price > 20',
    wrong: [
      miss('IF price > 20', 'SQL filters rows with the WHERE clause; IF is a programming-language construct, not the row filter in a SELECT.', 'Recall the SQL keyword that filters which rows are returned.'),
      miss('HAVING price > 20', 'HAVING filters groups after aggregation; for filtering individual rows before grouping you use WHERE.', 'Use WHERE for plain row filtering, HAVING for grouped results.'),
      miss('WHEN price > 20', 'WHEN appears inside CASE expressions, not as the row filter of a SELECT.', 'The clause that filters rows is a single keyword that is not WHEN.'),
    ],
    lesson: 'The WHERE clause keeps only the rows whose condition is true. Comparison operators like >, <, =, and != build the condition, and you can combine conditions with AND and OR. WHERE filters individual rows; HAVING is reserved for filtering after a GROUP BY aggregation.',
    source,
    generated: true,
  },
  {
    id: 7915510,
    chapter: 'Querying databases with SQL',
    title: 'Sorting results',
    prompt: 'Which clause sorts query results so the highest score appears first?',
    correct: 'ORDER BY score DESC',
    wrong: [
      miss('ORDER BY score ASC', 'ASC sorts ascending, putting the smallest value first; DESC is needed to put the highest first.', 'Pick the direction that places large values at the top.'),
      miss('SORT BY score DESC', 'The SQL keyword for ordering output is ORDER BY, not SORT BY.', 'Recall the exact keyword pair used to order rows.'),
      miss('GROUP BY score DESC', 'GROUP BY collapses rows into groups for aggregation; it does not simply sort the output.', 'Sorting and grouping are different operations.'),
    ],
    lesson: 'ORDER BY arranges result rows by one or more columns. DESC sorts descending (largest first) and ASC sorts ascending (smallest first); ascending is the default if you omit the keyword. ORDER BY only changes display order, unlike GROUP BY, which combines rows for aggregate calculations.',
    source,
    generated: true,
  },
  {
    id: 7915511,
    chapter: 'Querying databases with SQL',
    title: 'Aggregating with a function',
    prompt: 'Which query returns the total of all values in the amount column of a payments table?',
    correct: 'SELECT SUM(amount) FROM payments;',
    wrong: [
      miss('SELECT COUNT(amount) FROM payments;', 'COUNT returns how many rows have a value, not the total of the values; you need SUM to add them.', 'Distinguish counting rows from adding their values.'),
      miss('SELECT TOTAL amount FROM payments;', 'TOTAL is not the standard SQL aggregate keyword here; the function that adds numeric values is SUM().', 'Recall the aggregate function that adds a column.'),
      miss('SELECT AVG(amount) FROM payments;', 'AVG returns the mean, not the total; the total requires SUM.', 'Average and sum are different aggregates.'),
    ],
    lesson: 'Aggregate functions compute a single value from many rows. SUM adds a numeric column, COUNT tallies rows, AVG finds the mean, and MIN/MAX find extremes. They are often paired with GROUP BY to produce one result per group, but used alone they summarize the whole table.',
    source,
    generated: true,
  },
  {
    id: 7915512,
    chapter: 'Querying databases with SQL',
    title: 'Creating a table',
    prompt: 'Which statement defines a new table named books with an integer id and a text title?',
    correct: 'CREATE TABLE books (id INTEGER, title TEXT);',
    wrong: [
      miss('MAKE TABLE books (id INTEGER, title TEXT);', 'The SQL keyword to define a table is CREATE TABLE, not MAKE TABLE.', 'Recall the standard data-definition keyword.'),
      miss('CREATE TABLE books (INTEGER id, TEXT title);', 'Each column is written as a name followed by its type, so it should be "id INTEGER", not "INTEGER id".', 'Column name comes before its data type.'),
      miss('INSERT TABLE books (id INTEGER, title TEXT);', 'INSERT adds rows of data into an existing table; it does not define the table structure.', 'Defining structure and adding rows are separate commands.'),
    ],
    lesson: 'CREATE TABLE defines a table\'s structure: its name and a list of columns, each written as a column name followed by a data type such as INTEGER or TEXT. Once a table exists, INSERT INTO adds rows of data. Keeping definition (CREATE) separate from data entry (INSERT) is fundamental to relational databases.',
    source,
    generated: true,
  },
  {
    id: 7915513,
    chapter: 'Querying databases with SQL',
    title: 'Inserting a row',
    prompt: 'Which statement adds a new row to a table named pets with the values name = "Rex" and legs = 4?',
    correct: 'INSERT INTO pets (name, legs) VALUES ("Rex", 4);',
    wrong: [
      miss('UPDATE pets SET name = "Rex", legs = 4;', 'UPDATE changes existing rows; without a WHERE it would overwrite every row, and it never creates a new one.', 'Adding a brand-new row uses a different command than editing rows.'),
      miss('INSERT INTO pets VALUES (name = "Rex", legs = 4);', 'The VALUES list holds the values themselves in order, not column = value assignments.', 'Columns are listed once before VALUES, then values follow in matching order.'),
      miss('ADD ROW pets (name, legs) VALUES ("Rex", 4);', 'There is no ADD ROW statement in standard SQL; new rows are added with INSERT INTO.', 'Recall the standard keyword for inserting data.'),
    ],
    lesson: 'INSERT INTO adds rows. You list the target columns, then a VALUES clause supplying matching values in the same order. UPDATE, by contrast, modifies values in rows that already exist and is usually paired with WHERE to limit which rows change. DELETE removes rows entirely.',
    source,
    generated: true,
  },
  {
    id: 7915514,
    chapter: 'Querying databases with SQL',
    title: 'Joining two tables',
    prompt: 'You have an orders table with a customer_id column and a customers table with an id column. What does a JOIN on these tables let you do?',
    correct: 'Combine matching rows from both tables so each order can show its customer\'s details',
    wrong: [
      miss('Delete the rows that appear in both tables', 'A JOIN combines related rows into a result set; it does not delete data from the tables.', 'Joining reads and merges data, it does not remove it.'),
      miss('Merge the two tables permanently into a single stored table', 'A JOIN produces a temporary result for one query; the original tables stay separate on disk.', 'A query result is not a permanent change to storage.'),
      miss('Sort one table by the columns of another table', 'Sorting is done with ORDER BY; a JOIN is about matching related rows, not ordering.', 'Combining related data differs from ordering it.'),
    ],
    lesson: 'Relational databases split data across tables and link them with keys. A JOIN matches rows from two tables on a shared value (here orders.customer_id = customers.id), producing combined rows that draw columns from both. This lets one query report, for example, each order alongside its customer name without duplicating customer data in every table.',
    source,
    generated: true,
  },

  // ---- Advanced JS: Games and visualizations ----
  {
    id: 7915515,
    chapter: 'Advanced JS: Games and visualizations',
    title: 'The draw loop',
    prompt: 'In the ProcessingJS draw() function used for games, why is the code inside draw() placed there rather than run just once at the start?',
    correct: 'draw() runs repeatedly many times per second, which is what produces continuous motion and updates',
    wrong: [
      miss('draw() runs only once, so it is the right place for one-time setup', 'draw() is the opposite of one-time setup; it loops continuously. One-time setup typically goes outside the loop.', 'Recall which function repeats every frame.'),
      miss('Code in draw() runs faster than code elsewhere', 'Placement in draw() is about repetition each frame, not raw execution speed of individual statements.', 'The key property is how often it runs, not how fast.'),
      miss('draw() automatically saves the game state to a file each frame', 'draw() does not persist data to files; it just redraws and updates the on-screen frame.', 'The loop redraws the screen, it does not store data.'),
    ],
    lesson: 'In this animation model, draw() is a loop the environment calls roughly 30 to 60 times per second. Each call redraws the scene, so by changing positions a little each frame you create motion. Code that should happen only once, like creating starting objects, belongs outside draw() so it is not reset every frame.',
    source,
    generated: true,
  },
  {
    id: 7915516,
    chapter: 'Advanced JS: Games and visualizations',
    title: 'Moving an object each frame',
    prompt: 'To make a ball drift right across the screen, you increase its x value inside draw(). Which statement does this each frame?',
    correct: 'x = x + 2',
    wrong: [
      miss('x = 2', 'Setting x to the constant 2 every frame pins the ball at position 2; it never moves because the value never accumulates.', 'You need to add to the current position, not overwrite it.'),
      miss('x == x + 2', 'The double equals is a comparison that returns true or false; it does not change x at all.', 'Distinguish assignment from comparison.'),
      miss('2 = x + 2', 'You cannot assign to the number 2; the variable being updated must be on the left of the equals sign.', 'The variable to change goes on the left side.'),
    ],
    lesson: 'Animation comes from incremental change across frames. Because draw() repeats, x = x + 2 reads the current x, adds 2, and stores it back, nudging the object right a little each frame. Using == (comparison) or assigning a constant would not accumulate motion. The shorthand x += 2 means the same thing.',
    source,
    generated: true,
  },
  {
    id: 7915517,
    chapter: 'Advanced JS: Games and visualizations',
    title: 'Detecting a button click region',
    prompt: 'A square button spans x from 50 to 150 and y from 60 to 100. Which boolean expression is true exactly when the mouse point (mouseX, mouseY) is inside the button?',
    correct: 'mouseX >= 50 && mouseX <= 150 && mouseY >= 60 && mouseY <= 100',
    wrong: [
      miss('mouseX >= 50 || mouseX <= 150 || mouseY >= 60 || mouseY <= 100', 'Joining the checks with OR makes the expression true almost everywhere, since at least one bound is usually satisfied; all four must hold together.', 'Being inside requires every condition at once, not any one.'),
      miss('mouseX >= 50 && mouseX <= 150', 'Checking only x ignores y, so the test would be true for points far above or below the button.', 'A 2D region needs both x and y bounds.'),
      miss('mouseX > 50 && mouseY > 60', 'Using only lower bounds with > leaves the right and bottom edges unchecked, so points far to the right would wrongly count as inside.', 'You need upper bounds too, and the edges matter.'),
    ],
    lesson: 'A point is inside an axis-aligned rectangle only when it lies within both the x-range and the y-range. You must combine all four bounds with && (logical AND) so every condition is satisfied simultaneously. Using || would make the test true far too often, since most points satisfy at least one bound.',
    source,
    generated: true,
  },
  {
    id: 7915518,
    chapter: 'Advanced JS: Games and visualizations',
    title: 'Why use mouseClicked',
    prompt: 'In ProcessingJS, what is the purpose of defining a mouseClicked() function?',
    correct: 'The environment automatically calls it once each time the user clicks the mouse',
    wrong: [
      miss('It runs continuously every frame like draw()', 'mouseClicked runs only in response to a click event, not every frame; draw() is the per-frame loop.', 'Event functions react to events, not to every frame.'),
      miss('It returns the current mouse coordinates', 'Mouse coordinates are available through mouseX and mouseY; mouseClicked is an event handler, not a coordinate getter.', 'Coordinates and click events are accessed differently.'),
      miss('It must be called manually with mouseClicked() in your code', 'You define it, but the environment calls it for you when a click occurs; calling it yourself is not how event handlers work.', 'Event handlers are invoked by the system, not by you.'),
    ],
    lesson: 'Event functions like mouseClicked() are handlers the environment invokes automatically when the matching event happens, in this case a mouse click. You write what should happen, and the system calls it once per click. This is the event-driven model: code reacts to user actions rather than running on a fixed schedule.',
    source,
    generated: true,
  },

  // ---- Objects and arrays ----
  {
    id: 7915519,
    chapter: 'Objects and arrays',
    title: 'Accessing an object property',
    prompt: 'Given var dog = { name: "Rex", age: 3 }; which expression evaluates to the string "Rex"?',
    correct: 'dog.name',
    wrong: [
      miss('dog("name")', 'An object is not a function, so you cannot call it with parentheses; properties are read with dot or bracket notation.', 'Use dot or bracket access for object properties.'),
      miss('dog[name]', 'Without quotes, name is treated as a variable, not the literal key; the correct bracket form is dog["name"].', 'Bracket keys must be quoted strings unless you mean a variable.'),
      miss('name.dog', 'This reverses the order; you access a property of dog, so the object comes first, then the property.', 'The object name comes before the property name.'),
    ],
    lesson: 'Objects store data as key/value pairs. You read a property with dot notation (dog.name) or bracket notation with a quoted key (dog["name"]). Bracket notation also lets you use a variable as the key. The object always comes first, followed by the property you want.',
    source,
    generated: true,
  },
  {
    id: 7915520,
    chapter: 'Objects and arrays',
    title: 'Array length',
    prompt: 'After var scores = [90, 80, 70, 60]; what is the value of scores.length?',
    correct: '4',
    wrong: [
      miss('3', 'length counts the number of elements, which is 4; the value 3 is the index of the last element, not the count.', 'Distinguish the count of items from the highest index.'),
      miss('300', 'length is the number of elements, not the sum of their values.', 'length counts items, it does not add them.'),
      miss('60', 'length is not the last element; it is how many elements the array holds.', 'length is a count, not a value from the array.'),
    ],
    lesson: 'An array\'s length property is the number of elements it holds. For four elements the length is 4. Because indexing starts at 0, the valid indices run from 0 to length minus 1, so the last element here is scores[3]. Confusing the count with the final index is a common off-by-one mistake.',
    source,
    generated: true,
  },
  {
    id: 7915521,
    chapter: 'Objects and arrays',
    title: 'Indexing from zero',
    prompt: 'For var colors = ["red", "green", "blue"]; which expression returns "green"?',
    correct: 'colors[1]',
    wrong: [
      miss('colors[2]', 'Index 2 is the third element, "blue"; "green" is the second element, which sits at index 1 because indexing starts at 0.', 'Remember the first element is at index 0.'),
      miss('colors[0]', 'Index 0 is the first element, "red", not "green".', 'Count positions starting from 0.'),
      miss('colors["green"]', 'Arrays are indexed by numeric position, not by their string contents.', 'Use a numeric index, not the value itself.'),
    ],
    lesson: 'Array elements are accessed by a numeric index that starts at 0. So colors[0] is "red", colors[1] is "green", and colors[2] is "blue". This zero-based convention is shared by most programming languages and is a frequent source of off-by-one errors for beginners.',
    source,
    generated: true,
  },
  {
    id: 7915522,
    chapter: 'Objects and arrays',
    title: 'Adding to an array',
    prompt: 'Which call appends the value 99 to the end of an existing array named list?',
    correct: 'list.push(99)',
    wrong: [
      miss('list.add(99)', 'JavaScript arrays do not have an add method; the method that appends to the end is push.', 'Recall the array method name for appending.'),
      miss('list[length] = 99', 'length is a property of the array, not a standalone variable; the index would need to be list.length, and even then this overwrites rather than uses the method asked for.', 'A bare length is not the array\'s length here.'),
      miss('list.push = 99', 'Assigning to push replaces the method with the number 99; to append you must call push(99).', 'push is a method you call, not a property you set.'),
    ],
    lesson: 'Arrays grow with push, which adds one or more elements to the end and increases length. Related methods include pop (remove the last element), unshift (add to the front), and shift (remove the front). Knowing these lets you treat arrays as flexible, resizable lists.',
    source,
    generated: true,
  },
  {
    id: 7915523,
    chapter: 'Objects and arrays',
    title: 'Looping over an array',
    prompt: 'Which loop header correctly visits every index of an array named items exactly once, from first to last?',
    correct: 'for (var i = 0; i < items.length; i++)',
    wrong: [
      miss('for (var i = 1; i <= items.length; i++)', 'Starting at 1 skips index 0 and using <= items.length reads one past the end, where the value is undefined.', 'Check the very first index and the very last valid index.'),
      miss('for (var i = 0; i <= items.length; i++)', 'Using <= lets i reach items.length, which is one past the last valid index, so it accesses an undefined slot.', 'The last valid index is length minus 1.'),
      miss('for (var i = items.length; i > 0; i--)', 'This starts at items.length, an invalid index, and stops at i = 1, so it misses index 0 while touching one past the end.', 'Watch the starting index and where the loop stops.'),
    ],
    lesson: 'A standard array loop starts i at 0 and continues while i is strictly less than items.length, because valid indices run from 0 to length minus 1. Using <= length or starting at 1 are classic off-by-one bugs that either skip the first element or read past the end where the value is undefined.',
    source,
    generated: true,
  },

  // ---- Natural simulations ----
  {
    id: 7915524,
    chapter: 'Natural simulations',
    title: 'Representing a vector',
    prompt: 'In the natural-simulations unit, why is an object\'s velocity stored as a vector with separate x and y components rather than a single number?',
    correct: 'Motion in 2D has both direction and magnitude, which require independent horizontal and vertical components',
    wrong: [
      miss('Because a single number cannot store decimals', 'A single number can store decimals; the issue is that one number cannot capture two independent directions of motion.', 'Think about why two components are needed, not about decimals.'),
      miss('Because vectors run faster than plain numbers', 'Using a vector is about representing 2D motion correctly, not about execution speed.', 'The reason is conceptual, about direction, not performance.'),
      miss('Because the draw loop only accepts vector inputs', 'The draw loop does not require vectors; you can move objects with plain x and y numbers too.', 'Vectors are a modeling choice, not a loop requirement.'),
    ],
    lesson: 'Velocity describes how fast and in which direction something moves, so in two dimensions it needs both an x component and a y component. A vector bundles these together, letting you add velocity to position component-by-component. This makes simulating gravity, wind, and other forces cleaner because forces are also vectors that combine naturally.',
    source,
    generated: true,
  },
  {
    id: 7915525,
    chapter: 'Natural simulations',
    title: 'Applying acceleration',
    prompt: 'In a simple physics simulation, how do acceleration, velocity, and position usually update each frame?',
    correct: 'Acceleration is added to velocity, then velocity is added to position',
    wrong: [
      miss('Position is added to velocity, then velocity is added to acceleration', 'This reverses the dependency; acceleration changes velocity and velocity changes position, not the other way around.', 'Recall which quantity drives which.'),
      miss('Acceleration is added directly to position, skipping velocity', 'Acceleration affects position only indirectly, through velocity; adding it straight to position skips a layer and gives wrong motion.', 'Acceleration acts on velocity first.'),
      miss('Velocity is multiplied by acceleration to get position', 'Updating these quantities uses addition each frame, not multiplication of velocity by acceleration.', 'The standard update steps use addition.'),
    ],
    lesson: 'A common motion model layers three quantities: acceleration changes velocity, and velocity changes position. So each frame you add acceleration to velocity, then add the updated velocity to position. This mirrors real physics, where acceleration is the rate of change of velocity and velocity is the rate of change of position.',
    source,
    generated: true,
  },
  {
    id: 7915526,
    chapter: 'Natural simulations',
    title: 'Randomness in simulations',
    prompt: 'A simulation calls random(0, 5) inside draw(). What does this produce?',
    correct: 'A new random number between 0 and 5 on each frame',
    wrong: [
      miss('The same fixed random number chosen once for the whole program', 'Because the call sits in draw(), which repeats, it generates a fresh value every frame, not a single fixed one.', 'Consider that draw() runs the call repeatedly.'),
      miss('Exactly the integers 0, 1, 2, 3, 4, or 5 in order', 'random(0, 5) typically returns decimals across the range, not a fixed ordered sequence of integers.', 'random gives values across a range, not a counting sequence.'),
      miss('A random number that is always greater than 5', 'The arguments set the lower and upper bounds, so results fall within 0 to 5, not above 5.', 'The two arguments define the range of results.'),
    ],
    lesson: 'random(low, high) returns a value in the given range, and because draw() repeats each frame, calling it there yields a new value continuously. Randomness adds natural-looking variation to simulations, such as scattering particles or jittering motion. To reuse one random value, you would generate it once outside the loop and store it.',
    source,
    generated: true,
  },

  // ---- Functions and program structure ----
  {
    id: 7915527,
    chapter: 'Functions and program structure',
    title: 'Why functions return values',
    prompt: 'A function computes a result with a return statement. What does returning a value let the caller do that printing it does not?',
    correct: 'Use the result in further calculations because it is passed back to the calling code',
    wrong: [
      miss('Display the result faster on the screen', 'Returning is about handing data back to the caller, not about display speed; printing handles output.', 'Returning gives data to code, not to the screen.'),
      miss('Stop the entire program immediately', 'return ends the current function and gives back a value; it does not halt the whole program.', 'return exits the function, not the program.'),
      miss('Automatically store the result in a global variable', 'return does not create or assign any global variable on its own; the caller decides what to do with the returned value.', 'The caller, not return, chooses where to store the value.'),
    ],
    lesson: 'A return statement passes a value back to whatever called the function, so the caller can store it, test it, or feed it into more code. Printing only shows a value to a human and leaves nothing for the program to reuse. Returning is what makes functions composable building blocks rather than one-off display actions.',
    source,
    generated: true,
  },
  {
    id: 7915528,
    chapter: 'Functions and program structure',
    title: 'Parameters versus arguments',
    prompt: 'In function greet(name) { ... } and a later call greet("Sam"), which statement correctly distinguishes name and "Sam"?',
    correct: 'name is the parameter (a placeholder); "Sam" is the argument (the actual value passed in)',
    wrong: [
      miss('name is the argument and "Sam" is the parameter', 'This swaps the terms: the placeholder in the definition is the parameter, and the value supplied at the call is the argument.', 'The placeholder lives in the definition; the value lives at the call.'),
      miss('They are both arguments, just written in two places', 'name is not an argument; it is the parameter named in the function header that receives whatever argument is passed.', 'One is a placeholder, the other a real value.'),
      miss('name is a global variable that "Sam" overwrites', 'name is a local parameter scoped to the function, not a global variable that the argument overwrites.', 'Parameters are local to the function, not globals.'),
    ],
    lesson: 'A parameter is the named placeholder in a function\'s definition, while an argument is the concrete value supplied when the function is called. When you call greet("Sam"), the argument "Sam" is bound to the parameter name inside the function. Keeping these terms straight clarifies how data flows into functions.',
    source,
    generated: true,
  },
  {
    id: 7915529,
    chapter: 'Functions and program structure',
    title: 'The benefit of decomposition',
    prompt: 'Why is breaking a large program into several small functions generally considered good practice?',
    correct: 'It makes the code easier to read, test, and reuse, since each function does one clear job',
    wrong: [
      miss('It always makes the program run measurably faster', 'Decomposition is about clarity and maintainability; it does not guarantee faster execution and can add minor call overhead.', 'The main payoff is readability, not raw speed.'),
      miss('It removes the need to test the program', 'Smaller functions are easier to test, but they do not eliminate the need for testing.', 'Good structure aids testing, it does not replace it.'),
      miss('It lets the program run without any variables', 'Functions still use variables; decomposition organizes code, it does not remove variables.', 'Structure and variables are unrelated needs.'),
    ],
    lesson: 'Decomposition splits a problem into smaller functions, each responsible for one clear task. This makes code easier to read, test in isolation, debug, and reuse in other parts of a program or other programs. The goal is managing complexity; speed is rarely the point and may even cost a tiny amount due to function-call overhead.',
    source,
    generated: true,
  },
  {
    id: 7915530,
    chapter: 'Functions and program structure',
    title: 'Variable scope inside a function',
    prompt: 'A variable declared with var inside a function is used after the function finishes. What happens?',
    correct: 'It is not accessible outside, because a variable declared in a function has local scope',
    wrong: [
      miss('It is accessible everywhere, because all variables are global', 'Variables declared inside a function are local to it, not global; only variables declared at the top level are broadly accessible.', 'Where a variable is declared determines where it can be used.'),
      miss('It keeps its value and can be read by name from anywhere later', 'A local variable does not exist outside its function; its name is meaningless to outside code.', 'Local names disappear once the function ends.'),
      miss('It causes a syntax error when the function is defined', 'Declaring a local variable is valid; the issue only arises if outside code tries to use the name, which is a different kind of error.', 'Declaring locally is fine; using it outside is the problem.'),
    ],
    lesson: 'Scope defines where a name is visible. A variable declared inside a function has local scope and exists only while that function runs; code outside the function cannot see it. Variables declared in the outer program have wider scope. Keeping variables local reduces accidental interference between parts of a program.',
    source,
    generated: true,
  },

  // ---- Debugging and testing ----
  {
    id: 7915531,
    chapter: 'Debugging and testing',
    title: 'Syntax versus logic errors',
    prompt: 'A program runs without crashing but always prints the wrong total. What kind of error is this most likely to be?',
    correct: 'A logic error, because the code runs but produces an incorrect result',
    wrong: [
      miss('A syntax error, because something is spelled wrong', 'A syntax error would stop the program from running at all; here it runs, so the rules of the language are satisfied even though the result is wrong.', 'If it runs, the syntax is accepted.'),
      miss('A runtime crash, because the program stopped', 'The program did not stop; it completed and gave a wrong answer, so it is not a crash.', 'No crash occurred, so look elsewhere.'),
      miss('No error at all, since the program finished', 'Finishing is not proof of correctness; a wrong result is still a real defect, called a logic error.', 'Running to completion does not mean it is correct.'),
    ],
    lesson: 'Syntax errors break the rules of the language so the program will not run. Logic errors obey the rules, so the program runs, but the reasoning is wrong and it produces incorrect output. Because logic errors do not announce themselves with a crash, they are found through testing, tracing values, and checking results against expectations.',
    source,
    generated: true,
  },
  {
    id: 7915532,
    chapter: 'Debugging and testing',
    title: 'Using console.log to debug',
    prompt: 'A loop seems to run the wrong number of times. What is the most useful first debugging step?',
    correct: 'Print the loop variable each pass with console.log to see how its value actually changes',
    wrong: [
      miss('Delete the loop and rewrite the whole program from scratch', 'Rewriting everything discards working code and information; observing the variable first is faster and more targeted.', 'Investigate before discarding code.'),
      miss('Add more loops in case one is too few', 'Adding loops blindly guesses at the cause; you should first observe what the existing loop does.', 'Gather evidence before changing structure.'),
      miss('Assume the language is broken and switch languages', 'The bug is almost certainly in your logic, not the language; switching languages avoids understanding the problem.', 'Suspect your own logic first.'),
    ],
    lesson: 'Effective debugging is about gathering evidence before changing code. Printing key values, such as the loop counter and condition, with console.log reveals exactly how the program behaves versus how you expected. This often pinpoints an off-by-one bound or a condition that never becomes false, so you can fix the real cause rather than guess.',
    source,
    generated: true,
  },
  {
    id: 7915533,
    chapter: 'Debugging and testing',
    title: 'Choosing test cases',
    prompt: 'When testing a function that grades a score, why is it important to include edge cases like the exact boundary value (for example, 70 when 70 and above passes)?',
    correct: 'Boundaries are where comparison mistakes such as > versus >= most often cause wrong results',
    wrong: [
      miss('Edge cases make the test run faster', 'Edge cases are about catching boundary bugs, not about execution speed.', 'The purpose is correctness at boundaries, not speed.'),
      miss('Only edge cases need to be tested, so typical values can be skipped', 'You should test typical values too; edge cases are added because they are bug-prone, not because they replace ordinary tests.', 'Edge cases supplement, not replace, normal tests.'),
      miss('Edge cases prove the function has no bugs anywhere', 'Passing some tests, even good ones, cannot prove the complete absence of bugs.', 'Tests build confidence, they do not prove perfection.'),
    ],
    lesson: 'Bugs cluster at boundaries because that is where the choice between operators like > and >= matters. Testing the exact boundary value (such as 70 when 70 passes) reveals whether the comparison is off by one. Good test sets mix typical inputs, boundary values, and unusual inputs, but no finite test set proves a program is completely bug-free.',
    source,
    generated: true,
  },

  // ---- Conditionals and boolean logic ----
  {
    id: 7915534,
    chapter: 'Conditionals and boolean logic',
    title: 'Comparison versus assignment',
    prompt: 'Inside an if statement you want to check whether x equals 5. Which condition is correct in JavaScript?',
    correct: 'if (x === 5)',
    wrong: [
      miss('if (x = 5)', 'A single equals assigns 5 to x and evaluates to 5 (truthy), so the condition is not a real comparison and the branch always runs.', 'One equals assigns; you need a comparison operator.'),
      miss('if (x =< 5)', 'The operator is mistyped; less-than-or-equal is written <=, and in any case the task is to test equality.', 'Check the exact operator spelling and what you mean to test.'),
      miss('if (x equals 5)', 'JavaScript has no equals keyword for comparison; it uses == or === operators.', 'Use the symbolic comparison operator.'),
    ],
    lesson: 'In a condition, comparison uses == (loose) or === (strict), while a single = is assignment. Writing if (x = 5) sets x to 5 and is almost always a bug because the condition becomes truthy regardless. Strict equality === also checks type, which avoids surprises from automatic type conversion.',
    source,
    generated: true,
  },
  {
    id: 7915535,
    chapter: 'Conditionals and boolean logic',
    title: 'Logical AND',
    prompt: 'A login succeeds only when the username is correct and the password is correct. Which condition expresses this?',
    correct: 'if (userOk && passOk)',
    wrong: [
      miss('if (userOk || passOk)', 'OR succeeds when either check passes, so a correct username with any password would log in; you need both to be true.', 'Requiring both conditions calls for AND, not OR.'),
      miss('if (userOk && passOk == false)', 'This requires the password check to be false, which is the opposite of what you want.', 'You want both checks true, not one of them false.'),
      miss('if (!userOk && !passOk)', 'The NOT operators require both checks to be false, which would let in only completely wrong credentials.', 'Negating both inverts the whole condition.'),
    ],
    lesson: 'The logical AND operator && is true only when both operands are true, which matches a rule that needs every condition met. OR (||) is true when at least one operand is true. NOT (!) flips a boolean. Choosing the right operator is essential: AND for all-must-hold rules, OR for any-one-suffices rules.',
    source,
    generated: true,
  },
  {
    id: 7915536,
    chapter: 'Conditionals and boolean logic',
    title: 'else if chains',
    prompt: 'You assign a letter grade by range using if / else if / else. Why use else if rather than several separate if statements?',
    correct: 'else if stops checking once a condition matches, so each input takes exactly one branch',
    wrong: [
      miss('Separate if statements run faster than an else if chain', 'The choice is about correctness and exclusivity of branches, not speed; the difference in performance is negligible.', 'The reason concerns which branch runs, not timing.'),
      miss('else if is required by the language; plain if cannot be used in groups', 'Multiple plain if statements are perfectly valid syntax; the issue is that they may all run, not that they are illegal.', 'Plain ifs are allowed but behave differently.'),
      miss('else if lets you skip writing any conditions', 'else if still needs a condition; only the final else is condition-free.', 'You still specify a condition for each else if.'),
    ],
    lesson: 'An if / else if / else chain is mutually exclusive: once one condition is true, the rest are skipped, so exactly one branch runs. Separate if statements are each evaluated independently, so several can fire for the same input, which can wrongly assign two grades. Use else if when the cases should not overlap.',
    source,
    generated: true,
  },
  {
    id: 7915537,
    chapter: 'Conditionals and boolean logic',
    title: 'Negation with NOT',
    prompt: 'If the boolean variable isOpen is true, what is the value of !isOpen?',
    correct: 'false',
    wrong: [
      miss('true', 'The NOT operator inverts a boolean, so applying it to true yields false, not true.', 'NOT flips the value to its opposite.'),
      miss('It causes an error because you cannot negate a boolean', 'Negating a boolean is exactly what ! is for; it is valid and common.', 'NOT is designed to operate on booleans.'),
      miss('isOpen, unchanged', '! returns the inverted value rather than leaving the operand unchanged.', 'The operator produces the opposite, not the original.'),
    ],
    lesson: 'The logical NOT operator (!) inverts a boolean: !true is false and !false is true. It is useful for writing conditions like if (!isReady) to mean when something is not ready. Combined with && and ||, NOT lets you express the full range of boolean logic in conditions.',
    source,
    generated: true,
  },

  // ---- Loops and iteration ----
  {
    id: 7915538,
    chapter: 'Loops and iteration',
    title: 'Counting loop iterations',
    prompt: 'How many times does the body run in for (var i = 0; i < 5; i++) { ... }?',
    correct: '5',
    wrong: [
      miss('4', 'The loop runs for i = 0, 1, 2, 3, and 4, which is 5 iterations; counting 4 forgets that it starts at 0.', 'List the values i takes and count them.'),
      miss('6', 'i never reaches 5 because the condition is i < 5, so there are 5 iterations (0 through 4), not 6.', 'The loop stops before i equals 5.'),
      miss('Infinite, because i never equals 5 exactly', 'i increases by 1 each pass and reaches 5, at which point i < 5 is false and the loop ends.', 'Trace i until the condition becomes false.'),
    ],
    lesson: 'A for loop with i starting at 0 and condition i < 5 runs while i is 0, 1, 2, 3, and 4: five iterations. The body never runs for i = 5 because the condition is checked first and fails. This 0-to-n-1 pattern matches array indexing and is worth tracing by hand until it is automatic.',
    source,
    generated: true,
  },
  {
    id: 7915539,
    chapter: 'Loops and iteration',
    title: 'Avoiding infinite loops',
    prompt: 'A while loop\'s condition is i < 10, but the body never changes i. What happens?',
    correct: 'It loops forever, because the condition can never become false',
    wrong: [
      miss('It runs exactly 10 times then stops on its own', 'A while loop does not count automatically; without changing i, the condition stays true and it never stops.', 'Something inside the loop must move toward ending it.'),
      miss('It runs once and then exits', 'A while loop keeps repeating as long as the condition is true; with i unchanged it does not exit after one pass.', 'The loop rechecks the condition every pass.'),
      miss('It refuses to start because i is not updated', 'The loop starts as long as the condition is initially true; the missing update only prevents it from ending.', 'The problem is ending, not starting.'),
    ],
    lesson: 'A while loop repeats as long as its condition is true. Something inside the body must eventually make the condition false, usually by updating the variable the condition tests. If nothing changes i, the condition i < 10 stays true forever and the program hangs. This is a classic infinite-loop bug.',
    source,
    generated: true,
  },
  {
    id: 7915540,
    chapter: 'Loops and iteration',
    title: 'Nested loops',
    prompt: 'An outer loop runs 3 times and, inside it, an inner loop runs 4 times each pass. How many times does the innermost body execute in total?',
    correct: '12',
    wrong: [
      miss('7', 'Nested loops multiply rather than add; 3 plus 4 is 7, but the inner loop runs fully for each outer pass, so it is 3 times 4.', 'Each outer pass triggers a full inner loop.'),
      miss('3', 'Three counts only the outer passes; for each one the inner body runs 4 times, so the total is larger.', 'Count the inner runs across all outer passes.'),
      miss('4', 'Four is one inner loop\'s count for a single outer pass; there are three outer passes.', 'Multiply by the number of outer passes.'),
    ],
    lesson: 'With nested loops, the inner loop completes fully on every iteration of the outer loop, so total executions multiply: 3 outer times 4 inner equals 12. This pattern naturally produces grids and tables. Recognizing the multiplication is also the basis for analyzing how nested loops affect running time.',
    source,
    generated: true,
  },

  // ---- Strings and text processing ----
  {
    id: 7915541,
    chapter: 'Strings and text processing',
    title: 'String concatenation',
    prompt: 'What is the result of the expression "Score: " + 42 in JavaScript?',
    correct: '"Score: 42" (the number is converted to text and joined)',
    wrong: [
      miss('A type error, because you cannot add a string and a number', 'JavaScript does not error here; the + operator with a string converts the number to a string and concatenates.', 'Recall how + behaves when one side is a string.'),
      miss('44, because "Score: " counts as 2', 'Strings are not turned into numbers and added arithmetically; with a string operand, + joins text.', 'A string operand triggers concatenation, not arithmetic.'),
      miss('"Score: " only, because the number is ignored', 'The number is not dropped; it is converted to "42" and appended.', 'Both operands appear in the result.'),
    ],
    lesson: 'The + operator does double duty: with two numbers it adds, but if either operand is a string it converts the other to a string and concatenates. So "Score: " + 42 yields "Score: 42". This implicit conversion is handy for building messages but can surprise you when you expected arithmetic.',
    source,
    generated: true,
  },
  {
    id: 7915542,
    chapter: 'Strings and text processing',
    title: 'String length',
    prompt: 'What does "hello".length evaluate to?',
    correct: '5',
    wrong: [
      miss('4', 'length counts every character, and "hello" has 5: h, e, l, l, o.', 'Count each character including repeats.'),
      miss('6', 'There is no trailing space or hidden character; "hello" is exactly 5 characters.', 'Count only the visible characters in the quotes.'),
      miss('"hello"', 'length returns the count of characters as a number, not the string itself.', 'length gives a number, not the text.'),
    ],
    lesson: 'A string\'s length property is the number of characters it contains, so "hello".length is 5. Characters are counted from the first to the last, and like arrays, string characters are indexed from 0, so the last character of "hello" is at index 4. length is widely used to loop over or validate text.',
    source,
    generated: true,
  },
  {
    id: 7915543,
    chapter: 'Strings and text processing',
    title: 'Accessing a character',
    prompt: 'For the string word = "code", which expression returns the character "c"?',
    correct: 'word.charAt(0)',
    wrong: [
      miss('word.charAt(1)', 'Index 1 is the second character, "o"; the first character "c" is at index 0.', 'String indexing starts at 0.'),
      miss('word.charAt("c")', 'charAt expects a numeric position, not the character you are looking for.', 'Pass an index number, not the letter.'),
      miss('word.first()', 'There is no first() method on strings; you access characters by index with charAt or bracket notation.', 'Use index-based access, not a first method.'),
    ],
    lesson: 'Strings are indexed from 0, so charAt(0) returns the first character. word.charAt(0) on "code" is "c", and bracket notation word[0] does the same. To get later characters, increase the index, remembering that the last valid index is length minus 1. Out-of-range indices return an empty string with charAt.',
    source,
    generated: true,
  },

  // ---- Algorithms and problem solving ----
  {
    id: 7915544,
    chapter: 'Algorithms and problem solving',
    title: 'What an algorithm is',
    prompt: 'Which description best defines an algorithm?',
    correct: 'A finite, ordered set of clear steps that solves a problem or completes a task',
    wrong: [
      miss('A programming language used to write apps', 'An algorithm is the plan or set of steps; a programming language is one way to express it, not the algorithm itself.', 'Separate the plan from the language used to write it.'),
      miss('A single line of code that runs a program', 'An algorithm is the overall step-by-step method, usually many steps, not one line of code.', 'Think method and sequence, not a single statement.'),
      miss('A piece of hardware that speeds up calculations', 'An algorithm is an abstract procedure, not a physical device.', 'Algorithms are ideas and steps, not hardware.'),
    ],
    lesson: 'An algorithm is a precise, finite sequence of unambiguous steps that takes some input and produces a result. It is independent of any particular programming language: the same algorithm can be written in JavaScript, Python, or even plain English. Good algorithms are clear, correct, and terminate after finitely many steps.',
    source,
    generated: true,
  },
  {
    id: 7915545,
    chapter: 'Algorithms and problem solving',
    title: 'Linear search behavior',
    prompt: 'A linear search looks for a value in an unsorted list of 100 items. In the worst case, how many items might it check?',
    correct: '100, because it may have to look at every item',
    wrong: [
      miss('1, because it finds the item immediately', 'Finding it on the first try is the best case, not the worst case; the worst case examines the whole list.', 'Worst case means the least convenient outcome.'),
      miss('50, because on average it checks half', 'Half is the typical average for a present item, but the worst case is the full list.', 'Average and worst case are different measures.'),
      miss('10, because lists are searched in chunks of ten', 'Linear search checks items one by one, not in chunks of ten.', 'Linear search inspects elements individually.'),
    ],
    lesson: 'Linear search inspects elements one at a time from the start. In the worst case the target is last or absent, so it checks all n items; for 100 items that is up to 100 checks. On average it checks about half when the item is present. Because it does not require sorting, linear search is simple but slow for large lists compared with binary search.',
    source,
    generated: true,
  },
  {
    id: 7915546,
    chapter: 'Algorithms and problem solving',
    title: 'Why binary search needs sorting',
    prompt: 'Binary search is much faster than linear search, but it has a requirement. What is it?',
    correct: 'The list must be sorted, so each comparison can rule out half the remaining items',
    wrong: [
      miss('The list must contain only numbers, never text', 'Binary search works on any orderable data, including text sorted alphabetically, not just numbers.', 'The requirement is order, not a specific data type.'),
      miss('The list must have an even number of items', 'Binary search works regardless of whether the count is even or odd.', 'List size parity does not matter.'),
      miss('The list must be stored in a database', 'Binary search applies to ordinary sorted arrays in memory; a database is not required.', 'Sorting in memory is enough.'),
    ],
    lesson: 'Binary search repeatedly checks the middle of a sorted range and discards the half that cannot contain the target, so each step halves the search space. This only works if the data is sorted, because that is what lets a single comparison eliminate half the items. The payoff is roughly log-base-2 comparisons instead of up to n.',
    source,
    generated: true,
  },
  {
    id: 7915547,
    chapter: 'Algorithms and problem solving',
    title: 'Pseudocode purpose',
    prompt: 'Why do programmers often write an algorithm in pseudocode before coding it in a specific language?',
    correct: 'It lets them plan the logic clearly without getting stuck on exact language syntax',
    wrong: [
      miss('Pseudocode runs directly on the computer faster than real code', 'Pseudocode is not executable; it is a planning notation, not something the computer runs.', 'Pseudocode is for humans, not the machine.'),
      miss('Pseudocode is required by every programming language before compiling', 'No language requires pseudocode; it is an optional design aid.', 'It is a habit, not a language rule.'),
      miss('Pseudocode guarantees the final program will have no bugs', 'Planning helps, but it cannot guarantee a bug-free implementation.', 'Good planning reduces, but does not eliminate, bugs.'),
    ],
    lesson: 'Pseudocode is an informal, language-independent way to describe an algorithm using plain steps. Writing it first lets you focus on the logic and structure without fighting the precise syntax of a language. Once the plan is sound, translating it into real code is more straightforward, and the pseudocode also documents your intent.',
    source,
    generated: true,
  },

  // ---- Recursion ----
  {
    id: 7915548,
    chapter: 'Recursion',
    title: 'The base case',
    prompt: 'In a recursive function, what is the role of the base case?',
    correct: 'It is the condition that stops the recursion, so the function does not call itself forever',
    wrong: [
      miss('It is the step where the function calls itself again', 'That describes the recursive case; the base case is the one that does not recurse and ends the chain.', 'The base case stops recursion rather than continuing it.'),
      miss('It is the first value the user types in', 'The base case is a stopping condition in the code, not user input.', 'It is about when recursion ends, not where data comes from.'),
      miss('It is the largest input the function can accept', 'The base case is not about input size limits; it is the smallest sub-problem that is solved directly.', 'Think smallest solvable case, not a maximum.'),
    ],
    lesson: 'A recursive function solves a problem by calling itself on a smaller version of the problem. The base case is the simplest situation that is answered directly, without further recursion, and it is what stops the chain of calls. Without a reachable base case, recursion never ends and the program crashes from too many calls.',
    source,
    generated: true,
  },
  {
    id: 7915549,
    chapter: 'Recursion',
    title: 'Missing base case',
    prompt: 'What typically happens if a recursive function has no reachable base case?',
    correct: 'It calls itself endlessly until the program runs out of stack space and errors',
    wrong: [
      miss('It returns 0 by default', 'A missing base case does not produce a default return; the calls keep nesting until the program fails.', 'Without a stopping point, the calls do not resolve.'),
      miss('It runs exactly once and stops', 'A recursive call triggers another call; with no base case it does not stop after one run.', 'Each call spawns another call.'),
      miss('It automatically switches to a loop', 'The language does not convert runaway recursion into a loop; it simply keeps recursing.', 'Nothing converts the recursion for you.'),
    ],
    lesson: 'Every recursive call adds a frame to the call stack. A correct base case lets the calls eventually stop and unwind. Without a reachable base case, the function keeps calling itself, the stack grows without limit, and the program crashes (a stack overflow). This is the recursive analogue of an infinite loop.',
    source,
    generated: true,
  },
])
