import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe col-intro-to-cs-python top-up'

export const colIntroToCsPythonHigh103TopUpQuestions: Question[] = makeQuestionBank('High', [
  // ---- Unit 1: Computational thinking with variables ----
  {
    id: 7987000,
    chapter: 'Computational thinking with variables',
    title: 'Assignment direction',
    prompt: 'After the statement score = 7 + 3 runs in Python, what value is stored in the variable score?',
    correct: '10',
    wrong: [
      miss('"7 + 3"', 'The right side is an arithmetic expression, not a string; it is evaluated to a number before being stored.', 'Numbers without quotes are calculated, not kept as text.'),
      miss('7', 'Only the first number would be wrong; the whole expression is evaluated first.', 'The entire right-hand side is computed before assignment.'),
      miss('Nothing, because score was never defined first', 'In Python you do not declare a variable before assigning; the assignment creates it.', 'A first assignment both creates and fills the variable.'),
    ],
    lesson: 'An assignment statement evaluates the expression on the right of the = sign and then binds that resulting value to the name on the left. So 7 + 3 is computed to 10, and 10 is stored in score. Python creates the variable automatically the first time you assign to it.',
    source,
    generated: true,
  },
  {
    id: 7987001,
    chapter: 'Computational thinking with variables',
    title: 'String versus integer',
    prompt: 'In Python, what is the data type of the value "42" (written with quotation marks)?',
    correct: 'A string (str)',
    wrong: [
      miss('An integer (int)', 'The quotation marks make it text; integers are written without quotes, like 42.', 'Quotes signal text, not a number.'),
      miss('A float', 'A float would be a number with a decimal point and no quotes, like 42.0.', 'Look at the quotation marks, not the digits.'),
      miss('A boolean', 'Booleans are only True or False, not sequences of characters.', 'A boolean holds a truth value, not digits in quotes.'),
    ],
    lesson: 'In Python, anything enclosed in quotation marks is a string, even if it looks like a number. So "42" is the text characters 4 and 2, while 42 without quotes is an integer. This distinction matters because "42" + "42" gives "4242" but 42 + 42 gives 84.',
    source,
    generated: true,
  },
  {
    id: 7987002,
    chapter: 'Computational thinking with variables',
    title: 'Naming rules',
    prompt: 'Which of these is a valid Python variable name?',
    correct: 'total_score',
    wrong: [
      miss('2nd_place', 'A variable name cannot begin with a digit; it must start with a letter or underscore.', 'Check the very first character of each name.'),
      miss('total score', 'Spaces are not allowed inside a variable name; Python would read it as two tokens.', 'Use an underscore instead of a space.'),
      miss('total-score', 'A hyphen is read as the subtraction operator, not part of a name.', 'The dash means minus, so it cannot join words in a name.'),
    ],
    lesson: 'Python variable names must start with a letter or underscore and may contain only letters, digits, and underscores, with no spaces or punctuation. Programmers commonly use snake_case, joining words with underscores, to keep names readable. Names like 2nd_place, total score, and total-score all break one of these rules.',
    source,
    generated: true,
  },
  {
    id: 7987003,
    chapter: 'Computational thinking with variables',
    title: 'Integer division operator',
    prompt: 'What does the expression 17 // 5 evaluate to in Python?',
    correct: '3',
    wrong: [
      miss('3.4', 'The // operator performs integer (floor) division and discards the fractional part; / would give 3.4.', 'A double slash keeps only the whole-number part.'),
      miss('2', 'That is the remainder (17 % 5), not the quotient from floor division.', 'Floor division gives how many times 5 fits, not what is left over.'),
      miss('4', 'Floor division rounds toward the lower whole number, not up.', '5 fits into 17 three full times.'),
    ],
    lesson: 'The // operator is floor division: it divides and then rounds down to the nearest whole number, discarding any remainder. Since 5 goes into 17 three whole times (with 2 left over), 17 // 5 is 3. The related operator % returns the remainder, so 17 % 5 is 2.',
    source,
    generated: true,
  },
  {
    id: 7987004,
    chapter: 'Computational thinking with variables',
    title: 'The modulo operator',
    prompt: 'What is the value of 23 % 4 in Python?',
    correct: '3',
    wrong: [
      miss('5', 'That is the quotient 23 // 4, not the remainder.', 'The % sign asks for what is left over, not how many times.'),
      miss('5.75', 'That is ordinary division 23 / 4; the % operator returns only the remainder.', 'Modulo never gives a decimal for whole numbers.'),
      miss('4', 'A remainder when dividing by 4 is always less than 4.', 'The leftover after dividing by 4 ranges from 0 to 3.'),
    ],
    lesson: 'The modulo operator % returns the remainder after integer division. Dividing 23 by 4 gives 5 with a remainder of 3, so 23 % 4 is 3. Modulo is handy for tasks like checking if a number is even (n % 2 == 0) or wrapping values around a fixed range.',
    source,
    generated: true,
  },
  {
    id: 7987005,
    chapter: 'Computational thinking with variables',
    title: 'Reassigning variables',
    prompt: 'After the two statements x = 5 then x = x + 2 run in order, what is the value of x?',
    correct: '7',
    wrong: [
      miss('5', 'The second line reassigns x; the old value 5 is replaced.', 'Trace what the right side evaluates to before the second assignment.'),
      miss('2', 'x + 2 uses the current value of x (5) plus 2, not just 2.', 'Substitute the current value of x into the expression.'),
      miss('It causes an error because x cannot equal itself plus 2', 'The right side is evaluated first using the old value, then stored back; this is valid.', 'Read the right side fully before the assignment happens.'),
    ],
    lesson: 'A statement like x = x + 2 is not a mathematical equation; it is an instruction. Python evaluates the right side using the current value of x (5 + 2 = 7) and then stores that result back into x. This pattern of updating a variable using its own value is extremely common in programming.',
    source,
    generated: true,
  },
  {
    id: 7987006,
    chapter: 'Computational thinking with variables',
    title: 'String concatenation with input',
    prompt: 'Python\'s input() function returns a value of which type, no matter what the user types?',
    correct: 'A string (str)',
    wrong: [
      miss('An integer if the user types digits', 'input() always returns a string; you must call int() yourself to convert it.', 'Think about what type you get before any conversion.'),
      miss('A float for numbers and a string for words', 'Python does not guess; it always hands back a string.', 'The same type is returned regardless of what is typed.'),
      miss('A boolean', 'Booleans come from comparisons, not from input().', 'input() captures typed text, not True or False.'),
    ],
    lesson: 'The input() function always returns a string, even when the user types a number. To use that text in arithmetic, you must convert it with int() or float(). Forgetting this conversion is a classic bug: "5" + "3" gives "53" instead of 8.',
    source,
    generated: true,
  },
  {
    id: 7987007,
    chapter: 'Computational thinking with variables',
    title: 'Exponent operator',
    prompt: 'What does 2 ** 3 evaluate to in Python?',
    correct: '8',
    wrong: [
      miss('6', 'That is 2 times 3; ** means exponentiation, not multiplication.', 'A double star raises to a power.'),
      miss('5', 'That would be 2 + 3; ** is not addition.', 'Two stars together mean a power, not a sum.'),
      miss('9', 'That is 3 squared; here 2 is the base and 3 is the exponent.', 'Identify which number is the base and which is the power.'),
    ],
    lesson: 'The ** operator raises a number to a power. So 2 ** 3 means 2 raised to the power 3, which is 2 x 2 x 2 = 8. The base is on the left of ** and the exponent is on the right.',
    source,
    generated: true,
  },

  // ---- Unit 2: Booleans and conditionals ----
  {
    id: 7987008,
    chapter: 'Booleans and conditionals',
    title: 'Equality versus assignment',
    prompt: 'Inside an if statement, which operator should you use to test whether two values are equal?',
    correct: '== (double equals)',
    wrong: [
      miss('= (single equals)', 'A single = assigns a value; it does not compare. Using it in a condition is a syntax error in Python.', 'Comparison and assignment use different symbols.'),
      miss('=== (triple equals)', 'Python has no triple-equals operator; that is from JavaScript.', 'Python uses only two equals signs to compare.'),
      miss('equals', 'Python has no keyword named equals for comparison.', 'The comparison operator is a symbol, not a word.'),
    ],
    lesson: 'In Python, a single = is the assignment operator that stores a value in a variable, while == is the comparison operator that tests whether two values are equal and produces True or False. Mixing them up is a common beginner error: if x = 5 is invalid, but if x == 5 is correct.',
    source,
    generated: true,
  },
  {
    id: 7987009,
    chapter: 'Booleans and conditionals',
    title: 'Result of a comparison',
    prompt: 'What is the data type of the result of the expression 8 > 3 in Python?',
    correct: 'A boolean (bool), specifically True',
    wrong: [
      miss('An integer, specifically 1', 'Although True behaves like 1 in arithmetic, the expression\'s type is bool, not int.', 'A comparison yields a truth value.'),
      miss('A string, specifically "True"', 'The result is the boolean True, not the text "True" in quotes.', 'No quotation marks are involved.'),
      miss('A float', 'Comparisons never produce decimals; they produce True or False.', 'The answer is a yes/no value.'),
    ],
    lesson: 'Comparison operators such as >, <, ==, and != evaluate to a boolean value, either True or False. So 8 > 3 produces the boolean True. Booleans are the foundation of decision-making in programs and control which branch an if statement takes.',
    source,
    generated: true,
  },
  {
    id: 7987010,
    chapter: 'Booleans and conditionals',
    title: 'The and operator',
    prompt: 'For the expression (age >= 13) and (age <= 19) to be True, the value of age must be in which range?',
    correct: 'Between 13 and 19 inclusive',
    wrong: [
      miss('Either below 13 or above 19', 'That describes or with the opposite comparisons; and requires both parts to be true at once.', 'With and, both conditions must hold simultaneously.'),
      miss('Exactly 13 or exactly 19', 'The >= and <= operators include everything between, not just the endpoints.', 'Inclusive comparisons cover the whole span.'),
      miss('Any number, because and always returns True', 'and returns True only when both operands are True; it is not a constant.', 'Both sides must be satisfied for the whole to be True.'),
    ],
    lesson: 'The and operator returns True only when both of its operands are True. Here both age >= 13 and age <= 19 must hold, which is satisfied by any value from 13 through 19 inclusive. If either condition fails, the whole expression is False.',
    source,
    generated: true,
  },
  {
    id: 7987011,
    chapter: 'Booleans and conditionals',
    title: 'Role of elif',
    prompt: 'In an if / elif / else structure, when is the block under an elif executed?',
    correct: 'Only when all earlier if and elif conditions were False and this elif\'s condition is True',
    wrong: [
      miss('Whenever its own condition is True, regardless of the earlier conditions', 'Python checks branches in order and skips an elif if an earlier condition already matched.', 'Earlier matches stop later branches from running.'),
      miss('Always, as a backup after the if block runs', 'Only the else branch is the unconditional fallback, and even it runs only if nothing matched.', 'elif still has a condition to test.'),
      miss('Only if the else block also runs', 'Exactly one branch in the whole chain runs; elif and else are mutually exclusive.', 'A chain executes at most one block.'),
    ],
    lesson: 'In an if / elif / else chain, Python tests each condition in order and runs the block for the first one that is True, then skips the rest of the chain. An elif block runs only when every condition above it was False and its own condition is True. The optional else runs only if none of the conditions matched.',
    source,
    generated: true,
  },
  {
    id: 7987012,
    chapter: 'Booleans and conditionals',
    title: 'The not operator',
    prompt: 'If the variable raining holds the boolean value False, what does not raining evaluate to?',
    correct: 'True',
    wrong: [
      miss('False', 'The not operator flips a boolean, so not False is True, not False again.', 'not reverses the truth value.'),
      miss('"raining"', 'not produces a boolean, not the variable\'s name as text.', 'The result is True or False.'),
      miss('It causes an error because you cannot negate a variable', 'not works on any boolean value, including one stored in a variable.', 'Negation is valid on a boolean variable.'),
    ],
    lesson: 'The not operator reverses a boolean value: not True is False and not False is True. Since raining holds False, not raining evaluates to True. This is useful for writing conditions like if not finished: to act while something is still incomplete.',
    source,
    generated: true,
  },
  {
    id: 7987013,
    chapter: 'Booleans and conditionals',
    title: 'Indentation defines blocks',
    prompt: 'In Python, how does the interpreter know which lines belong inside an if block?',
    correct: 'By their indentation (the lines indented under the if header form the block)',
    wrong: [
      miss('By curly braces { } that surround the block', 'Curly braces are used in languages like Java and C, but Python uses indentation instead.', 'Python relies on whitespace, not braces.'),
      miss('By an end keyword that closes the block', 'Python has no end keyword; the block ends when indentation returns to the previous level.', 'There is no closing keyword in Python.'),
      miss('By a semicolon at the end of the block', 'Semicolons separate statements on one line; they do not mark block boundaries.', 'Block grouping is shown by leading spaces.'),
    ],
    lesson: 'Python uses indentation, not braces or keywords, to group statements into blocks. The lines indented under an if header form its body, and the block ends when the indentation returns to the outer level. Consistent indentation is therefore part of Python\'s syntax, not just style.',
    source,
    generated: true,
  },
  {
    id: 7987014,
    chapter: 'Booleans and conditionals',
    title: 'Combining comparisons with or',
    prompt: 'For which value of day is the expression (day == "Sat") or (day == "Sun") True?',
    correct: 'When day is either "Sat" or "Sun"',
    wrong: [
      miss('Only when day is both "Sat" and "Sun" at the same time', 'A variable holds one value at a time; or needs just one side to be True, not both.', 'or is satisfied by either alternative.'),
      miss('Only when day is "Sat"', 'The or means the second comparison also makes the whole thing True.', 'There are two ways for this to be True.'),
      miss('For any value of day, since or is always True', 'or is True only when at least one operand is True, not unconditionally.', 'A False on both sides makes the whole False.'),
    ],
    lesson: 'The or operator returns True when at least one of its operands is True. So the expression is True whenever day equals "Sat" or day equals "Sun", which is the standard way to check for weekend days. It is False only when day is neither value.',
    source,
    generated: true,
  },

  // ---- Unit 3: Loops ----
  {
    id: 7987015,
    chapter: 'Loops',
    title: 'range with one argument',
    prompt: 'How many times does the loop for i in range(5): print(i) run its body, and what is the last value printed?',
    correct: 'It runs 5 times and the last value printed is 4',
    wrong: [
      miss('It runs 5 times and the last value printed is 5', 'range(5) produces 0, 1, 2, 3, 4; it stops before 5.', 'The stop value itself is not included.'),
      miss('It runs 4 times and the last value printed is 4', 'range(5) yields five values starting at 0, so the body runs five times.', 'Count the values: 0 through 4 is five numbers.'),
      miss('It runs 6 times, counting from 0 through 5', 'range(5) does not include 5, so there are only five values.', 'The upper bound is exclusive.'),
    ],
    lesson: 'With a single argument, range(n) generates the sequence 0, 1, ..., n-1, which is n values that start at 0 and stop before n. So range(5) gives 0, 1, 2, 3, 4: the loop runs five times and the last value is 4. This zero-based, stop-exclusive behavior trips up many beginners.',
    source,
    generated: true,
  },
  {
    id: 7987016,
    chapter: 'Loops',
    title: 'range with start and stop',
    prompt: 'Which sequence of numbers does range(2, 6) generate?',
    correct: '2, 3, 4, 5',
    wrong: [
      miss('2, 3, 4, 5, 6', 'The stop value 6 is excluded; range stops just before it.', 'The second number is the stop and is not produced.'),
      miss('2, 4, 6', 'Without a third argument the step is 1, not 2, so no numbers are skipped.', 'The default step is one.'),
      miss('0, 1, 2, 3, 4, 5', 'The first argument 2 is the start value, so counting begins at 2, not 0.', 'The first number tells you where to start.'),
    ],
    lesson: 'With two arguments, range(start, stop) counts from start up to but not including stop, stepping by 1. So range(2, 6) produces 2, 3, 4, 5. The stop value is always excluded, which is consistent with how range(n) works.',
    source,
    generated: true,
  },
  {
    id: 7987017,
    chapter: 'Loops',
    title: 'while loop condition',
    prompt: 'A while loop in Python repeats its body for as long as which statement is true?',
    correct: 'As long as the loop\'s condition evaluates to True',
    wrong: [
      miss('As long as the condition evaluates to False', 'A while loop continues while the condition is True and stops when it becomes False.', 'The loop keeps going on a True condition.'),
      miss('Exactly the number of times given in parentheses, like a for loop', 'while loops are condition-controlled, not count-controlled; there is no fixed count.', 'Think about what controls a while loop versus a for loop.'),
      miss('Forever, since while loops always run infinitely', 'A while loop ends as soon as its condition becomes False, which usually happens when a variable is updated inside.', 'Something in the body must change the condition.'),
    ],
    lesson: 'A while loop checks its condition before each pass and runs the body only while that condition is True. When the condition becomes False, the loop stops. To avoid an infinite loop, the body must eventually do something that makes the condition False, such as updating a counter.',
    source,
    generated: true,
  },
  {
    id: 7987018,
    chapter: 'Loops',
    title: 'Cause of an infinite loop',
    prompt: 'In the loop count = 0; while count < 3: print(count), what mistake causes it to run forever?',
    correct: 'The variable count is never increased inside the loop, so the condition stays True',
    wrong: [
      miss('The condition uses < instead of <=', 'Either comparison would terminate normally if count were updated; the comparison choice is not the bug.', 'Look at whether anything changes count.'),
      miss('print(count) should be print("count")', 'How the value is printed has no effect on whether the loop ends.', 'The fault is about the loop ending, not the output.'),
      miss('count starts at 0 instead of 1', 'The starting value does not stop the loop from terminating if count is updated.', 'Focus on what keeps the condition True forever.'),
    ],
    lesson: 'A while loop only ends when its condition becomes False. Here count starts at 0 and the condition count < 3 is True, but nothing inside the loop ever changes count, so the condition stays True forever. Adding count = count + 1 inside the loop makes it eventually reach 3 and stop.',
    source,
    generated: true,
  },
  {
    id: 7987019,
    chapter: 'Loops',
    title: 'Accumulator pattern',
    prompt: 'In a loop that adds up numbers, why is total = 0 written before the loop rather than inside it?',
    correct: 'So the running total exists and starts at zero; resetting it inside the loop would erase the sum each pass',
    wrong: [
      miss('It does not matter where total = 0 goes; the result is the same', 'Placing it inside the loop resets total to 0 every iteration, destroying the accumulated sum.', 'Think about what happens to total on each pass if it is reset.'),
      miss('It must go inside the loop so total updates each time', 'The update (total = total + value) goes inside, but the initialization must stay outside.', 'Separate creating the variable from updating it.'),
      miss('Python requires all variables to be created at the top of the file', 'Python lets you create variables anywhere; the placement here is about logic, not a rule.', 'This is about loop behavior, not a language requirement.'),
    ],
    lesson: 'The accumulator pattern uses a variable that builds up a result across loop iterations. You initialize it once before the loop (total = 0) so it has a starting value, then update it inside the loop (total = total + value). If you reset it to 0 inside the loop, every pass would wipe out the sum so far.',
    source,
    generated: true,
  },
  {
    id: 7987020,
    chapter: 'Loops',
    title: 'Looping over a string',
    prompt: 'How many times does the body run in the loop for letter in "code": print(letter)?',
    correct: '4 times, once for each character c, o, d, e',
    wrong: [
      miss('1 time, printing the whole word "code"', 'Looping over a string visits one character per iteration, not the whole string at once.', 'Each pass binds the loop variable to a single character.'),
      miss('5 times, including a final empty character', 'The word "code" has exactly four characters; there is no extra empty one.', 'Count the letters in the string.'),
      miss('0 times, because a string is not a sequence', 'Strings are sequences in Python and can be iterated over directly.', 'A for loop can step through a string.'),
    ],
    lesson: 'A for loop can iterate directly over a string, assigning each character to the loop variable in turn. The string "code" has four characters, so the loop runs four times, printing c, then o, then d, then e. This works because strings are ordered sequences of characters.',
    source,
    generated: true,
  },
  {
    id: 7987021,
    chapter: 'Loops',
    title: 'Nested loop iteration count',
    prompt: 'In the nested loops for i in range(3): for j in range(4): print(i, j), how many lines are printed in total?',
    correct: '12',
    wrong: [
      miss('7', 'You add the ranges (3 + 4) instead of multiplying; nested loops multiply their counts.', 'For each outer pass, the inner loop runs fully.'),
      miss('3', 'Only the outer loop count is counted; the inner loop runs multiple times per outer pass.', 'Account for the inner loop repeating each time.'),
      miss('4', 'Only the inner loop count is counted; it repeats once for every outer iteration.', 'The outer loop multiplies the inner loop\'s work.'),
    ],
    lesson: 'In nested loops, the inner loop runs completely for each iteration of the outer loop, so the total number of inner executions is the product of the two counts. Here the outer loop runs 3 times and the inner loop runs 4 times each, giving 3 x 4 = 12 printed lines.',
    source,
    generated: true,
  },

  // ---- Unit 4: Functions ----
  {
    id: 7987022,
    chapter: 'Functions',
    title: 'Defining a function',
    prompt: 'Which keyword is used to define a new function in Python?',
    correct: 'def',
    wrong: [
      miss('function', 'function is the keyword in JavaScript, not Python; Python uses def.', 'Python\'s keyword is a short three-letter abbreviation.'),
      miss('func', 'func is not a Python keyword; the correct one is shorter.', 'It is an abbreviation of the word define.'),
      miss('define', 'Although a function defines behavior, Python uses the abbreviated keyword def.', 'Python shortens the word to three letters.'),
    ],
    lesson: 'In Python you create a function with the def keyword, followed by the function name, parentheses for any parameters, and a colon. The indented lines below form the function body. For example, def greet(): begins a function named greet.',
    source,
    generated: true,
  },
  {
    id: 7987023,
    chapter: 'Functions',
    title: 'Parameters versus arguments',
    prompt: 'In def area(width, height):, the names width and height are best described as what?',
    correct: 'Parameters, which are placeholders for the values passed in when the function is called',
    wrong: [
      miss('Arguments, the actual values supplied when the function runs', 'Arguments are the concrete values passed at call time; the names in the definition are parameters.', 'Distinguish the definition\'s names from the call\'s values.'),
      miss('Global variables available everywhere in the program', 'Parameters are local to the function, not global.', 'These names exist only inside the function.'),
      miss('Return values that the function sends back', 'Return values come from a return statement, not from the parameter list.', 'Look at where these names appear in the definition.'),
    ],
    lesson: 'The names listed in a function definition\'s parentheses are parameters: placeholders that receive values when the function is called. The actual values you pass in at call time, such as area(4, 5), are the arguments. Parameters behave like local variables inside the function.',
    source,
    generated: true,
  },
  {
    id: 7987024,
    chapter: 'Functions',
    title: 'Return versus print',
    prompt: 'What is the key difference between a function that uses return and one that only uses print?',
    correct: 'return sends a value back to the caller for further use, while print only displays text on the screen',
    wrong: [
      miss('There is no difference; return and print do the same thing', 'They serve different purposes: one produces a usable value, the other shows output.', 'Ask whether the result can be stored in a variable afterward.'),
      miss('print can send a value back to be stored, but return cannot', 'It is the reverse: only return hands a value back for storage or reuse.', 'Think about which one lets you write result = func().'),
      miss('return displays text and print stores a value', 'This swaps their roles; print displays and return gives back a value.', 'Match each keyword to displaying versus returning.'),
    ],
    lesson: 'The print function only displays output on the screen; it does not produce a value you can keep. The return statement, by contrast, sends a value back to wherever the function was called, so you can store it in a variable or use it in further calculations. A function that prints but does not return effectively returns None.',
    source,
    generated: true,
  },
  {
    id: 7987025,
    chapter: 'Functions',
    title: 'Function with no return',
    prompt: 'If a function ends without any return statement, what value does a call to it produce?',
    correct: 'None',
    wrong: [
      miss('0', 'Python does not default missing returns to zero; it returns the special value None.', 'The default is a special empty value, not a number.'),
      miss('An empty string ""', 'Missing returns give None, not an empty string.', 'The default result is a unique placeholder value.'),
      miss('It causes an error because every function must return something', 'Functions without a return are valid; they simply return None.', 'A return statement is optional.'),
    ],
    lesson: 'When a function has no return statement (or reaches the end without hitting one), Python automatically returns the special value None, which represents the absence of a value. Such functions are typically called for their side effects, like printing or modifying data, rather than for a result you store.',
    source,
    generated: true,
  },
  {
    id: 7987026,
    chapter: 'Functions',
    title: 'Local variable scope',
    prompt: 'A variable created inside a function and used only within it has what scope?',
    correct: 'Local scope: it exists only while the function runs and cannot be used outside it',
    wrong: [
      miss('Global scope, so it can be used anywhere in the program', 'Variables created inside a function are local and disappear when the function ends.', 'Consider where the variable can still be read after the function finishes.'),
      miss('Permanent scope, lasting until the program ends', 'A local variable\'s lifetime ends when the function call returns.', 'The variable does not outlive the function call.'),
      miss('No scope, because variables inside functions are not real variables', 'They are genuine variables, just confined to the function.', 'Local variables are real but limited in reach.'),
    ],
    lesson: 'A variable defined inside a function has local scope: it is created when the function is called and destroyed when the function returns, and code outside the function cannot see it. This isolation prevents functions from accidentally interfering with each other\'s data. To share a value, return it from the function.',
    source,
    generated: true,
  },
  {
    id: 7987027,
    chapter: 'Functions',
    title: 'Why use functions',
    prompt: 'Which of these is the strongest reason to break a long program into functions?',
    correct: 'Functions let you reuse code and avoid repeating the same logic, making programs easier to read and maintain',
    wrong: [
      miss('Functions make a program run hundreds of times faster automatically', 'Functions organize code; they do not by themselves give large automatic speed boosts.', 'The main benefit is about structure, not raw speed.'),
      miss('Functions are required by Python; a program will not run without them', 'You can write valid Python with no functions at all.', 'Functions are a design choice, not a requirement.'),
      miss('Functions reduce the file size on disk dramatically', 'Saving disk space is not the purpose of using functions.', 'Think about reuse and readability, not storage.'),
    ],
    lesson: 'Functions support decomposition: breaking a large problem into smaller, named, reusable pieces. By writing logic once in a function and calling it wherever needed, you avoid duplicated code, reduce bugs, and make programs easier to read, test, and update. This reuse and organization is their core benefit.',
    source,
    generated: true,
  },
  {
    id: 7987028,
    chapter: 'Functions',
    title: 'Order of function definition and call',
    prompt: 'For a normal top-level function call to work in a Python script, when must the function be defined relative to the call?',
    correct: 'The def must run before the line that calls the function',
    wrong: [
      miss('The function can be defined anywhere, even after the call, and Python will find it', 'Python executes top to bottom; calling a name before its def runs raises a NameError.', 'The interpreter must have already run the def.'),
      miss('The function must be defined inside the same function that calls it', 'Functions are usually defined at the top level and called from elsewhere; nesting is not required.', 'Definitions do not have to be nested in callers.'),
      miss('The order never matters because Python reads the whole file twice', 'Python does not pre-scan and run definitions; it runs lines in order.', 'Code executes once, top to bottom.'),
    ],
    lesson: 'Python runs a script from top to bottom, and a def statement creates the function only when that line executes. So the def must appear and run before any line that calls the function; otherwise Python raises a NameError because the name is not yet defined. This is why definitions usually go near the top.',
    source,
    generated: true,
  },

  // ---- Unit 5: Lists ----
  {
    id: 7987029,
    chapter: 'Lists',
    title: 'List index of first element',
    prompt: 'Given scores = [90, 85, 70, 60], what does scores[0] evaluate to?',
    correct: '90',
    wrong: [
      miss('85', 'Indexing starts at 0, so index 0 is the first element, not the second.', 'The first item sits at index 0.'),
      miss('The whole list [90, 85, 70, 60]', 'scores[0] selects a single element, not the entire list.', 'A subscript picks one item.'),
      miss('60', 'That is the last element; the first index is 0, not the end.', 'Index 0 points to the front, not the back.'),
    ],
    lesson: 'Python lists are zero-indexed, meaning the first element is at index 0, the second at index 1, and so on. So scores[0] gives 90. To reach the last element you can use index -1, which here gives 60.',
    source,
    generated: true,
  },
  {
    id: 7987030,
    chapter: 'Lists',
    title: 'Negative indexing',
    prompt: 'Given colors = ["red", "green", "blue"], what does colors[-1] return?',
    correct: '"blue"',
    wrong: [
      miss('"red"', 'A negative index counts from the end, so -1 is the last item, not the first.', 'Negative indices count backward from the end.'),
      miss('An error, because indices cannot be negative', 'Python supports negative indices as a shorthand for counting from the end.', 'Negative indices are valid in Python.'),
      miss('"green"', '-1 refers to the final element; -2 would be the second from the end.', 'The most negative position closest to zero is the very last item.'),
    ],
    lesson: 'Negative indices count backward from the end of a list, with -1 referring to the last element, -2 to the second-to-last, and so on. So colors[-1] returns "blue", the final element. This avoids having to compute len(list) - 1 to reach the end.',
    source,
    generated: true,
  },
  {
    id: 7987031,
    chapter: 'Lists',
    title: 'The append method',
    prompt: 'After nums = [1, 2, 3] then nums.append(4), what does nums contain?',
    correct: '[1, 2, 3, 4]',
    wrong: [
      miss('[4, 1, 2, 3]', 'append adds the item to the end of the list, not the beginning.', 'append puts new items at the back.'),
      miss('[1, 2, 4]', 'append does not replace any element; it adds a new one.', 'The list grows; nothing is overwritten.'),
      miss('[1, 2, 3] (unchanged, with 4 returned separately)', 'append modifies the list in place and returns None; the new item is added to the list itself.', 'append changes the list directly.'),
    ],
    lesson: 'The append method adds a single new element to the end of an existing list, modifying the list in place. So after nums.append(4), the list becomes [1, 2, 3, 4]. Note that append returns None, so you should not write nums = nums.append(4).',
    source,
    generated: true,
  },
  {
    id: 7987032,
    chapter: 'Lists',
    title: 'The len function',
    prompt: 'What does len(["a", "b", "c", "d"]) return?',
    correct: '4',
    wrong: [
      miss('3', 'len counts all elements, so a four-item list has length 4, not 3.', 'Count every element, starting the count at 1.'),
      miss('"d"', 'len returns the number of elements, not the last element.', 'The result is a count, not an item.'),
      miss('0', 'A non-empty list has a length equal to its number of items.', 'Only an empty list has length 0.'),
    ],
    lesson: 'The built-in len function returns the number of elements in a list (or characters in a string). The list ["a", "b", "c", "d"] has four elements, so len returns 4. Because indices start at 0, the valid indices for this list are 0 through 3.',
    source,
    generated: true,
  },
  {
    id: 7987033,
    chapter: 'Lists',
    title: 'Modifying a list element',
    prompt: 'Given data = [10, 20, 30], what is the effect of the statement data[1] = 99?',
    correct: 'It changes the element at index 1 to 99, giving [10, 99, 30]',
    wrong: [
      miss('It adds 99 as a new element, giving [10, 20, 30, 99]', 'Assigning to an existing index replaces that element rather than adding one; append adds.', 'Index assignment overwrites, it does not extend.'),
      miss('It changes the first element, giving [99, 20, 30]', 'Index 1 is the second element, not the first; the first is index 0.', 'Remember lists start counting at 0.'),
      miss('It causes an error because lists cannot be changed', 'Lists are mutable, so individual elements can be reassigned.', 'Lists can be edited in place.'),
    ],
    lesson: 'Lists are mutable, meaning you can change their contents after creation. Assigning to a valid index, like data[1] = 99, replaces the element at that position. Since index 1 is the second element (20), the list becomes [10, 99, 30].',
    source,
    generated: true,
  },
  {
    id: 7987034,
    chapter: 'Lists',
    title: 'The in operator with lists',
    prompt: 'Given fruits = ["apple", "pear", "kiwi"], what does the expression "pear" in fruits evaluate to?',
    correct: 'True',
    wrong: [
      miss('1', 'The in operator returns a boolean (True or False), not the index or a count.', 'Membership tests give a yes/no value.'),
      miss('"pear"', 'in reports whether the item is present, not the item itself.', 'The result is True or False, not the value searched for.'),
      miss('False', '"pear" is indeed an element of the list, so the test is True.', 'Check whether the value appears in the list.'),
    ],
    lesson: 'The in operator tests for membership and returns the boolean True if the value is an element of the list, otherwise False. Since "pear" is in the list, the expression is True. This is a convenient way to check whether a list contains a particular value before acting on it.',
    source,
    generated: true,
  },
  {
    id: 7987035,
    chapter: 'Lists',
    title: 'Summing a list with a loop',
    prompt: 'What value does total hold after this code: total = 0; for n in [4, 5, 6]: total = total + n?',
    correct: '15',
    wrong: [
      miss('6', 'The loop adds each element cumulatively; it does not just keep the last value.', 'Each pass adds to the running total, not replaces it.'),
      miss('0', 'total starts at 0 but is increased by each element during the loop.', 'Trace total after each iteration.'),
      miss('456', 'The numbers are added arithmetically (4+5+6), not joined as digits.', 'These are integers being summed, not strings.'),
    ],
    lesson: 'This is the accumulator pattern applied to a list. total starts at 0, then the loop adds each element in turn: 0 + 4 = 4, 4 + 5 = 9, 9 + 6 = 15. After the loop, total holds 15. The same result comes from the built-in sum([4, 5, 6]).',
    source,
    generated: true,
  },
  {
    id: 7987036,
    chapter: 'Lists',
    title: 'List slicing',
    prompt: 'Given letters = ["a", "b", "c", "d", "e"], what does letters[1:3] return?',
    correct: '["b", "c"]',
    wrong: [
      miss('["b", "c", "d"]', 'The end index 3 is excluded, so the slice stops before index 3.', 'Slices stop just before the second index.'),
      miss('["a", "b", "c"]', 'The slice starts at index 1, not index 0.', 'The first number tells where the slice begins.'),
      miss('["c", "d"]', 'Index 1 is "b", not "c"; remember the list is zero-indexed.', 'Index 1 is the second element.'),
    ],
    lesson: 'A slice list[start:stop] returns a new list containing the elements from index start up to but not including index stop. So letters[1:3] takes elements at indices 1 and 2, which are "b" and "c". The stop index is always excluded, mirroring how range works.',
    source,
    generated: true,
  },

  // ---- Unit 6: Dictionaries ----
  {
    id: 7987037,
    chapter: 'Dictionaries',
    title: 'Accessing a dictionary value',
    prompt: 'Given ages = {"Ana": 16, "Ben": 17}, what does ages["Ben"] return?',
    correct: '17',
    wrong: [
      miss('"Ben"', 'Indexing a dictionary by a key returns the associated value, not the key itself.', 'A key looks up its paired value.'),
      miss('1', 'Dictionaries are accessed by key, not by a numeric position like a list.', 'You do not use position numbers with dictionaries.'),
      miss('16', 'That is the value paired with "Ana"; the key "Ben" maps to a different value.', 'Match the requested key to its own value.'),
    ],
    lesson: 'A dictionary stores key-value pairs, and you retrieve a value by writing the dictionary name followed by the key in square brackets. So ages["Ben"] looks up the key "Ben" and returns its value, 17. Unlike lists, dictionaries are indexed by keys rather than by numeric positions.',
    source,
    generated: true,
  },
  {
    id: 7987038,
    chapter: 'Dictionaries',
    title: 'Adding a key-value pair',
    prompt: 'Given prices = {"pen": 2}, what is the effect of prices["book"] = 9?',
    correct: 'It adds a new key "book" with the value 9, giving {"pen": 2, "book": 9}',
    wrong: [
      miss('It causes an error because "book" does not exist yet', 'Assigning to a key that is not present creates it; you do not need to predefine keys.', 'Assignment can create a brand-new key.'),
      miss('It replaces the value of "pen" with 9', 'The assignment targets the key "book", leaving "pen" unchanged.', 'Only the named key is affected.'),
      miss('It changes the key "pen" to "book"', 'Keys are not renamed by assignment; a new pair is added instead.', 'Assignment sets a value, it does not rename keys.'),
    ],
    lesson: 'Assigning to a dictionary key sets that key\'s value. If the key already exists, its value is replaced; if it does not exist, the key is created. So prices["book"] = 9 adds the new pair, leaving "pen" untouched. This is how dictionaries grow dynamically.',
    source,
    generated: true,
  },
  {
    id: 7987039,
    chapter: 'Dictionaries',
    title: 'Looping over a dictionary',
    prompt: 'When you write for k in my_dict: in Python, what does the loop variable k take on each iteration?',
    correct: 'Each key of the dictionary, one at a time',
    wrong: [
      miss('Each value of the dictionary', 'Iterating directly over a dictionary yields its keys; to get values you use my_dict.values().', 'The default iteration gives keys, not values.'),
      miss('Each key-value pair as a single combined string', 'You get keys alone, not joined pairs; use my_dict.items() for pairs.', 'A plain loop hands you keys only.'),
      miss('The number of items in the dictionary', 'The loop variable holds keys, not a count; len gives the count.', 'k is a key, not a length.'),
    ],
    lesson: 'Iterating over a dictionary with for k in my_dict: visits each key in turn. To access the matching value inside the loop, you write my_dict[k]. If you want values directly, loop over my_dict.values(), and for both at once, loop over my_dict.items().',
    source,
    generated: true,
  },
  {
    id: 7987040,
    chapter: 'Dictionaries',
    title: 'Valid dictionary keys',
    prompt: 'Which property must a value have to be usable as a dictionary key in Python?',
    correct: 'It must be unique within the dictionary (duplicate keys are not allowed)',
    wrong: [
      miss('It must be a number; only numbers can be keys', 'Strings, numbers, and other immutable values can all be keys, not just numbers.', 'Keys are not limited to numbers.'),
      miss('It must appear in alphabetical order with the other keys', 'Dictionaries do not require keys to be sorted; order is not a constraint on validity.', 'There is no alphabetical requirement.'),
      miss('It must be the same data type as the value it maps to', 'A key and its value can be completely different types.', 'Key type and value type are independent.'),
    ],
    lesson: 'Each key in a dictionary must be unique, because the key is how you look up its value; if you assign to an existing key, you simply overwrite the old value. Keys must also be of an immutable type (such as strings or numbers), but they do not need to match the type of their values or be in any particular order.',
    source,
    generated: true,
  },
  {
    id: 7987041,
    chapter: 'Dictionaries',
    title: 'Checking for a key',
    prompt: 'Given inventory = {"sword": 1, "shield": 2}, what does "potion" in inventory evaluate to?',
    correct: 'False',
    wrong: [
      miss('True', 'The in operator on a dictionary checks the keys, and "potion" is not a key.', 'Look at whether "potion" appears among the keys.'),
      miss('0', 'in returns a boolean (True or False), not a number.', 'A membership test gives a yes/no value.'),
      miss('An error, because you can only check lists with in', 'in works on dictionaries too, testing the keys.', 'in is valid on dictionaries.'),
    ],
    lesson: 'When used with a dictionary, the in operator tests whether a value is one of the keys, returning True or False. Since "potion" is not a key in inventory, the expression is False. Checking key membership with in before indexing avoids a KeyError from looking up a missing key.',
    source,
    generated: true,
  },
  {
    id: 7987042,
    chapter: 'Dictionaries',
    title: 'List versus dictionary choice',
    prompt: 'You want to store each student\'s name together with their test score and look up a score quickly by name. Which structure fits best?',
    correct: 'A dictionary mapping each name to a score',
    wrong: [
      miss('A single list of all the scores with no names', 'Without names you cannot look up a score by student; you lose the association.', 'You need to connect each score to a name.'),
      miss('A single integer variable holding all scores at once', 'One integer cannot hold multiple named scores.', 'A single number cannot store many labeled values.'),
      miss('A boolean for each student', 'A boolean only stores True or False, not a numeric score.', 'Scores are numbers, not yes/no values.'),
    ],
    lesson: 'A dictionary is ideal when you need to associate each item with a unique label and look items up by that label. Mapping names (keys) to scores (values) lets you retrieve any student\'s score directly with scores["Ana"]. A plain list keeps order but has no built-in way to look up by name.',
    source,
    generated: true,
  },

  // ---- Unit 7: Binary and data representation ----
  {
    id: 7987043,
    chapter: 'Binary and data representation',
    title: 'Bits and binary digits',
    prompt: 'In the binary number system used by computers, how many distinct digits are available?',
    correct: 'Two (0 and 1)',
    wrong: [
      miss('Ten (0 through 9)', 'That is the decimal system; binary uses only two symbols.', 'Binary means base two.'),
      miss('Eight (0 through 7)', 'That describes octal, not binary.', 'Count the symbols a single binary digit can be.'),
      miss('Sixteen (0 through F)', 'That is hexadecimal; binary has far fewer symbols.', 'A bit is the smallest unit, with only two states.'),
    ],
    lesson: 'The binary number system is base 2, so it uses only two digits: 0 and 1. A single binary digit is called a bit, and computers represent all data using sequences of bits because electronic components naturally have two stable states (off and on). Decimal uses ten digits and hexadecimal uses sixteen.',
    source,
    generated: true,
  },
  {
    id: 7987044,
    chapter: 'Binary and data representation',
    title: 'Binary to decimal',
    prompt: 'What is the decimal (base-10) value of the binary number 1011?',
    correct: '11',
    wrong: [
      miss('1011', 'You cannot just read the digits as decimal; each position has a power-of-two place value.', 'Multiply each bit by its place value of 8, 4, 2, or 1.'),
      miss('13', 'This likely adds the place values wrong; 1011 is 8 + 0 + 2 + 1, not 8 + 4 + 1.', 'The second bit from the left is 0, so the 4s place is not counted.'),
      miss('7', 'This counts 4 + 2 + 1 and ignores the leftmost 8s place that is set.', 'The leftmost 1 represents 8.'),
    ],
    lesson: 'In binary, each position from right to left represents an increasing power of two: 1, 2, 4, 8, and so on. For 1011, the set bits are in the 8s, 2s, and 1s places: 8 + 0 + 2 + 1 = 11. Adding up the place values of the 1 bits converts binary to decimal.',
    source,
    generated: true,
  },
  {
    id: 7987045,
    chapter: 'Binary and data representation',
    title: 'Bits in a byte',
    prompt: 'How many bits make up one byte?',
    correct: '8',
    wrong: [
      miss('1', 'A single bit is the smallest unit; a byte is a group of several bits.', 'A byte is larger than a single bit.'),
      miss('4', 'Four bits form a nibble, which is half a byte.', 'A byte is twice that size.'),
      miss('16', 'Sixteen bits make two bytes, not one.', 'One byte is half of sixteen.'),
    ],
    lesson: 'A byte is a standard grouping of 8 bits. With 8 bits you can represent 2 to the 8th power, or 256, different values (0 through 255), which is enough to encode a single character of text in schemes like ASCII. A group of 4 bits is called a nibble.',
    source,
    generated: true,
  },
  {
    id: 7987046,
    chapter: 'Binary and data representation',
    title: 'Encoding characters',
    prompt: 'Why must text characters like the letter A be converted to numbers before a computer can store them?',
    correct: 'Computers ultimately store all data as binary numbers, so each character is mapped to a numeric code',
    wrong: [
      miss('Because letters take up more space than numbers and must be shrunk', 'The reason is that hardware stores binary, not that letters are inherently larger.', 'Think about what form all data takes inside the machine.'),
      miss('Because Python cannot display letters, only numbers', 'Python displays letters fine; the conversion happens at the storage level using character codes.', 'The issue is how data is stored, not displayed.'),
      miss('Because numbers are easier for humans to read than letters', 'The conversion is for the machine, not for human readability.', 'Consider the needs of the hardware, not the reader.'),
    ],
    lesson: 'At the lowest level, computers store and process everything as binary numbers. To handle text, each character is assigned a numeric code using a standard like ASCII or Unicode; for example, the letter A is code 65. The computer stores that number in binary and converts it back to the character for display.',
    source,
    generated: true,
  },

  // ---- Unit 8: Debugging and program design ----
  {
    id: 7987047,
    chapter: 'Debugging and program design',
    title: 'Syntax versus logic errors',
    prompt: 'A program runs without crashing but always prints the wrong answer. What kind of error is this most likely to be?',
    correct: 'A logic error: the code is valid Python but the algorithm is incorrect',
    wrong: [
      miss('A syntax error', 'A syntax error stops the program from running at all; here the program runs but gives wrong output.', 'Syntax errors prevent the program from running.'),
      miss('An indentation error', 'An indentation error is a kind of syntax error and would prevent the code from running.', 'The program runs, so it is not refusing to start.'),
      miss('No error at all, since the program ran successfully', 'Running successfully does not mean it is correct; wrong output signals a logic bug.', 'Correct execution and correct results are different things.'),
    ],
    lesson: 'A syntax error breaks the rules of the language and stops the program from running. A logic error is different: the code is grammatically valid and runs to completion, but the algorithm produces the wrong result. Logic errors are often harder to find because there is no error message; you must trace the program\'s reasoning.',
    source,
    generated: true,
  },
  {
    id: 7987048,
    chapter: 'Debugging and program design',
    title: 'Reading a traceback',
    prompt: 'A Python program stops and shows a message ending in NameError: name \'totl\' is not defined. What is the most likely cause?',
    correct: 'A variable name was misspelled, so Python does not recognize totl',
    wrong: [
      miss('The computer has run out of memory', 'A NameError is about an unrecognized name, not memory; out-of-memory gives a different error.', 'The message points to an unknown name.'),
      miss('The internet connection was lost', 'NameError has nothing to do with networking.', 'The error is about a name in the code.'),
      miss('The program needs to be run a second time', 'Re-running will not fix a misspelled name; the code must be corrected.', 'Fix the name, then run again.'),
    ],
    lesson: 'A NameError means Python encountered a name it has not seen defined, very often because of a typo. Here totl was probably meant to be total. The traceback tells you the error type and the offending name, so reading it carefully points you straight to the bug.',
    source,
    generated: true,
  },
  {
    id: 7987049,
    chapter: 'Debugging and program design',
    title: 'Purpose of comments',
    prompt: 'What is the effect of a line beginning with the # symbol in Python?',
    correct: 'It is a comment that Python ignores when running the program; it is there to explain the code to humans',
    wrong: [
      miss('It is a special command that runs faster than normal code', 'Comments are not executed at all, so they have no runtime effect or speed.', 'The interpreter skips comment lines entirely.'),
      miss('It causes a syntax error because # is not allowed', '# is the valid character that starts a comment in Python.', 'The hash mark is the standard comment symbol.'),
      miss('It prints the text after it to the screen', 'Comments are ignored, not printed; print() is what displays text.', 'Comments produce no output.'),
    ],
    lesson: 'A line (or part of a line) beginning with # is a comment. Python ignores comments when executing the program; they exist purely to explain the code to people reading it. Well-placed comments make programs easier to understand and maintain, but they never affect what the program does.',
    source,
    generated: true,
  },
  {
    id: 7987050,
    chapter: 'Debugging and program design',
    title: 'Decomposition in problem solving',
    prompt: 'In computational thinking, what does decomposition mean?',
    correct: 'Breaking a large, complex problem down into smaller, more manageable sub-problems',
    wrong: [
      miss('Deleting parts of a program that are no longer needed', 'Decomposition is about dividing a problem, not removing code.', 'Think about splitting a problem, not erasing it.'),
      miss('Hiding the details of how a part works behind a simple interface', 'That describes abstraction, a related but distinct idea.', 'This term is specifically about breaking things into parts.'),
      miss('Finding patterns that repeat across different problems', 'That describes pattern recognition, not decomposition.', 'Focus on splitting rather than spotting similarities.'),
    ],
    lesson: 'Decomposition is a core computational-thinking skill: taking a big, intimidating problem and breaking it into smaller pieces that are each easier to solve. Once solved, the pieces combine into a full solution. It is closely related to writing functions, where each function handles one sub-problem.',
    source,
    generated: true,
  },
  {
    id: 7987051,
    chapter: 'Debugging and program design',
    title: 'Tracing code by hand',
    prompt: 'What is the main purpose of a trace table (hand-tracing) when debugging a program?',
    correct: 'To record how each variable\'s value changes step by step as the code runs, revealing where it goes wrong',
    wrong: [
      miss('To make the program execute faster', 'Tracing is a debugging technique for understanding behavior, not a performance optimization.', 'It helps you find bugs, not speed up code.'),
      miss('To automatically fix any errors in the code', 'Tracing helps you locate problems, but you still fix them yourself.', 'Tracing reveals bugs; it does not repair them.'),
      miss('To translate the code into another programming language', 'A trace table tracks values; it has nothing to do with translation.', 'Its job is following variable values.'),
    ],
    lesson: 'A trace table lets you step through code by hand, writing down the value of each variable after every statement. By following the program exactly as the computer would, you can pinpoint the moment a variable takes on an unexpected value, which reveals the location of a logic error.',
    source,
    generated: true,
  },

  // ---- Unit 9: Strings ----
  {
    id: 7987052,
    chapter: 'Strings',
    title: 'String concatenation',
    prompt: 'What is the result of the expression "Hello" + "World" in Python?',
    correct: '"HelloWorld"',
    wrong: [
      miss('"Hello World" (with a space)', 'The + operator joins strings exactly as given; it does not insert a space.', 'No space is added unless you include one.'),
      miss('An error, because you cannot add words', 'For strings, + means concatenation, which is allowed.', 'The + sign joins strings rather than adding numbers.'),
      miss('"Hello + World"', 'The + operator combines the two strings; it is not kept as literal text.', 'The operator acts; it does not appear in the result.'),
    ],
    lesson: 'When applied to two strings, the + operator performs concatenation, joining them end to end with no characters added in between. So "Hello" + "World" gives "HelloWorld". To include a space you would write "Hello" + " " + "World".',
    source,
    generated: true,
  },
  {
    id: 7987053,
    chapter: 'Strings',
    title: 'Indexing a string',
    prompt: 'Given word = "Python", what does word[0] return?',
    correct: '"P"',
    wrong: [
      miss('"y"', 'Index 0 is the first character; "y" is at index 1.', 'Strings are zero-indexed like lists.'),
      miss('"Python"', 'word[0] selects a single character, not the whole string.', 'A single subscript picks one character.'),
      miss('"n"', '"n" is the last character; the first character is at index 0.', 'Index 0 is at the start, not the end.'),
    ],
    lesson: 'Strings are sequences of characters and, like lists, are zero-indexed. So word[0] gives the first character "P", word[1] gives "y", and so on. You can also use negative indices, so word[-1] would give the last character "n".',
    source,
    generated: true,
  },
  {
    id: 7987054,
    chapter: 'Strings',
    title: 'Converting a number to a string',
    prompt: 'You have an integer count = 5 and want to build the message "Score: 5" using concatenation. Which expression works?',
    correct: '"Score: " + str(count)',
    wrong: [
      miss('"Score: " + count', 'You cannot concatenate a string with an integer directly; this raises a TypeError.', 'The number must be converted to text first.'),
      miss('"Score: " + int(count)', 'count is already an int; int() does not turn it into a string, so the concatenation still fails.', 'You need a function that produces text.'),
      miss('str("Score: ") + count', 'The text is already a string, but count is still an int, so this still fails.', 'It is the number, not the text, that needs converting.'),
    ],
    lesson: 'Python will not let you join a string and an integer with +, because the operator does not know whether to concatenate or add. You must convert the number to a string first using str(), so "Score: " + str(count) produces "Score: 5". The str() function turns any value into its text form.',
    source,
    generated: true,
  },
])
