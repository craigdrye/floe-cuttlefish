import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe softwareDesign top-up'

export const softwareDesignUniv103TopUpQuestions: Question[] = makeQuestionBank('University', [
  // ---------------- Chapter 1: SOLID Principles ----------------
  {
    id: 7702000,
    chapter: 'SOLID Principles',
    title: 'Single Responsibility',
    prompt: 'According to Robert Martin, the Single Responsibility Principle is best stated as which of the following?',
    correct: 'A module should have only one reason to change, meaning it should be responsible to one actor or stakeholder.',
    wrong: [
      miss('A class should contain only one public method.', 'SRP concerns reasons to change, not a count of methods; cohesive classes routinely expose several methods.', 'Think about who requests changes, not how many methods exist.'),
      miss('A function should do only one thing and call no other functions.', 'That confuses SRP with a guideline about function size; SRP is a module-level grouping principle.', 'SRP is about reasons to change, not call depth.'),
      miss('Each class should depend on only one other class.', 'That describes a coupling target, not SRP, which is about cohesion of responsibilities.', 'Reread the principle: one reason to change.'),
    ],
    lesson: 'SRP says a module should be responsible to a single actor, so that changes requested by one stakeholder do not ripple into code another stakeholder depends on. Martin reframed "one reason to change" as "one source of requirements" to avoid the common misreading that a class may do only one thing.',
    source, generated: true,
  },
  {
    id: 7702001,
    chapter: 'SOLID Principles',
    title: 'Open/Closed Mechanism',
    prompt: 'The Open/Closed Principle says software entities should be open for extension but closed for modification. Which technique most directly supports this?',
    correct: 'Programming to abstractions so new behavior is added by creating new implementations rather than editing existing ones.',
    wrong: [
      miss('Marking every class final so it can never be changed.', 'Closing for modification does not mean forbidding all change; it means designing so new requirements need new code, not edits.', 'Closure is about extension points, not sealing everything.'),
      miss('Copying a class and editing the copy whenever requirements change.', 'Duplicating code is the modification problem OCP tries to avoid and creates divergent copies.', 'Extension should reuse abstractions, not clone code.'),
      miss('Adding configuration flags and if/else branches for each new case.', 'A growing switch on type is exactly the modification OCP discourages; each new case edits existing code.', 'Prefer polymorphism over expanding conditionals.'),
    ],
    lesson: 'OCP is achieved by isolating the parts that vary behind stable abstractions, then adding new behavior as new implementations of those abstractions. This lets you extend a system without reopening and risking already-tested code.',
    source, generated: true,
  },
  {
    id: 7702002,
    chapter: 'SOLID Principles',
    title: 'Liskov Substitution',
    prompt: 'A subclass violates the Liskov Substitution Principle when it:',
    correct: 'Strengthens a precondition or weakens a postcondition relative to the base type, breaking code that relied on the base contract.',
    wrong: [
      miss('Adds new methods that the base class did not have.', 'Adding methods is fine; LSP concerns whether the subtype can stand in wherever the supertype is expected.', 'Focus on whether base-type code still works with the subtype.'),
      miss('Has more lines of code than its base class.', 'Code size is irrelevant to substitutability; LSP is about behavioral contracts.', 'Think contracts, not length.'),
      miss('Overrides a method to make it faster.', 'A faster override that preserves the contract is perfectly substitutable.', 'Performance changes do not break LSP if behavior holds.'),
    ],
    lesson: 'LSP requires that objects of a subtype be usable anywhere the supertype is expected without breaking correctness. The classic rule is that overrides may weaken preconditions and strengthen postconditions, but never the reverse; the Rectangle/Square problem is a canonical violation.',
    source, generated: true,
  },
  {
    id: 7702003,
    chapter: 'SOLID Principles',
    title: 'Interface Segregation',
    prompt: 'The Interface Segregation Principle advises that:',
    correct: 'Clients should not be forced to depend on methods they do not use, favoring many small role-specific interfaces over one fat interface.',
    wrong: [
      miss('Every class should implement exactly one interface.', 'ISP does not cap interface counts per class; it is about not forcing unused methods on clients.', 'The point is what each client must depend on.'),
      miss('Interfaces should expose every method a class could ever need.', 'A bloated interface is precisely what ISP warns against, since it couples clients to unused operations.', 'Smaller, focused interfaces are preferred.'),
      miss('Implementation details should be public so clients can extend them.', 'That contradicts encapsulation and is unrelated to ISP.', 'ISP is about the size and focus of client-facing contracts.'),
    ],
    lesson: 'ISP keeps interfaces narrow and role-focused so a client only depends on the operations it actually calls. Fat interfaces create accidental coupling: a change to a method used by one client can force recompilation or breakage for unrelated clients.',
    source, generated: true,
  },
  {
    id: 7702004,
    chapter: 'SOLID Principles',
    title: 'Dependency Inversion',
    prompt: 'The Dependency Inversion Principle states that:',
    correct: 'High-level modules and low-level modules should both depend on abstractions, and abstractions should not depend on details.',
    wrong: [
      miss('High-level modules should depend directly on low-level modules to stay efficient.', 'That is the traditional dependency direction DIP inverts; it couples policy to volatile details.', 'DIP flips this so both depend on an abstraction.'),
      miss('Dependencies should always be created with the new keyword inside the class that needs them.', 'Hard-coding construction couples a class to concrete types, the opposite of inverting dependencies.', 'Inject abstractions rather than constructing concretes.'),
      miss('All dependencies should be resolved at compile time, never at runtime.', 'DIP is about depending on abstractions, not about resolution timing; runtime injection is common.', 'The principle is about direction, not timing.'),
    ],
    lesson: 'DIP inverts the conventional dependency direction so that high-level policy does not depend on low-level details; instead both depend on a shared abstraction. This is what makes dependency injection and plugin architectures possible: details plug into stable policy interfaces.',
    source, generated: true,
  },
  {
    id: 7702005,
    chapter: 'SOLID Principles',
    title: 'Dependency Injection vs DIP',
    prompt: 'How does dependency injection relate to the Dependency Inversion Principle?',
    correct: 'Dependency injection is a technique for supplying dependencies from outside a class, and is one common way to realize DIP, but the two are not identical.',
    wrong: [
      miss('They are exactly the same thing under two names.', 'DIP is a design principle about dependency direction; DI is a concrete delivery mechanism that can help satisfy it.', 'A principle and a technique are different categories.'),
      miss('Dependency injection always violates the Dependency Inversion Principle.', 'DI is a primary means of achieving DIP, not a violation of it.', 'Injecting abstractions supports inversion.'),
      miss('Dependency injection only works with reflection-based frameworks.', 'DI can be done by hand through constructors or setters without any framework.', 'Frameworks automate DI but are not required.'),
    ],
    lesson: 'DIP is the goal: depend on abstractions, not concretions. Dependency injection is one mechanism to reach it by passing collaborators in rather than constructing them internally. You can apply DIP without a DI container, and a container does not guarantee DIP if you inject concrete classes.',
    source, generated: true,
  },

  // ---------------- Chapter 2: Creational Design Patterns ----------------
  {
    id: 7702006,
    chapter: 'Creational Design Patterns',
    title: 'Factory Method Intent',
    prompt: 'The Factory Method pattern primarily lets a class:',
    correct: 'Defer instantiation of an object to subclasses, so the class can work with products without knowing their concrete types.',
    wrong: [
      miss('Create families of related objects through a single object.', 'That is the Abstract Factory pattern; Factory Method produces one product via an overridable method.', 'Abstract Factory handles families; Factory Method handles one product.'),
      miss('Ensure only a single instance of a class exists.', 'That is the Singleton pattern, which controls instance count, not subclass-chosen construction.', 'Singleton is about uniqueness, not deferred instantiation.'),
      miss('Build a complex object step by step with a director.', 'That is the Builder pattern, which separates construction from representation.', 'Builder assembles; Factory Method selects a type.'),
    ],
    lesson: 'Factory Method defines an interface for creating an object but lets subclasses decide which class to instantiate, deferring construction to a polymorphic method. It decouples client code from concrete product classes while keeping a single product hierarchy.',
    source, generated: true,
  },
  {
    id: 7702007,
    chapter: 'Creational Design Patterns',
    title: 'Abstract Factory',
    prompt: 'Abstract Factory is most appropriate when you need to:',
    correct: 'Create families of related products that must be used together, while keeping clients independent of concrete classes.',
    wrong: [
      miss('Guarantee a class has just one global instance.', 'That is Singleton; Abstract Factory is about producing consistent families of objects.', 'Think families of products, not instance count.'),
      miss('Add responsibilities to an object dynamically at runtime.', 'That is the Decorator pattern, a structural concern, not object family creation.', 'Decorator wraps; Abstract Factory constructs families.'),
      miss('Convert one interface into another a client expects.', 'That is the Adapter pattern; it does no family creation.', 'Adapter translates interfaces, not creates products.'),
    ],
    lesson: 'Abstract Factory provides an interface for creating whole families of related or dependent objects without specifying concrete classes, ensuring products from one family are used together. A common example is a UI toolkit producing matching buttons, scrollbars, and menus per platform.',
    source, generated: true,
  },
  {
    id: 7702008,
    chapter: 'Creational Design Patterns',
    title: 'Builder Purpose',
    prompt: 'The Builder pattern is most useful when:',
    correct: 'An object requires many steps or optional parameters to construct, and you want to separate the construction process from the final representation.',
    wrong: [
      miss('You need to ensure only one instance of an object exists.', 'That is Singleton; Builder is about staged construction of complex objects.', 'Builder constructs; it does not limit instances.'),
      miss('You want to clone an existing object to avoid expensive initialization.', 'That is the Prototype pattern, which copies an exemplar.', 'Prototype copies; Builder assembles step by step.'),
      miss('You want subclasses to choose which product type to create.', 'That is Factory Method; Builder fixes the type but varies how it is assembled.', 'Builder controls assembly, not type selection.'),
    ],
    lesson: 'Builder separates the construction of a complex object from its representation, so the same construction process can yield different representations. It shines when a constructor would otherwise need many parameters, replacing telescoping constructors with a fluent, readable assembly process.',
    source, generated: true,
  },
  {
    id: 7702009,
    chapter: 'Creational Design Patterns',
    title: 'Singleton Drawbacks',
    prompt: 'A frequently cited criticism of the Singleton pattern in modern design is that it:',
    correct: 'Introduces global mutable state and hidden dependencies that make code harder to test and reason about.',
    wrong: [
      miss('Cannot be implemented in object-oriented languages.', 'Singletons are easily implemented in OO languages; the criticism is about their design effects, not feasibility.', 'The issue is testability and coupling, not implementability.'),
      miss('Always requires multithreaded locking even in single-threaded apps.', 'Lazy initialization needs care under concurrency, but locking is not inherently required in single-threaded use.', 'The core complaint is global state, not mandatory locks.'),
      miss('Violates the Single Responsibility Principle by definition.', 'A Singleton may have one responsibility; the concern is that it also controls its own lifecycle and is globally reachable.', 'The drawback is global access and testability.'),
    ],
    lesson: 'Singleton couples callers to a global access point and hidden shared state, which makes substitution in tests difficult and obscures who depends on what. Many teams prefer a single instance managed by a dependency-injection container, which keeps one instance without the global access point.',
    source, generated: true,
  },
  {
    id: 7702010,
    chapter: 'Creational Design Patterns',
    title: 'Prototype Pattern',
    prompt: 'The Prototype pattern creates new objects by:',
    correct: 'Cloning an existing prototypical instance rather than instantiating a class directly.',
    wrong: [
      miss('Reading a class name from configuration and using reflection.', 'That is a reflective factory approach, not Prototype, which copies an existing object.', 'Prototype copies an instance, not a class name.'),
      miss('Calling a subclass-overridden creation method.', 'That is Factory Method; Prototype relies on a clone operation on an exemplar.', 'Prototype clones; Factory Method overrides creation.'),
      miss('Maintaining a single shared instance returned to all callers.', 'That is Singleton; Prototype produces independent copies.', 'Prototype yields copies, not one shared object.'),
    ],
    lesson: 'Prototype specifies the kinds of objects to create using a prototypical instance and produces new objects by copying that prototype. It is valuable when instantiation is expensive or when the set of object configurations is determined at runtime rather than compile time.',
    source, generated: true,
  },

  // ---------------- Chapter 3: Structural Design Patterns ----------------
  {
    id: 7702011,
    chapter: 'Structural Design Patterns',
    title: 'Adapter vs Decorator',
    prompt: 'What is the key difference between the Adapter and Decorator patterns?',
    correct: 'Adapter changes an object’s interface to match what a client expects, while Decorator keeps the same interface but adds responsibilities.',
    wrong: [
      miss('Adapter adds new behavior, while Decorator only renames methods.', 'That reverses their roles: Decorator adds behavior, Adapter reconciles incompatible interfaces.', 'Decorator adds behavior; Adapter converts interfaces.'),
      miss('Both change the interface, but Adapter does it at compile time only.', 'Decorator preserves the interface; the distinction is not about compile time.', 'Focus on whether the interface changes.'),
      miss('Adapter and Decorator are two names for the same pattern.', 'They have distinct intents: interface conversion versus responsibility addition.', 'Their intents differ even if both wrap an object.'),
    ],
    lesson: 'Both patterns wrap an object, but their intent differs: Adapter converts one interface to another so otherwise incompatible classes can collaborate, while Decorator keeps the wrapped object’s interface and transparently layers extra behavior. Decorators are often stackable; adapters typically are not.',
    source, generated: true,
  },
  {
    id: 7702012,
    chapter: 'Structural Design Patterns',
    title: 'Facade Intent',
    prompt: 'The Facade pattern is used to:',
    correct: 'Provide a simplified, unified interface to a complex subsystem, reducing the coupling between clients and that subsystem.',
    wrong: [
      miss('Add behavior to individual objects at runtime.', 'That is Decorator; Facade simplifies access to a whole subsystem.', 'Facade is a front door to many classes, not a wrapper on one.'),
      miss('Ensure subsystem classes are never instantiated more than once.', 'That is Singleton; Facade does not constrain instance counts.', 'Facade simplifies access, not lifecycle.'),
      miss('Let incompatible interfaces work together by translation.', 'That is Adapter; Facade does not translate one interface to another but offers a new high-level one.', 'Facade simplifies; Adapter converts.'),
    ],
    lesson: 'A Facade offers a single high-level entry point that hides the complexity of a subsystem, so clients can perform common tasks without knowing the subsystem’s internal classes. Unlike Adapter it does not aim to match a specific expected interface; it defines a convenient new one.',
    source, generated: true,
  },
  {
    id: 7702013,
    chapter: 'Structural Design Patterns',
    title: 'Composite Pattern',
    prompt: 'The Composite pattern allows clients to:',
    correct: 'Treat individual objects and compositions of objects uniformly through a common interface, modeling part-whole hierarchies.',
    wrong: [
      miss('Add new operations to a class hierarchy without modifying it.', 'That is the Visitor pattern; Composite is about part-whole tree structures.', 'Composite is about tree uniformity, not new operations.'),
      miss('Share fine-grained objects efficiently using intrinsic state.', 'That is the Flyweight pattern, which reduces memory by sharing state.', 'Flyweight shares; Composite composes trees.'),
      miss('Decouple an abstraction from its implementation.', 'That is the Bridge pattern; Composite handles recursive containment.', 'Bridge separates two hierarchies; Composite builds one tree.'),
    ],
    lesson: 'Composite composes objects into tree structures to represent part-whole hierarchies and lets clients treat leaves and branches uniformly. A file system where files and directories share an interface, and operations like size recurse through children, is the textbook example.',
    source, generated: true,
  },
  {
    id: 7702014,
    chapter: 'Structural Design Patterns',
    title: 'Bridge Pattern',
    prompt: 'The Bridge pattern is intended to:',
    correct: 'Decouple an abstraction from its implementation so the two can vary independently.',
    wrong: [
      miss('Compose objects into part-whole tree structures.', 'That is Composite; Bridge separates two distinct hierarchies.', 'Bridge splits abstraction from implementation.'),
      miss('Provide a placeholder that controls access to another object.', 'That is the Proxy pattern; Bridge is about independent variation of two dimensions.', 'Proxy guards access; Bridge separates dimensions.'),
      miss('Convert the interface of a class into one a client expects.', 'That is Adapter; Bridge is designed up front, not retrofitted to reconcile interfaces.', 'Adapter reconciles; Bridge is planned separation.'),
    ],
    lesson: 'Bridge splits a class that would otherwise explode along two dimensions into an abstraction hierarchy and a separate implementation hierarchy connected by a reference. This avoids a combinatorial blow-up of subclasses when both dimensions vary, for example shapes by rendering API.',
    source, generated: true,
  },
  {
    id: 7702015,
    chapter: 'Structural Design Patterns',
    title: 'Proxy Pattern',
    prompt: 'Which scenario is a textbook use of the Proxy pattern?',
    correct: 'Deferring the expensive creation of an object until it is actually needed by standing in with a placeholder (a virtual proxy).',
    wrong: [
      miss('Adding logging behavior by subclassing every concrete class.', 'A proxy provides a stand-in with the same interface; broad subclassing is not the proxy mechanism.', 'Proxy wraps via the same interface, not by subclassing all classes.'),
      miss('Combining many small objects into one large object to save memory.', 'That is closer to Flyweight’s sharing goal, not Proxy’s access control.', 'Proxy controls access; Flyweight shares state.'),
      miss('Letting a client traverse a collection without exposing its structure.', 'That is the Iterator pattern, unrelated to access placeholders.', 'Iterator traverses; Proxy controls access.'),
    ],
    lesson: 'Proxy provides a surrogate with the same interface as the real subject to control access to it. Common variants include virtual proxies for lazy loading, protection proxies for access control, and remote proxies that represent objects in another address space.',
    source, generated: true,
  },
  {
    id: 7702016,
    chapter: 'Structural Design Patterns',
    title: 'Flyweight Pattern',
    prompt: 'The Flyweight pattern reduces memory use by:',
    correct: 'Sharing common intrinsic state across many objects and keeping extrinsic, context-dependent state outside the shared object.',
    wrong: [
      miss('Compressing object fields into a single packed integer.', 'Flyweight shares whole objects; it is not about bit packing.', 'The mechanism is sharing intrinsic state, not compression.'),
      miss('Caching the single global instance of a class.', 'That is Singleton; Flyweight maintains many shared fine-grained objects.', 'Flyweight shares many; Singleton keeps one.'),
      miss('Lazily loading objects through a placeholder.', 'That is a virtual Proxy; Flyweight is about sharing state, not deferring creation.', 'Proxy defers; Flyweight shares.'),
    ],
    lesson: 'Flyweight supports large numbers of fine-grained objects efficiently by separating intrinsic state (shared and context-independent) from extrinsic state (passed in by clients). Rendering thousands of identical glyphs by sharing a single character object and supplying position externally is the classic case.',
    source, generated: true,
  },

  // ---------------- Chapter 4: Behavioral Design Patterns ----------------
  {
    id: 7702017,
    chapter: 'Behavioral Design Patterns',
    title: 'Observer Pattern',
    prompt: 'The Observer pattern is designed to:',
    correct: 'Define a one-to-many dependency so that when one object changes state, all its dependents are notified and updated automatically.',
    wrong: [
      miss('Encapsulate a request as an object so it can be queued or undone.', 'That is the Command pattern; Observer is about change notification.', 'Command parametrizes requests; Observer notifies dependents.'),
      miss('Let an object alter its behavior when its internal state changes.', 'That is the State pattern; Observer broadcasts changes to other objects.', 'State changes behavior; Observer notifies subscribers.'),
      miss('Provide a way to access elements of a collection sequentially.', 'That is the Iterator pattern, unrelated to state-change notification.', 'Iterator traverses; Observer publishes updates.'),
    ],
    lesson: 'Observer establishes a publish-subscribe relationship: a subject maintains a list of observers and notifies them on state changes, keeping them loosely coupled. It underlies event systems and the model layer of MVC, letting views update when the model changes without the model knowing view details.',
    source, generated: true,
  },
  {
    id: 7702018,
    chapter: 'Behavioral Design Patterns',
    title: 'Strategy Pattern',
    prompt: 'The Strategy pattern lets you:',
    correct: 'Define a family of interchangeable algorithms, encapsulate each one, and select which to use at runtime.',
    wrong: [
      miss('Define the skeleton of an algorithm and let subclasses fill in steps.', 'That is the Template Method pattern, which uses inheritance rather than composition of interchangeable algorithms.', 'Template Method uses inheritance; Strategy uses composition.'),
      miss('Allow an object to change behavior when its internal state changes.', 'That is the State pattern; Strategy selects an algorithm explicitly, not by state transitions.', 'State is driven by internal transitions; Strategy by explicit choice.'),
      miss('Notify dependents automatically when an object changes.', 'That is Observer, a notification pattern, not algorithm selection.', 'Observer notifies; Strategy swaps algorithms.'),
    ],
    lesson: 'Strategy encapsulates interchangeable algorithms behind a common interface so the choice can vary independently of the clients that use it, typically via composition. It removes large conditional blocks that select behavior and supports the Open/Closed Principle by adding new strategies without editing the context.',
    source, generated: true,
  },
  {
    id: 7702019,
    chapter: 'Behavioral Design Patterns',
    title: 'Strategy vs State',
    prompt: 'Although Strategy and State share the same structure, their intents differ in that:',
    correct: 'Strategy lets a client choose an algorithm, while State lets an object change its own behavior as its internal state transitions.',
    wrong: [
      miss('Strategy uses inheritance and State uses composition.', 'Both typically use composition with an interface; the structural shape is essentially the same.', 'The difference is intent, not inheritance vs composition.'),
      miss('State objects never know about each other, but strategies always do.', 'There is no such rule; State objects often trigger transitions to one another.', 'The distinguishing factor is intent, not mutual awareness.'),
      miss('Strategy can only hold one algorithm, while State holds many.', 'Both can range over many implementations; the count is not the distinction.', 'Look at who drives the change and why.'),
    ],
    lesson: 'Structurally Strategy and State look alike: a context delegates to an interface with multiple implementations. The difference is purpose and who drives change: in Strategy a client picks the algorithm, whereas in State the object transitions itself between states, often with states aware of their successors.',
    source, generated: true,
  },
  {
    id: 7702020,
    chapter: 'Behavioral Design Patterns',
    title: 'Command Pattern',
    prompt: 'The Command pattern encapsulates a request as an object, which makes it natural to support:',
    correct: 'Queuing, logging, and undo/redo of operations by storing command objects.',
    wrong: [
      miss('Automatic notification of observers when a subject changes.', 'That is Observer; Command is about reifying requests, not broadcasting changes.', 'Command objectifies actions; Observer publishes changes.'),
      miss('Uniform traversal of a collection.', 'That is Iterator; Command does not traverse collections.', 'Iterator walks data; Command represents an action.'),
      miss('Sharing fine-grained objects to save memory.', 'That is Flyweight, a structural memory optimization.', 'Flyweight shares state; Command reifies requests.'),
    ],
    lesson: 'Command turns a request into a standalone object carrying the action and its parameters, decoupling the invoker from the receiver. Because each invocation becomes a stored object, you can queue commands, log them for replay, and implement undo/redo by pairing execute with an inverse operation.',
    source, generated: true,
  },
  {
    id: 7702021,
    chapter: 'Behavioral Design Patterns',
    title: 'Template Method',
    prompt: 'The Template Method pattern works by:',
    correct: 'Defining the skeleton of an algorithm in a base method and letting subclasses override specific steps without changing the algorithm’s structure.',
    wrong: [
      miss('Composing interchangeable algorithm objects selected at runtime.', 'That is Strategy, which uses composition; Template Method uses inheritance and a fixed skeleton.', 'Template Method fixes the skeleton in a superclass.'),
      miss('Letting a request be passed along a chain of handlers.', 'That is Chain of Responsibility, not an algorithm skeleton with overridable steps.', 'Template Method varies steps, not handlers.'),
      miss('Encapsulating each request as an object for later execution.', 'That is Command; Template Method structures one algorithm with hook methods.', 'Command reifies requests; Template Method structures one algorithm.'),
    ],
    lesson: 'Template Method captures the invariant parts of an algorithm in a base class while deferring variable steps to subclasses through overridable (often abstract or hook) methods. It embodies the Hollywood principle, "don’t call us, we’ll call you," because the base class controls the flow and calls into subclass steps.',
    source, generated: true,
  },
  {
    id: 7702022,
    chapter: 'Behavioral Design Patterns',
    title: 'Chain of Responsibility',
    prompt: 'The Chain of Responsibility pattern is used to:',
    correct: 'Pass a request along a chain of handlers, each deciding to handle it or forward it, decoupling sender from receiver.',
    wrong: [
      miss('Encapsulate a request so it can be undone later.', 'That is Command; Chain of Responsibility is about routing a request through handlers.', 'Chain routes; Command reifies and undoes.'),
      miss('Define a one-to-many notification of dependents.', 'That is Observer; the chain forwards to at most the handlers in sequence, not broadcasting to all.', 'Observer broadcasts; Chain forwards along a line.'),
      miss('Provide a unified interface to a complex subsystem.', 'That is Facade, a structural simplification, not a request-routing mechanism.', 'Facade simplifies; Chain forwards requests.'),
    ],
    lesson: 'Chain of Responsibility lets more than one object handle a request by linking handlers; each either processes the request or passes it to the next, so the sender need not know which handler responds. Middleware pipelines and event-bubbling in UIs are practical examples.',
    source, generated: true,
  },
  {
    id: 7702023,
    chapter: 'Behavioral Design Patterns',
    title: 'Visitor Pattern',
    prompt: 'The Visitor pattern is best suited when you want to:',
    correct: 'Add new operations to a stable object structure without modifying the classes of the elements, by moving the operation into a visitor.',
    wrong: [
      miss('Add new element classes to a structure frequently.', 'Visitor makes adding new operations easy but adding new element types hard, since every visitor must change.', 'Visitor favors stable element types and changing operations.'),
      miss('Traverse a collection without exposing its representation.', 'That is Iterator; Visitor is about applying operations across heterogeneous elements.', 'Iterator traverses; Visitor applies operations.'),
      miss('Compose objects into part-whole hierarchies.', 'That is Composite; Visitor often operates over a Composite but is a different pattern.', 'Composite builds the tree; Visitor operates on it.'),
    ],
    lesson: 'Visitor lets you define new operations over an object structure without changing the element classes, using double dispatch so the right operation runs for each element type. The tradeoff is asymmetric: adding operations is cheap, but adding new element types forces every visitor to be updated.',
    source, generated: true,
  },
  {
    id: 7702024,
    chapter: 'Behavioral Design Patterns',
    title: 'Iterator Pattern',
    prompt: 'The Iterator pattern provides a way to:',
    correct: 'Access the elements of an aggregate object sequentially without exposing its underlying representation.',
    wrong: [
      miss('Notify dependents when the aggregate changes.', 'That is Observer; Iterator is about traversal, not change notification.', 'Iterator traverses; Observer notifies.'),
      miss('Encapsulate the construction of a complex aggregate.', 'That is Builder; Iterator concerns reading, not building.', 'Builder constructs; Iterator reads.'),
      miss('Share elements between aggregates to save memory.', 'That is Flyweight; Iterator does not share state across aggregates.', 'Flyweight shares; Iterator walks.'),
    ],
    lesson: 'Iterator decouples traversal logic from the collection it traverses, offering a uniform interface (often hasNext/next) regardless of the underlying structure. It allows multiple simultaneous traversals and lets the collection change its internal representation without affecting client traversal code.',
    source, generated: true,
  },

  // ---------------- Chapter 5: Cohesion and Coupling ----------------
  {
    id: 7702025,
    chapter: 'Cohesion and Coupling',
    title: 'High Cohesion Goal',
    prompt: 'In module design, the generally desired combination is:',
    correct: 'High cohesion within modules and low coupling between modules.',
    wrong: [
      miss('Low cohesion and high coupling, to maximize reuse.', 'That is the worst combination; it produces tangled modules that are hard to change in isolation.', 'You want the opposite of this.'),
      miss('High cohesion and high coupling, so modules stay closely synchronized.', 'High coupling defeats independent change even if cohesion is high.', 'Couple loosely while keeping cohesion high.'),
      miss('Low cohesion and low coupling, for simple small modules.', 'Low cohesion means a module does unrelated things, which is undesirable regardless of coupling.', 'Cohesion should be high, not low.'),
    ],
    lesson: 'Good modular design seeks high cohesion, meaning each module groups closely related responsibilities, and low coupling, meaning modules depend on each other as little as possible. This combination localizes change, eases testing, and supports independent evolution of parts of the system.',
    source, generated: true,
  },
  {
    id: 7702026,
    chapter: 'Cohesion and Coupling',
    title: 'Functional Cohesion',
    prompt: 'Which type of cohesion is considered the strongest and most desirable?',
    correct: 'Functional cohesion, where all elements of a module contribute to a single well-defined task.',
    wrong: [
      miss('Coincidental cohesion, where elements are grouped arbitrarily.', 'Coincidental cohesion is the weakest, with no meaningful relationship among parts.', 'Arbitrary grouping is the worst, not the best.'),
      miss('Temporal cohesion, where elements run at the same time.', 'Temporal cohesion (e.g., a startup routine) is weak because the only link is timing.', 'Same-time execution is a weak bond.'),
      miss('Logical cohesion, where elements do similar kinds of things selected by a flag.', 'Logical cohesion is weak; a flag-driven grab-bag mixes related-looking but distinct operations.', 'Flag-selected operations are weakly cohesive.'),
    ],
    lesson: 'The classic cohesion scale runs from coincidental (weakest) through logical, temporal, procedural, communicational, and sequential, up to functional (strongest). Functional cohesion means every part of the module exists to perform one task, which makes the module easy to name, understand, reuse, and test.',
    source, generated: true,
  },
  {
    id: 7702027,
    chapter: 'Cohesion and Coupling',
    title: 'Content Coupling',
    prompt: 'Which form of coupling is the tightest and most harmful?',
    correct: 'Content coupling, where one module reaches in and relies on or modifies the internal details of another.',
    wrong: [
      miss('Data coupling, where modules share only simple parameters.', 'Data coupling is the loosest and most acceptable form, passing just the data needed.', 'Passing plain parameters is the best, not worst.'),
      miss('Stamp coupling, where a whole record is passed but only part is used.', 'Stamp coupling is tighter than data coupling but far looser than content coupling.', 'It is moderate, not the tightest.'),
      miss('Control coupling, where one module passes a flag controlling another’s logic.', 'Control coupling is undesirable but still looser than reaching into internals.', 'Reaching into internals is worse than a control flag.'),
    ],
    lesson: 'Coupling ranges from content (worst) through common, external, control, stamp, to data (best). Content coupling, where a module depends on or alters another’s internal data or code, is the most damaging because any internal change can silently break the dependent module.',
    source, generated: true,
  },
  {
    id: 7702028,
    chapter: 'Cohesion and Coupling',
    title: 'Control Coupling',
    prompt: 'A function that takes a boolean flag and behaves completely differently depending on its value exhibits:',
    correct: 'Control coupling, because the caller passes control information that dictates the callee’s internal logic.',
    wrong: [
      miss('Data coupling, since a boolean is just a small piece of data.', 'The boolean controls which branch executes, not merely supplying data, so it is control coupling.', 'When the parameter steers behavior, it is control coupling.'),
      miss('Content coupling, because the flag exposes internal state.', 'No internals are accessed across modules; the flag only steers behavior.', 'Content coupling means reaching into internals, which is not happening here.'),
      miss('Functional cohesion, since the function is doing related work.', 'Cohesion describes within-module relatedness, not the coupling created by a control flag.', 'This is a coupling question, not a cohesion one.'),
    ],
    lesson: 'Control coupling occurs when one module passes information that controls the flow of another, such as a flag selecting between behaviors. It is a code smell because the caller must know the callee’s internal branches; splitting into two clear functions usually removes it.',
    source, generated: true,
  },

  // ---------------- Chapter 6: Architecture Styles ----------------
  {
    id: 7702029,
    chapter: 'Architecture Styles',
    title: 'Layered Architecture',
    prompt: 'In a strict layered architecture, the standard rule is that:',
    correct: 'Each layer may depend only on the layer directly beneath it (or only on lower layers), and not on layers above.',
    wrong: [
      miss('Any layer may call any other layer freely for performance.', 'That destroys the layering benefit; unrestricted calls reintroduce the coupling layers aim to prevent.', 'Layers restrict the direction of dependencies.'),
      miss('Lower layers should depend on higher layers to stay coordinated.', 'Dependencies flow downward; higher layers (policy) depend on lower ones, not the reverse.', 'The presentation layer depends on services, not vice versa.'),
      miss('Each layer must be deployed as a separate process.', 'Layering is a logical organization; it does not mandate separate deployment units.', 'Layers are logical, not necessarily physical tiers.'),
    ],
    lesson: 'A layered (n-tier) architecture organizes code into horizontal layers such as presentation, business, and data, with dependencies flowing downward. Strict layering forbids a layer from calling layers above it or skipping layers, which keeps concerns separated and changes localized, at the cost of some indirection.',
    source, generated: true,
  },
  {
    id: 7702030,
    chapter: 'Architecture Styles',
    title: 'Microservices Tradeoff',
    prompt: 'Compared with a monolith, a primary tradeoff of a microservices architecture is that it:',
    correct: 'Improves independent deployability and scaling of services at the cost of added operational and distributed-system complexity.',
    wrong: [
      miss('Eliminates all coupling between components automatically.', 'Microservices can still be tightly coupled (e.g., chatty synchronous calls); the style does not remove coupling by itself.', 'Boundaries must be designed; the style alone does not guarantee loose coupling.'),
      miss('Always reduces total system complexity.', 'It often increases overall complexity by adding network, deployment, and data-consistency concerns.', 'Distribution adds complexity even as it adds flexibility.'),
      miss('Guarantees stronger data consistency than a monolith.', 'Distributing data across services usually weakens consistency, favoring eventual consistency.', 'Distributed data tends to weaken, not strengthen, consistency.'),
    ],
    lesson: 'Microservices decompose a system into small, independently deployable services owning their own data, which improves team autonomy, targeted scaling, and fault isolation. The cost is distributed-system complexity: network latency and failure, eventual consistency, distributed tracing, and heavier operational tooling.',
    source, generated: true,
  },
  {
    id: 7702031,
    chapter: 'Architecture Styles',
    title: 'Event-Driven Architecture',
    prompt: 'A defining characteristic of an event-driven architecture is that:',
    correct: 'Components communicate by producing and reacting to events asynchronously, with producers decoupled from consumers.',
    wrong: [
      miss('All components must call each other synchronously through a single API gateway.', 'That describes request/response routing, not the asynchronous, decoupled event model.', 'Event-driven systems favor asynchronous notification over synchronous calls.'),
      miss('Each component must know the identity of every consumer of its output.', 'Decoupling means producers emit events without knowing who consumes them.', 'Producers publish; they do not address specific consumers.'),
      miss('Events are processed strictly in a globally ordered single thread.', 'Event systems are typically concurrent and often only locally ordered, not globally serialized.', 'Global single-thread ordering is not a defining trait.'),
    ],
    lesson: 'Event-driven architecture centers on the asynchronous production, detection, and consumption of events, so producers and consumers are decoupled in time and identity. This yields strong loose coupling and scalability, but introduces challenges in ordering, idempotency, and observing end-to-end flows.',
    source, generated: true,
  },
  {
    id: 7702032,
    chapter: 'Architecture Styles',
    title: 'Hexagonal Architecture',
    prompt: 'In hexagonal (ports and adapters) architecture, the core domain:',
    correct: 'Depends only on ports (interfaces), while adapters implement those ports to connect to databases, UIs, and external services.',
    wrong: [
      miss('Depends directly on the database driver for efficiency.', 'That couples the domain to infrastructure, which ports and adapters specifically avoids.', 'Infrastructure plugs into ports; the core does not depend on it.'),
      miss('Is split across the presentation, business, and data layers.', 'That is layered architecture; hexagonal centers the domain and pushes infrastructure to the edge.', 'Hexagonal is not strictly horizontal layers.'),
      miss('Must be a single class implementing every port.', 'The core can be many classes; the rule is about dependency direction, not class count.', 'It is about the direction of dependencies, not size.'),
    ],
    lesson: 'Hexagonal architecture isolates business logic in a core that exposes ports (interfaces) and lets adapters on the outside implement them for specific technologies. Because all dependencies point inward toward the domain, you can swap databases, UIs, or message brokers without touching core logic, and you can test the core with simple stub adapters.',
    source, generated: true,
  },
  {
    id: 7702033,
    chapter: 'Architecture Styles',
    title: 'Pipe and Filter',
    prompt: 'The pipe-and-filter architectural style is characterized by:',
    correct: 'A series of independent processing components (filters) connected by pipes that pass data streams from one filter to the next.',
    wrong: [
      miss('A central mediator coordinating all component interactions.', 'That describes a mediator or broker style, not the linear stream-processing of pipe-and-filter.', 'Pipe-and-filter is a chain of transformers, not a hub.'),
      miss('Components sharing a single global mutable data store they all edit.', 'That is the blackboard or shared-repository style, not pipes carrying data between filters.', 'Pipes carry data; filters do not share one global store.'),
      miss('A strict prohibition on transforming the data as it flows.', 'Filters exist precisely to transform data; passing it through unchanged would defeat the purpose.', 'Filters transform; that is their job.'),
    ],
    lesson: 'Pipe-and-filter decomposes a task into a sequence of filters that each read input, transform it, and write output, connected by pipes. Each filter is independent and reusable, which makes the style natural for compilers and Unix command pipelines, though it can be inefficient for interactive or stateful processing.',
    source, generated: true,
  },

  // ---------------- Chapter 7: UML and Modeling ----------------
  {
    id: 7702034,
    chapter: 'UML and Modeling',
    title: 'UML Aggregation vs Composition',
    prompt: 'In a UML class diagram, the difference between aggregation (hollow diamond) and composition (filled diamond) is that:',
    correct: 'Composition implies the part’s lifecycle is owned by the whole and dies with it, while aggregation is a weaker has-a relationship without that ownership.',
    wrong: [
      miss('Aggregation means the part dies with the whole, and composition does not.', 'That reverses the meaning: composition implies strong ownership and shared lifecycle.', 'Filled diamond (composition) is the strong, owned relationship.'),
      miss('Both notations mean exactly the same thing and are interchangeable.', 'They encode different lifecycle semantics, so they are not interchangeable.', 'The diamond fill signals different ownership strength.'),
      miss('Composition denotes inheritance while aggregation denotes interface use.', 'Neither diamond denotes inheritance; inheritance uses a hollow triangle arrowhead.', 'Inheritance is a triangle, not a diamond.'),
    ],
    lesson: 'In UML, aggregation (hollow diamond) is a whole-part association where parts can exist independently of the whole, while composition (filled diamond) is a stronger whole-part relationship in which the whole owns and controls the lifetime of its parts. Inheritance, by contrast, is shown with a hollow triangular arrowhead.',
    source, generated: true,
  },
  {
    id: 7702035,
    chapter: 'UML and Modeling',
    title: 'Sequence Diagram Purpose',
    prompt: 'A UML sequence diagram primarily shows:',
    correct: 'The time-ordered exchange of messages between objects to accomplish a scenario.',
    wrong: [
      miss('The static structure of classes and their relationships.', 'That is a class diagram; sequence diagrams emphasize dynamic message ordering over time.', 'Sequence diagrams show interactions over time, not static structure.'),
      miss('The allowable states of a single object and its transitions.', 'That is a state machine diagram; sequence diagrams span multiple interacting objects.', 'State diagrams track one object’s states; sequence diagrams track messages.'),
      miss('The physical deployment of components onto hardware nodes.', 'That is a deployment diagram, concerning runtime topology, not message order.', 'Deployment diagrams map software to hardware.'),
    ],
    lesson: 'A sequence diagram is an interaction diagram that lays objects along the top and time down the page, showing the order in which messages are sent to realize a use case or scenario. It is ideal for clarifying control flow across collaborating objects, including activations, returns, and asynchronous messages.',
    source, generated: true,
  },
  {
    id: 7702036,
    chapter: 'UML and Modeling',
    title: 'Class Diagram Multiplicity',
    prompt: 'In a UML class diagram, a multiplicity of 1..* on an association end means:',
    correct: 'One or more instances must participate at that end of the association.',
    wrong: [
      miss('Exactly one instance participates.', 'A multiplicity of 1..* allows many; exactly one would be written 1 or 1..1.', '* means "many," so the upper bound is unbounded.'),
      miss('Zero or more instances may participate.', 'Zero is excluded; 1..* requires at least one, whereas 0..* allows zero.', 'The lower bound 1 forbids zero.'),
      miss('Between one and a fixed maximum, such as ten.', 'The asterisk denotes an unbounded maximum, not a specific number.', '* means no fixed upper limit.'),
    ],
    lesson: 'Multiplicity constrains how many objects can be linked across an association. The notation lower..upper uses an asterisk for "many," so 1..* means at least one and possibly unbounded, 0..* means optional and unbounded, and a bare 1 means exactly one. Reading these correctly is essential to translating diagrams into code.',
    source, generated: true,
  },
  {
    id: 7702037,
    chapter: 'UML and Modeling',
    title: 'Use Case include vs extend',
    prompt: 'In UML use case diagrams, the difference between include and extend is that:',
    correct: 'An included use case is always executed as part of the base, while an extending use case adds optional behavior under a condition.',
    wrong: [
      miss('Include adds optional behavior, while extend is always executed.', 'That reverses them: include is mandatory and unconditional, extend is conditional.', 'Include is always run; extend is conditional.'),
      miss('Both denote inheritance between use cases.', 'Neither is inheritance; generalization is the separate relationship for that.', 'Inheritance among use cases is generalization, not include/extend.'),
      miss('Include and extend are synonyms with no semantic difference.', 'They differ in whether the additional behavior is mandatory or conditional.', 'Their conditions of execution differ.'),
    ],
    lesson: 'The include relationship factors out common behavior that the base use case always performs, reducing duplication. The extend relationship attaches optional or exceptional behavior that runs only when a specified condition holds at an extension point, leaving the base use case complete on its own.',
    source, generated: true,
  },

  // ---------------- Chapter 8: Refactoring ----------------
  {
    id: 7702038,
    chapter: 'Refactoring',
    title: 'Definition of Refactoring',
    prompt: 'In Fowler’s terminology, refactoring is best defined as:',
    correct: 'Changing the internal structure of code to make it easier to understand and modify without changing its observable behavior.',
    wrong: [
      miss('Rewriting code to add new features customers requested.', 'Adding features changes behavior; refactoring deliberately preserves observable behavior.', 'Refactoring keeps behavior the same.'),
      miss('Optimizing code so it runs faster, even if structure suffers.', 'Performance tuning may change structure for speed; refactoring targets clarity while holding behavior constant.', 'Refactoring is about structure and clarity, not primarily speed.'),
      miss('Fixing a reported bug to correct wrong output.', 'Fixing a bug changes observable behavior by design, so it is not refactoring.', 'A bug fix alters behavior; refactoring does not.'),
    ],
    lesson: 'Refactoring is a disciplined technique for restructuring existing code, altering its internal structure without changing its external behavior, to improve readability and reduce complexity. Because behavior is preserved, a strong test suite is the safety net that makes aggressive refactoring possible.',
    source, generated: true,
  },
  {
    id: 7702039,
    chapter: 'Refactoring',
    title: 'Extract Method',
    prompt: 'The Extract Method refactoring is applied when:',
    correct: 'A fragment of code can be grouped together and given a name that explains its purpose, then replaced by a call to a new method.',
    wrong: [
      miss('Two classes have grown too coupled and must be merged into one.', 'That is a different refactoring (often inline class); Extract Method works within a method.', 'Extract Method names a code fragment, not merges classes.'),
      miss('A method is too short and should absorb its caller.', 'That is Inline Method, the inverse refactoring.', 'Inlining is the opposite of extracting.'),
      miss('A field is used by another class more than its own.', 'That suggests Move Field; Extract Method concerns turning a code fragment into a method.', 'Moving a field is unrelated to extracting a method.'),
    ],
    lesson: 'Extract Method takes a coherent block of code, often inside a long method, and turns it into its own well-named method called in place of the original block. It improves readability, reduces duplication, and is one of the most common first steps when breaking down an overly long method.',
    source, generated: true,
  },
  {
    id: 7702040,
    chapter: 'Refactoring',
    title: 'Long Method Smell',
    prompt: 'Which code smell most directly suggests that a method is doing too much and should be broken up?',
    correct: 'Long Method, where a single method spans many lines and mixes several levels of detail.',
    wrong: [
      miss('Feature Envy, where a method uses another class’s data more than its own.', 'Feature Envy is about misplaced responsibility, not a method’s sheer length.', 'Feature Envy concerns location of logic, not size.'),
      miss('Data Clumps, where the same group of fields recurs together.', 'Data Clumps point to a missing value object, not an overlong method.', 'Data Clumps suggest grouping data, not splitting a method.'),
      miss('Shotgun Surgery, where one change touches many classes.', 'Shotgun Surgery is about scattered change, not a single bloated method.', 'Shotgun Surgery is cross-class, not a long single method.'),
    ],
    lesson: 'A Long Method is a classic smell: methods that grow long tend to mix multiple responsibilities and levels of abstraction, making them hard to read and reuse. The usual remedy is Extract Method, decomposing the method into smaller, well-named pieces that each operate at one level of detail.',
    source, generated: true,
  },
  {
    id: 7702041,
    chapter: 'Refactoring',
    title: 'Shotgun Surgery vs Divergent Change',
    prompt: 'How do the Shotgun Surgery and Divergent Change smells differ?',
    correct: 'Shotgun Surgery means one change forces edits across many classes, while Divergent Change means one class is changed for many different reasons.',
    wrong: [
      miss('They are the same smell described from two viewpoints.', 'They are distinct: one is change scattered across classes, the other is many unrelated reasons hitting one class.', 'One is many classes per change; the other is many reasons per class.'),
      miss('Shotgun Surgery means a class changes for many reasons, and Divergent Change spreads one change across classes.', 'That swaps the definitions of the two smells.', 'Reverse the two definitions.'),
      miss('Both refer to overly long methods needing extraction.', 'Neither is about method length; both concern how change is distributed across classes.', 'These smells are about change distribution, not method size.'),
    ],
    lesson: 'Divergent Change happens when a single class is repeatedly modified for unrelated reasons, signaling it holds too many responsibilities (fix by splitting it). Shotgun Surgery is the opposite: a single conceptual change forces many small edits across many classes, signaling responsibilities are too scattered (fix by consolidating them).',
    source, generated: true,
  },
  {
    id: 7702042,
    chapter: 'Refactoring',
    title: 'Replace Conditional with Polymorphism',
    prompt: 'The Replace Conditional with Polymorphism refactoring addresses code that:',
    correct: 'Switches on a type code or enum in several places, by moving each branch into an overriding method of a subclass.',
    wrong: [
      miss('Has a method that is never called from anywhere.', 'Dead code is removed, not refactored via polymorphism.', 'This refactoring targets type-based conditionals, not dead code.'),
      miss('Repeats the same literal value throughout the codebase.', 'That is fixed by introducing a named constant, not by polymorphism.', 'Magic numbers call for named constants, not subclasses.'),
      miss('Contains a deeply nested loop with poor performance.', 'Performance of loops is a separate concern from removing type conditionals.', 'Polymorphism replaces type switches, not slow loops.'),
    ],
    lesson: 'When the same conditional on an object’s type appears in multiple methods, Replace Conditional with Polymorphism creates a subclass per case and moves each branch into an overriding method. The conditional disappears because dynamic dispatch selects the right behavior, and adding a new case becomes adding a new subclass rather than editing every switch.',
    source, generated: true,
  },

  // ---------------- Chapter 9: Testing and Test Design ----------------
  {
    id: 7702043,
    chapter: 'Testing and Test Design',
    title: 'Test-Driven Development Cycle',
    prompt: 'The classic Test-Driven Development cycle is best summarized as:',
    correct: 'Red, green, refactor: write a failing test, write the minimum code to pass it, then refactor while keeping tests green.',
    wrong: [
      miss('Write all production code first, then add tests at the end.', 'That is test-after development, the opposite of TDD, which writes the test before the code.', 'In TDD the test comes first.'),
      miss('Write tests that always pass so the build stays green.', 'A test must first fail (red) to prove it actually exercises new behavior.', 'A good first test should fail before code exists.'),
      miss('Refactor first, then write tests to match the new structure.', 'TDD writes the test before adding behavior; refactoring is the final step of each cycle.', 'The order is red, then green, then refactor.'),
    ],
    lesson: 'TDD drives design through a short loop: write a small failing test (red), write just enough code to make it pass (green), then refactor to clean up while the tests stay green. Writing the test first clarifies the desired interface and ensures the test genuinely fails before the code makes it pass.',
    source, generated: true,
  },
  {
    id: 7702044,
    chapter: 'Testing and Test Design',
    title: 'Mock vs Stub',
    prompt: 'What is the key distinction between a mock and a stub in unit testing?',
    correct: 'A stub provides canned responses to calls, while a mock additionally verifies that expected interactions actually occurred.',
    wrong: [
      miss('A mock provides canned data, while a stub verifies interactions.', 'That reverses the roles: stubs supply data, mocks verify behavior.', 'Stubs feed data; mocks check calls.'),
      miss('They are identical and the terms are fully interchangeable.', 'They differ in purpose: state-based stubbing versus behavior verification.', 'One verifies interactions; the other only returns values.'),
      miss('A mock is a real object, while a stub is always the production class.', 'Both are test doubles standing in for collaborators; neither is the production object.', 'Both are doubles, not the real dependency.'),
    ],
    lesson: 'In Meszaros’s test-double taxonomy, a stub returns predetermined values to keep the test running (supporting state verification), whereas a mock is preprogrammed with expectations and fails the test if the expected calls do not happen (supporting behavior verification). Choosing the right double keeps tests focused and not brittle.',
    source, generated: true,
  },
  {
    id: 7702045,
    chapter: 'Testing and Test Design',
    title: 'Unit vs Integration Test',
    prompt: 'The primary difference between a unit test and an integration test is that:',
    correct: 'A unit test exercises a single component in isolation, while an integration test verifies that multiple components work together correctly.',
    wrong: [
      miss('Unit tests run in production while integration tests run only locally.', 'Both run in development and CI; the difference is scope, not environment.', 'Scope is the differentiator, not where they run.'),
      miss('Integration tests are always written before unit tests.', 'There is no fixed ordering rule; the distinction is what each test covers.', 'Ordering is not the defining difference.'),
      miss('Unit tests check the UI while integration tests check the database.', 'Neither term is defined by a specific layer; both can target any layer.', 'The difference is isolation versus collaboration, not a fixed layer.'),
    ],
    lesson: 'Unit tests isolate one unit (often a class or function), replacing collaborators with test doubles to pinpoint failures and run fast. Integration tests deliberately combine multiple real components to verify their interactions, catching mismatches in contracts, wiring, and data flow that isolated unit tests cannot see.',
    source, generated: true,
  },
  {
    id: 7702046,
    chapter: 'Testing and Test Design',
    title: 'Testability and Coupling',
    prompt: 'Why does designing for low coupling tend to improve unit testability?',
    correct: 'Loosely coupled components can be tested with their dependencies replaced by simple test doubles, isolating the unit under test.',
    wrong: [
      miss('Low coupling guarantees 100 percent code coverage automatically.', 'Coverage depends on the tests you write; coupling affects how easily you can write them, not coverage by itself.', 'Coupling eases test writing; it does not auto-produce coverage.'),
      miss('Low coupling removes the need for any test doubles.', 'Doubles are still used; low coupling just makes substituting them straightforward.', 'You still inject doubles; loose coupling makes it easy.'),
      miss('Tightly coupled code is always faster to test because everything is together.', 'Tight coupling makes isolation hard, often forcing slow, brittle end-to-end tests.', 'Tight coupling hinders isolation, slowing tests.'),
    ],
    lesson: 'When a component depends on abstractions rather than concrete collaborators, you can inject lightweight stubs or mocks during testing, isolating the unit and controlling its environment. Tightly coupled code resists this substitution, pushing teams toward slower, more fragile integration or end-to-end tests to cover the same logic.',
    source, generated: true,
  },

  // ---------------- Chapter 10: Design Principles and Heuristics ----------------
  {
    id: 7702047,
    chapter: 'Design Principles and Heuristics',
    title: 'DRY Principle',
    prompt: 'The DRY (Don’t Repeat Yourself) principle, as stated by Hunt and Thomas, says that:',
    correct: 'Every piece of knowledge should have a single, unambiguous, authoritative representation within a system.',
    wrong: [
      miss('No two functions in a program may share any identical lines.', 'DRY targets duplication of knowledge, not incidental textual similarity; some look-alike code represents different knowledge.', 'DRY is about knowledge, not literal line matching.'),
      miss('All code must be written exactly once and never edited again.', 'DRY does not forbid editing; it forbids duplicating the same knowledge in multiple places.', 'It is about single representation, not immutability.'),
      miss('Comments should never repeat what the code already says.', 'That is a comment guideline, not the DRY principle’s definition.', 'DRY concerns duplicated knowledge in the system.'),
    ],
    lesson: 'DRY means each fact or rule lives in exactly one place, so a change requires editing only one location. The danger of violating it is that duplicated knowledge drifts out of sync; the subtlety is that not all duplicate-looking code is true duplication, since identical code may encode genuinely different concepts.',
    source, generated: true,
  },
  {
    id: 7702048,
    chapter: 'Design Principles and Heuristics',
    title: 'YAGNI',
    prompt: 'The YAGNI ("You Aren’t Gonna Need It") principle advises developers to:',
    correct: 'Avoid building functionality until it is actually required, rather than adding speculative features for anticipated future needs.',
    wrong: [
      miss('Always add extension points everywhere in case requirements change.', 'That is exactly the speculative generality YAGNI warns against.', 'YAGNI discourages building for hypothetical futures.'),
      miss('Never write tests until a feature is complete.', 'YAGNI is about not building unneeded functionality, not about test timing.', 'It targets speculative features, not test scheduling.'),
      miss('Refactor only when the code stops compiling.', 'YAGNI does not concern compilation; it concerns avoiding premature functionality.', 'YAGNI is about scope, not build errors.'),
    ],
    lesson: 'YAGNI counsels against implementing features or abstractions on the speculation that they will be needed later, because such speculation is often wrong and the unused code adds cost and risk. It pairs with DRY and simplicity heuristics to keep designs lean, deferring complexity until a real requirement justifies it.',
    source, generated: true,
  },
  {
    id: 7702049,
    chapter: 'Design Principles and Heuristics',
    title: 'Law of Demeter',
    prompt: 'The Law of Demeter (principle of least knowledge) advises a method to only call methods on:',
    correct: 'Itself, its parameters, objects it creates, and its direct component objects, avoiding long chains through strangers.',
    wrong: [
      miss('Any object reachable through a chain of getters, for convenience.', 'Long getter chains like a.getB().getC().doX() are precisely what the Law of Demeter discourages.', 'Avoid talking to "strangers" reached through chains.'),
      miss('Only static methods to avoid creating instances.', 'The law is about whom you may message, not about avoiding instances or using statics.', 'It restricts message targets, not instance creation.'),
      miss('Only methods declared in the same package.', 'Package membership is not the criterion; it is about immediate collaborators.', 'It concerns immediate collaborators, not packages.'),
    ],
    lesson: 'The Law of Demeter, or principle of least knowledge, limits an object to interacting only with its immediate collaborators, reducing coupling to the internal structure of other objects. Violations show up as train-wreck chains; honoring it pushes you to add methods that hide navigation, so callers do not depend on a neighbor’s neighbors.',
    source, generated: true,
  },
  {
    id: 7702050,
    chapter: 'Design Principles and Heuristics',
    title: 'Composition over Inheritance',
    prompt: 'The guideline "favor composition over inheritance" is motivated mainly by the fact that:',
    correct: 'Inheritance creates tight white-box coupling to a superclass, while composition allows flexible black-box reuse and behavior changes at runtime.',
    wrong: [
      miss('Inheritance is always slower at runtime than composition.', 'The guideline is about flexibility and coupling, not raw runtime speed.', 'The concern is coupling and flexibility, not performance.'),
      miss('Composition is impossible to use without a framework.', 'Composition is plain object holding and delegation; no framework is required.', 'You compose by holding references and delegating.'),
      miss('Inheritance cannot model is-a relationships.', 'Inheritance models is-a well; the warning is against overusing it for code reuse where has-a fits better.', 'Use inheritance for true is-a, composition for reuse.'),
    ],
    lesson: 'Subclassing exposes the parent’s implementation (white-box reuse), so changes to the superclass can break subclasses, and behavior is fixed at compile time. Composition delegates to objects through their interfaces (black-box reuse), enabling looser coupling and the ability to swap behavior at runtime, which is why many patterns prefer it.',
    source, generated: true,
  },
  {
    id: 7702051,
    chapter: 'Design Principles and Heuristics',
    title: 'Separation of Concerns',
    prompt: 'Separation of concerns is a design principle stating that:',
    correct: 'A system should be divided so each part addresses a distinct concern, minimizing overlap between different responsibilities.',
    wrong: [
      miss('Each module should depend on as many others as possible for reuse.', 'That increases coupling, which separation of concerns aims to reduce.', 'The goal is independent concerns, not maximal dependency.'),
      miss('All concerns should be handled in a single central module for consistency.', 'Centralizing every concern is the opposite of separating them and creates a god module.', 'Separate concerns into distinct parts, not one module.'),
      miss('Concerns should be mixed so related code stays physically close.', 'Mixing unrelated concerns is precisely what the principle discourages.', 'Keep distinct concerns apart.'),
    ],
    lesson: 'Separation of concerns organizes a system so that distinct aspects, such as persistence, presentation, and business rules, are handled by distinct, minimally overlapping parts. This localizes change, improves comprehensibility, and underpins patterns and architectures like MVC and layering that exist to keep concerns apart.',
    source, generated: true,
  },

  // ---------------- Chapter 11: Object-Oriented Design Concepts ----------------
  {
    id: 7702052,
    chapter: 'Object-Oriented Design Concepts',
    title: 'Encapsulation',
    prompt: 'Encapsulation in object-oriented design primarily means:',
    correct: 'Bundling data with the methods that operate on it and restricting direct external access to the internal state.',
    wrong: [
      miss('Allowing a subclass to inherit the methods of its superclass.', 'That is inheritance; encapsulation is about hiding and protecting internal state.', 'Inheritance shares behavior; encapsulation hides state.'),
      miss('Letting one interface stand for many underlying types.', 'That is polymorphism; encapsulation concerns information hiding.', 'Polymorphism varies behavior; encapsulation hides data.'),
      miss('Making all fields public so any code can read them.', 'Public fields break encapsulation by exposing internal state directly.', 'Encapsulation restricts, rather than exposes, internal access.'),
    ],
    lesson: 'Encapsulation groups state and the operations on that state into one unit and controls access through a well-defined interface, hiding implementation details. This information hiding lets the internal representation change without affecting clients and protects invariants by funneling all modifications through controlled methods.',
    source, generated: true,
  },
  {
    id: 7702053,
    chapter: 'Object-Oriented Design Concepts',
    title: 'Polymorphism',
    prompt: 'Subtype (runtime) polymorphism allows:',
    correct: 'A single interface or base-type reference to invoke different implementations depending on the actual object’s type at runtime.',
    wrong: [
      miss('A function to be defined with several signatures chosen at compile time.', 'That describes overloading (ad hoc polymorphism), resolved statically, not runtime dispatch.', 'Overloading is compile-time; subtype polymorphism is runtime.'),
      miss('A class to hide its internal fields from outside code.', 'That is encapsulation, not polymorphism.', 'Hiding state is encapsulation, not polymorphism.'),
      miss('A subclass to reuse the code of its superclass.', 'Code reuse via subclassing is inheritance; polymorphism is about uniform calls dispatching to different behavior.', 'Reuse is inheritance; varied dispatch is polymorphism.'),
    ],
    lesson: 'Subtype polymorphism lets code written against a base type or interface invoke the appropriate overridden method of whatever concrete object is supplied, with the choice resolved by dynamic dispatch at runtime. This is what makes the Open/Closed Principle and patterns like Strategy and Template Method work.',
    source, generated: true,
  },
  {
    id: 7702054,
    chapter: 'Object-Oriented Design Concepts',
    title: 'Abstraction vs Encapsulation',
    prompt: 'How are abstraction and encapsulation related but distinct?',
    correct: 'Abstraction focuses on exposing only essential features and hiding complexity, while encapsulation is the mechanism that bundles and restricts access to the details being hidden.',
    wrong: [
      miss('They are two names for the same concept.', 'They are related but distinct: one is about what to expose conceptually, the other about how to enforce hiding.', 'One is the idea; the other is the enforcing mechanism.'),
      miss('Abstraction means making all members private, while encapsulation means making them public.', 'Neither maps to a fixed access modifier in that way; this misstates both.', 'Access modifiers are tools, not the definitions.'),
      miss('Abstraction is a runtime activity, while encapsulation happens only at compile time.', 'Both are design-time concepts realized in code, not split by runtime vs compile time.', 'Neither is defined by execution phase.'),
    ],
    lesson: 'Abstraction is a design perspective: it models a thing by its essential, externally relevant features while suppressing irrelevant detail. Encapsulation is the supporting mechanism that packages state and behavior together and controls access, enforcing the boundary the abstraction defines. You use abstraction to decide what to expose and encapsulation to keep the rest hidden.',
    source, generated: true,
  },
])
