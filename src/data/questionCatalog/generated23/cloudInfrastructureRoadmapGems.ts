import { makeSimpleQuestion } from '../base'
import type { Question } from '../types'

export const cloudInfrastructureRoadmapGems: Question[] = [
  // ----------------------------------------------------------------------------
  // Observability and Reliability
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10024000,
    'Career Skills',
    'Observability and Reliability',
    "The green dashboard and the angry customer",
    "It is 2 a.m. CPU, memory, and disk on every server sit comfortably under 40%, and the infrastructure dashboard is a wall of green. Yet the support queue is filling with users who say checkout spins forever and then fails. The on-call engineer points at the green graphs and argues nothing is wrong. What is the correct read of this situation?",
    "Resource metrics like CPU prove the machines are not starved, but they are not user-impact signals; checkout latency and checkout error rate are the symptoms that prove harm, and those are what the dashboard is failing to surface",
    [
      ["The customers must be mistaken or experiencing local network issues, because healthy CPU and memory mean the service is healthy", "A backend can be idle precisely because requests are stalling upstream, timing out, or erroring before they do work; low resource use can be a symptom of failure, not proof of health.", "Idle hardware can mean work is not flowing through, which is worse than busy hardware."],
      ["The fix is to add more CPU and memory headroom so the green metrics stay green under load", "Adding capacity to resources that are already underused addresses nothing; the failure is in the request path, not in resource saturation.", "You cannot scale your way out of a problem that is not a capacity problem."],
      ["Raise the alert thresholds so CPU pages at 30% instead of 40% to catch the issue sooner", "Tightening a cause-based threshold that was never the cause just manufactures noise; the missing signal is a user-journey metric, not a stricter resource alarm.", "Lowering a threshold on the wrong metric adds fatigue without adding insight."],
    ],
    "This is the central illusion of observability: a system can be perfectly healthy at the resource layer and completely broken at the user layer. CPU, memory, and disk are saturation signals about the machine; latency and errors on the checkout path are signals about the customer. The deep point is that 'green' only means whatever you chose to measure is fine, and infrastructure teams habitually instrument what is easy to collect (resources) rather than what proves value delivered (the user journey). Reliability is defined from the customer's side of the request, not the server's.",
    'Floe generated',
    true,
    "Ask which metric would change if every user's checkout failed but every server stayed idle.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10024001,
    'Career Skills',
    'Observability and Reliability',
    "Why an SLO is a budget, not a promise of perfection",
    "A product manager wants the team to commit to '100% uptime' for the API and put it in the SLA. The SRE pushes back and proposes a 99.9% SLO with an error budget instead. The PM hears this as the engineers asking for permission to fail. What is the real reason 99.9% with an error budget is the stronger commitment?",
    "100% is unmeasurable and unaffordable, so it becomes a promise no one can hold; 99.9% defines an explicit budget of allowed failure that can be measured, spent deliberately on risky releases, and used to decide when to slow down and stabilize",
    [
      ["Because 99.9% and 100% are effectively identical in practice, so the lower number is just easier to write down", "The gap between 99.9% and 100% is roughly 43 minutes of allowed downtime a month versus zero; that difference is exactly the budget that funds change, and erasing it is not cosmetic.", "The fourth nine is enormously more expensive than it looks; the gap is the whole point."],
      ["Because a lower target lets the team avoid accountability when the service breaks", "An error budget increases accountability: it converts reliability into a quantity you can overspend, and overspending triggers an agreed response like a release freeze.", "A budget is a control mechanism, not an excuse; spending it has consequences."],
      ["Because customers never notice anything above 99% anyway, so chasing more nines is pure vanity", "Whether customers notice depends entirely on the workload; the argument for an error budget is not that reliability is unimportant but that perfect reliability is unbuyable and prevents all change.", "The case rests on measurability and cost, not on assuming users are indifferent."],
    ],
    "An error budget reframes reliability from a moral absolute into an economic quantity. '100% uptime' sounds like the strongest possible promise, but because it is unmeasurable and infinitely expensive at the margin, it is actually the weakest: it cannot be tracked and cannot be honored. A 99.9% SLO does something subtle and powerful, it grants the team a finite allowance of failure to spend on shipping features and taking risks, and it makes 'we are out of budget, stop changing things' a data-driven decision rather than a fight. The lingering tension is that pursuing more reliability than you committed to can be as wrong as pursuing less, because every extra nine is paid for with velocity.",
    'Floe generated',
    true,
    "Think of reliability as money you are allowed to spend, then ask what zero budget forbids.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10024002,
    'Career Skills',
    'Observability and Reliability',
    "Alert on the symptom, not the cause",
    "A team's pager fires constantly: 'disk queue depth high,' 'GC pause detected,' 'replica lag rising,' 'thread pool 80% full.' On-call is exhausted, and people now ignore pages. A reliability review recommends deleting most of these and replacing them with one alert. What single alert best fixes both the fatigue and the blind spots?",
    "An alert on the user-facing symptom, such as the error-budget burn rate of the request path, which fires only when users are actually being harmed regardless of which internal cause produced it",
    [
      ["Keep all the cause alerts but route them to a separate low-priority channel so the pager is quieter", "Demoting the noise does not solve the core problem: none of those causes is guaranteed to mean user harm, and any of them can be harmless, so you still alert on the wrong thing.", "Moving cause-based noise elsewhere still leaves you blind to causes you never predicted."],
      ["Add even more cause alerts so every possible internal failure has its own dedicated page", "More cause-based alerts deepen fatigue and can never be exhaustive; novel failure modes you did not foresee will still slip through while real harm hides in the noise.", "You cannot enumerate every cause, but you can always measure the symptom."],
      ["Set the existing alerts to only fire if two or more conditions trigger at once", "Combining causes is still cause-based alerting; a single unforeseen failure that harms users would not trip the combination, and harmless co-occurrences still page.", "Correlating causes is cleverer noise, not a measure of user impact."],
    ],
    "Good alerting tells you that users are suffering, not that an internal component is behaving in a way that might or might not matter. High GC pauses, replica lag, and full thread pools are causes, and a system has effectively infinite causes, most of which are survivable. Symptom-based alerting on the error-budget burn rate inverts this: it pages when, and only when, the service is actually missing its commitment, so it both silences the harmless noise that burns out on-call and catches novel failures you never thought to instrument. The deep idea is that you should alert on the contract you made with the user, because that single signal covers every cause at once, including the ones you have not imagined yet.",
    'Floe generated',
    true,
    "Ask which alert would still catch a brand-new failure mode nobody predicted.",
    { challengeRating: 6 },
  ),
  // ----------------------------------------------------------------------------
  // Data Protection and Disaster Recovery
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10024003,
    'Career Skills',
    'Data Protection and Disaster Recovery',
    "Why a replica is not a backup",
    "A database has real-time replication to a second region, and leadership considers backups redundant: 'every write is already copied within milliseconds.' Then an engineer runs a bad migration that deletes a critical table, and within a second the deletion is faithfully present on the replica too. Why does replication fail as a backup here?",
    "Replication copies every change instantly, including the destructive ones, so a deletion or corruption is propagated to the replica as fast as it happened; a backup protects you precisely because it preserves an earlier point in time that the bad change has not reached",
    [
      ["Replication would have worked if the second region had simply been in a different cloud provider", "The provider is irrelevant; the flaw is temporal, not geographic, because a same-time copy of corrupted data is still corrupted no matter where it lives.", "Distance and vendor diversity do not undo a change that was replicated everywhere at once."],
      ["The real mistake was that replication was synchronous; asynchronous replication would have left the old table intact", "Asynchronous replication only lags by seconds and would still carry the deletion through; lag is not retention, and it does not give you a chosen point to restore from.", "A few seconds of lag is not the same as a preserved historical point."],
      ["Backups were unnecessary; the team just needed a second replica so one could be paused during risky changes", "Manually pausing a replica before every risky operation is a fragile human ritual, not a protection strategy, and it does nothing for the unplanned corruption nobody saw coming.", "Ad hoc pausing depends on predicting the disaster in advance, which defeats the purpose."],
    ],
    "Replication and backup answer different questions. Replication answers 'is there another live copy if this server or region dies,' and it is brilliant at surviving hardware and site failures. But it is a mirror, and a mirror reflects your mistakes instantly: an accidental deletion, a bad migration, or ransomware encryption is replicated with the same fidelity as legitimate data. A backup is fundamentally about time, not place, it preserves a state the destructive event has not yet touched, so you can roll back to before the harm. The deep lesson is that high availability and recoverability are orthogonal: you can have perfect redundancy and still lose everything to a single wrong command, because every copy was loyally kept in sync.",
    'Floe generated',
    true,
    "Ask whether the copy preserves a moment in time or merely the same moment, faithfully.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10024004,
    'Career Skills',
    'Data Protection and Disaster Recovery',
    "RPO and RTO are two different fears",
    "A business owner says the order system 'can basically never go down and we can never lose anything.' The platform lead needs concrete targets and asks two questions: how much recent data could you tolerate losing if we restore from the last good copy, and how long could the system be unavailable while we recover? The owner answers 'at most five minutes of orders' and 'up to two hours offline is survivable.' Which targets has the owner just set?",
    "An RPO of five minutes (the maximum acceptable data loss, measured backward from the failure) and an RTO of two hours (the maximum acceptable time to restore service), and they drive different investments, frequent replication or journaling for RPO, fast restore tooling and standby capacity for RTO",
    [
      ["Both answers describe the RTO, since both are about how quickly the team must react to an incident", "Only the two-hour figure is RTO; the five-minute figure is about how much data can be lost, which is RPO, a distinct objective measured in the opposite direction from the failure point.", "Two numbers about different fears cannot both be the same objective."],
      ["An RTO of five minutes and an RPO of two hours, since recovery time is always the smaller number", "The mapping is reversed: five minutes of tolerable data loss is the RPO and two hours of tolerable downtime is the RTO; there is no rule that recovery time is the smaller value.", "Match each number to the question it answered, not to its size."],
      ["A single combined target of two hours and five minutes that defines the overall recovery service level", "Collapsing them hides that they fund opposite engineering work; a tiny RPO with a generous RTO needs frequent backups but not hot standby, while the reverse needs the opposite.", "Merging the two objectives erases the design tradeoff between data loss and downtime."],
    ],
    "RPO and RTO are easy to confuse because both sound like 'how bad can the outage be,' but they point in opposite directions in time. RPO (recovery point objective) looks backward from the failure: how much recent work am I willing to lose, which is set by how often I capture data. RTO (recovery time objective) looks forward: how long can I be down, which is set by how fast I can restore. The deep insight is that these two are independent dials with independent costs, you can want near-zero data loss but tolerate hours of downtime (cheap on standby, expensive on capture frequency) or the reverse, and conflating them leads teams to either overspend or quietly fail the objective that actually mattered to the business.",
    'Floe generated',
    true,
    "One number looks back at lost data, the other looks forward at lost time.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10024005,
    'Career Skills',
    'Data Protection and Disaster Recovery',
    "The backup you never restored",
    "An audit finds nightly backups have completed successfully for three years; the job log is a clean column of green check marks. During a real ransomware event the team tries to restore and discovers the backups are technically intact but unusable: the restore needs an encryption key no one kept, and the process takes eleven hours against a two-hour RTO. Which principle did the green check marks hide?",
    "A backup that has never been restore-tested is an unverified assumption; the only proof a backup works is a rehearsed restore that meets the RTO, which is exactly what the '0' for zero errors in modern backup rules and immutable, recoverable copies are meant to guarantee",
    [
      ["The backups were fine; the team simply needed to run the restore on faster hardware at the time", "Throwing hardware at it during the live incident is improvisation under pressure; the missing key and the unmeasured restore time were latent failures that only a prior rehearsal would have caught.", "You cannot discover an unrecoverable backup for the first time during the disaster."],
      ["The real fix is to take backups more often so there is always a more recent copy to fall back on", "Frequency improves RPO, not recoverability; ten times as many untested, unrestorable backups still cannot be restored, so the cadence was never the problem.", "More copies of an unverified backup is more of the same unverified thing."],
      ["A successful backup job is sufficient evidence; restore testing is a nice-to-have for compliance theater", "This is exactly the belief the scenario punishes: 'backup succeeded' only proves data was written, never that it can be read back, decrypted, and made live within the RTO.", "A write that was never read back is a hope, not a guarantee."],
    ],
    "The most dangerous backup is the one everyone trusts and no one has tried to restore. A green backup job proves only that bytes were written; it says nothing about whether you still hold the decryption key, whether the format is readable, whether dependencies exist, or whether the restore finishes inside your RTO. This is why modern guidance adds a literal '0' for zero recovery errors, and why immutability matters against ransomware that hunts down backups, recoverability is a property you must continuously verify, not a checkbox you tick once. The deep and uncomfortable truth is that an untested backup and no backup are nearly the same risk, because both leave you discovering whether recovery works at the worst possible moment.",
    'Floe generated',
    true,
    "Ask what 'backup succeeded' actually proves about reading the data back.",
    { challengeRating: 6 },
  ),
  // ----------------------------------------------------------------------------
  // IAM and Network Boundaries
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10024006,
    'Career Skills',
    'IAM and Network Boundaries',
    "Outbound internet without an open front door",
    "A junior engineer needs application servers to call an external payment API over the internet, so they place those servers in a public subnet with public IPs. A reviewer flags this as an unnecessary exposure and says the same outbound access is achievable while keeping the servers unreachable from the internet. How?",
    "Put the servers in a private subnet with no public IPs and route their outbound traffic through a NAT gateway; NAT lets them initiate connections out and receive the responses, but nothing on the internet can initiate a connection in to them",
    [
      ["Outbound internet access genuinely requires a public IP, so a public subnet is the only option and the reviewer is wrong", "This is the exact misconception the review targets: a private subnet plus NAT provides outbound reachability without any public IP, decoupling 'can call out' from 'can be called in to.'", "Calling out and being reachable from outside are separable, and NAT separates them."],
      ["Keep the public subnet but add a security group that denies all inbound traffic; that makes it private", "A security group can restrict inbound, but the instances still have public IPs in an internet-routable subnet, so they remain part of the public attack surface and one rule mistake away from exposure.", "A locked door on a house facing the street is weaker than a house with no street-facing door."],
      ["Move the servers to a private subnet but give them Elastic/public IPs so they can still reach the API", "Attaching public IPs to private-subnet instances reintroduces the very exposure you removed; the public address is what makes them targetable, and NAT exists so you do not need one.", "The public IP is the exposure; you cannot keep it and call the result private."],
    ],
    "This question breaks the intuitive but false equation 'needs the internet, therefore needs a public address.' Direction is everything: a NAT gateway performs stateful translation that allows your servers to start outbound conversations and accept the matching replies, while presenting no inbound entry point for anyone on the internet to exploit. Putting workloads in a public subnet with public IPs solely to reach an external API needlessly enlarges the attack surface, every instance becomes directly addressable. The deep principle is that network design is about controlling who can initiate a connection, not merely who can talk; least exposure means defaulting to private and granting egress through a controlled chokepoint rather than handing every box a doorway from the open internet.",
    'Floe generated',
    true,
    "Separate 'can start a connection outward' from 'can be reached from outside.'",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10024007,
    'Career Skills',
    'IAM and Network Boundaries',
    "The convenient role and the blast radius",
    "Under deadline pressure, an engineer grants a small batch-processing service a broad administrator role 'just so it stops failing on permissions.' It works, the ticket closes, and the role is forgotten. Eighteen months later an attacker steals that service's credentials. A security reviewer explains why this single shortcut is the worst kind of finding. What is the core problem?",
    "The over-scoped role made the blast radius enormous: a compromise of one minor service now grants the attacker everything the admin role can touch, so least privilege matters precisely because it caps the damage of an inevitable credential leak",
    [
      ["The problem is purely that the credentials leaked; with strong secret rotation the broad role would have been harmless", "Rotation reduces how long a leaked credential is useful but not what it can do while valid; the catastrophe is the scope of access, which least privilege, not rotation alone, would have contained.", "Rotation shortens the window of misuse; scoping shrinks the damage per misuse."],
      ["It is mostly a documentation gap; the role was fine, it just should have been written down in a runbook", "Documenting a dangerous grant does not make it safe; the finding is that the permission itself vastly exceeded the service's actual need, and a runbook entry would not have shrunk the blast radius.", "Recording an over-grant is not the same as not making it."],
      ["The risk is low because a batch service is unimportant, so even full compromise of it barely matters", "Importance of the service is irrelevant once it holds admin rights; the attacker inherits the role's reach, not the service's modest job, which is exactly why lateral movement starts from forgotten low-value identities.", "Attackers value an identity for its permissions, not for the dignity of its day job."],
    ],
    "Least privilege is not bureaucratic caution; it is blast-radius engineering. Every credential will eventually leak, be phished, or be left in a log, so the design question is not 'how do we prevent compromise' but 'how much can a single compromise reach.' A batch job that only needs to read one bucket and write to one queue should be able to do exactly that and nothing else, so that stealing its identity yields almost nothing. The 'just give it admin' shortcut feels harmless because it is invisible until exploited, and the forgotten, over-scoped, low-value service account is precisely the foothold attackers use to move laterally. The deep point is that security is measured by the cost of your worst plausible breach, and over-permissioning quietly inflates that cost everywhere at once.",
    'Floe generated',
    true,
    "Ask what an attacker inherits, not what the service was supposed to do.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10024008,
    'Career Skills',
    'IAM and Network Boundaries',
    "Why the firewall remembered and the gate forgot",
    "An engineer adds a security group rule allowing inbound HTTPS to a web server and it works immediately; the server can reply to clients with no extra rule. Later they add an equivalent allow rule on the subnet's network ACL, and connections mysteriously hang until they add a second rule for the return traffic on high-numbered ports. What underlying difference explains this?",
    "Security groups are stateful, so allowing a request automatically permits its return traffic, while network ACLs are stateless and evaluate each packet independently, so the reply on ephemeral ports must be explicitly allowed as a separate rule",
    [
      ["The network ACL was simply misconfigured with the wrong port number; both controls behave identically once set correctly", "They do not behave identically: even a correctly numbered ACL needs an explicit return rule because it has no memory of the outbound request, which is the whole stateless-versus-stateful distinction.", "The extra rule was not a typo fix; it was required by the ACL's lack of connection memory."],
      ["Security groups operate at the subnet level and ACLs at the instance level, which is why one needed two rules", "The scoping is the reverse, security groups attach to instances and ACLs to subnets, and in any case scope is not what makes one remember return traffic; statefulness is.", "The reason is connection state, not which layer the control sits at."],
      ["The hang happened because ACLs are evaluated after security groups, so the request was already dropped", "Evaluation order does not create the hang; the request arrived fine, it was the return packet that the stateless ACL silently blocked for lack of an explicit allow.", "Order of evaluation does not explain a blocked reply on a connection that was allowed in."],
    ],
    "Stateful versus stateless is one of the most consequential distinctions in network control. A stateful firewall like a security group remembers the connections it allowed and automatically lets the matching replies back through, so you reason in terms of conversations. A stateless control like a network ACL has no such memory; it judges every packet on its own, so an allowed inbound request whose response leaves on a random high-numbered ephemeral port will be dropped unless you also allow that return range explicitly. The deep lesson is that 'allow' means radically different things depending on whether the control tracks state, and many baffling 'it connects but hangs' incidents come from applying conversational intuition to a control that only ever sees individual packets.",
    'Floe generated',
    true,
    "Ask whether the control remembers the request when the reply comes back.",
    { challengeRating: 6 },
  ),
]
