import { makeSimpleQuestion } from '../base'
import type { Question } from '../types'

export const softwareGems: Question[] = [
  // ----------------------------------------------------------------------------
  // Module 2: Graphs
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10095000,
    'Software',
    'Module 2: Graphs',
    "Why Dijkstra refuses to look back",
    "You map a delivery network as a graph and run Dijkstra to find the cheapest route. A teammate adds an edge whose cost is negative (a leg that pays a rebate), reasoning that a cheaper edge can only help. Dijkstra now sometimes returns a route that is provably not the cheapest. What broke?",
    "Dijkstra finalizes each node the moment it is popped as the current minimum, betting no future path can lower it; a negative edge can later undercut an already-finalized node, but the algorithm never reopens it",
    [
      ["The negative edge created a cycle, so the shortest path became undefined", "A single negative edge does not imply a negative cycle; Dijkstra returns wrong answers even on a tree with one negative edge and no cycle at all.", "Separate 'a negative edge exists' from 'a negative cycle exists'; only the latter makes a shortest path undefined."],
      ["The priority queue can no longer order the nodes once a weight is below zero", "Heaps order fine with negative keys; the flaw is the greedy finalization rule, not the comparison of numbers.", "The bug is in the once-finalized-always-finalized invariant, not in how the queue sorts values."],
      ["Negative weights overflow the distance accumulator and corrupt the totals", "This is an implementation detail unrelated to the conceptual failure; the algorithm is wrong even with perfect arithmetic.", "Ask whether the math is wrong or the strategy is wrong; here the strategy itself is invalid."],
    ],
    "Dijkstra is a greedy algorithm whose correctness rests on one assumption: extending a path never makes it cheaper, so the smallest tentative distance popped is already final. Non-negative weights guarantee this; a negative edge can revisit and undercut a settled node, and Dijkstra's refusal to reopen it then locks in a wrong answer. The deep point is that an algorithm's speed often comes from an assumption it makes about the world, and the assumption, not the code, is what fails. Bellman-Ford trades that speed for the right to keep relaxing edges, which is why it tolerates negative weights.",
    'Floe generated',
    true,
    "Ask what assumption lets Dijkstra mark a node 'done' and never revisit it.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10095001,
    'Software',
    'Module 2: Graphs',
    "When the build order does not exist",
    "Your build tool compiles modules in an order where every module comes after the ones it imports, computed by a topological sort. One day the sort produces no valid order at all and the build halts. Before blaming the tool, what does this failure actually prove about your code?",
    "The import graph contains a cycle: two or more modules depend on each other directly or transitively, so no module can legitimately come first and no valid order can exist",
    [
      ["The graph is too large, so the sort ran out of orderings to try", "Topological sort is linear and deterministic; it does not 'try orderings' or exhaust them, and graph size never causes the absence of an order.", "A missing order is a structural fact about the graph, not a capacity limit of the algorithm."],
      ["Some module has no dependencies, leaving the sort with no place to start", "A dependency-free module is the easiest starting point, not a blocker; it is precisely where a valid sort begins.", "Nodes with no incoming edges enable an ordering; the obstacle is mutual dependence, not independence."],
      ["The modules form a tree, which cannot be linearized into a sequence", "Any directed acyclic graph, trees included, can always be topologically ordered; only a cycle makes it impossible.", "Acyclic structures always linearize; look for a back edge, not a tree."],
    ],
    "A topological sort succeeds exactly when the dependency graph is acyclic, so its failure is not a bug to route around but a diagnosis: somewhere your modules form a circular dependency that no compilation order can satisfy. The elegant idea is that the same algorithm that orders work also detects the impossibility of ordering it; the absence of an output carries information. This is why package managers, build systems, and task schedulers lean on it, and why a circular import is a design problem the tool can only surface, never solve.",
    'Floe generated',
    true,
    "Ask under what single condition a valid dependency order is guaranteed to exist.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10095002,
    'Software',
    'Module 2: Graphs',
    "BFS, DFS, and the shape of the answer",
    "You must find the fewest hops between two users in an unweighted social graph. A colleague reaches for depth-first search because it is simple to write recursively and uses less memory on deep graphs. You push back and recommend breadth-first search. What is the precise reason BFS is correct here and DFS is not?",
    "On an unweighted graph BFS expands outward layer by layer, so the first time it reaches the target it has used the minimum number of edges; DFS may reach the target down a long branch first and report a path that is not the shortest",
    [
      ["DFS cannot reach every node, so it might miss the target entirely", "Both DFS and BFS visit every reachable node; DFS finds a path, just not necessarily the shortest one.", "The issue is path length, not reachability; DFS will find the target, only via a possibly longer route."],
      ["BFS is faster than DFS, so it finds the answer sooner", "They share the same linear time complexity; BFS wins on correctness of the shortest-path answer, not raw speed.", "Distinguish 'gives the right shortest path' from 'runs faster'; here it is about correctness."],
      ["DFS uses a queue that reorders nodes and corrupts the distance count", "DFS uses a stack (or recursion), not a queue; the queue versus stack difference is exactly why BFS, not DFS, yields layered distances.", "It is BFS that uses a queue; that queue is the mechanism producing correct hop counts."],
    ],
    "BFS and DFS visit the same nodes, but the order they visit them encodes different guarantees. BFS's queue forces it to finish all nodes at distance k before any at distance k+1, so first contact with the target is provably the shortest hop count on an unweighted graph; DFS's stack dives deep and can stumble onto the target via a detour. The lasting lesson is that choosing a traversal is really choosing which property you want the search order to preserve, and 'simpler to code' is not the same property as 'correct for this question.'",
    'Floe generated',
    true,
    "Ask which traversal reaches nodes strictly in order of increasing distance.",
    { challengeRating: 6 },
  ),
  // ----------------------------------------------------------------------------
  // Module 3: DP
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10095003,
    'Software',
    'Module 3: DP',
    "Why the obvious coin grab loses",
    "Making change for 6 with coin denominations 1, 3, and 4, a greedy routine takes the largest coin that fits each step: 4, then 1, then 1 — three coins. The true optimum is 3 + 3, just two coins. The denominations are correct and the code has no bug. Why did the locally best choice produce a globally worse answer?",
    "Greedy assumes the locally cheapest first move stays optimal, but taking the 4 leaves a remainder of 2 that only small coins can fill; this denomination set lacks the greedy-choice property, so a locally optimal step poisons the rest of the solution",
    [
      ["The greedy code has an off-by-one error in how it subtracts coins", "The arithmetic is exact; greedy correctly returns three coins for its strategy. The strategy itself is wrong for these denominations.", "When a correct implementation gives a suboptimal answer, suspect the strategy, not the arithmetic."],
      ["Dynamic programming is faster, so it found a shorter answer first", "DP is typically slower here, not faster; it wins by considering every combination, not by speed.", "DP's advantage is completeness of search, not raw runtime."],
      ["The problem has no optimal substructure, so no method can solve it cleanly", "It does have optimal substructure (an optimal solution for n is built from optimal solutions for smaller amounts); that is exactly why DP works. It lacks only the greedy-choice property.", "Separate optimal substructure (DP applies) from the greedy-choice property (greedy applies)."],
    ],
    "Greedy and dynamic programming both decompose a problem into subproblems, but greedy commits to the locally best move and never reconsiders, which is only valid when a problem has the greedy-choice property. Coin change with arbitrary denominations does not: grabbing the biggest coin can strand the remainder. DP instead evaluates every coin as the last one taken and keeps the best, trading speed for the guarantee of optimality. The enduring idea is that 'take the best thing you can see right now' is a bet on the future, and dynamic programming is what you reach for when that bet is not safe.",
    'Floe generated',
    true,
    "Ask whether the locally cheapest first coin can ever leave a remainder that costs more to complete.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10095004,
    'Software',
    'Module 3: DP',
    "The exponential cost of forgetting",
    "A naive recursive Fibonacci, fib(n) = fib(n-1) + fib(n-2), takes seconds at n=40 and effectively never finishes at n=60, even though there are only about 60 distinct values to compute. Adding a single dictionary that caches each fib(k) the first time it is computed makes n=1000 instant. What was the recursion wasting?",
    "The same subproblems were being recomputed an exponential number of times; fib(n) has only n distinct values, but the unmemoized tree re-solves each one repeatedly, and storing each result once collapses that redundant work to linear",
    [
      ["The recursion was hitting a stack-depth limit that the cache removes", "Stack depth grows only linearly with n and is not the bottleneck; the cache fixes redundant recomputation, not recursion depth.", "The blow-up is in repeated work across the tree's width, not in how deep any single chain goes."],
      ["Each addition of large Fibonacci numbers is slow, and caching avoids the arithmetic", "Individual additions are cheap; the cost was calling fib on the same argument billions of times, not the additions themselves.", "Count how many times fib(k) is invoked, not how expensive one addition is."],
      ["The cache lets the computer parallelize the two recursive branches", "Memoization is purely about reuse, not parallelism; it stays single-threaded and still wins by eliminating repeated work.", "The speedup is from not repeating work, not from doing work simultaneously."],
    ],
    "Dynamic programming pays off precisely when a problem has overlapping subproblems: the recursion keeps asking the same questions, and the naive tree answers each one from scratch, exploding to exponential time. Memoization writes each answer down once, so the count of real work drops to the number of distinct subproblems. The conceptual hook is that the gap between exponential and linear here is not cleverer math but simply refusing to forget, which is why Richard Bellman framed dynamic programming around a table of solved subproblems rather than a faster recurrence.",
    'Floe generated',
    true,
    "Ask how many genuinely distinct values fib must compute, then how many times the naive version recomputes each.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10095005,
    'Software',
    'Module 3: DP',
    "When dynamic programming has nothing to offer",
    "An engineer wraps every recursive function in a memoization cache by reflex, expecting a speedup. On a merge-sort implementation the cache only adds overhead and memory with zero benefit, even though the recursion is deep. What property must a problem have for dynamic programming to help, and why is it absent here?",
    "DP helps only when subproblems overlap and recur; merge sort splits the input into disjoint halves that are never solved twice, so there is nothing to reuse and the cache can only waste memory",
    [
      ["Merge sort lacks optimal substructure, so caching has nothing correct to store", "Merge sort does have optimal substructure (sorted halves merge into a sorted whole); it simply lacks overlapping subproblems, so DP gains nothing.", "Optimal substructure alone does not justify DP; the missing ingredient is overlap."],
      ["Merge sort is iterative under the hood, so memoization never triggers", "Merge sort is genuinely recursive; the cache triggers fine but finds no repeated arguments to hit.", "The cache runs; it just never scores a hit because no subproblem repeats."],
      ["The cache keys collide because sublists are hard to hash, hiding the benefit", "Even with perfect keys there would be no benefit, because each sublist is sorted exactly once.", "Assume ideal hashing; the result is unchanged, which points to absence of overlap, not key collisions."],
    ],
    "Dynamic programming is not a universal speed switch; it is the right tool only when a problem has both optimal substructure and overlapping subproblems. Divide-and-conquer algorithms like merge sort have the first but not the second: their recursion partitions the work into pieces that never reappear, so a cache stores answers no one will ask for again. The deeper lesson is that the cost of an abstraction is real, and recognizing when subproblems do not overlap is as important as recognizing when they do, since misapplied DP buys you memory pressure in exchange for nothing.",
    'Floe generated',
    true,
    "Ask whether merge sort ever solves the exact same sublist more than once.",
    { challengeRating: 6 },
  ),
  // ----------------------------------------------------------------------------
  // Module 4: System Design
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10095006,
    'Software',
    'Module 4: System Design',
    "The choice CAP only forces sometimes",
    "In a design review someone declares, 'By the CAP theorem we must permanently sacrifice either consistency or availability, so let's pick AP forever.' You want to design more precisely. When does CAP actually force the consistency-versus-availability tradeoff, and what does that imply for normal operation?",
    "The tradeoff is forced only during an actual network partition; when the network is healthy a distributed system can deliver both consistency and availability, so the real design decision is how to behave specifically when nodes cannot reach each other",
    [
      ["CAP forces the choice at all times, because every distributed system is always partitioned to some degree", "Outside a partition a system can be both consistent and available; CAP constrains behavior only during the partition itself, not continuously.", "Treat a partition as a discrete failure event, not a permanent background condition."],
      ["You can simply choose all three by adding more replicas to tolerate partitions", "More replicas do not escape the theorem; during a partition you still must choose whether a cut-off side answers (availability) or refuses (consistency).", "Replication improves durability and read scale but does not repeal the partition-time tradeoff."],
      ["Partition tolerance is the property you drop, keeping consistency and availability", "For any real network that can drop messages, partition tolerance is non-negotiable; the genuine choice is between C and A under partition.", "You cannot opt out of network failures, so P is assumed and the trade is between the other two."],
    ],
    "CAP is widely misread as a permanent, three-way menu. In truth, partitions are inevitable, so P is assumed, and the theorem only bites during a partition: while the network is healthy a system can be both consistent and available. That reframes design from a slogan into a concrete question, namely what a node should do when it is cut off, answer with possibly-stale data (AP) or refuse to answer to stay correct (CP). The lingering insight is that the hard architectural decisions live in the rare failure window, not in the common case everyone tests.",
    'Floe generated',
    true,
    "Ask what specific event has to be happening for CAP to force you to choose at all.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10095007,
    'Software',
    'Module 4: System Design',
    "The stampede a single expiring key can cause",
    "A read-heavy service caches a popular item with a short time-to-live. The cache works perfectly until, under peak traffic, that one key expires; the database instantly jumps from roughly 1,300 to 17,000 queries per second and nearly falls over. The cache did exactly what it was told. What pattern caused the spike, and what is the fix in spirit?",
    "A thundering herd: the instant the hot key expired, every concurrent request missed and hit the database simultaneously to recompute it; the fix is to let only one request recompute while others wait or serve stale, for example via a lock, lease, or single-flight",
    [
      ["The cache was simply too small and evicted too much data under load", "Capacity was not the issue; a single expiring hot key triggered the surge, and a bigger cache would not stop simultaneous misses on that one key.", "Look at what happens at the moment one popular entry expires, not at overall cache size."],
      ["The database had a slow query that only appeared at high traffic", "The query speed did not change; the count of identical concurrent recomputations exploded, overwhelming an otherwise-fine query.", "The spike is in request volume converging on one recompute, not in per-query latency."],
      ["A longer time-to-live would have permanently prevented the problem", "A longer TTL only delays the same stampede to whenever the key eventually expires; it does not address the simultaneous-miss behavior.", "Tuning TTL postpones the herd rather than coordinating who recomputes."],
    ],
    "Caching trades correctness risk for load relief, and the thundering herd is where that trade turns sharp: a single popular key expiring lets thousands of in-flight requests all miss at once and stampede the origin. The remedy is coordination, not a bigger cache, by ensuring exactly one request regenerates the value while the rest wait or serve slightly stale data (locks, leases, or single-flight, the technique that cut Facebook's peak database load by over 90%). The deep idea is that synchronized expiry turns independent clients into a mob, so good cache design manages the moment of invalidation, the famously hardest part of caching.",
    'Floe generated',
    true,
    "Ask what every concurrent reader does in the same instant the one hot key expires.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10095008,
    'Software',
    'Module 4: System Design',
    "Estimating before you architect",
    "You are designing a photo-sharing service for 100 million daily active users who each upload 2 photos a day, averaging 1.5 MB per photo. An interviewer asks for the daily new-storage estimate before any architecture talk. Roughly how much new storage per day, and why does this number come first?",
    "About 300 TB per day (100M users times 2 photos times 1.5 MB); the back-of-the-envelope estimate comes first because it tells you whether one disk, one database, or massive sharding and object storage is even in the right ballpark",
    [
      ["About 300 GB per day, so a single large server can hold months of uploads", "The arithmetic is off by a thousandfold: 200 million photos times 1.5 MB is roughly 300 TB, not 300 GB, which rules out a single server immediately.", "Recheck the order of magnitude; 200M times ~1.5MB lands in terabytes, not gigabytes."],
      ["You cannot estimate without the read-to-write ratio, so storage comes later", "Daily new storage depends only on writes and is computable now; the read ratio matters for caching and bandwidth, not for how much new data lands.", "New-storage volume is a write-side quantity you can size before discussing reads."],
      ["About 300 PB per day, so even global object storage cannot keep pace", "That overshoots by a thousandfold; 300 TB per day is large but well within real object-storage systems, so the design is hard but not impossible.", "Avoid inflating the estimate; 200M times 1.5MB is ~300TB, demanding but tractable."],
    ],
    "Capacity estimation is the cheapest decision-making tool in system design: a one-line calculation (100M times 2 times 1.5 MB is roughly 300 TB per day) instantly tells you that a single server or a naive relational table is hopeless and that sharded object storage is the only sane starting point. Doing the math first prevents you from designing an elegant architecture for the wrong scale. The conceptual point is that order-of-magnitude reasoning, not precision, is what unlocks the design space, and being off by a factor of a thousand, as the tempting wrong answers are, would send you down an entirely wrong path.",
    'Floe generated',
    true,
    "Multiply users by photos by size, then watch the order of magnitude, not the exact digits.",
    { challengeRating: 6 },
  ),
]
