import { makeSimpleQuestion } from '../base'
import type { Question } from '../types'

export const cybersecOpsJargonGems: Question[] = [
  // ===== Threat Vocabulary and Frameworks =====
  makeSimpleQuestion(
    10031000,
    'Career Skills',
    'Threat Vocabulary and Frameworks',
    'The block that the attacker never noticed',
    "Your team proudly blocks the SHA-256 hash of a malware sample seen in last week's intrusion. A skeptical lead asks: \"How long until that block stops mattering to a competent adversary?\" In the Pyramid of Pain framework, why does blocking a file hash cost a capable attacker almost nothing?",
    "A hash identifies one exact file, so the attacker recompiles or re-packs the payload to produce a brand-new hash in seconds; the behavior is unchanged but your indicator is already dead",
    [
      ["Because hashes expire on a fixed schedule, so the block silently lapses after a set retention period and must be renewed", "Hash blocks do not time out on their own; the problem is that the artifact itself is trivially regenerated, not that the rule has a clock.", "The indicator dies because the file changes, not because the rule ages out; nothing about retention is doing the work here."],
      ["Because a hash only matches inbound email attachments, so the attacker simply switches to a download link and sidesteps the rule entirely", "A hash can be matched anywhere the file appears, not just in email; the weakness is that any byte change makes a new hash, regardless of delivery channel.", "Delivery channel is not the issue; the same payload over any channel still yields a new hash once it is recompiled."],
      ["Because hashes are too computationally expensive to check at scale, so defenders quietly drop the rule under load and the attacker walks in", "Hash lookups are cheap and routinely run at line rate; the framework's point is about adversary cost to evade, not defender cost to detect.", "Performance is not the lesson; even a perfectly enforced hash block is defeated by a one-bit change to the file."],
    ],
    "The Pyramid of Pain (David Bianco, 2013) ranks indicators by how much pain it costs the attacker when you deny them. Hashes sit at the very bottom: they pin one exact file, and an attacker recompiles or re-packs to mint a fresh hash in seconds, leaving their actual behavior untouched. The deep idea is that not all detections are equal even when all of them 'fire' correctly. A block can be technically perfect and strategically worthless. Climbing the pyramid toward tactics, techniques, and procedures (TTPs) means detecting how the adversary operates rather than which bytes they shipped, because TTPs are the one thing they cannot swap out without redesigning the attack. The question to ask of any indicator is not 'will it match?' but 'how cheaply can they make it stop mattering?'",
    'Floe generated',
    true,
    'Ask what it costs the attacker to evade the indicator, not whether the indicator matches.',
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10031001,
    'Career Skills',
    'Threat Vocabulary and Frameworks',
    'IOC versus IOA, after the fact versus in the act',
    "Two detections land. The first: a connection to an IP address that appeared on a threat feed yesterday (an IOC). The second: a Word document spawning PowerShell, which then spawns a network connection (an IOA). A new analyst says they are basically the same alert in different clothes. What does the IOA give you that the IOC fundamentally cannot?",
    "The IOA describes a behavior in progress, so it can catch the attacker even when every file, IP, and hash is brand-new and absent from any feed; the IOC can only match things someone has already seen and catalogued",
    [
      ["Nothing meaningful in practice, because both are just entries on a threat-intelligence list and the only real difference is how recently each was added", "An IOA is not a feed entry at all; it is a pattern of activity, which is why it can fire on infrastructure no list has ever recorded.", "The two are categorically different: one matches known artifacts, the other matches behavior, so 'recency on a list' misses the point entirely."],
      ["The IOA is always higher confidence than the IOC, so you can skip triage and declare a confirmed incident the moment a behavioral rule fires", "Behavioral rules can absolutely fire on legitimate activity (admins use PowerShell too); an IOA raises a behavioral question, it does not pre-confirm a breach.", "An IOA improves coverage of novel attacks, but it still needs scoping and confirmation; treating it as auto-confirmed is the over-trust trap."],
      ["The IOA tells you exactly which malware family is involved, while the IOC only gives you a meaningless address with no attribution value", "Attribution is not the dividing line; an IOA describes a method, not a family name, and IOCs frequently carry rich attribution context.", "The real distinction is known-artifact versus live-behavior, not which one names the threat actor."],
    ],
    "An Indicator of Compromise is a fingerprint of something already observed: a hash, an IP, a domain. An Indicator of Attack is a description of behavior unfolding now: this kind of process spawning that kind of child and reaching out to the network. The conceptual hook is the difference between memory and pattern. IOCs are a memory of past attacks and can only ever recognize what someone has already catalogued, which means they are structurally blind to anything new. IOAs encode a pattern of how attacks behave, so they can catch a campaign whose every file and address is freshly minted. That is also their cost: behavior is shared with legitimate activity, so IOAs trade the IOC's precision for the ability to see the genuinely novel. A mature program runs both, and knows that a quiet IOC feed is not the same as a quiet network.",
    'Floe generated',
    true,
    'One indicator remembers known artifacts; the other recognizes live behavior. Which survives a brand-new attack?',
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10031002,
    'Career Skills',
    'Threat Vocabulary and Frameworks',
    'A CVSS 9.8 that your environment can ignore',
    "A vendor advisory drops with a CVSS base score of 9.8 (Critical, in the 9.0-10.0 band). A junior analyst wants to declare an all-hands emergency. Before anyone panics, what is the most important thing the CVSS base score is NOT telling you?",
    "Whether the vulnerable product is even present, reachable, and exploitable in your specific environment; the base score rates the flaw's intrinsic severity in the abstract, not your actual risk",
    [
      ["Whether the flaw has been assigned an official CVE identifier yet, since a 9.8 score is meaningless until the CVE number is published", "A CVSS score and a CVE ID are separate things; a high base score already implies the flaw is characterized, and the missing piece is local applicability, not paperwork.", "The gap is environmental relevance, not whether an identifier exists; a CVE number does not tell you if you are exposed."],
      ["Whether the weakness belongs to a recognized CWE category, because severity is only valid once the underlying weakness class is confirmed", "CWE classifies the type of weakness for engineering purposes; it neither raises nor lowers the urgency of patching an asset you actually run.", "Knowing the weakness class is useful for prevention, but it is not what converts a 9.8 into your risk; exposure of your assets is."],
      ["Whether the score will rise over time, since CVSS base scores are provisional and routinely revised upward after disclosure", "The base score is the stable, intrinsic component by design; what varies is your environment, which the base score deliberately excludes.", "The missing factor is local context, not future score inflation; the base metric is meant to be the fixed part."],
    ],
    "CVSS base scores measure the intrinsic, environment-independent severity of a vulnerability, and in CVSS 4.0 a 9.0-10.0 lands in the Critical band. But intrinsic is the operative word: the base score deliberately knows nothing about whether you run the affected product, whether the vulnerable path is exposed, or whether a compensating control already blocks it. The deeper lesson is that a number can be perfectly accurate and still answer the wrong question. Treating the base score as your risk is how teams burn a weekend patching software they do not deploy while a Medium-rated flaw on an internet-facing box waits. Risk is severity multiplied by exposure; CVSS hands you only the first factor, and the analyst's job is to supply the second before sounding the alarm.",
    'Floe generated',
    true,
    'A base score rates the flaw in the abstract. Risk needs the part it leaves out: are you actually exposed?',
    { challengeRating: 6 },
  ),

  // ===== Identity and Access Language =====
  makeSimpleQuestion(
    10031003,
    'Career Skills',
    'Identity and Access Language',
    'The breach with no failed login',
    "A user is fully phished through a proxy page: the attacker captures not the password but the live session token issued after a successful MFA prompt. The attacker now reaches the mailbox. The SOC stares at the logs and sees only normal, successful sign-ins. Why does multi-factor authentication fail to stop this, and what is the right response?",
    "MFA protects the moment of logging in, not the session that follows; with a valid token the attacker is already 'inside' and never re-authenticates, so the fix is to revoke the active sessions and rotate the credential, not just reset the password",
    [
      ["MFA did stop it, so the logs are simply wrong; the right move is to trust the MFA system, ignore the anomaly, and close the case as a misconfigured log source", "The logs are accurate: a stolen token replays as a legitimate successful session, which is exactly why there is no failed prompt to alert on.", "Believing MFA covers the whole session is the core misconception here; the absence of failures is the attack's signature, not a logging bug."],
      ["The user must have approved a fraudulent push, so resetting the password closes the hole completely and no further action is required", "Even if a push were involved, resetting the password leaves the already-issued token alive; the live session keeps working until it is explicitly revoked.", "Rotating the password without killing the session is the classic half-fix; the attacker's token outlives the new password."],
      ["The attacker brute-forced the second factor, so enabling rate limiting on MFA attempts is the correct and sufficient remediation", "Nothing was brute-forced; a single successful authentication produced a token that was then stolen, so attempt-limiting addresses an attack that did not happen.", "The defense must target session validity, not failed-attempt volume; rate limiting protects a door the attacker walked through legitimately."],
    ],
    "Multi-factor authentication answers one question, 'is this the right person logging in right now?', and once it says yes, it issues a session token that vouches for the user without asking again. Steal that token and you inherit the authenticated context with no password, no prompt, and no failed attempt to trip an alert; to the logs it is a clean, successful sign-in. This is the conceptual heart of modern identity attacks (pass-the-cookie, the cloud cousin of pass-the-hash): the credential is no longer the only thing worth stealing, because the proof of authentication is itself a portable object. The practical consequence is brutal: resetting a password does nothing to a live token. Effective response means revoking sessions and rotating the credential together, because the attacker's foothold is the session, not the secret.",
    'Floe generated',
    true,
    'Ask what MFA actually proves, and for how long, after it says yes.',
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10031004,
    'Career Skills',
    'Identity and Access Language',
    'Rotate, revoke, or disable',
    "An incident touches three things at once: a leaked API key still being used by automation, a stolen browser session token actively reaching your SaaS, and the account of an employee who resigned amicably last month but whose access was never removed. A teammate wants to 'just reset everyone's passwords.' For these three, which action actually addresses each exposure?",
    "Rotate the API key (issue a new secret and retire the old), revoke the session token (invalidate it so it can no longer be replayed), and disable the departed employee's account (remove the standing access path entirely); a password reset addresses none of them cleanly",
    [
      ["Reset the password on all three, because a fresh password is the universal remedy that invalidates keys, tokens, and dormant accounts alike", "A password is not the secret behind an API key or a live token, and a friendly departure still needs the account removed; one lever cannot fix three different access paths.", "Each exposure is a different kind of artifact, so the correct verbs differ: rotate the key, revoke the token, disable the account."],
      ["Disable all three accounts immediately, since fully locking every identity is always the safest containment and avoids overthinking the distinctions", "Disabling the automation identity may break production it legitimately runs, and a token can be revoked without disabling the human user; over-disabling trades one outage for the incident.", "Containment should be the narrowest action that closes each specific path, not a blanket lockout that breaks working systems."],
      ["Only rotate the API key and treat the token and the ex-employee account as low priority, since a friendly resignation poses no real risk", "A live stolen token is an active intrusion, and a friendly departure that left standing access is exactly the offboarding gap attackers reuse; neither is low priority.", "An amicable exit is a governance risk, not a trust question; unremoved access is dangerous regardless of how the person left."],
    ],
    "Credential, token, and account are three different access artifacts, and each has its own correct verb. You rotate a secret (key or password) by minting a new one and retiring the old. You revoke a session or token by invalidating the live artifact so it cannot be replayed, which a password change does not do. You disable an account to remove a standing identity that should no longer exist, such as a departed employee's. The deeper point is that 'reset the password' is a reflex that maps to only one of these and misses the other two, and that a friendly resignation is a governance exposure, not a trust judgment: unremoved access is dangerous no matter how warmly someone left. Naming the artifact precisely is what turns panic into the right, narrowest action.",
    'Floe generated',
    true,
    'Name the artifact first: secret, live session, or standing identity. The right verb follows from the noun.',
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10031005,
    'Career Skills',
    'Identity and Access Language',
    'What "impossible travel" really claims',
    "An alert reads: \"Impossible travel — user signed in from Boston at 09:00 and from Singapore at 09:20.\" An executive asks whether this proves the account is hacked. What does the signal actually assert, and why must you still investigate rather than declare a breach?",
    "It asserts only that two sign-ins are too far apart in space to be the same human in the elapsed time, which is highly suspicious but has innocent explanations like a VPN, a corporate proxy, or a mobile carrier routing traffic through a distant data center",
    [
      ["It proves two different people used the account, so you can skip investigation and immediately notify regulators of a confirmed data breach", "The signal flags geographic impossibility, not identity; a single user behind a VPN or roaming SIM trivially produces it, so it is a lead, not a confirmation.", "Declaring a breach on a geolocation heuristic is exactly the 'breach on aesthetics' error; the alert opens an investigation, it does not close one."],
      ["It measures that the user typed too fast to be human, so it is really a bot-detection signal unrelated to login location", "Impossible travel is about the distance between two login geolocations versus the time between them, not typing cadence; it has nothing to do with input speed.", "The mechanism is geographic distance over elapsed time, so framing it as keystroke analysis misreads the signal entirely."],
      ["It confirms the password was stolen specifically, so rotating the password alone fully resolves whatever the alert detected", "The alert says nothing about which credential or token was used, and if a live session is involved a password reset leaves it intact; the signal does not pin the mechanism.", "Impossible travel localizes nothing about the attack method, so jumping straight to a password reset assumes facts the alert never established."],
    ],
    "Impossible travel is a geolocation heuristic: it fires when two authenticated sessions originate from places too far apart for one person to physically traverse in the time between them. The phrase sounds like proof, and that is the trap. It is genuinely useful precisely because real account takeovers often light it up, but it asserts only a spatial impossibility, and benign causes abound: a VPN exit node, a corporate proxy in another region, or a phone whose carrier routes traffic through a distant gateway. The lasting lesson is the discipline of separating a strong signal from a confirmed conclusion. A good analyst treats impossible travel as a reason to enrich and scope (whose IPs, which device, was MFA satisfied, is a session live) rather than a verdict to broadcast. Declaring breach on the name of an alert is how teams notify the world about a salesperson's VPN.",
    'Floe generated',
    true,
    'The alert claims a distance, not an attacker. Decide what innocent thing could also produce it.',
    { challengeRating: 6 },
  ),

  // ===== Containment, Eradication, and Recovery =====
  makeSimpleQuestion(
    10031006,
    'Career Skills',
    'Containment, Eradication, and Recovery',
    'The reboot that destroyed the case',
    "A workstation is beaconing to a command-and-control server. An eager responder's first instinct is to reboot it to 'clear' the malware. Before they touch the power, the incident commander stops them. Citing the order of volatility, what is lost the instant that machine is rebooted, and what should happen first?",
    "Volatile evidence that exists only in memory — running processes, live network connections, and encryption keys — is destroyed on reboot, so you should capture RAM and the live connection state before any action that cuts power or restarts the host",
    [
      ["Nothing important is lost, because a reboot writes everything to disk first, so the forensic image of the drive captures the full picture afterward", "Memory contents, live sockets, and in-RAM keys are not flushed to disk on reboot; they simply vanish, which is why volatile data is collected before powering down.", "Disk imaging captures what was written, not what lived only in RAM; the reboot erases exactly the volatile layer the case may depend on."],
      ["The malware's hash is lost, so the priority is to record the file hash and then reboot freely once the indicator is documented", "A file hash is the least volatile, most reproducible artifact; the irreversible loss is the live memory and connection state, not a hash you can recompute later.", "Hashes survive a reboot trivially; the order of volatility says to rush the things that disappear, which are registers, memory, and active connections."],
      ["The patch level is lost, so you must record the installed updates before restarting and then the reboot is safe to proceed", "Patch and configuration data are among the most stable, least volatile items and survive a restart; the order of volatility prioritizes memory and live state over static config.", "Configuration data is durable; the reboot's real cost is the volatile in-memory evidence, which no amount of patch-level notes can recover."],
    ],
    "The order of volatility (codified in RFC 3227) ranks evidence from most fleeting to most durable: CPU registers and cache, then RAM and live network connections, then temporary files, then disk, then logs and archives. Collect from the top down, because a reboot or shutdown vaporizes everything in the volatile tiers in an instant. The conceptual hook is a real tension in incident response: the instinct that feels most like 'fixing it' (reboot, reimage, power off) is often the very act that destroys the proof of what happened and where the attacker went. A C2 beacon means the live connection and the malware's footprint in memory are sitting right there, recoverable only while the machine stays running. Capture volatile state first; only then contain. The fastest hand is not always the right one, because some evidence does not get a second chance.",
    'Floe generated',
    true,
    'Rank evidence by how fast it disappears, and grab the fleeting layer before any action that cuts power.',
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10031007,
    'Career Skills',
    'Containment, Eradication, and Recovery',
    'Contained is not cured',
    "You isolate a compromised server from the network within minutes. The incident commander asks: \"Are we done?\" A teammate says yes — the bleeding has stopped. Using the classic NIST SP 800-61 Rev. 2 four-phase lifecycle (Preparation; Detection and Analysis; Containment, Eradication, and Recovery; Post-Incident Activity), why is containment not the finish line, and what still remains?",
    "Containment only stops the spread; eradication still has to remove the root cause — the malware, the exploited vulnerability, and especially any persistence the attacker planted — before recovery, or the moment you reconnect the host the attacker is back",
    [
      ["Yes, you are done, because once a host is isolated the attacker has no path back and the remaining steps are optional documentation", "Persistence mechanisms (scheduled tasks, implanted accounts, backdoors) survive isolation and reactivate on reconnection; eradication is mandatory, not paperwork.", "Isolation pauses the threat; it does not remove it, so declaring victory at containment is the textbook 'persistence still live' failure."],
      ["No, the next step is to restore the server from its most recent backup, since a fresh restore automatically eliminates whatever the attacker did", "If the backup was taken after the compromise it carries the attacker's persistence forward, so recovery before eradication can reinstate the very foothold you isolated.", "Recovery must follow eradication, and the backup's timing matters; restoring a post-compromise image reintroduces the threat under a clean-looking name."],
      ["No, the next step is to immediately patch every server in the fleet, since broad patching is what eradication means and it can run before scoping the breach", "Eradication is targeted removal of this incident's root cause and persistence, scoped to what was actually affected; mass patching without scoping is neither eradication nor sequenced correctly.", "Eradication means removing the attacker's foothold from affected systems, not a fleet-wide patch sprint that skips analysis of what was compromised."],
    ],
    "The classic NIST SP 800-61 Rev. 2 lifecycle deliberately groups Containment, Eradication, and Recovery into one phase because they are interdependent, but they are distinct acts that must run in order (Rev. 3, published 2025, drops the rigid four-phase model and reorganizes incident response around the CSF 2.0 Functions, but this sequencing logic still holds). Containment stops the spread (isolate the host, block the IP) and buys time. Eradication removes the root cause from the environment: the malware, the exploited flaw, and the persistence the attacker planted to survive exactly the kind of isolation you just performed. Recovery restores normal operations only after eradication. The lingering insight is that the most satisfying moment in a response (the bleeding stops) is also the most dangerous place to relax, because a contained host with live persistence is a trap that re-arms the instant it reconnects, and a backup taken after the compromise can quietly carry the foothold across the recovery you thought was clean. 'Contained' answers 'is it spreading?' It never answers 'is it gone?'",
    'Floe generated',
    true,
    'Separate "stopped spreading" from "root cause removed." Which one does isolation actually achieve?',
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10031008,
    'Career Skills',
    'Containment, Eradication, and Recovery',
    'The scoped block versus the kill switch',
    "Malware on one finance workstation is beaconing to a single external IP. A panicked manager wants to cut all internet egress for the entire company to be safe. The incident commander proposes isolating that one host and blocking that one destination instead. Why is the narrower action usually the better containment, and what discipline does it reflect?",
    "Good containment is the smallest action that stops this threat while keeping production alive; isolating the one host and blocking the one destination halts the beacon without an enterprise-wide outage, and you can name its rollback if it turns out to be wrong",
    [
      ["The company-wide egress cut is correct, because maximum containment is always safest and the resulting outage is an acceptable cost in any incident", "Over-containment manufactures its own outage and can do more damage than the incident, so 'maximum' is not 'best'; containment is scoped to the threat, not to fear.", "Blanket egress blocking is the classic over-containment trap: it breaks the whole business to address a single-host beacon that a targeted block already stops."],
      ["Neither action matters, because the only real containment is unplugging the building's main internet line until eradication is fully complete", "Pulling the whole organization offline is the most extreme over-containment of all and is rarely justified by one infected host; targeted isolation handles it.", "Containment is proportional by design; a single beaconing workstation does not warrant taking the entire site dark."],
      ["The narrower action is wrong because partial blocks tip off the attacker, so you must take everything offline at once to preserve surprise", "Containment's goal is reducing harm, not maintaining stealth, and a scoped isolation already cuts the host's path; trading a company outage for 'surprise' inverts the priorities.", "Stealth is not the objective of containment; stopping the spread with the least collateral damage is, which the targeted block accomplishes."],
    ],
    "Containment is a balancing act, not a maximization problem. The aim is to reduce harm with the smallest intervention that actually stops the threat, while keeping the business running and leaving yourself a clean rollback path. Isolating one beaconing host and blocking one destination severs the attacker's channel without darkening the whole company; the company-wide egress cut 'to be safe' simply trades the incident for a self-inflicted outage, which is the textbook over-containment error. The conceptual hook is that fear pushes toward the biggest possible action, but responsibility pulls toward the most precise one: the best containment step is the one you can justify, scope, and undo. Always pair a containment action with the answer to 'what does this break, and how do we reverse it?' If you cannot name the rollback, you have not chosen a containment step, you have caused a second incident.",
    'Floe generated',
    true,
    'Pick the narrowest action that stops the threat and can be rolled back, not the biggest one that feels safe.',
    { challengeRating: 6 },
  ),
]
