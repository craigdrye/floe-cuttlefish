import { makeSimpleQuestion } from '../base'
import type { Question } from '../types'

export const softwareEngineeringRoadmapGems: Question[] = [
  // ----------------------------------------------------------------------------
  // Releases and Migrations
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10096000,
    'Career Skills',
    'Releases and Migrations',
    "Why you rename a column in three deploys, not one",
    "A service stores a user's `phone` column, and the team wants to rename it to `phone_e164`. The quick fix is one migration that renames the column and one deploy of the new code that reads `phone_e164`. A senior engineer blocks it and insists on adding the new column first, then deploying code, then dropping the old column in a later release. Why does the one-step rename create an outage that the three-step version avoids?",
    "A deploy is not instantaneous: for a window, old code (still reading `phone`) and new code (reading `phone_e164`) run at the same time against one database, so whichever single-column schema you pick, half the running instances are querying a column that does not exist",
    [
      ["The single rename is fine; the database simply locks the table during the rename, so requests queue briefly and then resume against the new column", "Even if the rename itself were instant and lock-free, the problem is the code, not the lock: the old still-running instances reference a column name that no longer exists, and they fail for the whole rollout window.", "The hazard is the version skew between running code and schema, not how long the ALTER holds a lock."],
      ["Three deploys is just cautious ceremony; you could do it in one deploy if you wrapped the rename and the code change in a single database transaction", "A transaction cannot span the application rollout: your code rolls out instance by instance over minutes, and no DB transaction holds across that, so old and new code still coexist on one schema.", "Atomicity inside the database does not make a fleet-wide code rollout atomic."],
      ["The real fix is to take a short maintenance window so no requests hit the service while the rename and deploy happen together", "A maintenance window 'solves' it only by accepting downtime, which is exactly what the expand/contract sequence exists to avoid; reversibility and zero-downtime are the goal, not a guaranteed-quiet database.", "Choosing downtime is conceding the problem, not the engineering answer to it."],
    ],
    "This is the expand/contract (parallel change) pattern, and the deep idea is that a deployment is a period, not a moment. While instances roll over, two versions of your code run against one shared schema, so any schema that only one version understands will break the other. Expand first (add `phone_e164`, write to both, keep `phone`), then migrate reads, then contract (drop `phone`) once nothing references it. Each step is independently backward compatible, which is also what makes each step reversible. The lingering point: 'backward compatible' is not politeness toward old clients, it is the only way change and uptime can coexist while a system is mid-transition with itself.",
    'Floe generated',
    true,
    "During a rollout, ask which column the not-yet-upgraded instances are still querying.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10096001,
    'Career Skills',
    'Releases and Migrations',
    "The version number is a promise, not a changelog",
    "A library is at version 2.4.1. A maintainer changes the default timeout and removes an old function that some callers still import, then releases it as 2.5.0 because 'it is mostly new features and the removal is tiny.' Downstream builds that pinned `^2.4` (any 2.x) start failing on upgrade. By the rules of semantic versioning, what should the new version have been, and why?",
    "It should have been 3.0.0: removing a function that callers import is a backward-incompatible change, and semver reserves the MAJOR bump precisely so that anyone who allows automatic minor upgrades is guaranteed their code still compiles and behaves",
    [
      ["2.5.0 is correct, because the release does add new features, and the MINOR number is what tracks added functionality", "MINOR means backward-compatible additions only; the moment one existing caller must change code, the additions are irrelevant and the change is MAJOR.", "A release is graded by its most breaking change, not by its most flattering one."],
      ["2.4.2 would have been correct, since removing dead-looking code is a cleanup and PATCH covers fixes and tidying", "PATCH is for backward-compatible bug fixes that change nothing callers depend on; deleting a public function callers import is the opposite of backward compatible.", "Whether code looks unused to you does not make removing it a patch for the people still calling it."],
      ["The version number does not really matter here; the fix is for downstream teams to pin the exact version 2.4.1 and never use range upgrades", "Telling every consumer to abandon ranges defeats the entire point of semver, which exists so ranges are safe; the version is a contract the publisher is responsible for honoring.", "Shifting the burden to consumers excuses the publisher from the promise the version number is supposed to encode."],
    ],
    "Semantic versioning is a compatibility contract compressed into three numbers: PATCH = backward-compatible fix, MINOR = backward-compatible addition, MAJOR = something existing callers must adapt to. The discipline is graded by the worst change in the release, so a single removal forces a MAJOR bump no matter how many features ship alongside it. The deeper idea is that the number is a promise to a stranger you will never meet: someone who wrote `^2.4` is trusting you that no 2.x will ever break their build. Mislabeling a breaking change as MINOR does not just annoy people, it silently breaks the one mechanism (safe automatic upgrades) that the whole scheme exists to provide.",
    'Floe generated',
    true,
    "Ask whether any existing caller must change their code; if yes, it is a MAJOR release.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10096002,
    'Career Skills',
    'Releases and Migrations',
    "Why 'just write to both systems' quietly drifts apart",
    "To migrate orders from an old database to a new one with zero downtime, an engineer proposes dual-writing: every order is saved to the old DB and the new DB in the same request handler, one after the other. 'They both get written, so they stay identical.' Months later the two stores disagree on thousands of orders. What is the flaw baked into the dual-write plan from the start?",
    "The two writes are not a single atomic operation: there is no shared transaction across the two systems, so any crash, timeout, or partial failure between write one and write two leaves the first done and the second not, and these gaps accumulate into permanent divergence",
    [
      ["The flaw is write order; if the engineer had written to the new DB first and the old one second, the two stores would have stayed consistent", "Swapping the order changes which store is ahead when a failure lands, not whether failures land; either ordering still leaves one write done and one missing when the handler dies in between.", "Reordering two non-atomic writes relocates the inconsistency; it does not remove it."],
      ["There is no real flaw; dual-write is the standard zero-downtime migration technique, and the divergence must have come from a separate bug in the application logic", "Dual-write is exactly the technique whose hidden cost is divergence; treating it as inherently safe is the named trap, and the gaps come from ordinary partial failures, not an extra unrelated bug.", "Calling the drift 'some other bug' is how teams keep trusting a method whose failure mode is the drift itself."],
      ["The fix is to wrap both writes in a try/catch so that if the second write fails, the code retries it until it succeeds", "Retrying in-process cannot survive the process crashing, the request timing out, or the retry itself failing; durable reconciliation has to live outside the request, not inside a catch block.", "A retry loop narrows the window but cannot close it, because the thing that fails can be the loop's own host."],
    ],
    "The dual-write problem is one of the cleanest lessons in distributed systems: two separate datastores have no common transaction coordinator, so 'write to both' is really 'write to one, then hope you survive long enough to write to the other.' Across millions of requests, the rare in-between failure is a certainty, and small divergences compound silently because nothing is checking. The reason it is a famous trap is that it works perfectly in every demo and every happy-path test, so confidence grows right up until the stores disagree in production. The honest patterns (an outbox, change-data-capture, or a backfill plus reconciliation) all share one admission: you cannot make two systems agree by writing to them twice; you make them agree by giving the truth a single owner and deriving the copy from it.",
    'Floe generated',
    true,
    "Ask what state the data is in if the handler crashes between the first write and the second.",
    { challengeRating: 6 },
  ),
  // ----------------------------------------------------------------------------
  // Incidents and Reliability
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10096003,
    'Career Skills',
    'Incidents and Reliability',
    "Restore the service before you understand it",
    "Checkout is failing for most users. The on-call engineer notices the outage began four minutes after a routine deploy went out. The team is split: one engineer wants to read logs and find the true cause before touching anything 'so we do not act blindly,' another wants to roll back the deploy immediately. Users are losing money every minute. What is the correct first move, and why?",
    "Roll back the deploy now: the timing is strong evidence the deploy caused it, rollback is a fast and reversible way to restore users, and finding the precise root cause is a separate job that can happen after service is back, not while customers are down",
    [
      ["Read the logs and confirm the exact root cause first, because rolling back without proof might remove the wrong change and waste time", "During an active outage the goal is mitigation, not diagnosis; rollback is cheap and reversible, so even if the deploy turns out innocent you have lost little, while every minute spent reading logs costs real users.", "Demanding certainty before acting confuses the incident (stop the bleeding) with the postmortem (explain the wound)."],
      ["Leave the deploy in place and push a quick hotfix forward, since rolling back loses the new feature the deploy contained", "Rolling forward an untested hotfix during an incident adds a second unknown change on top of the first; rollback returns you to a state you already know worked.", "Preserving a feature is never worth prolonging an outage when a known-good state is one button away."],
      ["Open a debate about whether the deploy or an infrastructure issue is more likely, and assign owners to investigate both in parallel before changing anything", "Debating likelihoods while users are down is the classic incident failure; the deploy correlation is enough to justify the reversible action, and analysis is what the blameless postmortem is for.", "Parallel investigation is a postmortem activity; in the moment it just delays the one move that restores service."],
    ],
    "Incident response has a strict order: restore first, explain second, prevent third. The instinct to fully understand a failure before acting feels responsible, but during an outage it is expensive, because diagnosis is slow and uncertain while users keep losing service. Rollback is powerful precisely because it is reversible: a recent deploy plus a sharp onset is enough evidence to act, and if rollback does not help you have lost almost nothing and learned something. The deep tension is that good engineering normally rewards understanding-before-acting, but reliability work inverts it under fire: the most defensible move is the reversible one that helps now, and curiosity is deferred to the postmortem where it cannot cost a customer.",
    'Floe generated',
    true,
    "Separate the question 'what stops the harm now' from 'what exactly went wrong'; do the first one first.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10096004,
    'Career Skills',
    'Incidents and Reliability',
    "The SLA you owe versus the SLO you aim for",
    "A team's customer contract (the SLA) promises 99.9% monthly availability, with refunds owed if they miss it. Internally, an SRE argues the team should run its own SLO at 99.95%, a stricter target than the contract requires. A manager objects: 'Why promise ourselves something harder than what we sold the customer? Just match the contract.' What is the real reason the internal target should be stricter than the external one?",
    "The SLO is an early-warning line set tighter than the SLA so that breaching the internal target triggers action (freeze risky releases, investigate) while there is still budget left before the contract is violated; matching them exactly means the first sign of trouble is already a refund-owing breach",
    [
      ["There is no good reason; an SLO and an SLA measure the same thing, so the internal number should simply equal the contractual one to avoid confusion", "They are not the same thing: an SLA is an external promise with penalties, an SLO is an internal target used to govern behavior, and keeping the SLO tighter buys reaction time before the SLA is at risk.", "Treating SLO and SLA as synonyms erases the buffer that lets a team act before it owes refunds."],
      ["Because a higher number always means better engineering, so the team should target the highest reliability it can regardless of the contract", "Chasing maximum reliability is itself a mistake, because every extra nine costs velocity; the SLO is set deliberately above the SLA for warning room, not pushed as high as possible for its own sake.", "More nines are not free; the point is a margin over the contract, not perfection."],
      ["So the team can advertise the stricter 99.95% figure to customers and win deals on reliability", "An SLO is an internal operating target, not a marketing or contractual number; promising it externally would just turn it into a stricter SLA and destroy its function as a private safety margin.", "Publishing the SLO converts your warning line into a new liability, defeating its purpose."],
    ],
    "An SLA is a contract with consequences; an SLO is an internal target you steer by; an SLI is the actual measured number. Setting the SLO tighter than the SLA creates a margin: when you breach the SLO you have not yet breached the contract, so the breach becomes a prompt to slow down and stabilize rather than a bill you already owe. If the two are equal, your first warning and your first penalty arrive at the same instant, which is too late to be a warning at all. The conceptual hook is that reliability targets are not aspirations to maximize but control thresholds: the gap between SLO and SLA is the room you give yourself to react, and an error budget turns that room into a quantity you can spend on shipping until it runs low.",
    'Floe generated',
    true,
    "Ask which number, when crossed, should make the team change behavior, and which one costs money when crossed.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10096005,
    'Career Skills',
    'Incidents and Reliability',
    "Alert on the symptom, not the cause",
    "A team keeps getting paged at night by an alert that fires whenever a background job queue grows beyond 500 items. Most of the time the queue drains on its own and users never notice, so people have started ignoring the page. An engineer proposes deleting that alert and instead paging when the user-facing API's error rate or latency crosses a threshold. Why is moving the alert to the user-facing symptom the better design?",
    "A growing queue is a possible cause that often resolves itself and may never harm anyone, so paging on it produces false alarms that train people to ignore the page; alerting on user-visible error rate and latency fires only when harm is actually occurring, which keeps the page trustworthy and actionable",
    [
      ["Queue depth is just the wrong threshold; the alert is fine if they raise it from 500 to 5,000 so it only fires on truly large backlogs", "Changing the number does not change the category: queue depth is still a cause that may not reach users, so a higher threshold reduces frequency but keeps alerting on something that does not prove harm.", "Tuning the threshold of a cause-based alert treats a symptom-versus-cause problem as if it were a calibration problem."],
      ["The queue alert should be kept and the API alert added on top, because more alerts always mean faster detection of problems", "Piling on alerts is how you get alert fatigue, where every page is half-trusted and the noisy one trains people to ignore the channel that also carries the real one.", "More alerts is not more safety; an alert nobody trusts is worse than no alert."],
      ["The fix is to keep paging on the queue but route it to a dashboard email instead of a phone page, so the cause is still tracked", "Queue depth is genuinely useful to graph for diagnosis; the error is paging a human on it, and the proposal correctly reserves the page for user impact while leaving the cause visible on a dashboard.", "This is half-right but misframes the change as a delivery-channel tweak rather than the principle of paging on symptoms."],
    ],
    "Good alerting distinguishes a page (wake a human, something is harming users) from a signal (worth graphing, may be diagnostic). A queue backlog is a cause: it sometimes leads to harm and often does not, so paging on it manufactures false alarms, and false alarms are corrosive because they teach people to mute the very channel that must be believed. Alerting on user-visible symptoms (errors and latency) means a page only fires when harm is real, which is what keeps the page worth waking up for. The deeper idea is that an alert is not just a detector, it is a behavioral contract with the on-call human: every cry of wolf spends trust, and a system that pages on causes slowly trains its own responders to ignore it, so the failure that matters arrives to a muted phone.",
    'Floe generated',
    true,
    "Ask whether the thing you are paging on always means a user is being harmed; if it can be loud while everyone is fine, it is a cause.",
    { challengeRating: 6 },
  ),
  // ----------------------------------------------------------------------------
  // Architecture and Tradeoffs
  // ----------------------------------------------------------------------------
  makeSimpleQuestion(
    10096006,
    'Career Skills',
    'Architecture and Tradeoffs',
    "CAP forces a choice only when the network breaks",
    "In a design review, someone says 'our database is AP, so we have chosen availability over consistency, and that is why reads can be stale all the time.' A colleague pushes back: that is not quite what the CAP theorem says. Under CAP, when does a distributed system actually have to choose between consistency and availability?",
    "Only during a network partition: when nodes cannot reach each other, a system must either refuse some requests to stay consistent or answer them and risk returning stale or divergent data; when the network is healthy it can deliver both consistency and availability, and the CAP choice does not apply",
    [
      ["The system must permanently sacrifice one of the three; choosing partition tolerance means it can never offer both consistency and availability at any time", "Partition tolerance is not a thing you trade away on a healthy network; the real tradeoff between consistency and availability only bites during an actual partition, and the rest of the time a system can provide both.", "The 'pick two of three, forever' phrasing is the classic misreading; the sacrifice is conditional on a partition, not permanent."],
      ["It must choose at every single request, because every read in a distributed database is a fresh consistency-versus-availability decision", "Individual reads are governed by the system's consistency model, not by CAP; CAP is specifically about behavior when nodes are cut off from each other, not about routine request handling.", "This inflates a partition-time theorem into a per-request rule it never claimed to be."],
      ["It must choose between consistency and availability whenever traffic is high, since heavy load is what forces the tradeoff", "High load is a capacity and latency concern, not a partition; you can be overloaded and still consistent, and the CAP tradeoff is triggered by nodes losing contact, not by request volume.", "This swaps the partition condition for a load condition, conflating scaling with CAP."],
    ],
    "CAP is one of the most misquoted ideas in engineering. 'Pick two of three' makes it sound like a permanent menu, but partition tolerance is not optional for a real distributed system (networks do fail), and the genuine fork only opens during a partition: when nodes lose contact, you either stop answering to protect consistency or keep answering and accept divergence. When the network is healthy, a system can be both consistent and available, which is why 'we are AP so reads are always stale' is a category error. The lingering insight is that CAP is not a description of normal life but a statement about your worst day, the design question is not 'do you value consistency or availability' in the abstract, but 'when the network splits, which one will you sacrifice and to whom.'",
    'Floe generated',
    true,
    "Ask what changes the instant two nodes can no longer talk to each other; that is the only moment CAP forces a choice.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10096007,
    'Career Skills',
    'Architecture and Tradeoffs',
    "The shortcut that quietly couples two teams",
    "Team A's billing service needs a customer's address, which lives in Team B's user service. To avoid the overhead of calling B's API, an engineer on A connects directly to B's database table and reads the address column. It is faster, fewer moving parts, and it works perfectly in testing. What is the hidden cost that makes this a bad architectural decision?",
    "Reading B's table directly turns B's internal schema into A's dependency: now Team B can no longer rename, restructure, or move that table without breaking A, often without even knowing A exists, so a private implementation detail has silently become a shared contract neither team agreed to",
    [
      ["There is no real cost as long as the database grants read-only access, since A only reads and never writes, so it cannot corrupt B's data", "The danger is not data corruption but coupling: even a read binds A to the exact shape of B's table, so B loses the freedom to change its own schema; the harm is to changeability, not data integrity.", "Read-only access prevents one risk while creating the bigger one, a hidden dependency on B's internal structure."],
      ["The only downside is performance, because querying another team's database adds load, and that can be solved by adding a read replica for A to hit", "A replica addresses load but deepens the coupling, because A is still bound to B's raw schema; the API exists precisely to let B change its internals freely behind a stable contract.", "Solving for load misses the point; the API boundary is about who is allowed to depend on what, not about throughput."],
      ["It is actually the better design, because going through B's API is unnecessary indirection and direct access keeps the system simpler with fewer layers", "The API 'indirection' is the boundary that lets the two teams evolve independently; removing it does not simplify the system, it fuses two services into one tangled unit that cannot change safely.", "Fewer layers here means fewer seams, and seams are exactly what let independent teams move without breaking each other."],
    ],
    "An API contract is a promise about what stays stable; an internal database schema is explicitly not that promise. When A reads B's table directly, B's private structure becomes A's dependency, so B can no longer refactor its own data model without breaking a consumer it may not even know it has. The reason this is so tempting is that it works flawlessly until the day B makes a perfectly reasonable change to its own internals and A breaks in production for reasons that look like B's fault but are really A's. The deep idea is that coupling is invisible at the moment you create it and only reveals its cost when someone tries to change something later; a service boundary is not bureaucratic overhead but the thing that preserves each team's freedom to evolve, and a 'harmless' shortcut across it is a future change tax paid by whoever touches that table next.",
    'Floe generated',
    true,
    "Ask what Team B is now unable to change freely, and whether they ever agreed to that constraint.",
    { challengeRating: 6 },
  ),
  makeSimpleQuestion(
    10096008,
    'Career Skills',
    'Architecture and Tradeoffs',
    "The decision record is for the person who inherits it",
    "Two years ago the team chose a message queue over direct service-to-service calls. Today a new engineer, finding the queue slow and confusing, says 'this was clearly a mistake, nobody remembers why we did it, let us just rip it out and call the service directly.' The original decision was never written down. What does the missing artifact and this reaction together reveal about the deeper failure?",
    "The team lost the reasoning, not just the decision: without an architecture decision record capturing the tradeoff and the constraints at the time, a past choice becomes unquestionable folklore, and the new engineer cannot tell whether the queue is solving a real problem (like absorbing traffic spikes) or is genuinely obsolete",
    [
      ["The failure is purely technical: the queue was the wrong choice, and the lesson is that synchronous direct calls are simpler and should have been used from the start", "Whether the queue was wrong is exactly what nobody can now establish, which is the point; declaring it a technical mistake assumes the very reasoning that was never recorded.", "Calling it a wrong technical choice repeats the original error of judging a tradeoff without knowing the constraints it was made under."],
      ["The failure is that the team chose a queue at all; messaging is inherently over-engineering, and the new engineer is right to remove complexity wherever it appears", "A queue is a deliberate tradeoff (decoupling and buffering at the cost of latency and complexity), not inherent over-engineering; treating all complexity as removable is how teams delete the load-shedding that was keeping them up.", "Branding the pattern itself as the mistake ignores that it may be doing essential work the record would have named."],
      ["The failure is that the queue is too slow, so the real fix is performance tuning, and once it is fast enough the question of why it exists no longer matters", "Speed is a separate concern from purpose; even a fast queue should be removable only after you know what it was protecting against, which is precisely what the missing decision record would tell you.", "Optimizing the queue answers 'is it fast' while dodging the unanswered 'why is it here at all.'"],
    ],
    "An architecture decision record (ADR) exists to defeat a specific failure mode: a choice made under real constraints decays into folklore the moment the people who understood it leave, after which every successor either worships it or rips it out, both blindly. The new engineer's confidence is the tell, because without the why, 'this is obviously wrong' and 'this is obviously right' are equally ungrounded. A queue typically trades latency and complexity for decoupling and the ability to absorb bursts; if that was the reason, removing it could reintroduce the exact outage it prevented. The conceptual hook is that architecture is not just the system you build but the rationale you preserve: a decision you cannot reconstruct is one you cannot safely revisit, so the record is less a courtesy to history than a precondition for ever changing your mind responsibly.",
    'Floe generated',
    true,
    "Before removing it, ask what problem the queue was put there to solve, and whether anyone can actually answer.",
    { challengeRating: 6 },
  ),
]
