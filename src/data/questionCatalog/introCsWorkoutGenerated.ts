import { makeQuestionBank } from './base'
import { polish as runPolish } from './polishPipeline'
import {
  UNI_CSBUSINESS_WORKOUT_SUB_TOPICS,
  UNI_CSBUSINESS_WORKOUT_MENTOR_HINTS,
  UNI_CSBUSINESS_WORKOUT_CORRECT_SHORTENED,
} from './uniCsBusinessWorkoutPolish'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]

function lessonFor(chapter: string, title: string, correct: string) {
  if (/computational thinking/i.test(chapter)) {
    return `${title} is about the habits programmers use before writing code. The useful answer is "${correct}"; look for the option that turns a messy problem into clear steps, smaller parts, patterns, or a simpler model.`
  }
  if (/programs/i.test(chapter)) {
    return `${title} is about how code becomes behavior. The useful answer is "${correct}"; separate written instructions, translation/execution, grammar errors, and mistakes in logic.`
  }
  if (/variables|expressions/i.test(chapter)) {
    return `${title} is about values in a program. The useful answer is "${correct}"; ask what is being stored, computed, compared, or evaluated.`
  }
  if (/conditionals|loops/i.test(chapter)) {
    return `${title} is about control flow. The useful answer is "${correct}"; follow which code runs, when it repeats, and what condition makes the path change or stop.`
  }
  if (/functions/i.test(chapter)) {
    return `${title} is about packaging behavior into reusable pieces. The useful answer is "${correct}"; separate inputs, returned outputs, and side effects outside the function.`
  }
  if (/collections|strings|files/i.test(chapter)) {
    return `${title} is about working with structured data. The useful answer is "${correct}"; track whether the data is ordered, keyed, textual, stored on disk, or processed one item at a time.`
  }
  if (/errors|debugging|testing/i.test(chapter)) {
    return `${title} is about finding out whether code really does what you think it does. The useful answer is "${correct}"; distinguish runtime failures, inspection tools, test cases, and boundary mistakes.`
  }
  if (/style|problem solving/i.test(chapter)) {
    return `${title} is about becoming a practical programmer. The useful answer is "${correct}"; prefer clarity, small pieces, testing, and reasoning over memorizing one lucky example.`
  }
  return `${title} is an intro computer-science concept. The useful answer is "${correct}"; connect the term to the job it does in code before choosing.`
}

const q = (
  id: number,
  chapter: string,
  title: string,
  prompt: string,
  correct: string,
  wrong: [string, string, string][],
) => ({
  id,
  chapter,
  title,
  prompt,
  correct,
  wrong,
  lesson: lessonFor(chapter, title, correct),
  source: 'Generated from Intro CS coverage',
})

const _baseIntroCsWorkoutGeneratedQuestions = makeQuestionBank('Software', [
  q(416001, 'Computational Thinking', 'Algorithm', 'An algorithm is best described as:', 'A precise step-by-step procedure for solving a problem', [miss('A programming language used to write the solution', 'A language can express an algorithm, but the algorithm is the ordered procedure itself.', 'Think procedure.'), miss('Any vague idea with no order', 'Algorithms need clear steps.', 'Precise sequence.'), miss('Only the finished program after it has been built', 'A program may implement an algorithm, but the algorithm is the problem-solving process underneath it.', 'Problem-solving process.')]),
  q(416002, 'Computational Thinking', 'Decomposition', 'Decomposition means:', 'Breaking a large problem into smaller manageable parts', [miss('Combining every feature into one giant step', 'That makes the problem harder.', 'Break it down.'), miss('Skipping the problem statement and starting with code immediately', 'Decomposition begins by understanding the problem well enough to split it into parts.', 'Separate pieces.'), miss('Only changing variable names', 'Naming may help, but decomposition is structural.', 'Smaller subproblems.')]),
  q(416003, 'Computational Thinking', 'Pattern Recognition', 'Pattern recognition helps programmers by:', 'Finding repeated structure that can become a reusable rule or function', [miss('Ignoring similarities between cases', 'That wastes structure.', 'Look for repetition.'), miss('Forcing every case to be hand-coded separately', 'Patterns reduce duplication.', 'Generalize common logic.'), miss('Guessing a separate solution each time without looking for common structure', 'Pattern recognition is about spotting repeatable structure, not starting from scratch on every case.', 'Identify structure.')]),
  q(416004, 'Computational Thinking', 'Abstraction', 'In introductory programming, abstraction means:', 'Focusing on the important idea while hiding unnecessary detail', [miss('Adding every possible implementation detail to the prompt', 'That is the opposite.', 'Hide detail.'), miss('Avoiding functions forever', 'Functions are common abstractions.', 'Simplify interface.'), miss('Removing all meaning from names', 'Good abstraction usually needs meaningful names.', 'Essential idea.')]),
  q(416005, 'Programs', 'Source Code', 'Source code is:', 'Human-readable instructions written in a programming language', [miss('The compiled machine instructions after translation', 'Compiled output may come from source code, but source code is the human-readable version before or during translation.', 'Instructions in text.'), miss('A data file the program opens while running', 'Programs can read data files, but source code is the written instructions that define what the program does.', 'Code text.'), miss('The result a program displays after it runs', 'Output is produced by running code; it is not the source code itself.', 'Program instructions.')]),
  q(416006, 'Programs', 'Interpreter', 'An interpreter generally:', 'Runs program instructions directly, often line by line or statement by statement', [miss('Explains code comments to the user but never executes instructions', 'Interpreters are about running program statements, not just reading comments.', 'Executes code.'), miss('Always converts code into a separate executable before any run', 'That is more compiler-like.', 'Direct execution model.'), miss('Stores source code in a database without running it', 'Storage is separate from interpretation; the interpreter executes the program path.', 'Runtime execution.')]),
  q(416007, 'Programs', 'Syntax Error', 'A syntax error means:', 'The code violates the grammar rules of the language', [miss('The program is logically perfect', 'Syntax errors prevent normal parsing.', 'Grammar problem.'), miss('The program cannot connect to a server at runtime', 'Network failures can happen in valid code; syntax errors happen before normal execution because the language grammar is broken.', 'Language structure.'), miss('The output is always mathematically wrong but parseable', 'That is a logic error, not syntax.', 'Cannot parse correctly.')]),
  q(416008, 'Programs', 'Logic Error', 'A logic error occurs when:', 'The code runs but does the wrong thing', [miss('The code cannot be parsed at all', 'That is syntax.', 'Runs but wrong.'), miss('The program crashes before any intended behavior can be observed', 'Crashes can be runtime errors; a logic error can run normally while producing the wrong result.', 'Behavior problem.'), miss('A variable name is shorter than the team style guide prefers', 'Style problems can hurt readability, but they are not the definition of incorrect program logic.', 'Incorrect logic.')]),
  q(416009, 'Variables', 'Variable', 'A variable is mainly used to:', 'Store a value under a name so the program can refer to it later', [miss('Guarantee the value never changes in any language', 'Some variables can change; constants are separate.', 'Named storage.'), miss('Replace all functions', 'Variables and functions serve different roles.', 'Store data.'), miss('Control only the visual appearance of a program', 'Variables can affect visuals, but their core job is named storage for values the program can use.', 'Named value.')]),
  q(416010, 'Variables', 'Assignment', 'The statement `x = 5` usually means:', 'Store the value 5 in the variable x', [miss('Ask whether x equals 5 in every language', 'Single equals is commonly assignment.', 'Set/store value.'), miss('Remove x from memory so it cannot be used later', 'Assignment gives a name a value; deletion or deallocation is a different operation.', 'Assign value.'), miss('Print x automatically', 'Assignment does not necessarily print.', 'Store, not display.')]),
  q(416011, 'Variables', 'String', 'A string is:', 'A sequence of characters such as text', [miss('Only a whole number', 'That is an integer.', 'Text characters.'), miss('A repeated block of code', 'That is closer to a loop; a string is data made of characters.', 'Text data.'), miss('A lookup position inside a table', 'Indexes may point into strings or tables, but the string itself is the character sequence.', 'Characters.')]),
  q(416012, 'Variables', 'Boolean', 'A Boolean value is usually:', 'True or false', [miss('Any paragraph of text', 'That is string-like.', 'Two truth values.'), miss('Only a decimal number', 'Numbers can be compared to produce Booleans, but the Boolean value itself is a truth value.', 'Truth value.'), miss('A value with many possible categories', 'A Boolean has two main states, so it is not used when many categories are needed.', 'True/false.')]),
  q(416013, 'Variables', 'Type', 'A data type tells the program:', 'What kind of value something is and what operations make sense on it', [miss('Only which file contains the code', 'A file location may help organize code, but the type describes the value itself.', 'Kind of value.'), miss('That every operation is allowed', 'Types restrict or define operations.', 'Valid operations.'), miss('Only how the value should be displayed on the screen', 'Display formatting can use a value, but the type is about what kind of value it is and which operations are valid.', 'Value category.')]),
  q(416014, 'Expressions', 'Expression', 'An expression is code that:', 'Evaluates to a value', [miss('Can never produce a value', 'Expressions do produce values.', 'Evaluate.'), miss('Is only a comment', 'Comments are ignored by execution.', 'Computed value.'), miss('Must always be a full program', 'Expressions can be small parts.', 'Value-producing code.')]),
  q(416015, 'Expressions', 'Operator Precedence', 'Operator precedence decides:', 'Which operations are grouped first when an expression is evaluated', [miss('Which line of code was written earliest in the file', 'File order matters for execution, but precedence is about grouping operations inside one expression.', 'Evaluation order.'), miss('Whether comments are visible in the editor', 'Comments are not evaluated as operations, so visibility is separate from precedence.', 'Operations grouping.'), miss('That addition always happens last in every context', 'Rules depend on operators and parentheses.', 'Order rules.')]),
  q(416016, 'Expressions', 'Modulo', 'The modulo operator is useful for finding:', 'The remainder after division', [miss('The exact quotient only', 'That is division.', 'Remainder.'), miss('Whether one number is evenly divisible by another', 'Modulo can help check divisibility, but it does that by returning a remainder, often 0.', 'Division remainder.'), miss('The rounded result of division', 'Rounding changes a quotient; modulo reports what is left over after division.', 'Remainder arithmetic.')]),
  q(416017, 'Conditionals', 'If Statement', 'An `if` statement lets a program:', 'Run code only when a condition is true', [miss('Repeat code a fixed number of times by itself', 'That is a loop role.', 'Conditional branch.'), miss('Choose a path based only on where the code is saved', 'File location does not decide an if statement; the Boolean condition does.', 'Branching.'), miss('Guarantee both branches run', 'Usually one branch path is selected.', 'Condition controls execution.')]),
  q(416018, 'Conditionals', 'Else Branch', 'An `else` branch runs when:', 'The preceding condition was false', [miss('The preceding condition was true', 'Then the if branch usually runs.', 'Fallback branch.'), miss('The program has no condition', 'Else is tied to a condition.', 'When if fails.'), miss('Every earlier line in the file has finished successfully', 'An else is not a general next-step marker; it is the fallback for a false condition.', 'False branch.')]),
  q(416019, 'Conditionals', 'Comparison', 'A comparison such as `age >= 18` usually produces:', 'A Boolean result', [miss('The number 18 every time', 'The expression uses 18 as a threshold, but the result is whether the comparison is true or false.', 'True/false.'), miss('The original value of age unchanged as the result', 'The variable may stay unchanged, but the comparison evaluates to one truth value.', 'One truth value.'), miss('A syntax error in every language', 'This is common comparison syntax.', 'Boolean expression.')]),
  q(416020, 'Conditionals', 'Compound Condition', 'The expression `a and b` is true only when:', 'Both a and b are true', [miss('At least one is true', 'That is OR.', 'AND needs both.'), miss('Neither is true', 'That would make the AND expression false because both required conditions fail.', 'Both true.'), miss('a is a string', 'Type is not the logic rule.', 'Logical AND.')]),
  q(416021, 'Loops', 'Loop', 'A loop is used to:', 'Repeat a block of code', [miss('Run a block exactly once no matter what', 'Loops are for repetition.', 'Repeat behavior.'), miss('Reset all program state before every statement', 'A loop may update state, but its defining feature is repeated execution.', 'Repetition.'), miss('Only define a reusable type or class', 'Classes organize data and behavior; loops repeat instructions.', 'Repeat instructions.')]),
  q(416022, 'Loops', 'For Loop', 'A `for` loop is especially natural when:', 'You want to process each item in a collection or a known range', [miss('You never want repetition', 'Then a loop is unnecessary.', 'Iterate items.'), miss('You only need a single unrelated assignment', 'Too small for loop need.', 'Known iteration.'), miss('You do not know what should be repeated or how items are selected', 'A for loop is strongest when the repeated work and sequence of items are clear.', 'Repeated processing.')]),
  q(416023, 'Loops', 'While Loop', 'A `while` loop repeats while:', 'Its condition remains true', [miss('Its condition is permanently ignored', 'The condition controls it.', 'Repeat while true.'), miss('The code has no Boolean expression', 'While needs a condition.', 'Conditional repetition.'), miss('A counter reaches the starting value once', 'A while loop depends on the condition each time, not on reaching the initial state again.', 'Condition-driven loop.')]),
  q(416024, 'Loops', 'Infinite Loop', 'An infinite loop often happens when:', 'The loop condition never becomes false', [miss('The loop condition becomes false immediately', 'Then it stops.', 'Never exits.'), miss('There is no repeated execution', 'Infinite loop repeats indefinitely.', 'Condition stays true.'), miss('The loop updates a counter in the wrong direction', 'This can cause an infinite loop because the update may keep the condition true instead of moving toward the exit.', 'Exit condition failure.')]),
  q(416025, 'Loops', 'Accumulator', 'An accumulator variable is commonly used to:', 'Build up a total or result across loop iterations', [miss('Prevent the loop from ever seeing data', 'An accumulator is updated from the data the loop processes, so hiding the data would not build a result.', 'Track running result.'), miss('Only store the loop condition before the loop starts', 'The loop condition controls repetition; an accumulator stores the running value produced during repetition.', 'Running total/state.'), miss('Delete prior results every iteration by design', 'That would defeat accumulation.', 'Add/update over time.')]),
  q(416026, 'Functions', 'Function', 'A function is useful because it:', 'Packages reusable behavior behind a name', [miss('Forces all code to be duplicated', 'Functions reduce duplication.', 'Reusable block.'), miss('Can never take input', 'Many functions take parameters.', 'Named behavior.'), miss('Runs only once and then disappears from the program', 'A function definition can usually be called whenever that named behavior is needed.', 'Reusable logic.')]),
  q(416027, 'Functions', 'Parameter', 'A parameter is:', 'A named input a function can receive', [miss('Only the function’s output', 'That is return value.', 'Input slot.'), miss('A local variable that callers can never influence', 'Parameters are named inside the function, but their values usually come from the caller at call time.', 'Named input.'), miss('A value that must be hard-coded inside the function body', 'Parameters let callers provide values instead of forcing one fixed value in the function.', 'Function input.')]),
  q(416028, 'Functions', 'Return Value', 'A return value is:', 'The value a function sends back to its caller', [miss('A note in the code that explains the function', 'Comments can describe behavior, but a return value is data passed back to the caller.', 'Output from function.'), miss('Only a printed line on screen', 'Printing and returning differ.', 'Send value back.'), miss('A table the function must create before it can run', 'Some functions use tables, but the return value is the result handed back after the call.', 'Function result.')]),
  q(416029, 'Functions', 'Side Effect', 'A side effect happens when a function:', 'Changes something outside its returned value', [miss('Only computes a value without changing anything else', 'That is closer to pure behavior.', 'External change.'), miss('Has no observable interaction beyond its return value', 'Side effects are the observable interactions outside the returned result, such as mutation or output.', 'Effect outside return.'), miss('Returns a value but leaves all outside state untouched', 'Returning a value is not a side effect by itself; the outside-world change is the side effect.', 'Mutation/output/global change.')]),
  q(416030, 'Functions', 'Pure Function', 'A pure function returns the same output for the same input and:', 'Does not change outside state', [miss('Writes to global state every time', 'That is a side effect.', 'No external mutation.'), miss('Depends on random time silently', 'Then same input may not give same output.', 'Deterministic.'), miss('Cannot be tested', 'Pure functions are often easier to test.', 'Predictable behavior.')]),
  q(416031, 'Collections', 'List', 'A list is useful when you need to:', 'Store an ordered collection of values', [miss('Store exactly one value only', 'Lists hold collections.', 'Many ordered items.'), miss('Represent only true or false', 'That is Boolean.', 'Collection.'), miss('Avoid all indexing forever', 'Lists often support indexing.', 'Ordered sequence.')]),
  q(416032, 'Collections', 'Index', 'In many languages, the first item in a list has index:', '0', [miss('1 in zero-indexed languages', 'Many introductory languages like Python use zero-based indexing.', 'First position is 0.'), miss('-1 because negative indexes mean the beginning', 'In languages with negative indexes, -1 often means the last item, not the first.', 'Start at zero.'), miss('The list length', 'That would be out of range for zero-indexed lists.', 'First index.')]),
  q(416033, 'Collections', 'Dictionary', 'A dictionary/map stores:', 'Key-value pairs', [miss('Only unnamed values in order', 'That is more list-like.', 'Lookup by key.'), miss('Only keys with no associated values', 'Keys are useful because each one maps to a value that can be looked up.', 'Keys mapped to values.'), miss('A single Boolean', 'A dictionary can store Booleans as values, but the structure itself maps many possible keys to values.', 'Associative collection.')]),
  q(416034, 'Collections', 'Set', 'A set is useful when you care about:', 'Membership and uniqueness', [miss('Preserving duplicate copies by default', 'Sets remove duplicates.', 'Unique members.'), miss('Keeping every item in insertion order in all languages', 'Some sets preserve order, but uniqueness is core.', 'Membership test.'), miss('Returning values by numeric position only', 'That is list-like.', 'Contains/unique.')]),
  q(416035, 'Collections', 'Iteration', 'Iterating over a collection means:', 'Visiting its items one by one', [miss('Deleting the collection before use', 'Iteration needs the collection available so each item can be processed.', 'Loop through items.'), miss('Looking only at the collection variable name', 'Iteration is about the elements inside the collection, not the spelling of its name.', 'Process each item.'), miss('Using the whole collection once without reaching individual items', 'Iteration steps through elements so the same logic can be applied item by item.', 'Visit elements.')]),
  q(416036, 'Strings', 'String Indexing', 'String indexing lets you:', 'Access a character at a position in the string', [miss('Access a word by its dictionary definition', 'String indexes use numeric positions in the text, not word meanings.', 'Character access.'), miss('Find every matching character automatically', 'Indexing reaches a specific position; searching is a different operation.', 'Position in text.'), miss('Guarantee the string contains only numbers', 'Indexing does not validate content.', 'Access by position.')]),
  q(416037, 'Strings', 'Substring', 'A substring is:', 'A contiguous piece of a larger string', [miss('Characters taken from scattered positions and rearranged', 'A substring keeps characters together in their original order.', 'Part of text.'), miss('The length of the string as a number', 'Length describes how many characters there are; a substring is actual text from within it.', 'Text segment.'), miss('A Boolean condition about whether text exists', 'A substring is character data, not the true/false result of a test.', 'Sequence of characters.')]),
  q(416038, 'Files', 'File Read', 'Reading a file means:', 'Loading data from storage so the program can use it', [miss('Always deleting the file', 'Reading should not delete by itself.', 'Load data.'), miss('Opening the file path but ignoring the stored contents', 'A read operation is useful because it brings the stored data into the program.', 'Access stored data.'), miss('Writing new data into it necessarily', 'Reading and writing differ.', 'Input from storage.')]),
  q(416039, 'Files', 'CSV', 'A CSV file commonly represents:', 'Tabular data with values separated by commas', [miss('Nested objects with no row or column structure', 'CSV is usually flat row-and-column text, not a deeply nested object format.', 'Comma-separated data.'), miss('A binary spreadsheet file with formulas and formatting preserved', 'CSV is plain text data; spreadsheet formatting and formulas are usually lost or flattened.', 'Rows and columns.'), miss('A file where commas are forbidden', 'CSV stands for comma-separated values, so commas commonly separate fields.', 'Table text format.')]),
  q(416040, 'Errors', 'Exception', 'An exception usually signals:', 'An error or unusual condition during program execution', [miss('A comment explaining what the next line does', 'Comments are ignored by execution; exceptions occur while the program is running.', 'Runtime issue.'), miss('A variable assignment that always succeeds', 'A successful assignment is normal execution, while an exception interrupts or changes the normal path.', 'Exceptional condition.'), miss('Only a successful login', 'A login could fail and raise an exception, but success by itself is not an exception.', 'Error-like event.')]),
  q(416041, 'Errors', 'Try Except', '`try`/`except` is used to:', 'Handle exceptions without crashing the whole program path', [miss('Guarantee no error can ever occur', 'Errors can still occur; handling controls response.', 'Catch and handle.'), miss('Skip running risky code entirely every time', 'The try block runs the risky code; the except block handles a matching failure if one occurs.', 'Exception handling.'), miss('Fix every possible bug automatically', 'Exception handling gives the program a planned response, but it does not repair incorrect logic by itself.', 'Runtime error handling.')]),
  q(416042, 'Debugging', 'Print Debugging', 'Print debugging helps by:', 'Showing intermediate values while the program runs', [miss('Proving the code is correct without reasoning', 'It gives evidence, not proof.', 'Inspect state.'), miss('Preventing all syntax errors automatically', 'Prints run only after the code can execute, so they do not automatically prevent parse errors.', 'Runtime visibility.'), miss('Changing the value because it was printed', 'Printing usually observes a value; it should not be confused with updating that value.', 'Display values.')]),
  q(416043, 'Debugging', 'Debugger', 'A debugger lets you:', 'Pause execution and inspect program state step by step', [miss('Deploy the program without checking what happened', 'Deployment is separate from debugging; a debugger helps inspect execution before or during a run.', 'Inspect execution.'), miss('Ignore all variable values', 'Opposite.', 'Look at state.'), miss('Automatically make slow algorithms efficient', 'A debugger can reveal behavior, but it does not change algorithmic complexity for you.', 'Debug tool.')]),
  q(416044, 'Debugging', 'Trace', 'Tracing a program by hand means:', 'Following variable values and control flow step by step', [miss('Guessing the output without reading code', 'Tracing is systematic.', 'Step through.'), miss('Only reading the first line and ignoring later changes', 'Tracing follows each executed step, including updates that happen later.', 'Follow execution.'), miss('Deleting all conditions', 'Conditions are part of the path you trace, not something to remove.', 'Track path and values.')]),
  q(416045, 'Testing', 'Test Case', 'A test case should include:', 'Input, expected output, and the behavior being checked', [miss('Only a description of the feature with no concrete input', 'A feature description helps context, but a test case needs a specific situation to check.', 'Concrete check.'), miss('No expected result', 'Then pass/fail is unclear.', 'Expected behavior.'), miss('Only real production secrets copied from users', 'Tests should avoid unsafe sensitive data and still define inputs and expectations clearly.', 'Inputs and expectations.')]),
  q(416046, 'Testing', 'Edge Case', 'An edge case is:', 'An unusual or boundary input that may reveal bugs', [miss('Only the most typical input', 'That is a normal case.', 'Boundary/unusual case.'), miss('A visual border around the test result screen', 'In testing, edge means boundary of expected input behavior, not page decoration.', 'Boundary condition.'), miss('A guaranteed impossible scenario always', 'Some edge cases are rare but possible.', 'Test boundaries.')]),
  q(416047, 'Testing', 'Off By One', 'An off-by-one bug often appears when:', 'A loop includes one too many or one too few items', [miss('The loop start and stop bounds are chosen correctly', 'Correct bounds avoid the extra or missing item that defines an off-by-one bug.', 'Boundary count error.'), miss('Every index is handled perfectly', 'Then no off-by-one.', 'Check start/end.'), miss('A loop uses inclusive and exclusive limits carefully', 'Off-by-one bugs often come from mixing those limits up, not from handling them carefully.', 'Loop bounds.')]),
  q(416048, 'Style', 'Readable Code', 'Readable code usually favors:', 'Clear names, simple structure, and small understandable pieces', [miss('Maximum cleverness with no explanation', 'Cleverness can hurt maintainability.', 'Clarity wins.'), miss('One giant function for everything', 'Usually harder to read.', 'Small pieces.'), miss('Misleading variable names', 'Names should clarify.', 'Readable intent.')]),
  q(416049, 'Style', 'Comment', 'A useful comment usually explains:', 'Why non-obvious code exists or what constraint it handles', [miss('Only repeats every obvious assignment', 'Low-value comments add noise.', 'Explain why.'), miss('Lists unrelated notes that do not match the code', 'Comments should help explain the nearby code; unrelated or stale notes make it harder to trust.', 'Context not clutter.'), miss('Makes broken code correct', 'Comments do not fix behavior.', 'Clarify intent.')]),
  q(416050, 'Problem Solving', 'Blank File Skill', 'A strong intro CS student should be able to:', 'Turn a small problem statement into working code and debug it', [miss('Only watch tutorials without writing code', 'Watching is not enough.', 'Build and debug.'), miss('Memorize one syntax example and stop', 'Transfer matters.', 'Solve new small problems.'), miss('Avoid testing any program they write', 'Testing is part of programming.', 'Run and inspect.')]),
])

export const introCsWorkoutGeneratedQuestions = runPolish(_baseIntroCsWorkoutGeneratedQuestions, [
  { subTopics: UNI_CSBUSINESS_WORKOUT_SUB_TOPICS, mentorHints: UNI_CSBUSINESS_WORKOUT_MENTOR_HINTS, correctShortened: UNI_CSBUSINESS_WORKOUT_CORRECT_SHORTENED, source: 'uniCsBusinessWorkout' },
])
