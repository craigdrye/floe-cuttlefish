import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe algorithms top-up'

export const algorithmsUniv103TopUpQuestions: Question[] = makeQuestionBank('University', [
  // ---------------- Chapter 1: Asymptotic Analysis ----------------
  {
    id: 7722000,
    chapter: 'Asymptotic Analysis',
    title: 'Sum of Big-O Terms',
    prompt: 'What is the tightest Big-O bound for the running time T(n) = 3n^2 + 100n log n + 50000?',
    correct: 'O(n^2)',
    wrong: [
      miss('O(n log n)', 'The n log n term is dominated by the n^2 term for large n, so it is not the dominant growth.', 'Compare the asymptotic growth of each term and keep only the fastest-growing one.'),
      miss('O(n^2 log n)', 'No single term grows like n^2 log n; the largest term is purely quadratic.', 'You cannot multiply separate additive terms together to form the bound.'),
      miss('O(50000)', 'A constant cannot bound a function that grows without limit as n increases.', 'Constants are dominated by any positive power of n.'),
    ],
    lesson: 'In a sum of terms, the asymptotically largest term determines Big-O. Here n^2 grows faster than n log n and constants, so T(n) = O(n^2). Coefficients and lower-order terms are dropped.',
    source,
    generated: true,
  },
  {
    id: 7722001,
    chapter: 'Asymptotic Analysis',
    title: 'Omega Lower Bound',
    prompt: 'Saying an algorithm is Omega(n^2) tells you what about its running time?',
    correct: 'Its running time grows at least as fast as n^2 for large inputs (a lower bound).',
    wrong: [
      miss('Its running time never exceeds c·n^2 for some constant c.', 'That describes an upper bound, which is the meaning of Big-O, not Omega.', 'Omega is the asymptotic lower bound, not the upper bound.'),
      miss('Its running time is exactly proportional to n^2.', 'Exact tight growth is captured by Theta, which requires both upper and lower bounds to match.', 'A single matching bound (Theta) is stronger than Omega alone.'),
      miss('Its running time is at most quadratic in the average case.', 'Omega says nothing specifically about the average case, and it is a lower not upper bound.', 'Omega bounds growth from below regardless of case.'),
    ],
    lesson: 'Omega(g(n)) is an asymptotic lower bound: the function is at least c·g(n) for large n. Big-O is the upper bound, and Theta is a tight bound holding when O and Omega coincide.',
    source,
    generated: true,
  },
  {
    id: 7722002,
    chapter: 'Asymptotic Analysis',
    title: 'Comparing Growth Rates',
    prompt: 'Which of the following functions grows the fastest as n approaches infinity?',
    correct: '2^n',
    wrong: [
      miss('n^10', 'Any fixed-degree polynomial is eventually overtaken by an exponential function.', 'Exponentials dominate every polynomial for large n.'),
      miss('n log n', 'This grows only slightly faster than linear and is far slower than exponential growth.', 'Compare against pure exponential growth.'),
      miss('n!', 'Although factorial grows faster than 2^n, that makes the claim that 2^n is fastest false only if n! were an option to exceed it — here n! does grow faster.', 'Be careful: n! actually grows faster than 2^n.'),
    ],
    lesson: 'The standard ordering is constant < log n < n < n log n < polynomial < 2^n < n!. Among 2^n, n^10, and n log n the exponential 2^n dominates. Note that n! in fact grows faster than 2^n, which is why the n! distractor is genuinely tempting.',
    source,
    generated: true,
  },
  {
    id: 7722003,
    chapter: 'Asymptotic Analysis',
    title: 'Little-o Meaning',
    prompt: 'The statement f(n) = o(g(n)) (little-o) means precisely that:',
    correct: 'The ratio f(n)/g(n) approaches 0 as n approaches infinity.',
    wrong: [
      miss('f(n)/g(n) approaches a positive constant.', 'A nonzero limiting ratio corresponds to Theta, where f and g grow at the same rate.', 'Little-o requires the ratio to vanish, not stabilize.'),
      miss('f(n) is bounded above by some constant multiple of g(n).', 'That is ordinary Big-O, which allows f and g to grow at the same rate.', 'Little-o is a strictly stronger, non-tight upper bound.'),
      miss('f(n) eventually exceeds g(n).', 'Little-o means f grows strictly slower than g, so it is eventually dominated, not dominant.', 'Reverse the direction: f is negligible relative to g.'),
    ],
    lesson: 'Little-o is a strict upper bound: f(n) = o(g(n)) means f becomes negligible relative to g, formally lim f/g = 0. Big-O allows equal growth, but little-o forbids it.',
    source,
    generated: true,
  },
  {
    id: 7722004,
    chapter: 'Asymptotic Analysis',
    title: 'Amortized vs Worst Case',
    prompt: 'A dynamic array doubles its capacity when full. A single push can cost O(n) due to copying, but the amortized cost per push is:',
    correct: 'O(1)',
    wrong: [
      miss('O(n)', 'Although one resize costs O(n), expensive resizes are rare enough that the cost spread over all operations is constant.', 'Spread the total copying cost across the sequence of operations.'),
      miss('O(log n)', 'Doubling happens a logarithmic number of times, but the total copy work is geometric and sums to O(n) over n pushes, giving O(1) each.', 'Sum the geometric series of copy costs.'),
      miss('O(n log n)', 'The total work over n pushes is linear, not n log n, so the per-operation amortized cost is constant.', 'Total work is O(n), divided by n operations.'),
    ],
    lesson: 'Doubling makes the total copy cost over n insertions a geometric series summing to O(n). Dividing by n operations gives O(1) amortized cost per push, even though individual pushes can be O(n).',
    source,
    generated: true,
  },

  // ---------------- Chapter 2: Sorting Algorithms ----------------
  {
    id: 7722005,
    chapter: 'Sorting Algorithms',
    title: 'Quicksort Worst Case',
    prompt: 'What is the worst-case time complexity of standard quicksort, and when does it occur?',
    correct: 'O(n^2), when partitioning is maximally unbalanced (e.g., a sorted array with a poor pivot choice).',
    wrong: [
      miss('O(n log n) in all cases.', 'Quicksort degrades to quadratic when the pivot repeatedly splits off only one element.', 'Consider what happens when each partition is maximally lopsided.'),
      miss('O(n^2), when the array is already randomly shuffled.', 'Random shuffling generally yields balanced partitions and good performance, not the worst case.', 'The worst case needs consistently unbalanced splits, often from sorted input.'),
      miss('O(n log n), because the pivot always splits the array in half.', 'A fixed pivot rule does not guarantee a balanced split; adversarial input can defeat it.', 'Balanced splits are the best case, not guaranteed.'),
    ],
    lesson: 'Quicksort is O(n log n) on average but O(n^2) in the worst case, occurring when partitions are maximally unbalanced. Randomized pivot selection makes such cases astronomically unlikely.',
    source,
    generated: true,
  },
  {
    id: 7722006,
    chapter: 'Sorting Algorithms',
    title: 'Stable Sorting',
    prompt: 'A sorting algorithm is called stable if it:',
    correct: 'Preserves the relative order of records with equal keys.',
    wrong: [
      miss('Runs in O(n log n) time regardless of input.', 'Stability concerns ordering of equal keys, not running-time guarantees.', 'Stability is about output ordering, not speed.'),
      miss('Uses only a constant amount of extra memory.', 'That describes an in-place algorithm; stability is unrelated to space usage.', 'Do not confuse stability with in-place behavior.'),
      miss('Never makes more than n comparisons.', 'No comparison sort can sort in fewer than about n log n comparisons in the worst case.', 'Stability is a property of equal-key ordering, not comparison count.'),
    ],
    lesson: 'Stability means two elements with equal keys keep their original relative order after sorting. Merge sort and insertion sort are naturally stable; standard quicksort and heapsort are not.',
    source,
    generated: true,
  },
  {
    id: 7722007,
    chapter: 'Sorting Algorithms',
    title: 'Comparison Sort Lower Bound',
    prompt: 'Why can no comparison-based sorting algorithm beat O(n log n) in the worst case?',
    correct: 'There are n! possible orderings, and a binary decision tree distinguishing them needs height at least log2(n!) = Theta(n log n).',
    wrong: [
      miss('Because every comparison takes O(log n) time.', 'Each comparison is O(1); the bound comes from the number of comparisons needed, not their cost.', 'Think about how many orderings must be distinguished.'),
      miss('Because the array must be scanned log n times.', 'The bound is information-theoretic about distinguishing permutations, not about a fixed number of scans.', 'Count the leaves a decision tree needs.'),
      miss('Because recursion always adds a logarithmic factor.', 'Not all sorts use recursion, yet the lower bound still applies to all comparison sorts.', 'The bound is about permutations, independent of recursion.'),
    ],
    lesson: 'Any comparison sort corresponds to a binary decision tree with n! leaves (one per permutation). A binary tree with n! leaves has height at least log2(n!), which by Stirling is Theta(n log n), giving the lower bound.',
    source,
    generated: true,
  },
  {
    id: 7722008,
    chapter: 'Sorting Algorithms',
    title: 'Counting Sort Applicability',
    prompt: 'Counting sort can sort n integers in O(n + k) time. What does k represent, and what limits its use?',
    correct: 'k is the range of input values; it is efficient only when k is not much larger than n.',
    wrong: [
      miss('k is the number of comparisons; it works for any data type.', 'Counting sort makes no comparisons and requires integer-like keys in a bounded range.', 'Counting sort is a non-comparison sort tied to a value range.'),
      miss('k is the recursion depth; it is limited by stack size.', 'Counting sort is not recursive; k is the range of key values.', 'Recall what the counting array is indexed by.'),
      miss('k is a constant, so counting sort is always O(n).', 'The range k is not constant in general; if k is huge, the algorithm becomes impractical.', 'Consider sorting integers up to 2^32 with only 10 elements.'),
    ],
    lesson: 'Counting sort runs in O(n + k) where k is the value range, because it allocates a count array indexed by value. It beats comparison sorts only when k = O(n); for very large ranges it wastes time and space.',
    source,
    generated: true,
  },
  {
    id: 7722009,
    chapter: 'Sorting Algorithms',
    title: 'Heapsort Space',
    prompt: 'Compared to merge sort, heapsort has which key advantage in space?',
    correct: 'Heapsort sorts in place using O(1) extra space, while merge sort needs O(n) auxiliary space.',
    wrong: [
      miss('Heapsort is stable while merge sort is not.', 'It is the opposite: merge sort is stable and heapsort is not.', 'Recall which of the two preserves equal-key order.'),
      miss('Heapsort runs in O(n) time while merge sort is O(n log n).', 'Heapsort is O(n log n), not linear; both share the same time class.', 'No comparison sort sorts in linear time in general.'),
      miss('Heapsort needs O(n log n) extra space for the heap.', 'The heap is built in place within the input array, needing only O(1) extra space.', 'A binary heap can be stored implicitly in the array.'),
    ],
    lesson: 'Heapsort builds a binary heap implicitly in the input array and repeatedly extracts the max, using only O(1) extra space. Merge sort needs O(n) auxiliary space for merging but is stable, unlike heapsort.',
    source,
    generated: true,
  },
  {
    id: 7722010,
    chapter: 'Sorting Algorithms',
    title: 'Insertion Sort Best Case',
    prompt: 'What is the best-case running time of insertion sort, and on what input does it occur?',
    correct: 'O(n), on an already-sorted array.',
    wrong: [
      miss('O(n log n), on a randomly ordered array.', 'Insertion sort never reaches the comparison-sort optimum; its best case is linear on sorted input.', 'Consider an array that needs no shifting.'),
      miss('O(n^2), because it always compares every pair.', 'On sorted input each element is placed after a single comparison, giving linear time.', 'It stops shifting as soon as the order is correct.'),
      miss('O(1), because no work is needed on sorted data.', 'It must still scan all n elements once to confirm order, which is O(n).', 'Even verifying sortedness requires a linear pass.'),
    ],
    lesson: 'Insertion sort is O(n^2) worst case but O(n) best case: on a sorted array each new element triggers only one comparison and no shifts. This adaptivity makes it excellent for nearly-sorted data.',
    source,
    generated: true,
  },

  // ---------------- Chapter 3: Searching ----------------
  {
    id: 7722011,
    chapter: 'Searching',
    title: 'Binary Search Precondition',
    prompt: 'Binary search achieves O(log n) lookup, but only if the input satisfies which precondition?',
    correct: 'The array must be sorted by the search key.',
    wrong: [
      miss('The array must contain distinct elements.', 'Binary search works with duplicates; it only requires sorted order.', 'Sortedness, not distinctness, is the key requirement.'),
      miss('The array length must be a power of two.', 'Binary search handles any length; it simply takes the floor of the midpoint.', 'No size restriction is needed.'),
      miss('The array must be stored in a linked list.', 'Binary search needs O(1) random access, which linked lists do not provide.', 'It requires indexable, sorted storage like an array.'),
    ],
    lesson: 'Binary search repeatedly halves a sorted range, giving O(log n) comparisons. Its correctness depends entirely on the data being sorted; on unsorted input it can miss the target.',
    source,
    generated: true,
  },
  {
    id: 7722012,
    chapter: 'Searching',
    title: 'Hash Table Average Lookup',
    prompt: 'Under simple uniform hashing with a good load factor, the average-case time to search a hash table is:',
    correct: 'O(1)',
    wrong: [
      miss('O(log n)', 'Logarithmic search is characteristic of balanced trees, not hashing under uniform hashing.', 'Hashing computes a position directly rather than narrowing a range.'),
      miss('O(n)', 'Linear search reflects the worst case when all keys collide, not the average case.', 'Good hashing spreads keys to keep chains short on average.'),
      miss('O(n log n)', 'That is a sorting-style cost; a single hash lookup does not sort anything.', 'A lookup touches only one bucket on average.'),
    ],
    lesson: 'With a good hash function and bounded load factor, expected chain length is constant, so search, insert, and delete are O(1) on average. The worst case is O(n) when all keys collide into one bucket.',
    source,
    generated: true,
  },
  {
    id: 7722013,
    chapter: 'Searching',
    title: 'Interpolation Search',
    prompt: 'Interpolation search can outperform binary search on average for which kind of data?',
    correct: 'Uniformly distributed sorted numeric keys, achieving O(log log n) average time.',
    wrong: [
      miss('Unsorted data of any distribution.', 'Like binary search, interpolation search requires sorted input to estimate positions.', 'It still needs sorted data to interpolate a position.'),
      miss('Highly skewed sorted data, where it guarantees O(log log n).', 'On skewed distributions interpolation search can degrade to O(n); the speedup needs uniformity.', 'The fast bound assumes roughly uniform spacing.'),
      miss('Linked lists of strings.', 'It needs random access and numeric keys to compute interpolated indices.', 'Position estimation requires indexable numeric data.'),
    ],
    lesson: 'Interpolation search guesses the probe position proportionally to the key value rather than always probing the middle. On uniformly distributed sorted keys it averages O(log log n), but on skewed data it can degrade to O(n).',
    source,
    generated: true,
  },
  {
    id: 7722014,
    chapter: 'Searching',
    title: 'Quickselect Average Time',
    prompt: 'Quickselect finds the k-th smallest element. What is its average-case time complexity?',
    correct: 'O(n)',
    wrong: [
      miss('O(n log n)', 'Quickselect avoids fully sorting; it recurses into only one partition, giving linear average time.', 'Unlike quicksort, it discards one side each step.'),
      miss('O(log n)', 'It must at least scan the array to partition, so it cannot be sublinear.', 'Each partition step is linear in the current subarray.'),
      miss('O(n^2) on average.', 'The quadratic time is the worst case; with random pivots the average is linear.', 'Average behavior comes from a geometric reduction in subarray size.'),
    ],
    lesson: 'Quickselect partitions like quicksort but recurses into only the side containing the target rank. The expected work forms a geometric series summing to O(n), though adversarial pivots give O(n^2) worst case.',
    source,
    generated: true,
  },

  // ---------------- Chapter 4: Divide and Conquer ----------------
  {
    id: 7722015,
    chapter: 'Divide and Conquer',
    title: 'Master Theorem Case',
    prompt: 'For the recurrence T(n) = 2 T(n/2) + O(n), what does the Master Theorem give?',
    correct: 'T(n) = O(n log n)',
    wrong: [
      miss('T(n) = O(n)', 'The work per level is balanced across log n levels, adding a logarithmic factor to the linear work.', 'Add up the O(n) work done at each of the log n levels.'),
      miss('T(n) = O(n^2)', 'There is no quadratic blow-up; the combine cost is linear and levels are logarithmic.', 'Multiply per-level cost by the number of levels, not by n.'),
      miss('T(n) = O(log n)', 'Each level does linear work, so the total is far more than logarithmic.', 'Account for the O(n) merge at every level.'),
    ],
    lesson: 'This is the classic merge sort recurrence. With a = 2, b = 2, f(n) = n, we have n^(log_b a) = n, matching f(n), so by the Master Theorem case 2 the result is O(n log n).',
    source,
    generated: true,
  },
  {
    id: 7722016,
    chapter: 'Divide and Conquer',
    title: 'Karatsuba Multiplication',
    prompt: 'Karatsuba multiplication improves on the schoolbook O(n^2) by reducing four subproblems to three. Its recurrence and complexity are:',
    correct: 'T(n) = 3 T(n/2) + O(n), giving O(n^log2(3)) which is about O(n^1.585).',
    wrong: [
      miss('T(n) = 4 T(n/2) + O(n), giving O(n^2).', 'Karatsuba’s whole point is reducing four multiplications to three; four would not improve on schoolbook.', 'The clever identity cuts the subproblem count to three.'),
      miss('T(n) = 2 T(n/2) + O(n), giving O(n log n).', 'Karatsuba makes three recursive calls, not two, so the exponent is log2(3), not 1.', 'Count the recursive multiplications carefully.'),
      miss('T(n) = 3 T(n/3) + O(n), giving O(n).', 'It splits each number into two halves, not three thirds, so the branching is on n/2.', 'The split is into halves with three subproblems.'),
    ],
    lesson: 'Karatsuba splits each n-digit number in half and uses an algebraic trick so only three half-size multiplications are needed: T(n) = 3 T(n/2) + O(n). By the Master Theorem this is O(n^log2(3)) is approximately O(n^1.585), beating O(n^2).',
    source,
    generated: true,
  },
  {
    id: 7722017,
    chapter: 'Divide and Conquer',
    title: 'Master Theorem Heavy Leaves',
    prompt: 'For T(n) = 8 T(n/2) + O(n^2), the Master Theorem gives which result?',
    correct: 'T(n) = O(n^3), since n^(log2 8) = n^3 dominates the n^2 combine cost.',
    wrong: [
      miss('T(n) = O(n^2 log n)', 'That would be case 2, which requires f(n) to match n^(log_b a); here n^2 is smaller than n^3.', 'Compare the exponent of f(n) to log_b a.'),
      miss('T(n) = O(n^2)', 'The recursion tree work grows like n^3 at the leaves, dominating the n^2 combine.', 'The leaf count grows faster than the per-node combine here.'),
      miss('T(n) = O(n^3 log n)', 'A logarithmic factor appears only in the balanced case 2, not in this leaf-heavy case 1.', 'Case 1 has no extra log factor.'),
    ],
    lesson: 'Here a = 8, b = 2, so n^(log_b a) = n^3, while f(n) = n^2 is polynomially smaller. This is Master Theorem case 1, where the leaves dominate, giving T(n) = O(n^3) with no log factor.',
    source,
    generated: true,
  },
  {
    id: 7722018,
    chapter: 'Divide and Conquer',
    title: 'Closest Pair of Points',
    prompt: 'The divide-and-conquer algorithm for the closest pair of points in the plane runs in:',
    correct: 'O(n log n)',
    wrong: [
      miss('O(n^2)', 'The naive all-pairs approach is quadratic, but divide and conquer with a clever strip check beats it.', 'The whole point of the algorithm is to avoid checking all pairs.'),
      miss('O(n)', 'Sorting and the recursive structure prevent a purely linear bound.', 'Recall the recurrence resembling merge sort.'),
      miss('O(n^2 log n)', 'The strip step is done in linear time per level, keeping the total at n log n.', 'Each merge-like step is linear, over log n levels.'),
    ],
    lesson: 'By recursively splitting points by x-coordinate and checking a thin strip near the dividing line (where only a constant number of neighbors per point matter), the closest-pair algorithm achieves O(n log n), beating the O(n^2) brute force.',
    source,
    generated: true,
  },

  // ---------------- Chapter 5: Greedy Algorithms ----------------
  {
    id: 7722019,
    chapter: 'Greedy Algorithms',
    title: 'Activity Selection Greedy Choice',
    prompt: 'In the activity-selection problem, the greedy choice that yields an optimal schedule is to repeatedly pick the activity that:',
    correct: 'Has the earliest finish time among those compatible with the chosen set.',
    wrong: [
      miss('Has the shortest duration.', 'Shortest activities can block two others, so this greedy rule is not optimal.', 'Consider a short activity that overlaps two longer non-overlapping ones.'),
      miss('Starts the earliest.', 'An early-starting activity may run very long and crowd out many others.', 'Earliest start ignores how long the activity ties up the resource.'),
      miss('Conflicts with the fewest other activities.', 'This heuristic seems reasonable but can be shown not to always give the optimum.', 'The proven optimal rule is based on finishing soonest, not fewest conflicts.'),
    ],
    lesson: 'Selecting the compatible activity with the earliest finish time leaves the most remaining time for others and is provably optimal via an exchange argument. Shortest-duration or earliest-start heuristics can fail.',
    source,
    generated: true,
  },
  {
    id: 7722020,
    chapter: 'Greedy Algorithms',
    title: 'Huffman Coding',
    prompt: 'Huffman coding builds an optimal prefix code by repeatedly:',
    correct: 'Merging the two lowest-frequency nodes into a new node whose frequency is their sum.',
    wrong: [
      miss('Merging the two highest-frequency nodes first.', 'Merging frequent symbols early would give them longer codes, which is suboptimal.', 'Frequent symbols should get the shortest codes, so merge the rarest first.'),
      miss('Assigning each symbol a fixed-length code based on its rank.', 'That yields a fixed-length code, not the variable-length optimal code Huffman produces.', 'Huffman codes vary in length by frequency.'),
      miss('Splitting the alphabet in half by frequency at each step.', 'Huffman builds bottom-up by merging, not top-down by splitting like Shannon-Fano.', 'The greedy step merges the two smallest, repeatedly.'),
    ],
    lesson: 'Huffman repeatedly merges the two least frequent nodes, ensuring rare symbols sit deepest (longest codes) and frequent symbols stay shallow (shortest codes). This greedy strategy provably minimizes the expected code length and is prefix-free.',
    source,
    generated: true,
  },
  {
    id: 7722021,
    chapter: 'Greedy Algorithms',
    title: 'Fractional vs 0/1 Knapsack',
    prompt: 'Why does a greedy "highest value-per-weight first" strategy solve the fractional knapsack optimally but fail for 0/1 knapsack?',
    correct: 'In the fractional version you can take a fraction of an item to fill remaining capacity exactly; in 0/1 you must take whole items, so the greedy choice can leave wasted capacity.',
    wrong: [
      miss('The fractional version has fewer items, so greedy always works.', 'The number of items is not the issue; the difference is divisibility of items.', 'Focus on whether items can be split.'),
      miss('0/1 knapsack has no optimal solution.', '0/1 knapsack does have an optimal solution; it just is not found by this greedy rule.', 'It is solvable, typically by dynamic programming.'),
      miss('Greedy fails for fractional knapsack but works for 0/1.', 'It is the reverse: greedy is optimal for fractional and can fail for 0/1.', 'Recall which version allows splitting items.'),
    ],
    lesson: 'Fractional knapsack lets you fill leftover capacity with a slice of the next-best item, so the greedy value-density order is optimal. In 0/1 knapsack indivisible items can leave unfillable gaps, so greedy fails and dynamic programming is needed.',
    source,
    generated: true,
  },
  {
    id: 7722022,
    chapter: 'Greedy Algorithms',
    title: 'Coin Change Greedy Failure',
    prompt: 'For which coin system can the greedy "use the largest coin first" algorithm fail to produce the minimum number of coins?',
    correct: 'Denominations {1, 3, 4} when making 6: greedy gives 4+1+1 (three coins) but 3+3 uses only two.',
    wrong: [
      miss('Standard US coins {1, 5, 10, 25} for any amount.', 'The canonical US system is designed so greedy is always optimal.', 'Greedy works for canonical coin systems like US currency.'),
      miss('Any system where 1 is a denomination.', 'Having a penny only guarantees a solution exists, not that greedy is optimal.', 'The penny ensures feasibility, not greedy optimality.'),
      miss('No coin system; greedy is always optimal for coin change.', 'Greedy is provably suboptimal for some non-canonical systems.', 'Counterexamples exist for certain denomination sets.'),
    ],
    lesson: 'Greedy coin change is optimal only for canonical systems (like US coins). With denominations {1, 3, 4} making 6, greedy picks 4 then 1+1 (three coins), but 3+3 needs only two, showing greedy is not universally optimal. Dynamic programming solves the general case.',
    source,
    generated: true,
  },

  // ---------------- Chapter 6: Dynamic Programming ----------------
  {
    id: 7722023,
    chapter: 'Dynamic Programming',
    title: 'Optimal Substructure',
    prompt: 'Dynamic programming applies when a problem has optimal substructure, meaning:',
    correct: 'An optimal solution to the problem contains optimal solutions to its subproblems.',
    wrong: [
      miss('The problem can be split into independent subproblems with no overlap.', 'That describes plain divide and conquer; DP specifically exploits overlapping subproblems.', 'DP is most useful precisely when subproblems overlap.'),
      miss('Every subproblem has a unique solution.', 'Uniqueness of subproblem solutions is not required for DP to apply.', 'What matters is reusing optimal subsolutions, not their uniqueness.'),
      miss('The greedy choice property holds at every step.', 'The greedy choice property is a separate, stronger condition used by greedy algorithms.', 'Optimal substructure is weaker and shared by both DP and greedy.'),
    ],
    lesson: 'Optimal substructure means an optimal solution is built from optimal solutions of subproblems, letting DP solve each subproblem once and reuse it. Combined with overlapping subproblems, this justifies memoization or tabulation.',
    source,
    generated: true,
  },
  {
    id: 7722024,
    chapter: 'Dynamic Programming',
    title: 'Memoization vs Tabulation',
    prompt: 'What is the main difference between memoization and tabulation in dynamic programming?',
    correct: 'Memoization is top-down recursion that caches results lazily; tabulation is bottom-up iteration that fills a table in order.',
    wrong: [
      miss('Memoization always uses less memory than tabulation.', 'Both typically store the same set of subproblem results; memory is comparable, not strictly less.', 'The distinction is the direction of computation, not memory savings.'),
      miss('Tabulation can solve problems memoization cannot.', 'They solve the same class of problems; they differ only in evaluation order.', 'Both compute the same subproblems, just in different orders.'),
      miss('Memoization avoids recursion entirely.', 'Memoization is fundamentally a recursive, top-down approach with caching.', 'It is tabulation, not memoization, that is iterative.'),
    ],
    lesson: 'Memoization is top-down: recurse and cache each subproblem result on first computation. Tabulation is bottom-up: iteratively fill a table from base cases upward. Both eliminate redundant recomputation; tabulation avoids recursion overhead, while memoization computes only reachable subproblems.',
    source,
    generated: true,
  },
  {
    id: 7722025,
    chapter: 'Dynamic Programming',
    title: 'Longest Common Subsequence',
    prompt: 'The standard dynamic programming algorithm for the Longest Common Subsequence of two strings of lengths m and n runs in:',
    correct: 'O(m·n) time and O(m·n) space (reducible to O(min(m,n)) space if only the length is needed).',
    wrong: [
      miss('O(m + n) time.', 'A linear scan cannot resolve the two-dimensional alignment choices LCS requires.', 'The DP table has m by n cells, each needing constant work.'),
      miss('O(2^(m+n)) time, which is unavoidable.', 'The exponential cost is the naive recursion; DP brings it down to polynomial.', 'Overlapping subproblems let DP avoid the exponential blow-up.'),
      miss('O(m·n·log(m·n)) time.', 'Each table cell is computed in O(1), so no log factor appears.', 'Filling each of the m·n cells is constant work.'),
    ],
    lesson: 'LCS fills an (m+1) by (n+1) table where each cell uses O(1) work, giving O(m·n) time and space. Space can be cut to O(min(m,n)) when only the length is needed by keeping two rows, since each cell depends only on neighbors.',
    source,
    generated: true,
  },
  {
    id: 7722026,
    chapter: 'Dynamic Programming',
    title: 'Pseudo-Polynomial Time',
    prompt: 'The classic dynamic program for 0/1 knapsack runs in O(nW) time. Why is this called pseudo-polynomial rather than polynomial?',
    correct: 'W is a numeric value, so O(nW) is exponential in the number of bits used to represent W, not polynomial in input size.',
    wrong: [
      miss('Because it sometimes gives only an approximate answer.', 'The DP is exact; the "pseudo" refers to the dependence on a numeric magnitude, not approximation.', 'Pseudo-polynomial concerns input encoding length, not accuracy.'),
      miss('Because nW is always larger than 2^n.', 'nW is not necessarily larger than 2^n; the term refers to dependence on a value, not a comparison.', 'Think about how W scales with its bit-length.'),
      miss('Because W must be irrational.', 'W is an integer weight capacity; irrationality is irrelevant here.', 'The issue is magnitude versus encoding length.'),
    ],
    lesson: 'A capacity W of value V is encoded in about log2(V) bits, so an algorithm running in time proportional to W = V is exponential in the input size in bits. Such algorithms are called pseudo-polynomial; 0/1 knapsack is NP-hard and has no truly polynomial exact algorithm unless P = NP.',
    source,
    generated: true,
  },
  {
    id: 7722027,
    chapter: 'Dynamic Programming',
    title: 'Bellman Equation Edit Distance',
    prompt: 'In the Levenshtein edit-distance DP, the recurrence for matching prefixes of lengths i and j considers a minimum over which three operations?',
    correct: 'Insertion, deletion, and substitution (with substitution free when the characters match).',
    wrong: [
      miss('Insertion, deletion, and transposition only.', 'Standard Levenshtein distance uses substitution, not transposition; transposition is the Damerau extension.', 'Recall the three basic single-character edits.'),
      miss('Substitution, reversal, and rotation.', 'Reversal and rotation are not single-character edits in the Levenshtein model.', 'Edit distance counts per-character insert, delete, and substitute.'),
      miss('Only insertion and deletion.', 'Substitution is a distinct third operation that often gives a cheaper path than an insert plus delete.', 'There is a third operation that replaces one character with another.'),
    ],
    lesson: 'Levenshtein edit distance minimizes over deleting a character (i-1, j), inserting one (i, j-1), and substituting (i-1, j-1, with cost 0 if characters match). The DP table is filled in O(m·n) time.',
    source,
    generated: true,
  },

  // ---------------- Chapter 7: Graph Algorithms ----------------
  {
    id: 7722028,
    chapter: 'Graph Algorithms',
    title: 'BFS Shortest Paths',
    prompt: 'Breadth-first search computes shortest paths from a source in which kind of graph?',
    correct: 'Unweighted graphs (or graphs where every edge has equal weight).',
    wrong: [
      miss('Any weighted graph, including those with varying positive weights.', 'BFS treats every edge as one step, so it is wrong when edges have differing weights.', 'For varying positive weights you need Dijkstra, not BFS.'),
      miss('Graphs with negative edge weights.', 'Negative weights require Bellman-Ford; BFS ignores weights entirely.', 'BFS does not consider weights at all.'),
      miss('Only directed acyclic graphs.', 'BFS works on cyclic and undirected graphs too; it just needs unweighted edges.', 'The restriction is on edge weights, not on cycles.'),
    ],
    lesson: 'BFS explores vertices in layers by distance, so it finds shortest paths counting edges, valid only when all edges have equal weight. For general positive weights use Dijkstra; for negative weights use Bellman-Ford.',
    source,
    generated: true,
  },
  {
    id: 7722029,
    chapter: 'Graph Algorithms',
    title: 'Dijkstra Negative Edges',
    prompt: 'Why can Dijkstra’s algorithm produce incorrect shortest paths when a graph has negative edge weights?',
    correct: 'It finalizes a vertex’s distance once popped, assuming no later path can be shorter; a negative edge can violate that assumption.',
    wrong: [
      miss('It cannot represent negative numbers in the priority queue.', 'The data structure handles negatives fine; the flaw is the greedy finalization logic.', 'The problem is algorithmic, not a numeric representation issue.'),
      miss('Negative edges create infinite loops in the queue.', 'Dijkstra terminates; it just gives wrong answers, and negative cycles are a separate concern.', 'The issue is incorrectness, not non-termination.'),
      miss('It only works on undirected graphs.', 'Dijkstra works on directed graphs too; the limitation is about negative weights.', 'Directedness is fine; the weights matter.'),
    ],
    lesson: 'Dijkstra greedily fixes the closest unvisited vertex, relying on the invariant that no shorter path can appear later. A negative edge can create a cheaper route to an already-finalized vertex, breaking this invariant. Bellman-Ford handles negative edges (and detects negative cycles).',
    source,
    generated: true,
  },
  {
    id: 7722030,
    chapter: 'Graph Algorithms',
    title: 'Bellman-Ford Complexity',
    prompt: 'Bellman-Ford computes single-source shortest paths in a graph with V vertices and E edges in time:',
    correct: 'O(V·E)',
    wrong: [
      miss('O(E log V)', 'That is the complexity of Dijkstra with a binary heap, not Bellman-Ford.', 'Bellman-Ford relaxes all edges repeatedly, V-1 times.'),
      miss('O(V^2)', 'Bellman-Ford’s cost depends on the number of edges times V-1 passes, not V squared directly.', 'Count the relaxations: V-1 passes over all E edges.'),
      miss('O(V + E)', 'That is the cost of a single traversal like BFS, not the V-1 rounds of edge relaxation.', 'Bellman-Ford repeats a full edge sweep V-1 times.'),
    ],
    lesson: 'Bellman-Ford relaxes all E edges in each of V-1 passes, giving O(V·E). It is slower than Dijkstra but handles negative edge weights and detects negative-weight cycles via a final extra pass.',
    source,
    generated: true,
  },
  {
    id: 7722031,
    chapter: 'Graph Algorithms',
    title: 'Topological Sort Requirement',
    prompt: 'A topological ordering of a graph’s vertices exists if and only if the graph is:',
    correct: 'A directed acyclic graph (DAG).',
    wrong: [
      miss('Connected and undirected.', 'Undirected edges have no direction to order, and cycles would block any consistent ordering.', 'Topological order needs directed edges and no cycles.'),
      miss('Any directed graph.', 'A directed graph with a cycle has no valid topological order.', 'Cycles make a consistent linear order impossible.'),
      miss('A complete graph.', 'Completeness is unrelated; a complete directed graph generally contains cycles.', 'The defining requirement is acyclicity, not density.'),
    ],
    lesson: 'A topological sort linearly orders vertices so every directed edge points forward. This is possible exactly when the graph is a DAG; any directed cycle makes a consistent ordering impossible.',
    source,
    generated: true,
  },
  {
    id: 7722032,
    chapter: 'Graph Algorithms',
    title: 'Minimum Spanning Tree Cut Property',
    prompt: 'The cut property used to prove MST algorithms correct states that:',
    correct: 'For any cut of the graph, the minimum-weight edge crossing the cut belongs to some minimum spanning tree.',
    wrong: [
      miss('The maximum-weight edge in any cycle must be in every MST.', 'That is the (related) cycle property and concerns exclusion, plus it is the heaviest cycle edge that is excluded, not included.', 'The cut property is about including the lightest crossing edge.'),
      miss('Every edge in the MST must cross every cut.', 'A single MST edge crosses only the cuts that separate its endpoints, not all cuts.', 'An edge crosses a cut only when its endpoints lie on opposite sides.'),
      miss('The MST always contains the globally lightest edge and never the heaviest.', 'The globally heaviest edge can be in an MST if it is a bridge; the property is about edges crossing a specific cut.', 'Focus on the lightest edge across a particular cut.'),
    ],
    lesson: 'The cut property guarantees that the cheapest edge crossing any cut is safe to add to an MST. Prim’s and Kruskal’s algorithms both rely on it: they repeatedly add a minimum-weight edge across an appropriate cut.',
    source,
    generated: true,
  },
  {
    id: 7722033,
    chapter: 'Graph Algorithms',
    title: 'Kruskal Data Structure',
    prompt: 'Kruskal’s algorithm sorts edges by weight and adds each if it joins two different components. Which data structure makes the component checks efficient?',
    correct: 'A disjoint-set (union-find) structure, giving near-constant amortized find and union.',
    wrong: [
      miss('A min-heap of vertices keyed by distance.', 'That structure is central to Prim’s, not Kruskal’s, which needs component membership tests.', 'Kruskal needs to test whether two endpoints are already connected.'),
      miss('A hash table mapping edges to weights.', 'Storing weights does not answer whether adding an edge creates a cycle.', 'You need to track which vertices are in the same component.'),
      miss('A balanced binary search tree of edges.', 'Edges are simply sorted once; the recurring need is fast component union and find.', 'The cycle check, not edge ordering, dominates the design.'),
    ],
    lesson: 'Kruskal adds the next-lightest edge only if its endpoints lie in different components, detected with a union-find structure. With path compression and union by rank, find and union are nearly O(1) amortized, making the sort the dominant cost at O(E log E).',
    source,
    generated: true,
  },
  {
    id: 7722034,
    chapter: 'Graph Algorithms',
    title: 'Floyd-Warshall',
    prompt: 'The Floyd-Warshall algorithm computes all-pairs shortest paths in time:',
    correct: 'O(V^3)',
    wrong: [
      miss('O(V·E)', 'That is the cost of running Bellman-Ford once; Floyd-Warshall uses a triple nested loop over vertices.', 'It iterates over all intermediate vertices for every pair.'),
      miss('O(E log V)', 'That is Dijkstra from one source; all-pairs via Floyd-Warshall is cubic in V.', 'Floyd-Warshall does not use a priority queue.'),
      miss('O(V^2 log V)', 'There is no log factor; the algorithm is three plain nested loops over the vertex set.', 'Count the three nested loops over V.'),
    ],
    lesson: 'Floyd-Warshall uses dynamic programming with a triple loop over all intermediate, source, and destination vertices, giving O(V^3). It handles negative edges (but not negative cycles) and computes shortest paths between every pair at once.',
    source,
    generated: true,
  },

  // ---------------- Chapter 8: Hashing ----------------
  {
    id: 7722035,
    chapter: 'Hashing',
    title: 'Load Factor',
    prompt: 'In a hash table with separate chaining, the load factor alpha is defined as:',
    correct: 'The number of stored entries divided by the number of buckets.',
    wrong: [
      miss('The number of buckets divided by the number of entries.', 'That is the reciprocal; load factor is entries per bucket, which grows as the table fills.', 'Load factor rises as more entries are added.'),
      miss('The fraction of buckets that are empty.', 'Load factor measures average occupancy, not the empty-bucket fraction.', 'It counts entries relative to buckets, including non-empty ones.'),
      miss('The maximum chain length observed.', 'The maximum chain is a worst-case measure; load factor is the average over all buckets.', 'Load factor is an average, not a maximum.'),
    ],
    lesson: 'The load factor alpha = n/m (entries over buckets) is the expected chain length under uniform hashing. Keeping alpha bounded by a constant (often via resizing) keeps expected operations O(1).',
    source,
    generated: true,
  },
  {
    id: 7722036,
    chapter: 'Hashing',
    title: 'Open Addressing Probing',
    prompt: 'In open addressing, what is the main advantage of double hashing over linear probing?',
    correct: 'It reduces primary clustering by using a second hash to determine a key-dependent probe step.',
    wrong: [
      miss('It allows the load factor to safely exceed 1.', 'No open-addressing scheme can store more entries than slots; alpha cannot exceed 1.', 'All open addressing is bounded by table size.'),
      miss('It eliminates the need for a hash function.', 'Double hashing uses two hash functions, not zero.', 'It adds a hash, it does not remove one.'),
      miss('It guarantees O(1) worst-case lookup.', 'Worst-case lookup can still degrade; double hashing improves average behavior, not the worst case.', 'The benefit is reduced clustering on average.'),
    ],
    lesson: 'Linear probing suffers primary clustering: long runs of occupied slots grow and merge. Double hashing computes a probe step from a second hash of the key, so colliding keys follow different probe sequences, spreading them out and reducing clustering.',
    source,
    generated: true,
  },
  {
    id: 7722037,
    chapter: 'Hashing',
    title: 'Universal Hashing Purpose',
    prompt: 'The purpose of choosing a hash function at random from a universal family is to:',
    correct: 'Guarantee a low expected collision probability for any fixed input set, defeating adversarial inputs.',
    wrong: [
      miss('Make every hash function in the family collision-free.', 'No fixed function avoids all collisions when keys outnumber buckets; universality bounds expected collisions.', 'Collisions are inevitable; universality bounds them in expectation.'),
      miss('Speed up the hash computation to O(1).', 'Hash evaluation is already O(1); universality is about collision behavior, not speed.', 'Universality concerns distribution quality, not computation time.'),
      miss('Ensure the table never needs resizing.', 'Universality does not affect when resizing is triggered by load factor.', 'Resizing depends on load factor, not on the hashing family.'),
    ],
    lesson: 'A universal family guarantees that for any two distinct keys, a randomly chosen function collides them with probability at most 1/m. Because the function is secret and random, no adversary can craft inputs that force many collisions, ensuring O(1) expected performance.',
    source,
    generated: true,
  },
  {
    id: 7722038,
    chapter: 'Hashing',
    title: 'Bloom Filter Errors',
    prompt: 'A Bloom filter can produce which kind of error when queried for membership?',
    correct: 'False positives (it may report present for an absent element) but never false negatives.',
    wrong: [
      miss('False negatives but never false positives.', 'It is the reverse: a Bloom filter never misses a truly inserted element but may falsely claim presence.', 'Recall that inserted elements always set their bits.'),
      miss('Both false positives and false negatives.', 'Once bits are set they are never cleared, so a present element is always reported present.', 'Insertions only set bits, so true members are never missed.'),
      miss('Neither; Bloom filters are exact.', 'Bloom filters trade exactness for space and have a tunable false-positive rate.', 'Their compactness comes at the cost of some false positives.'),
    ],
    lesson: 'A Bloom filter sets several hash-indexed bits per inserted element. A query returns absent only if some bit is unset, so inserted elements are never missed (no false negatives), but unrelated elements can coincidentally have all bits set, causing false positives.',
    source,
    generated: true,
  },

  // ---------------- Chapter 9: Complexity Classes ----------------
  {
    id: 7722039,
    chapter: 'Complexity Classes',
    title: 'P vs NP Definition',
    prompt: 'The class NP is best described as the set of decision problems for which:',
    correct: 'A proposed solution (certificate) can be verified in polynomial time.',
    wrong: [
      miss('No polynomial-time algorithm can ever exist.', 'NP does not assert intractability; in fact P is a subset of NP.', 'NP is about verification, and P problems are also in NP.'),
      miss('Solutions require nondeterministic hardware to compute.', 'NP is defined abstractly via verification; it does not require special physical hardware.', 'Think verifiability, not a hardware requirement.'),
      miss('Solutions can be found in nondeterministic logarithmic space.', 'That describes a space class (NL), not the time-based class NP.', 'NP is defined by polynomial-time verification.'),
    ],
    lesson: 'NP is the class of decision problems whose yes-instances have a polynomial-size certificate verifiable in polynomial time. Equivalently, they are solvable in polynomial time on a nondeterministic machine. Whether P equals NP remains open.',
    source,
    generated: true,
  },
  {
    id: 7722040,
    chapter: 'Complexity Classes',
    title: 'NP-Complete Meaning',
    prompt: 'A problem is NP-complete if it is in NP and:',
    correct: 'Every problem in NP reduces to it in polynomial time (it is NP-hard).',
    wrong: [
      miss('It can be solved in polynomial time.', 'No NP-complete problem is known to be in P; if one were, then P would equal NP.', 'NP-completeness is the opposite of a known efficient solution.'),
      miss('It is harder than every problem outside NP.', 'NP-completeness compares within NP via reductions, not against problems outside NP.', 'The defining property is reductions from all of NP.'),
      miss('It has no verifiable certificate.', 'Being in NP requires a polynomial-time verifiable certificate, which NP-complete problems have.', 'NP-complete problems are in NP, so they have certificates.'),
    ],
    lesson: 'NP-complete problems are the hardest in NP: each lies in NP and is NP-hard, meaning every NP problem reduces to it in polynomial time. A polynomial algorithm for any one would solve all of NP, proving P = NP.',
    source,
    generated: true,
  },
  {
    id: 7722041,
    chapter: 'Complexity Classes',
    title: 'First NP-Complete Problem',
    prompt: 'The Cook-Levin theorem established that which problem is NP-complete, making it the first proven NP-complete problem?',
    correct: 'Boolean satisfiability (SAT).',
    wrong: [
      miss('The traveling salesman problem.', 'TSP was later shown NP-complete via reduction; SAT was the first, proven directly by Cook-Levin.', 'Recall which problem the Cook-Levin theorem addressed.'),
      miss('Sorting an array.', 'Sorting is in P with an O(n log n) algorithm and is not NP-complete.', 'NP-complete problems lack known polynomial algorithms.'),
      miss('The shortest path problem.', 'Single-source shortest paths is solvable in polynomial time and is not NP-complete.', 'Shortest path has efficient algorithms like Dijkstra.'),
    ],
    lesson: 'The Cook-Levin theorem (1971) proved Boolean satisfiability is NP-complete by showing any NP computation can be encoded as a SAT instance. Karp then reduced SAT to many other problems, building the catalog of NP-complete problems.',
    source,
    generated: true,
  },
  {
    id: 7722042,
    chapter: 'Complexity Classes',
    title: 'Reduction Direction',
    prompt: 'To prove a new problem B is NP-hard, you construct a polynomial-time reduction:',
    correct: 'From a known NP-hard problem A to B (A reduces to B).',
    wrong: [
      miss('From B to a known NP-hard problem A.', 'Reducing B to A shows B is no harder than A, which does not prove B is hard.', 'The reduction must go from the known-hard problem into B.'),
      miss('From B to a problem known to be in P.', 'Reducing B to an easy problem would suggest B is easy, the opposite of NP-hardness.', 'Reduce a hard problem to B, not B to an easy one.'),
      miss('From any problem in NP to any problem in P.', 'NP-hardness of B specifically requires mapping a known-hard problem onto B.', 'The target of the reduction must be B itself.'),
    ],
    lesson: 'To show B is NP-hard, take a known NP-hard problem A and give a polynomial-time mapping that turns A-instances into equivalent B-instances. If B could be solved efficiently, so could A, so B is at least as hard as A.',
    source,
    generated: true,
  },
  {
    id: 7722043,
    chapter: 'Complexity Classes',
    title: 'co-NP',
    prompt: 'The complexity class co-NP consists of decision problems for which:',
    correct: 'No-instances have a polynomial-time verifiable certificate (the complement is in NP).',
    wrong: [
      miss('Yes-instances have a polynomial-time verifiable certificate.', 'That is the definition of NP; co-NP concerns short proofs for the no-answers.', 'Swap which answer has the easy-to-verify certificate.'),
      miss('Solutions require exponential time to verify.', 'co-NP problems have polynomial-time verifiable certificates for no-instances.', 'Verification is polynomial, just for the complementary answer.'),
      miss('There is no known nondeterministic algorithm.', 'co-NP is defined via nondeterministic verification of no-instances, not the absence of such algorithms.', 'co-NP mirrors NP on the complementary answer.'),
    ],
    lesson: 'co-NP is the set of problems whose complements are in NP: their no-instances admit short, polynomial-time verifiable certificates. Whether NP equals co-NP is open and is related to but distinct from the P vs NP question.',
    source,
    generated: true,
  },

  // ---------------- Chapter 10: Data Structures for Algorithms ----------------
  {
    id: 7722044,
    chapter: 'Data Structures for Algorithms',
    title: 'Binary Heap Operations',
    prompt: 'In a binary min-heap of n elements, what are the time complexities of insert and extract-min?',
    correct: 'Both are O(log n), since an element may sift up or down the tree height.',
    wrong: [
      miss('Insert is O(1) and extract-min is O(n).', 'Insert may bubble up the full height (O(log n)), and extract-min sifts down in O(log n), not O(n).', 'Both operations are bounded by the heap height.'),
      miss('Both are O(1).', 'Restoring the heap property after either operation can require traversing the tree height.', 'Maintaining the heap invariant takes logarithmic work.'),
      miss('Both are O(n log n).', 'That is the cost of sorting all elements, not a single heap operation.', 'A single insert or extract touches only one root-to-leaf path.'),
    ],
    lesson: 'A binary heap stored in an array has height O(log n). Insert appends and sifts up, while extract-min removes the root, moves the last element up, and sifts down; both follow one root-to-leaf path, so both are O(log n). Finding the min itself is O(1).',
    source,
    generated: true,
  },
  {
    id: 7722045,
    chapter: 'Data Structures for Algorithms',
    title: 'Balanced BST Guarantee',
    prompt: 'Why do self-balancing trees like AVL or red-black trees guarantee O(log n) search, while a plain BST does not?',
    correct: 'They maintain a height of O(log n) via rotations; an unbalanced BST can degrade to a linked list of height O(n).',
    wrong: [
      miss('They store elements in a hash table internally.', 'Balanced trees use rotations to keep height low, not hashing.', 'The mechanism is structural rebalancing, not hashing.'),
      miss('They forbid duplicate keys, which keeps them short.', 'Forbidding duplicates does not bound height; an ordered insert sequence still skews a plain BST.', 'Height control comes from rebalancing operations.'),
      miss('They sort the data before every search.', 'Re-sorting on each search would be wasteful and is not how balanced trees work.', 'Balance is maintained incrementally during insertions and deletions.'),
    ],
    lesson: 'A plain BST built from sorted insertions degenerates into a chain of height O(n), making search linear. AVL and red-black trees perform rotations on insert and delete to keep height O(log n), guaranteeing logarithmic search, insert, and delete.',
    source,
    generated: true,
  },
  {
    id: 7722046,
    chapter: 'Data Structures for Algorithms',
    title: 'Union-Find Amortized Cost',
    prompt: 'With both path compression and union by rank, the amortized cost per disjoint-set operation is:',
    correct: 'O(alpha(n)), the inverse Ackermann function, which is effectively constant for all practical n.',
    wrong: [
      miss('O(log n) per operation.', 'Using only one of the two optimizations gives O(log n); together they yield the much smaller inverse Ackermann bound.', 'Combining both heuristics beats logarithmic.'),
      miss('O(1) exactly, in the worst case.', 'The tight bound is the inverse Ackermann function, which is not constant though it is essentially constant in practice.', 'The exact bound is the inverse Ackermann, not a true constant.'),
      miss('O(n) per operation.', 'Naive union-find without optimizations can be O(n), but with both heuristics it is nearly constant.', 'The optimizations dramatically reduce the per-operation cost.'),
    ],
    lesson: 'Union by rank keeps trees shallow and path compression flattens them during finds. Together they give an amortized cost of O(alpha(n)) per operation, where alpha is the inverse Ackermann function, below 5 for any conceivable input size.',
    source,
    generated: true,
  },
  {
    id: 7722047,
    chapter: 'Data Structures for Algorithms',
    title: 'Priority Queue for Dijkstra',
    prompt: 'Using a binary heap as the priority queue, Dijkstra’s algorithm runs in:',
    correct: 'O((V + E) log V)',
    wrong: [
      miss('O(V^2) regardless of the queue.', 'O(V^2) is the cost with a simple array; a binary heap improves sparse graphs to O((V+E) log V).', 'A heap speeds up the extract-min and decrease-key steps.'),
      miss('O(V·E)', 'That is Bellman-Ford’s cost; Dijkstra with a heap is log-factor faster on sparse graphs.', 'Dijkstra does not relax every edge V times.'),
      miss('O(E log E) with no dependence on V.', 'The extract-min operations contribute a V log V term, so V appears in the bound.', 'Both vertex extractions and edge relaxations contribute.'),
    ],
    lesson: 'With a binary heap, each of V extract-min operations and up to E decrease-key operations costs O(log V), giving O((V + E) log V). A Fibonacci heap reduces decrease-key to amortized O(1), yielding O(E + V log V).',
    source,
    generated: true,
  },

  // ---------------- Chapter 11: String and Number Algorithms ----------------
  {
    id: 7722048,
    chapter: 'String and Number Algorithms',
    title: 'KMP Preprocessing',
    prompt: 'The Knuth-Morris-Pratt string matcher achieves O(n + m) time on text of length n and pattern of length m by:',
    correct: 'Precomputing a failure (prefix) function so the text pointer never moves backward.',
    wrong: [
      miss('Hashing every substring of the text in advance.', 'That describes a Rabin-Karp-style approach; KMP uses a prefix function, not hashing.', 'KMP avoids backtracking via a precomputed table.'),
      miss('Sorting the pattern characters before matching.', 'Sorting would destroy the pattern’s order, which matching depends on.', 'The order of pattern characters must be preserved.'),
      miss('Building a suffix tree of the text.', 'Suffix trees are a different (more powerful, heavier) technique; KMP uses a simple prefix array.', 'KMP’s structure is the lightweight prefix function.'),
    ],
    lesson: 'KMP precomputes, for each pattern prefix, the length of the longest proper prefix that is also a suffix. On a mismatch this lets the pattern shift forward without re-examining text characters, so the text pointer only advances, giving O(n + m) total time.',
    source,
    generated: true,
  },
  {
    id: 7722049,
    chapter: 'String and Number Algorithms',
    title: 'Rabin-Karp Rolling Hash',
    prompt: 'The rolling hash in Rabin-Karp lets the algorithm:',
    correct: 'Update the hash of a sliding window in O(1) by removing the leaving character and adding the entering one.',
    wrong: [
      miss('Recompute each window hash from scratch in O(m), keeping total time low.', 'Recomputing from scratch each step would cost O(nm); the rolling update is what makes it efficient.', 'The advantage is incremental O(1) hash updates.'),
      miss('Guarantee no false matches without verification.', 'Hash collisions can cause spurious matches, so candidate matches must be verified character by character.', 'A hash match still needs confirmation.'),
      miss('Sort the text to enable binary search of the pattern.', 'Rabin-Karp scans windows in place and never sorts the text.', 'It slides a window and updates a hash incrementally.'),
    ],
    lesson: 'A rolling hash updates in O(1) per shift by subtracting the contribution of the outgoing character and adding the incoming one. This gives O(n + m) expected time, though hash collisions require a direct comparison to confirm each candidate match.',
    source,
    generated: true,
  },
  {
    id: 7722050,
    chapter: 'String and Number Algorithms',
    title: 'Fast Exponentiation',
    prompt: 'Computing a^n by repeated squaring (exponentiation by squaring) takes how many multiplications?',
    correct: 'O(log n)',
    wrong: [
      miss('O(n)', 'Naive repeated multiplication is O(n); squaring halves the exponent each step for O(log n).', 'Each squaring roughly doubles the exponent reached.'),
      miss('O(n log n)', 'There is no n-sized loop here; the exponent is reduced multiplicatively.', 'The number of steps is logarithmic in n.'),
      miss('O(sqrt(n))', 'Square-root cost arises in other algorithms; exponentiation by squaring is logarithmic.', 'Halving the exponent each step gives a log bound.'),
    ],
    lesson: 'Exponentiation by squaring writes n in binary and squares the base while conditionally multiplying, performing O(log n) multiplications. It underlies fast modular exponentiation used throughout cryptography.',
    source,
    generated: true,
  },
  {
    id: 7722051,
    chapter: 'String and Number Algorithms',
    title: 'Euclidean GCD',
    prompt: 'The Euclidean algorithm for gcd(a, b) repeatedly replaces the pair using which operation?',
    correct: 'Replace (a, b) with (b, a mod b) until the second number is zero.',
    wrong: [
      miss('Replace (a, b) with (a - b, b) only.', 'Repeated subtraction is the slow variant; using the remainder (mod) is exponentially faster.', 'Subtraction works but is far slower than using the remainder.'),
      miss('Replace (a, b) with (a/2, b/2) each step.', 'Halving both is the binary GCD’s idea only when both are even, not the standard Euclidean step.', 'The standard step uses the modulo operation.'),
      miss('Replace (a, b) with (a·b, a + b).', 'Multiplying and adding does not reduce the numbers toward a common divisor.', 'The numbers must shrink, via remainders, toward the gcd.'),
    ],
    lesson: 'The Euclidean algorithm uses gcd(a, b) = gcd(b, a mod b), terminating when b = 0, at which point a is the gcd. Using the remainder rather than subtraction makes it run in O(log(min(a, b))) steps.',
    source,
    generated: true,
  },

  // ---------------- Chapter 12: Algorithm Design Paradigms ----------------
  {
    id: 7722052,
    chapter: 'Algorithm Design Paradigms',
    title: 'Backtracking vs Brute Force',
    prompt: 'How does backtracking improve on naive brute-force enumeration of a search space?',
    correct: 'It prunes partial solutions that cannot possibly extend to a valid solution, avoiding exploring those subtrees.',
    wrong: [
      miss('It guarantees polynomial-time solutions to NP-complete problems.', 'Backtracking can still be exponential; it merely prunes, without changing worst-case hardness.', 'Pruning helps in practice but does not break exponential worst cases.'),
      miss('It randomly samples the search space to find a likely answer.', 'Backtracking is a systematic, exhaustive-with-pruning search, not random sampling.', 'It explores systematically, abandoning dead ends.'),
      miss('It converts the problem into a linear program.', 'Backtracking is a combinatorial search technique, unrelated to linear programming.', 'The improvement is pruning the search tree, not reformulation.'),
    ],
    lesson: 'Backtracking builds solutions incrementally and abandons a partial candidate as soon as constraints make completion impossible, cutting off whole subtrees of the search space. It is still worst-case exponential but often far faster than blind enumeration.',
    source,
    generated: true,
  },
  {
    id: 7722053,
    chapter: 'Algorithm Design Paradigms',
    title: 'Branch and Bound',
    prompt: 'In branch and bound for optimization problems, the "bound" is used to:',
    correct: 'Prune a branch when its best possible objective value cannot beat the best solution found so far.',
    wrong: [
      miss('Limit the recursion depth to a fixed constant.', 'The bound concerns objective values, not a fixed depth cap.', 'Pruning is based on optimistic objective estimates.'),
      miss('Randomly choose which branch to explore next.', 'Bounds guide pruning, not random branch selection.', 'The bound compares achievable values against the incumbent.'),
      miss('Guarantee the algorithm runs in polynomial time.', 'Branch and bound can still be exponential; bounds only prune, not change the worst case.', 'It improves typical performance, not the worst-case class.'),
    ],
    lesson: 'Branch and bound computes an optimistic bound on the best objective achievable within a subtree. If that bound is no better than the incumbent best solution, the entire subtree is pruned. It finds exact optima while often avoiding most of the search space.',
    source,
    generated: true,
  },
  {
    id: 7722054,
    chapter: 'Algorithm Design Paradigms',
    title: 'Approximation Ratio',
    prompt: 'For a minimization problem, a 2-approximation algorithm guarantees that:',
    correct: 'Its output cost is at most twice the optimal cost on every input.',
    wrong: [
      miss('It finds the optimum on at least half of all inputs.', 'An approximation ratio bounds every instance’s cost, not the fraction solved exactly.', 'The guarantee holds on all inputs, not a fraction of them.'),
      miss('It runs in at most twice the time of the optimal algorithm.', 'The ratio bounds solution quality, not running time.', 'Approximation ratio is about cost, not speed.'),
      miss('Its output is within an additive constant 2 of the optimum.', 'A 2-approximation is a multiplicative factor, not an additive one.', 'The bound multiplies the optimum by 2, not adds 2.'),
    ],
    lesson: 'A 2-approximation algorithm for a minimization problem produces, on every instance, a solution whose cost is at most 2 times the optimal cost. Such guarantees let us solve NP-hard problems approximately in polynomial time with a provable quality bound.',
    source,
    generated: true,
  },
])
