import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe sqlFoundations top-up'

export const sqlFoundationsUniv103TopUpQuestions: Question[] = makeQuestionBank('University', [
  // ---------------- Chapter 1: SELECT and WHERE ----------------
  {
    id: 7727000,
    chapter: 'SELECT and WHERE',
    title: 'NULL Comparison',
    prompt: 'A column "manager_id" contains NULL for the CEO. Which WHERE clause correctly returns the row where manager_id is unknown?',
    correct: 'WHERE manager_id IS NULL',
    wrong: [
      miss('WHERE manager_id = NULL', 'Any comparison with NULL using = yields UNKNOWN, never TRUE, so the row is never returned.', 'NULL is not a value you can equate; use the IS NULL predicate.'),
      miss('WHERE manager_id = 0', 'NULL means "unknown", which is different from the numeric value 0, so this misses the CEO row.', 'A missing value is not the same as zero.'),
      miss('WHERE manager_id != manager_id', 'Comparing NULL to itself also yields UNKNOWN, so this returns no rows rather than the NULL row.', 'NULL is not equal nor unequal to anything, including itself.'),
    ],
    lesson: 'SQL uses three-valued logic (TRUE, FALSE, UNKNOWN). Any arithmetic or = / != comparison involving NULL evaluates to UNKNOWN, and WHERE only keeps rows that are TRUE. To test for missing values you must use the dedicated predicates IS NULL and IS NOT NULL.',
    source,
    generated: true,
  },
  {
    id: 7727001,
    chapter: 'SELECT and WHERE',
    title: 'DISTINCT Scope',
    prompt: 'In "SELECT DISTINCT department, job_title FROM employees", what does DISTINCT eliminate?',
    correct: 'Duplicate combinations of (department, job_title) taken together as a pair.',
    wrong: [
      miss('Duplicate values in the department column only.', 'DISTINCT applies to the entire selected row, not just the first listed column.', 'DISTINCT considers every column in the SELECT list at once.'),
      miss('Duplicate values in each column independently.', 'DISTINCT does not de-duplicate columns separately; that would scramble which department goes with which title.', 'The whole projected tuple is the unit of uniqueness.'),
      miss('NULL values from the result set.', 'DISTINCT keeps NULLs and even treats multiple NULLs as a single duplicate; it does not remove them.', 'DISTINCT is about uniqueness, not about filtering NULLs.'),
    ],
    lesson: 'DISTINCT operates on the full set of columns in the SELECT list, removing rows that are identical across all those columns. Two NULLs are considered equal for the purpose of DISTINCT even though NULL = NULL is UNKNOWN in WHERE logic.',
    source,
    generated: true,
  },
  {
    id: 7727002,
    chapter: 'SELECT and WHERE',
    title: 'LIKE Wildcards',
    prompt: 'Which pattern matches any name that starts with "Jo" followed by exactly two more characters (a 4-character name)?',
    correct: "LIKE 'Jo__'",
    wrong: [
      miss("LIKE 'Jo%'", 'The percent sign matches any number of characters, including zero, so it also matches "Jo", "Joseph", and "Joan".', 'Percent is variable-length; underscore is exactly one character.'),
      miss("LIKE 'Jo*'", 'The asterisk is not a standard SQL LIKE wildcard; LIKE uses % and _ only.', 'Recall which two wildcard characters LIKE recognizes.'),
      miss("LIKE 'Jo??'", 'The question mark is not a LIKE wildcard in standard SQL; single characters are matched with underscore.', 'The single-character wildcard is the underscore.'),
    ],
    lesson: 'In SQL LIKE patterns, the underscore (_) matches exactly one character and the percent sign (%) matches zero or more characters. To require precisely four characters beginning with "Jo", use two underscores after the literal prefix.',
    source,
    generated: true,
  },
  {
    id: 7727003,
    chapter: 'SELECT and WHERE',
    title: 'BETWEEN Inclusivity',
    prompt: 'How many integer salaries does "WHERE salary BETWEEN 50000 AND 60000" include at the boundaries?',
    correct: 'Both 50000 and 60000 are included; BETWEEN is inclusive of both endpoints.',
    wrong: [
      miss('Neither endpoint is included; BETWEEN is exclusive.', 'BETWEEN is defined as inclusive in standard SQL, so both bounds count.', 'BETWEEN x AND y means x <= value AND value <= y.'),
      miss('Only the lower endpoint 50000 is included.', 'BETWEEN includes the upper bound as well, not just the lower one.', 'Both comparisons use <=, not <.'),
      miss('Only the upper endpoint 60000 is included.', 'BETWEEN includes the lower bound too; it is symmetric and inclusive.', 'The lower side uses >=, including the lower value.'),
    ],
    lesson: 'BETWEEN a AND b is shorthand for the predicate value >= a AND value <= b, so it is inclusive of both boundary values. This catches people who assume it behaves like an open interval.',
    source,
    generated: true,
  },
  {
    id: 7727004,
    chapter: 'SELECT and WHERE',
    title: 'IN versus OR',
    prompt: 'Which expression is logically equivalent to "WHERE country IN (\'US\', \'CA\', \'MX\')"?',
    correct: "WHERE country = 'US' OR country = 'CA' OR country = 'MX'",
    wrong: [
      miss("WHERE country = 'US' AND country = 'CA' AND country = 'MX'", 'A single column cannot equal three different values at once, so the AND version always returns no rows.', 'IN means membership in a set, which is a disjunction (OR), not a conjunction.'),
      miss("WHERE country LIKE 'US|CA|MX'", 'LIKE does not interpret a pipe as alternation; it would look for the literal string with pipes.', 'LIKE matches patterns of one string, not a list of alternatives.'),
      miss("WHERE country BETWEEN 'US' AND 'MX'", 'BETWEEN tests a continuous range by ordering, not membership in a specific discrete set.', 'IN checks an explicit list, not an alphabetic range.'),
    ],
    lesson: 'The IN operator tests set membership and expands to a chain of equality checks joined by OR. It is purely a readability convenience over a long OR chain and behaves as a disjunction, not a conjunction.',
    source,
    generated: true,
  },

  // ---------------- Chapter 2: ORDER BY and Result Shaping ----------------
  {
    id: 7727005,
    chapter: 'ORDER BY and Result Shaping',
    title: 'Default Sort Direction',
    prompt: 'When you write "ORDER BY hire_date" without specifying ASC or DESC, how are rows sorted?',
    correct: 'Ascending, from earliest to latest hire_date (ASC is the default).',
    wrong: [
      miss('Descending, from latest to earliest, by default.', 'The SQL default sort direction is ascending, not descending.', 'You must add DESC explicitly to reverse the order.'),
      miss('In the physical storage order of the table.', 'ORDER BY always applies the requested logical sort; the unsorted physical order only appears when no ORDER BY is present.', 'Specifying a sort column forces a logical sort, not storage order.'),
      miss('Randomly, since direction was not specified.', 'Omitting the direction does not randomize results; it defaults to ascending.', 'A missing keyword chooses a default, it does not remove ordering.'),
    ],
    lesson: 'ORDER BY sorts ascending (ASC) by default. To sort largest-to-smallest or latest-to-earliest you must write DESC explicitly. Without any ORDER BY clause at all, the database is free to return rows in any order.',
    source,
    generated: true,
  },
  {
    id: 7727006,
    chapter: 'ORDER BY and Result Shaping',
    title: 'NULL Ordering',
    prompt: 'In a query with "ORDER BY commission ASC NULLS LAST", where do rows with a NULL commission appear?',
    correct: 'At the very end of the result, after all non-NULL values.',
    wrong: [
      miss('At the very beginning, before all values, because NULL sorts as the smallest.', 'The NULLS LAST clause explicitly overrides any default and forces NULLs to the end.', 'Read the NULLS LAST modifier literally.'),
      miss('They are omitted entirely from the result set.', 'ORDER BY never filters rows; it only arranges them, so NULL rows remain present.', 'Sorting changes order, not membership.'),
      miss('Interleaved with the zero-valued commissions.', 'NULL is not treated as zero, and NULLS LAST groups them together at the end.', 'NULL has no numeric position; the clause dictates placement.'),
    ],
    lesson: 'The NULLS FIRST and NULLS LAST modifiers control where unknown values land in a sort independently of ASC/DESC. Different databases have different defaults for NULL placement, so specifying it explicitly makes ordering portable and predictable.',
    source,
    generated: true,
  },
  {
    id: 7727007,
    chapter: 'ORDER BY and Result Shaping',
    title: 'LIMIT with ORDER BY',
    prompt: 'To get the three highest-paid employees reliably, why must "LIMIT 3" be paired with an ORDER BY?',
    correct: 'Without ORDER BY the rows have no defined order, so LIMIT could return any arbitrary three rows.',
    wrong: [
      miss('LIMIT automatically sorts by the primary key, so ORDER BY is redundant.', 'LIMIT does not impose any sort; it just caps the row count of whatever order the engine produced.', 'LIMIT truncates an already-ordered (or unordered) stream.'),
      miss('ORDER BY is only needed to make the query run faster, not for correctness.', 'The issue here is correctness of which rows appear, not performance.', 'Think about which rows you get, not how quickly.'),
      miss('LIMIT picks the maximum values automatically by scanning the salary column.', 'LIMIT has no knowledge of which column matters; it only counts rows.', 'You must tell the engine what "highest" means via ORDER BY.'),
    ],
    lesson: 'LIMIT (or TOP / FETCH FIRST) simply caps how many rows are returned from the ordered stream the query produces. Since a query without ORDER BY has no guaranteed row order, you must add ORDER BY salary DESC to make "the top 3" meaningful and reproducible.',
    source,
    generated: true,
  },
  {
    id: 7727008,
    chapter: 'ORDER BY and Result Shaping',
    title: 'Ordering by Alias',
    prompt: 'In "SELECT price * quantity AS total FROM orders ORDER BY total", what makes ordering by the alias "total" valid?',
    correct: 'ORDER BY is logically evaluated after SELECT, so the column alias is already defined and usable.',
    wrong: [
      miss('Aliases can be used anywhere in the query, including in WHERE, because they are defined first.', 'The SELECT alias is not yet available in WHERE, which runs before SELECT; ORDER BY is the exception that runs after.', 'Consider the logical order of clause evaluation.'),
      miss('It works only because total happens to also be a stored column name.', 'The alias is computed from an expression, not a stored column; that is exactly why it is useful here.', 'ORDER BY can reference computed projection aliases.'),
      miss('It is actually invalid; you must repeat the full expression price * quantity in ORDER BY.', 'Standard SQL permits ORDER BY to reference SELECT-list aliases, so repeating the expression is unnecessary.', 'ORDER BY sees the projected output names.'),
    ],
    lesson: 'In the logical processing order of a query, ORDER BY is evaluated after the SELECT list, so any aliases defined in SELECT are visible to ORDER BY. By contrast, WHERE and GROUP BY run before SELECT, so they cannot reference those aliases.',
    source,
    generated: true,
  },

  // ---------------- Chapter 3: Aggregate Functions ----------------
  {
    id: 7727009,
    chapter: 'Aggregate Functions',
    title: 'COUNT(*) vs COUNT(column)',
    prompt: 'A table has 10 rows, and the "phone" column is NULL in 3 of them. What does COUNT(phone) return?',
    correct: '7, because COUNT(column) ignores rows where that column is NULL.',
    wrong: [
      miss('10, because COUNT always counts every row in the table.', 'Only COUNT(*) counts all rows; COUNT(column) skips NULLs in that column.', 'There is a difference between COUNT(*) and COUNT(column).'),
      miss('3, because COUNT(column) counts only the NULL rows.', 'COUNT(column) counts the non-NULL values, the opposite of counting NULLs.', 'COUNT tallies present values, not missing ones.'),
      miss('0, because the presence of any NULL invalidates COUNT.', 'A few NULLs do not void the count; they are simply excluded from the tally.', 'COUNT degrades gracefully, skipping only the NULL rows.'),
    ],
    lesson: 'COUNT(*) counts every row regardless of NULLs, while COUNT(column) counts only rows where that specific column is not NULL. This distinction is a frequent source of off-by-N errors when a column has missing values.',
    source,
    generated: true,
  },
  {
    id: 7727010,
    chapter: 'Aggregate Functions',
    title: 'AVG and NULLs',
    prompt: 'For salaries {100, 200, NULL, 300}, what does AVG(salary) return?',
    correct: '200, because AVG sums 600 over the 3 non-NULL values.',
    wrong: [
      miss('150, because AVG divides the sum 600 by 4 total rows.', 'AVG divides by the count of non-NULL values, not the total row count.', 'NULLs are excluded from both the numerator and the denominator.'),
      miss('600, because AVG returns the sum of the present values.', 'That is SUM, not AVG; AVG additionally divides by the number of values.', 'Average means sum divided by count.'),
      miss('0, because the NULL makes the average undefined.', 'A single NULL does not make AVG undefined; it is simply ignored.', 'AVG over the present values is well defined.'),
    ],
    lesson: 'Aggregate functions like AVG, SUM, MAX, and MIN ignore NULL inputs. AVG computes the sum of non-NULL values divided by the count of non-NULL values, so the NULL salary affects neither the total nor the divisor.',
    source,
    generated: true,
  },
  {
    id: 7727011,
    chapter: 'Aggregate Functions',
    title: 'Empty Table Aggregates',
    prompt: 'On a table with zero rows, what do COUNT(*) and SUM(amount) each return?',
    correct: 'COUNT(*) returns 0, but SUM(amount) returns NULL.',
    wrong: [
      miss('Both return 0.', 'SUM over an empty set returns NULL, not 0, even though COUNT returns 0.', 'COUNT and SUM differ on the empty-set edge case.'),
      miss('Both return NULL.', 'COUNT(*) always returns a number (0 for no rows); it never returns NULL.', 'COUNT is defined to give a count, which is 0 when empty.'),
      miss('Both raise an error because there is nothing to aggregate.', 'Aggregates over an empty set are valid and produce defined results, not errors.', 'Aggregation of no rows is legal SQL.'),
    ],
    lesson: 'COUNT(*) over an empty set is 0 by definition. SUM, AVG, MAX, and MIN over an empty set (no rows to aggregate) return NULL because there is no value to report. Wrapping SUM in COALESCE(SUM(amount), 0) is the common fix when you want 0.',
    source,
    generated: true,
  },
  {
    id: 7727012,
    chapter: 'Aggregate Functions',
    title: 'COUNT DISTINCT',
    prompt: 'A "city" column has values: London, Paris, London, NULL, Paris, Tokyo. What does COUNT(DISTINCT city) return?',
    correct: '3, counting the distinct non-NULL cities London, Paris, and Tokyo.',
    wrong: [
      miss('4, because the NULL counts as its own distinct category.', 'COUNT(DISTINCT column) excludes NULL entirely, so it does not add to the tally.', 'NULL is not counted by COUNT(DISTINCT ...).'),
      miss('6, because there are six rows in total.', 'DISTINCT collapses repeated values, so duplicates of London and Paris count once.', 'DISTINCT removes duplicates before counting.'),
      miss('5, because only the second London is removed as a duplicate.', 'All duplicates collapse and the NULL is dropped, leaving three distinct values.', 'Both repeated values dedupe, and NULL is ignored.'),
    ],
    lesson: 'COUNT(DISTINCT column) first removes duplicate values and also discards NULLs, then counts what remains. Here the unique non-NULL cities are London, Paris, and Tokyo, giving 3.',
    source,
    generated: true,
  },

  // ---------------- Chapter 4: GROUP BY and HAVING ----------------
  {
    id: 7727013,
    chapter: 'GROUP BY and HAVING',
    title: 'SELECT List Rule',
    prompt: 'In "SELECT department, AVG(salary) FROM employees GROUP BY department", why can department appear without being aggregated?',
    correct: 'department is a grouping column, so it has a single value per group and may be selected directly.',
    wrong: [
      miss('Any column may appear in the SELECT list of a grouped query without aggregation.', 'Only the GROUP BY columns (or aggregates) may appear; non-grouped columns would be ambiguous per group.', 'Non-grouped, non-aggregated columns are not allowed in standard SQL.'),
      miss('Because AVG(salary) implicitly groups department for you.', 'The GROUP BY clause, not the aggregate function, is what defines the grouping columns.', 'It is GROUP BY department that makes department valid here.'),
      miss('Because department is the primary key of the table.', 'The reason is that it appears in GROUP BY, regardless of whether it is a key.', 'Grouping membership, not key status, permits the bare column.'),
    ],
    lesson: 'In a grouped query, every column in the SELECT list must either appear in the GROUP BY clause or be wrapped in an aggregate function. Grouping columns are valid because each group has exactly one value for them; other bare columns would have multiple possible values and are rejected.',
    source,
    generated: true,
  },
  {
    id: 7727014,
    chapter: 'GROUP BY and HAVING',
    title: 'WHERE versus HAVING',
    prompt: 'You want only departments whose average salary exceeds 80000. Which clause filters on that condition?',
    correct: 'HAVING AVG(salary) > 80000, because HAVING filters groups after aggregation.',
    wrong: [
      miss('WHERE AVG(salary) > 80000, because WHERE is the standard filter clause.', 'WHERE runs before aggregation and cannot reference aggregate results like AVG.', 'Aggregate conditions belong in HAVING, not WHERE.'),
      miss('WHERE salary > 80000, which filters out low salaries before grouping.', 'That filters individual rows by raw salary, not groups by their average; the result differs.', 'Per-row filtering is not the same as per-group filtering.'),
      miss('ORDER BY AVG(salary) > 80000, sorting by the condition.', 'ORDER BY arranges rows; it does not filter them, and a boolean is not a sort key here.', 'Sorting is not filtering.'),
    ],
    lesson: 'WHERE filters individual rows before grouping and cannot use aggregate functions. HAVING filters whole groups after aggregation and can reference aggregates like AVG, SUM, and COUNT. Use WHERE for row predicates and HAVING for group predicates.',
    source,
    generated: true,
  },
  {
    id: 7727015,
    chapter: 'GROUP BY and HAVING',
    title: 'Combining WHERE and HAVING',
    prompt: 'In "SELECT dept, COUNT(*) FROM emp WHERE active = 1 GROUP BY dept HAVING COUNT(*) > 5", what is filtered by WHERE?',
    correct: 'Individual rows where active = 1 are kept before any grouping occurs.',
    wrong: [
      miss('Groups whose count exceeds 5 are kept by the WHERE clause.', 'That filtering is done by HAVING; WHERE operates on rows before grouping.', 'WHERE precedes grouping; HAVING follows it.'),
      miss('Both row-level and group-level conditions are handled by WHERE together.', 'WHERE handles only row-level conditions; HAVING handles the group-level COUNT condition.', 'Each clause has a distinct stage in evaluation.'),
      miss('Nothing, because WHERE is ignored once a GROUP BY is present.', 'WHERE is always applied first and is never ignored when GROUP BY exists.', 'WHERE still runs and reduces rows before grouping.'),
    ],
    lesson: 'A query can use both: WHERE narrows the input rows first (here keeping only active employees), then GROUP BY forms groups, then HAVING removes groups failing the aggregate condition (count over 5). Applying WHERE first also makes grouping cheaper.',
    source,
    generated: true,
  },
  {
    id: 7727016,
    chapter: 'GROUP BY and HAVING',
    title: 'Grouping Granularity',
    prompt: 'How many result rows does "GROUP BY region, product" produce relative to "GROUP BY region" on the same data?',
    correct: 'Generally more (or equal) rows, because each region is split into separate (region, product) combinations.',
    wrong: [
      miss('Always fewer rows, because adding a column merges groups.', 'Adding a grouping column subdivides groups, increasing the number of result rows, not merging them.', 'More grouping columns means finer partitions.'),
      miss('Exactly the same number of rows, since the regions are unchanged.', 'Each region can contain multiple products, so it expands into several rows.', 'Granularity changes when you add a dimension.'),
      miss('Always exactly twice as many rows, one per added column.', 'The factor depends on how many distinct products occur per region, not on the column count.', 'The multiplier is data-dependent.'),
    ],
    lesson: 'Adding a column to GROUP BY makes the grouping finer-grained: each existing group is partitioned into one row per distinct combination of all grouping columns. The result count rises (or stays equal) and can never decrease compared with grouping by a subset of those columns.',
    source,
    generated: true,
  },

  // ---------------- Chapter 5: INNER and OUTER JOINs ----------------
  {
    id: 7727017,
    chapter: 'INNER and OUTER JOINs',
    title: 'INNER JOIN Semantics',
    prompt: 'An INNER JOIN between Orders and Customers on customer_id returns which rows?',
    correct: 'Only rows where a matching customer_id exists in both tables.',
    wrong: [
      miss('All rows from Orders plus matching customer details, with NULLs where unmatched.', 'That describes a LEFT OUTER JOIN, which preserves unmatched left rows; INNER JOIN drops them.', 'INNER JOIN keeps only matched pairs from both sides.'),
      miss('Every combination of every order with every customer.', 'That is a CROSS JOIN (Cartesian product); INNER JOIN restricts to the matching condition.', 'A join condition prevents the full Cartesian product.'),
      miss('All customers, including those with no orders, with NULL order data.', 'That is a RIGHT OUTER JOIN preserving unmatched customers; INNER JOIN excludes them.', 'INNER JOIN excludes rows with no match on either side.'),
    ],
    lesson: 'An INNER JOIN returns only rows where the join condition is satisfied in both tables, discarding any row from either side that has no match. Outer joins (LEFT, RIGHT, FULL) instead preserve unmatched rows from one or both sides, padding the missing columns with NULL.',
    source,
    generated: true,
  },
  {
    id: 7727018,
    chapter: 'INNER and OUTER JOINs',
    title: 'LEFT JOIN Unmatched Rows',
    prompt: 'In "Customers LEFT JOIN Orders ON Customers.id = Orders.customer_id", what happens to a customer with no orders?',
    correct: 'The customer row is kept once, with all the Orders columns filled with NULL.',
    wrong: [
      miss('The customer row is omitted because there is no matching order.', 'LEFT JOIN preserves all left-table rows regardless of whether a match exists.', 'The left table is fully retained in a LEFT JOIN.'),
      miss('The customer row is duplicated once for every order in the table.', 'Without a match, the customer appears exactly once with NULL order columns, not duplicated.', 'No match means a single padded row, not many.'),
      miss('The Orders columns are filled with zeros instead of NULLs.', 'Unmatched outer-join columns are padded with NULL, not zero.', 'The placeholder for missing join data is NULL.'),
    ],
    lesson: 'A LEFT (OUTER) JOIN keeps every row from the left table. When the right table has no matching row, the result still includes the left row once, with all right-table columns set to NULL. This is the standard way to find records lacking a related record.',
    source,
    generated: true,
  },
  {
    id: 7727019,
    chapter: 'INNER and OUTER JOINs',
    title: 'Finding Orphans',
    prompt: 'Which query finds customers who have never placed an order?',
    correct: 'Customers LEFT JOIN Orders ON id = customer_id, then WHERE Orders.customer_id IS NULL.',
    wrong: [
      miss('Customers INNER JOIN Orders, then WHERE Orders.id IS NULL.', 'An INNER JOIN already removes customers without orders, so the WHERE on NULL returns nothing.', 'You need an outer join to retain customers that lack orders.'),
      miss('Customers LEFT JOIN Orders, then WHERE Orders.amount > 0.', 'Filtering on a positive amount selects customers who DO have orders, the opposite of what you want.', 'Look for the unmatched (NULL) side, not positive values.'),
      miss('Customers RIGHT JOIN Orders, then WHERE Customers.id IS NULL.', 'A RIGHT JOIN keeps all orders and finds orphan orders, not orphan customers.', 'The side you want to fully preserve is Customers.'),
    ],
    lesson: 'The anti-join idiom finds rows in one table with no match in another: LEFT JOIN to keep all left rows, then filter WHERE <right key> IS NULL to keep only the unmatched ones. Here that isolates customers who never placed an order.',
    source,
    generated: true,
  },
  {
    id: 7727020,
    chapter: 'INNER and OUTER JOINs',
    title: 'FULL OUTER JOIN',
    prompt: 'What does a FULL OUTER JOIN between two tables return?',
    correct: 'All matched rows, plus unmatched rows from both tables padded with NULL on the missing side.',
    wrong: [
      miss('Only the rows that appear in both tables.', 'That is an INNER JOIN; FULL OUTER additionally keeps unmatched rows from each side.', 'FULL OUTER is more inclusive than INNER.'),
      miss('Unmatched rows from the left table only, with NULLs on the right.', 'That is a LEFT OUTER JOIN; FULL OUTER also keeps unmatched right rows.', 'FULL covers both sides, not just the left.'),
      miss('The Cartesian product of both tables ignoring any condition.', 'That is a CROSS JOIN; FULL OUTER still honors the ON condition for matching.', 'A full outer join still matches on the join condition.'),
    ],
    lesson: 'A FULL OUTER JOIN is the union of a LEFT and a RIGHT outer join: it returns matched pairs plus every unmatched row from both tables, filling absent columns with NULL. It is useful for reconciling two datasets where records may exist on either side only.',
    source,
    generated: true,
  },
  {
    id: 7727021,
    chapter: 'INNER and OUTER JOINs',
    title: 'Self Join',
    prompt: 'An employees table has columns id and manager_id (a self-reference). To list each employee next to their manager name, you should:',
    correct: 'Join the employees table to itself using two aliases, matching e.manager_id = m.id.',
    wrong: [
      miss('Use a GROUP BY on manager_id to collapse employees under managers.', 'GROUP BY aggregates rows; it cannot pair each employee with the manager name from the same table.', 'You need a join, not aggregation, to fetch the manager name.'),
      miss('Use a UNION of the table with itself to stack employees and managers.', 'UNION stacks rows vertically; it cannot align an employee row with its manager row.', 'Pairing related rows is a join, not a union.'),
      miss('Add a foreign key so the manager name appears automatically.', 'A foreign key enforces integrity but does not retrieve the related name; you still must join.', 'Constraints do not perform retrieval.'),
    ],
    lesson: 'A self join treats one table as two logical copies via aliases, letting you relate a row to another row in the same table. For a manager hierarchy you alias employees as e and m and join on e.manager_id = m.id to bring the manager name alongside each employee.',
    source,
    generated: true,
  },
  {
    id: 7727022,
    chapter: 'INNER and OUTER JOINs',
    title: 'Cross Join Size',
    prompt: 'A CROSS JOIN of a table with 5 rows and a table with 4 rows produces how many rows?',
    correct: '20 rows, the product 5 x 4 (the Cartesian product).',
    wrong: [
      miss('9 rows, the sum 5 + 4 of the two row counts.', 'A cross join multiplies the row counts; it does not add them.', 'Cartesian product is multiplication, not addition.'),
      miss('5 rows, matching the size of the larger... smaller table.', 'A cross join pairs every row with every row, far exceeding either single table.', 'Every left row pairs with every right row.'),
      miss('4 rows, limited by the smaller table.', 'No join condition limits a cross join, so it produces all combinations, not the minimum count.', 'Cross join has no ON clause to restrict rows.'),
    ],
    lesson: 'A CROSS JOIN forms the Cartesian product, pairing every row of one table with every row of the other, so the result has m x n rows. Because it can explode result sizes, it is used deliberately (e.g., generating combinations) rather than by accident.',
    source,
    generated: true,
  },

  // ---------------- Chapter 6: Subqueries ----------------
  {
    id: 7727023,
    chapter: 'Subqueries',
    title: 'Scalar Subquery',
    prompt: 'In "WHERE salary > (SELECT AVG(salary) FROM employees)", what kind of subquery is this and what must it return?',
    correct: 'A scalar subquery that must return exactly one row and one column (a single value).',
    wrong: [
      miss('A correlated subquery that runs once per outer row.', 'This subquery does not reference the outer query, so it runs once, not per row; it is uncorrelated and scalar.', 'Correlation requires referencing an outer column.'),
      miss('A multi-row subquery that can return a list of values.', 'Compared with > and not IN, it must collapse to a single scalar value, not a list.', 'A plain > comparison needs one value, not a set.'),
      miss('A derived table that must be given an alias in the FROM clause.', 'This subquery sits in WHERE producing a value, not in FROM producing a table needing an alias.', 'Its position determines its role: WHERE here, not FROM.'),
    ],
    lesson: 'A scalar subquery returns a single value (one row, one column) and can be used anywhere a single value is expected, such as the right side of a comparison. Because it does not reference any outer column, it is uncorrelated and the engine can evaluate it just once.',
    source,
    generated: true,
  },
  {
    id: 7727024,
    chapter: 'Subqueries',
    title: 'Correlated Subquery',
    prompt: 'What distinguishes a correlated subquery from a non-correlated one?',
    correct: 'A correlated subquery references a column from the outer query, so it is conceptually re-evaluated for each outer row.',
    wrong: [
      miss('A correlated subquery always returns multiple rows, whereas a non-correlated one returns a single row.', 'Correlation is about referencing the outer query, not about how many rows are returned.', 'Row count and correlation are independent properties.'),
      miss('A correlated subquery must appear in the FROM clause, not in WHERE.', 'Correlated subqueries most commonly appear in WHERE or SELECT, where they can see outer columns; FROM-clause derived tables cannot.', 'Correlation is defined by referencing outer columns, not by clause position.'),
      miss('A correlated subquery is evaluated once and cached for the whole query.', 'Because it depends on each outer row, it cannot be evaluated just once like an uncorrelated subquery.', 'Dependence on the outer row prevents one-time evaluation.'),
    ],
    lesson: 'A correlated subquery references one or more columns from the enclosing (outer) query, so its result depends on the current outer row and is conceptually computed once per outer row. An uncorrelated subquery is self-contained and can be evaluated a single time.',
    source,
    generated: true,
  },
  {
    id: 7727025,
    chapter: 'Subqueries',
    title: 'EXISTS Semantics',
    prompt: 'What does "WHERE EXISTS (SELECT 1 FROM orders o WHERE o.cust_id = c.id)" test for each customer c?',
    correct: 'Whether at least one matching order row exists; it returns TRUE as soon as one is found.',
    wrong: [
      miss('Whether exactly one matching order exists, failing if there are several.', 'EXISTS is satisfied by one or more matches; it does not require exactly one.', 'EXISTS checks for presence, not for a count of one.'),
      miss('The number of matching orders, returning that count.', 'EXISTS yields a boolean, not a count; the SELECT 1 is just a placeholder.', 'The selected value inside EXISTS is irrelevant; only row presence matters.'),
      miss('Whether the value 1 appears in the orders table.', 'SELECT 1 is a dummy constant; EXISTS ignores the projected value and only checks if any row qualifies.', 'EXISTS cares about row existence, not what is selected.'),
    ],
    lesson: 'EXISTS returns TRUE if its subquery yields at least one row and FALSE otherwise; the actual columns selected (often SELECT 1) are irrelevant. It short-circuits on the first matching row, making it an efficient way to test for the presence of related data.',
    source,
    generated: true,
  },
  {
    id: 7727026,
    chapter: 'Subqueries',
    title: 'IN with NULLs',
    prompt: 'Why can "WHERE x NOT IN (SELECT y FROM t)" return no rows if the subquery contains a NULL?',
    correct: 'A NULL in the list makes NOT IN evaluate to UNKNOWN for every x, so no row qualifies as TRUE.',
    wrong: [
      miss('NOT IN automatically excludes the whole table when any NULL is present, by design.', 'It is not a deliberate exclusion rule; it is a side effect of three-valued logic making the predicate UNKNOWN.', 'The cause is NULL comparison semantics, not a special rule.'),
      miss('NULL is treated as zero, so x = 0 rows are filtered out.', 'NULL is not zero; the issue is that comparisons with NULL yield UNKNOWN, not a numeric match.', 'Three-valued logic, not a zero substitution, is the culprit.'),
      miss('The subquery itself raises an error when it contains NULL.', 'No error occurs; the query runs and simply returns an empty (or reduced) result due to UNKNOWN logic.', 'It is a silent logic trap, not a runtime error.'),
    ],
    lesson: 'NOT IN expands to a chain of <> comparisons joined by AND. If the value list contains NULL, comparing x <> NULL yields UNKNOWN, and ANDing UNKNOWN prevents the whole predicate from ever being TRUE, so all rows are filtered out. Using NOT EXISTS avoids this trap.',
    source,
    generated: true,
  },
  {
    id: 7727027,
    chapter: 'Subqueries',
    title: 'Derived Table',
    prompt: 'In "SELECT * FROM (SELECT dept, COUNT(*) c FROM emp GROUP BY dept) d WHERE d.c > 10", what is the inner SELECT called?',
    correct: 'A derived table (subquery in the FROM clause) that must be given an alias such as d.',
    wrong: [
      miss('A correlated subquery that references the outer query.', 'It does not reference any outer column; it is a standalone subquery in FROM, a derived table.', 'No outer reference means it is not correlated.'),
      miss('A common table expression (CTE) defined with WITH.', 'A CTE uses the WITH keyword; this is an inline subquery in FROM, called a derived table.', 'CTEs are introduced by WITH, not by FROM (...).'),
      miss('A view, which persists in the database catalog.', 'A view is a stored, named object; this subquery exists only for the duration of the query.', 'Views are persistent; derived tables are inline and transient.'),
    ],
    lesson: 'A subquery placed in the FROM clause is a derived table (inline view). It produces a result set that the outer query treats like a table, and in most databases it must be assigned an alias. This lets you filter on aggregates (like c > 10) that HAVING could also handle.',
    source,
    generated: true,
  },

  // ---------------- Chapter 7: INSERT, UPDATE, DELETE ----------------
  {
    id: 7727028,
    chapter: 'INSERT, UPDATE, DELETE',
    title: 'UPDATE without WHERE',
    prompt: 'What is the effect of "UPDATE products SET price = price * 1.1" with no WHERE clause?',
    correct: 'Every row in the products table is updated, raising all prices by 10 percent.',
    wrong: [
      miss('Only the first row is updated, since no specific row was named.', 'Without WHERE the update is unfiltered and applies to all rows, not just the first.', 'No WHERE means no row restriction.'),
      miss('The statement is rejected because UPDATE requires a WHERE clause.', 'A WHERE clause is optional; omitting it is legal and affects every row.', 'WHERE is optional, which is exactly why this is dangerous.'),
      miss('Nothing changes because no rows were selected to update.', 'An absent WHERE selects all rows, not none, so every row is modified.', 'Absence of a filter targets everything, not nothing.'),
    ],
    lesson: 'In UPDATE and DELETE, the WHERE clause restricts which rows are affected. If you omit it, the operation applies to every row in the table. This is a classic and costly mistake, which is why running a SELECT with the same WHERE first is good practice.',
    source,
    generated: true,
  },
  {
    id: 7727029,
    chapter: 'INSERT, UPDATE, DELETE',
    title: 'DELETE versus TRUNCATE',
    prompt: 'Which statement about DELETE versus TRUNCATE on a table is correct?',
    correct: 'DELETE can use a WHERE clause to remove specific rows and is logged per row; TRUNCATE removes all rows quickly with minimal logging and no WHERE.',
    wrong: [
      miss('TRUNCATE can target specific rows with a WHERE clause, just like DELETE.', 'TRUNCATE has no WHERE clause; it always removes every row in the table.', 'TRUNCATE is all-or-nothing on the table.'),
      miss('DELETE drops the table structure, while TRUNCATE keeps it.', 'Both keep the table definition; only DROP removes the table structure.', 'Neither DELETE nor TRUNCATE removes the table itself.'),
      miss('DELETE always resets identity/auto-increment counters; TRUNCATE never does.', 'It is generally the reverse: TRUNCATE typically resets identity counters while DELETE does not.', 'Think about which one reseeds auto-increment.'),
    ],
    lesson: 'DELETE removes rows that match an optional WHERE clause, logs each row, and fires row triggers, so it can be rolled back row by row. TRUNCATE deallocates whole data pages, removes all rows with minimal logging, cannot use WHERE, and in many databases resets identity counters, making it faster but less granular.',
    source,
    generated: true,
  },
  {
    id: 7727030,
    chapter: 'INSERT, UPDATE, DELETE',
    title: 'INSERT Column Defaults',
    prompt: 'A table has columns (id SERIAL, name, created_at DEFAULT now()). When you run "INSERT INTO t (name) VALUES (\'Ada\')", what happens to id and created_at?',
    correct: 'id receives the next auto-generated sequence value and created_at receives its DEFAULT, the current timestamp.',
    wrong: [
      miss('id and created_at are both set to NULL because no value was supplied.', 'Columns with a SERIAL/auto-increment or DEFAULT get their generated/default value, not NULL.', 'Defaults and sequences fill in omitted columns.'),
      miss('The INSERT fails because all columns must be listed.', 'You may list a subset of columns; omitted ones take their defaults or generated values.', 'Partial column lists are valid INSERTs.'),
      miss('created_at must be provided manually since DEFAULT only applies on UPDATE.', 'DEFAULT applies when a column is omitted from an INSERT, supplying the now() value.', 'DEFAULT fills omitted columns at INSERT time.'),
    ],
    lesson: 'When an INSERT names only some columns, the omitted columns take their defined DEFAULT value, or the next value of an auto-increment/sequence, or NULL if none of those apply and NULL is allowed. So id auto-increments and created_at gets the current timestamp from now().',
    source,
    generated: true,
  },
  {
    id: 7727031,
    chapter: 'INSERT, UPDATE, DELETE',
    title: 'UPDATE from Join',
    prompt: 'You need to raise salary by 5 percent only for employees in departments located in "NYC". The most direct approach is to:',
    correct: 'UPDATE employees with a WHERE clause whose subquery or join restricts to dept_ids located in NYC.',
    wrong: [
      miss('Run a SELECT to view NYC employees; the SELECT itself applies the raise.', 'SELECT only reads data; it never modifies rows, so no raise is applied.', 'You must issue an UPDATE to change values.'),
      miss('Use GROUP BY location in the UPDATE to restrict which rows change.', 'GROUP BY is for aggregation in queries, not for restricting which rows an UPDATE touches.', 'Row restriction in UPDATE comes from WHERE, not GROUP BY.'),
      miss('Delete the non-NYC employees first, then raise everyone remaining.', 'Deleting valid rows is destructive and wrong; you only want to modify, not remove, employees.', 'Filtering an UPDATE should not require deleting data.'),
    ],
    lesson: 'To update a subset based on a related table, restrict the UPDATE with a WHERE clause that uses a join or correlated subquery against the related table (here, departments in NYC). UPDATE modifies in place; SELECT only reads, and you should never delete rows merely to scope a change.',
    source,
    generated: true,
  },

  // ---------------- Chapter 8: Indexes and Performance ----------------
  {
    id: 7727032,
    chapter: 'Indexes and Performance',
    title: 'Purpose of an Index',
    prompt: 'What is the primary purpose of creating an index on a column?',
    correct: 'To speed up lookups and range scans on that column by avoiding a full table scan.',
    wrong: [
      miss('To enforce that the column can only hold unique values.', 'Only a UNIQUE index enforces uniqueness; a plain index is purely for performance.', 'Uniqueness is a constraint feature, not the general purpose of indexing.'),
      miss('To reduce the disk space the table occupies.', 'Indexes add storage overhead rather than reducing it; they trade space for query speed.', 'Indexes cost extra space, not less.'),
      miss('To automatically sort the underlying table rows on disk permanently.', 'A non-clustered index keeps its own ordered structure without reordering the table; only a clustered index dictates physical order.', 'A standard index is a separate structure, not a re-sort of the table.'),
    ],
    lesson: 'An index is an auxiliary data structure (commonly a B-tree) that lets the database find rows by a column value without scanning every row. It accelerates SELECT lookups and range queries at the cost of extra storage and slower writes, since the index must be maintained on INSERT, UPDATE, and DELETE.',
    source,
    generated: true,
  },
  {
    id: 7727033,
    chapter: 'Indexes and Performance',
    title: 'Index Write Cost',
    prompt: 'Why can adding many indexes to a table hurt the performance of INSERT and UPDATE statements?',
    correct: 'Each index must be updated whenever rows change, adding maintenance work to every write.',
    wrong: [
      miss('Indexes lock the entire table, blocking all writes permanently.', 'Indexes do not permanently lock tables; the cost is incremental maintenance, not a lock-out.', 'The slowdown comes from extra work per write, not a permanent lock.'),
      miss('Indexes cause SELECT queries to ignore the WHERE clause.', 'Indexes assist WHERE filtering; they never cause it to be ignored.', 'Indexes help reads, not hinder filtering.'),
      miss('Indexes convert the table to read-only after creation.', 'Tables remain writable; writes simply have to maintain the indexes too.', 'Indexes coexist with writes, just at extra cost.'),
    ],
    lesson: 'Every index is a separate structure the database must keep in sync. When a row is inserted, updated, or deleted, all affected indexes are modified too, so heavily indexed tables pay a write penalty. The art of indexing is adding enough to speed reads without overburdening writes.',
    source,
    generated: true,
  },
  {
    id: 7727034,
    chapter: 'Indexes and Performance',
    title: 'Clustered vs Non-Clustered',
    prompt: 'How does a clustered index differ from a non-clustered index?',
    correct: 'A clustered index determines the physical order of the table rows, so a table can have only one; a non-clustered index is a separate structure pointing back to the rows.',
    wrong: [
      miss('A clustered index allows duplicate values, while a non-clustered index never does.', 'Clustering is about physical row order, not about whether duplicates are allowed; either kind can be unique or not.', 'The distinction is physical ordering, not duplicate handling.'),
      miss('A table may have many clustered indexes but only one non-clustered index.', 'It is the reverse: at most one clustered index (it dictates row order) but many non-clustered indexes.', 'Only one index can govern the physical order.'),
      miss('A clustered index is stored in memory while a non-clustered index is on disk.', 'Both are storage structures and can be cached; the difference is physical ordering, not memory residency.', 'Residency is not the defining trait here.'),
    ],
    lesson: 'A clustered index stores the table rows themselves in the index order, so the data and index are one structure and a table can have only one clustered index. A non-clustered index is a separate ordered structure containing key values and pointers (row locators) back to the data, and a table may have many of them.',
    source,
    generated: true,
  },
  {
    id: 7727035,
    chapter: 'Indexes and Performance',
    title: 'Composite Index Order',
    prompt: 'A composite index is created on (last_name, first_name). Which query can use this index efficiently for its filter?',
    correct: 'WHERE last_name = \'Lovelace\' (a prefix of the index columns is enough).',
    wrong: [
      miss('WHERE first_name = \'Ada\' alone.', 'A composite index is ordered by last_name first, so filtering on first_name without last_name cannot use the index seek.', 'The leftmost column must be constrained to use the index.'),
      miss('WHERE LOWER(last_name) = \'lovelace\'.', 'Wrapping the column in a function defeats the index unless a matching function-based index exists.', 'Applying a function to the column usually prevents an index seek.'),
      miss('WHERE last_name <> \'Lovelace\'.', 'An inequality that excludes one value typically scans most of the index rather than seeking efficiently.', 'Not-equal predicates rarely benefit from a B-tree seek.'),
    ],
    lesson: 'A composite (multi-column) index is ordered by its columns left to right, so it can be used for queries that constrain a leftmost prefix of those columns. Filtering on last_name (the first column) works; filtering only on first_name does not, and wrapping the indexed column in a function blocks the index.',
    source,
    generated: true,
  },
  {
    id: 7727036,
    chapter: 'Indexes and Performance',
    title: 'EXPLAIN Purpose',
    prompt: 'What does running EXPLAIN (or EXPLAIN PLAN) on a query show you?',
    correct: 'The execution plan the optimizer chose, including join methods, access paths, and whether indexes are used.',
    wrong: [
      miss('The exact result rows the query will return.', 'EXPLAIN reveals how the query will be executed, not the data it returns.', 'It describes the plan, not the output rows.'),
      miss('A guarantee that the query is syntactically and logically correct.', 'EXPLAIN shows the plan but does not validate business correctness of the logic.', 'A valid plan does not mean the query answers the right question.'),
      miss('The number of indexes that should be created on each table.', 'EXPLAIN does not prescribe new indexes; it reports the current plan, which you interpret yourself.', 'It is diagnostic, not prescriptive.'),
    ],
    lesson: 'EXPLAIN exposes the query optimizer execution plan: which indexes or full scans are used, the join algorithms, and estimated (or, with ANALYZE, actual) row counts and costs. Reading it is the primary way to diagnose slow queries and confirm whether an index is actually being used.',
    source,
    generated: true,
  },

  // ---------------- Chapter 9: Normalization ----------------
  {
    id: 7727037,
    chapter: 'Normalization',
    title: 'First Normal Form',
    prompt: 'A table violates First Normal Form (1NF) when:',
    correct: 'A single column stores multiple values (a repeating group or list) instead of one atomic value per row.',
    wrong: [
      miss('A non-key column depends on only part of a composite primary key.', 'That is a 2NF (partial dependency) violation, which is addressed after 1NF is already satisfied.', '1NF is about atomic values, not partial key dependencies.'),
      miss('A non-key column depends on another non-key column.', 'That transitive dependency violates 3NF, not 1NF.', '1NF concerns atomicity, not transitive dependencies.'),
      miss('The table lacks any foreign keys to related tables.', 'Missing foreign keys is a referential-integrity concern, not a normal-form violation.', '1NF says nothing about foreign keys.'),
    ],
    lesson: 'First Normal Form requires that each column holds a single atomic value and that there are no repeating groups or multi-valued attributes within a row. Storing a comma-separated list of phone numbers in one cell, for example, violates 1NF and should be split into separate rows or a related table.',
    source,
    generated: true,
  },
  {
    id: 7727038,
    chapter: 'Normalization',
    title: 'Second Normal Form',
    prompt: 'A table with composite key (order_id, product_id) stores product_name, which depends only on product_id. Which normal form does this violate?',
    correct: 'Second Normal Form (2NF), because product_name has a partial dependency on only part of the composite key.',
    wrong: [
      miss('First Normal Form (1NF), because the values are not atomic.', 'product_name is a single atomic value; the problem is a partial dependency, which is a 2NF issue.', '1NF is about atomicity, which holds here.'),
      miss('Third Normal Form (3NF), because of a transitive dependency.', 'A transitive dependency is non-key to non-key; here the dependency is on part of the key, which is 2NF.', 'Distinguish partial (2NF) from transitive (3NF) dependencies.'),
      miss('Boyce-Codd Normal Form (BCNF), because a determinant is not a candidate key.', 'BCNF is a stricter refinement; the more basic violation here is a partial-key dependency, which is 2NF.', 'Identify the simplest violated form first.'),
    ],
    lesson: 'Second Normal Form requires that, given a composite primary key, every non-key attribute depend on the whole key, not just part of it. Because product_name depends on product_id alone (a partial dependency), the table violates 2NF and product details should move to a separate Products table.',
    source,
    generated: true,
  },
  {
    id: 7727039,
    chapter: 'Normalization',
    title: 'Third Normal Form',
    prompt: 'A table keyed by employee_id stores dept_id and dept_name, where dept_name depends on dept_id. Which normal form does this violate?',
    correct: 'Third Normal Form (3NF), because dept_name has a transitive dependency on the key through dept_id.',
    wrong: [
      miss('First Normal Form (1NF), due to non-atomic data.', 'All values here are atomic; the issue is a transitive dependency, a 3NF matter.', '1NF concerns atomicity, which is satisfied.'),
      miss('Second Normal Form (2NF), due to a partial-key dependency.', 'The key is a single column (employee_id), so there is no partial dependency; this is a transitive one.', '2NF partial dependencies require a composite key.'),
      miss('No normal form is violated; this is a valid 3NF design.', 'It does violate 3NF, since a non-key column (dept_name) depends on another non-key column (dept_id).', 'A non-key to non-key dependency breaks 3NF.'),
    ],
    lesson: 'Third Normal Form requires that non-key attributes depend only on the key, not on other non-key attributes. Here dept_name depends on dept_id, which is itself a non-key column, forming a transitive dependency key -> dept_id -> dept_name. The fix is to put dept_name in a separate Departments table.',
    source,
    generated: true,
  },
  {
    id: 7727040,
    chapter: 'Normalization',
    title: 'Denormalization Tradeoff',
    prompt: 'Why might a team deliberately denormalize a reporting table by duplicating some data?',
    correct: 'To reduce the number of joins and speed up read-heavy queries, accepting redundancy and update complexity.',
    wrong: [
      miss('To guarantee that no anomalies can ever occur on insert, update, or delete.', 'Denormalization increases redundancy and thus the risk of update anomalies; normalization is what reduces them.', 'Redundancy is the trade you accept, not the benefit.'),
      miss('To save storage space by removing duplicate data.', 'Denormalization typically adds duplicated data and uses more space, not less.', 'It increases redundancy, raising storage use.'),
      miss('Because denormalized schemas are always required to enforce foreign keys.', 'Foreign keys are enforced through constraints regardless of normalization level.', 'Referential integrity does not depend on denormalizing.'),
    ],
    lesson: 'Denormalization intentionally introduces redundancy (e.g., storing a pre-joined or precomputed value) to cut down on expensive joins and accelerate reads in analytics or reporting workloads. The cost is more storage and the burden of keeping duplicated data consistent, so it is a deliberate performance trade-off.',
    source,
    generated: true,
  },
  {
    id: 7727041,
    chapter: 'Normalization',
    title: 'Update Anomaly',
    prompt: 'In an unnormalized table that repeats a supplier address on every product row, what is an "update anomaly"?',
    correct: 'Changing the supplier address requires updating many rows, and missing some leaves the data inconsistent.',
    wrong: [
      miss('The database refuses to update any duplicated value.', 'The database permits the updates; the anomaly is the risk of partial, inconsistent updates, not a refusal.', 'The danger is inconsistency, not rejection.'),
      miss('Two users updating different tables at the same time deadlock.', 'That describes a concurrency deadlock, which is unrelated to redundancy-driven update anomalies.', 'Anomalies stem from redundancy, not concurrency locks.'),
      miss('An index becomes stale and must be rebuilt after every change.', 'Index maintenance is a performance concern, not a normalization update anomaly.', 'Update anomalies are about data redundancy, not indexes.'),
    ],
    lesson: 'An update anomaly arises when redundant data is stored in many places: a single logical change (like a new supplier address) must be applied to every copy, and any missed copy produces contradictory data. Normalization eliminates this by storing each fact exactly once.',
    source,
    generated: true,
  },

  // ---------------- Chapter 10: Keys and Constraints ----------------
  {
    id: 7727042,
    chapter: 'Keys and Constraints',
    title: 'Primary Key Properties',
    prompt: 'Which combination of properties must a PRIMARY KEY satisfy?',
    correct: 'It must be unique across rows and never NULL.',
    wrong: [
      miss('It must be unique but may contain NULLs to mark unknown identifiers.', 'A primary key may not contain NULL; NULL would defeat its role of uniquely identifying each row.', 'Primary keys forbid NULL entirely.'),
      miss('It may repeat values as long as the whole row is unique.', 'A primary key value itself must be unique; allowing repeats would break row identification.', 'The key column(s) alone must be unique.'),
      miss('It must be a single integer column.', 'A primary key can be composite (multiple columns) and of various types, not necessarily a single integer.', 'Composite and non-integer keys are valid.'),
    ],
    lesson: 'A PRIMARY KEY uniquely identifies each row, so it must be both unique and NOT NULL. It may be a single column or a composite of several columns. A table has at most one primary key, though it can have additional UNIQUE constraints.',
    source,
    generated: true,
  },
  {
    id: 7727043,
    chapter: 'Keys and Constraints',
    title: 'Unique vs Primary Key',
    prompt: 'How does a UNIQUE constraint differ from a PRIMARY KEY constraint?',
    correct: 'A table can have many UNIQUE constraints and a UNIQUE column usually allows one NULL, whereas there is at most one PRIMARY KEY and it forbids NULL.',
    wrong: [
      miss('UNIQUE constraints and PRIMARY KEYs are identical in every respect.', 'They differ on NULL handling and on how many of each a table may have.', 'There are concrete differences in NULL and multiplicity.'),
      miss('A table can have many primary keys but only one unique constraint.', 'It is the reverse: at most one primary key, but many unique constraints.', 'Multiplicity is flipped here.'),
      miss('UNIQUE constraints never permit NULL, just like primary keys.', 'Most databases allow at least one NULL in a UNIQUE column, unlike a primary key.', 'NULL handling differs between the two.'),
    ],
    lesson: 'Both enforce uniqueness, but a table can have only one PRIMARY KEY (NOT NULL, the row identifier) while it may have several UNIQUE constraints. In most SQL databases a UNIQUE column permits NULL (often one NULL), since NULLs are treated as distinct, which is why UNIQUE is not a substitute for a primary key.',
    source,
    generated: true,
  },
  {
    id: 7727044,
    chapter: 'Keys and Constraints',
    title: 'Foreign Key Role',
    prompt: 'What does a FOREIGN KEY constraint enforce?',
    correct: 'That each value in the referencing column matches an existing value in the referenced table key (referential integrity), or is NULL.',
    wrong: [
      miss('That the referencing column values are unique within their own table.', 'Uniqueness is enforced by UNIQUE/PRIMARY KEY, not by a foreign key; foreign-key columns may repeat.', 'Foreign keys check references, not uniqueness.'),
      miss('That the referenced table can never be modified once linked.', 'Referenced tables can still be modified, subject to referential actions like CASCADE or RESTRICT.', 'Foreign keys govern valid references, not table immutability.'),
      miss('That both tables are stored in the same physical file.', 'Foreign keys are logical integrity rules, independent of physical storage layout.', 'It is about valid values, not storage location.'),
    ],
    lesson: 'A FOREIGN KEY ensures referential integrity: every non-NULL value in the child column must correspond to an existing value in the parent table referenced column (typically its primary key). Referential actions such as ON DELETE CASCADE or RESTRICT then govern what happens when a referenced row is changed or removed.',
    source,
    generated: true,
  },
  {
    id: 7727045,
    chapter: 'Keys and Constraints',
    title: 'CHECK Constraint',
    prompt: 'What is the purpose of a CHECK constraint such as "CHECK (age >= 0)"?',
    correct: 'To reject any INSERT or UPDATE whose row would make the boolean condition false, enforcing a domain rule.',
    wrong: [
      miss('To automatically correct invalid ages to 0 when they are entered.', 'A CHECK constraint rejects bad data; it does not silently alter or correct values.', 'CHECK validates, it does not auto-fix.'),
      miss('To index the age column for faster range queries.', 'A CHECK is a validation rule, not an index; it does not accelerate queries.', 'Constraints and indexes serve different roles.'),
      miss('To set a default age of 0 when no value is provided.', 'Supplying a default is the job of a DEFAULT clause, not a CHECK constraint.', 'Defaults and checks are separate features.'),
    ],
    lesson: 'A CHECK constraint enforces a boolean predicate on each row, rejecting any INSERT or UPDATE that would violate it. It is used to keep data within valid domains (e.g., non-negative ages, valid status codes), and it does not modify, default, or index anything.',
    source,
    generated: true,
  },

  // ---------------- Chapter 11: Set Operations and Combining Results ----------------
  {
    id: 7727046,
    chapter: 'Set Operations and Combining Results',
    title: 'UNION versus UNION ALL',
    prompt: 'What is the difference between UNION and UNION ALL?',
    correct: 'UNION removes duplicate rows from the combined result; UNION ALL keeps all rows including duplicates and is typically faster.',
    wrong: [
      miss('UNION ALL removes duplicates while UNION keeps them.', 'It is the reverse: UNION deduplicates and UNION ALL retains duplicates.', 'The keyword ALL signals keeping everything, including duplicates.'),
      miss('UNION joins tables side by side, adding columns; UNION ALL stacks rows.', 'Both stack rows vertically and require matching column counts; neither adds columns like a join.', 'Set operators combine rows, not columns.'),
      miss('UNION sorts the result while UNION ALL leaves it unsorted, but both keep duplicates.', 'The defining difference is duplicate removal, not sorting; UNION removes duplicates.', 'Focus on duplicate handling, not sort order.'),
    ],
    lesson: 'Both UNION and UNION ALL stack the rows of two result sets that have compatible columns. UNION additionally performs a duplicate-elimination pass (like an implicit DISTINCT), which costs extra work. UNION ALL skips that pass, so it is faster and is preferred when you know duplicates are impossible or acceptable.',
    source,
    generated: true,
  },
  {
    id: 7727047,
    chapter: 'Set Operations and Combining Results',
    title: 'Set Operator Requirements',
    prompt: 'For "query1 UNION query2" to be valid, what must be true of the two queries?',
    correct: 'They must produce the same number of columns with compatible data types in the same order.',
    wrong: [
      miss('They must select from the same single table.', 'The two queries can come from entirely different tables as long as their column shapes are compatible.', 'Set operators care about result shape, not source tables.'),
      miss('They must have identical column names in both queries.', 'Column names need not match; only the count and type compatibility matter, and the first query names the result.', 'Names are not the constraint; arity and type are.'),
      miss('They must each return exactly one row.', 'Each side can return any number of rows; the constraint is on column structure, not row count.', 'Row counts are unrestricted for UNION.'),
    ],
    lesson: 'Set operators (UNION, INTERSECT, EXCEPT) require that the participating queries be union-compatible: the same number of columns, in the same positional order, with compatible data types. Column names may differ; the names of the first query usually label the combined output.',
    source,
    generated: true,
  },
  {
    id: 7727048,
    chapter: 'Set Operations and Combining Results',
    title: 'INTERSECT and EXCEPT',
    prompt: 'What does "query1 EXCEPT query2" return?',
    correct: 'Distinct rows that appear in query1 but not in query2.',
    wrong: [
      miss('Rows that appear in both query1 and query2.', 'That is INTERSECT; EXCEPT returns rows in the first set that are absent from the second.', 'EXCEPT is a set difference, not an intersection.'),
      miss('All rows from both queries combined, like UNION ALL.', 'EXCEPT subtracts the second set from the first; it does not concatenate them.', 'EXCEPT removes rows rather than combining all.'),
      miss('Rows in query2 that are missing from query1.', 'EXCEPT is directional: it keeps rows from the left query not found in the right, not the reverse.', 'Order matters: it is query1 minus query2.'),
    ],
    lesson: 'EXCEPT (called MINUS in some databases) returns the set difference: the distinct rows produced by the first query that do not appear in the second. INTERSECT returns rows common to both. Like UNION, these operators eliminate duplicates unless the ALL variant is used.',
    source,
    generated: true,
  },

  // ---------------- Chapter 12: Transactions and Data Integrity ----------------
  {
    id: 7727049,
    chapter: 'Transactions and Data Integrity',
    title: 'ACID Atomicity',
    prompt: 'In the ACID properties, what does Atomicity guarantee about a transaction?',
    correct: 'All of its statements either commit together or none take effect; a failure rolls the whole transaction back.',
    wrong: [
      miss('That concurrent transactions never see each other partial changes.', 'That describes Isolation, not Atomicity.', 'Atomicity is about all-or-nothing within one transaction.'),
      miss('That committed data survives a subsequent system crash.', 'That is Durability, the persistence guarantee, not Atomicity.', 'Surviving crashes is Durability.'),
      miss('That the database moves from one valid state to another satisfying all constraints.', 'That is Consistency, which is about constraint preservation, not all-or-nothing execution.', 'Constraint preservation is Consistency.'),
    ],
    lesson: 'Atomicity is the all-or-nothing property: a transaction is treated as a single indivisible unit, so either every operation within it is applied or, on error, none are (a rollback). The other ACID properties are Consistency, Isolation, and Durability, each guarding a different aspect of reliable transactions.',
    source,
    generated: true,
  },
  {
    id: 7727050,
    chapter: 'Transactions and Data Integrity',
    title: 'COMMIT and ROLLBACK',
    prompt: 'Within a transaction you run two UPDATEs and then issue ROLLBACK. What is the result?',
    correct: 'Both UPDATEs are undone and the data returns to its state before the transaction began.',
    wrong: [
      miss('Only the second UPDATE is undone; the first one is already permanent.', 'Until COMMIT, no statement in the transaction is permanent, so ROLLBACK undoes both.', 'Nothing is durable before COMMIT.'),
      miss('Both UPDATEs are made permanent because work was done.', 'ROLLBACK discards the work; only COMMIT makes changes permanent.', 'ROLLBACK is the opposite of COMMIT.'),
      miss('The ROLLBACK is ignored because UPDATE statements auto-commit individually.', 'Inside an explicit transaction, statements do not auto-commit; ROLLBACK reverts them.', 'Explicit transactions defer commit until you say so.'),
    ],
    lesson: 'Within an explicit transaction, changes are provisional until COMMIT makes them durable. Issuing ROLLBACK discards every change made since the transaction (or last savepoint) began, restoring the prior state. This is what makes atomic, all-or-nothing operations possible.',
    source,
    generated: true,
  },
  {
    id: 7727051,
    chapter: 'Transactions and Data Integrity',
    title: 'Dirty Read',
    prompt: 'A "dirty read" concurrency phenomenon occurs when a transaction:',
    correct: 'Reads data that another transaction has modified but not yet committed, which may later be rolled back.',
    wrong: [
      miss('Reads the same row twice and gets two different committed values.', 'That is a non-repeatable read, where the second read sees a different committed value, not uncommitted data.', 'A dirty read involves uncommitted, not committed, changes.'),
      miss('Sees new rows appear in a range on re-query within the same transaction.', 'That is a phantom read, about new rows matching a predicate, not reading uncommitted values.', 'Phantoms concern row membership, not uncommitted edits.'),
      miss('Cannot read any data because another transaction holds a lock.', 'That is a blocking/lock-wait situation, not a dirty read of uncommitted data.', 'A dirty read is about reading too much, not being blocked.'),
    ],
    lesson: 'A dirty read happens when one transaction reads uncommitted changes made by another transaction; if that other transaction rolls back, the first has acted on data that never officially existed. Higher isolation levels (READ COMMITTED and above) prevent dirty reads, while non-repeatable reads and phantoms require still stricter levels.',
    source,
    generated: true,
  },
])
