import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe softwareEngineeringRoadmap top-up'

export const softwareEngineeringRoadmapTopUpQuestions: Question[] = makeQuestionBank('Career Skills', [
  // ===== Chapter 1: Code, Review, and Quality =====
  {
    id: 7303000,
    chapter: 'Code, Review, and Quality',
    title: 'Coverage hit 90%, bugs unchanged',
    prompt: 'A team raised line coverage from 60% to 90% but production bug rates did not move. What is the most likely explanation?',
    correct: 'Coverage measures which lines run during tests, not whether the tests assert correct behavior',
    wrong: [
      miss('The coverage tool is miscounting and should be replaced', 'Coverage tools count executed lines reliably; the gap is that executed is not the same as verified.', 'Ask what an assertion-free test that touches a line actually proves about that line.'),
      miss('90% is too low; only 100% coverage prevents bugs', '100% coverage of weak assertions still proves nothing, and the last 10% is usually low-value glue.', 'Think about a test that runs every line but checks no output.'),
      miss('Bug rates always lag coverage by exactly one quarter', 'There is no fixed lag law; the issue is test quality, not timing.', 'Look for a mechanism that ties coverage to defects, not a calendar rule.'),
    ],
    lesson: 'Coverage tells you what code executed under test, not whether the test checked the right thing. A suite can hit 90% of lines while asserting almost nothing. Confidence comes from meaningful assertions on risky behavior, not a percentage.',
    source,
    generated: true,
  },
  {
    id: 7303001,
    chapter: 'Code, Review, and Quality',
    title: 'Which test level catches this cheapest',
    prompt: 'A bug is a wrong calculation inside one pure function. Which test level catches it most cheaply and reliably?',
    correct: 'A unit test exercising that function with known inputs and expected outputs',
    wrong: [
      miss('An end-to-end browser test that clicks through the whole app', 'E2E tests are slow, flaky, and far from the defect; using one to pin a pure-function bug is the test pyramid upside down.', 'Match the test scope to the defect scope: a one-function bug wants a one-function test.'),
      miss('A manual QA pass before every release', 'Manual checking of a deterministic calculation does not scale and re-introduces the bug risk on every change.', 'Prefer the cheapest automated check that fails exactly when this logic breaks.'),
      miss('A load test to see if it breaks under traffic', 'Load tests probe performance and concurrency, not the correctness of a single calculation.', 'Ask whether the bug is about speed or about a wrong answer.'),
    ],
    lesson: 'The test pyramid says: catch each bug at the lowest, cheapest level that can see it. A wrong calculation in a pure function is a textbook unit test. Reserve slow end-to-end tests for whole-system flows that nothing smaller can cover.',
    source,
    generated: true,
  },
  {
    id: 7303002,
    chapter: 'Code, Review, and Quality',
    title: 'Reviewer rubber-stamps in 30 seconds',
    prompt: 'A reviewer approves a 600-line PR within 30 seconds with the comment "LGTM." What is the core problem?',
    correct: 'A non-review provides false assurance; the change merged with no real risk check',
    wrong: [
      miss('Nothing — fast approvals show the team trusts each other', 'Trust is not the same as verification; the value of review is catching defects, which a 30-second pass on 600 lines cannot do.', 'Ask what defect this approval could realistically have caught.'),
      miss('The PR should have been even larger to justify the review time', 'Larger PRs make review worse, not better; the fix is smaller changes, not bigger ones.', 'Think about which direction PR size moves review quality.'),
      miss('The author should approve their own PRs to save time', 'Self-approval removes the second pair of eyes entirely, which is the whole point of review.', 'Consider who has the independence to spot the author’s blind spots.'),
    ],
    lesson: 'A "LGTM" that arrives faster than anyone could read the diff is theater, not review. It launders a risky change with the appearance of scrutiny. Real review takes time proportional to risk, which is also why large PRs are discouraged.',
    source,
    generated: true,
  },
  {
    id: 7303003,
    chapter: 'Code, Review, and Quality',
    title: 'Unit test that talks to the real database',
    prompt: 'A "unit" test in CI fails intermittently because it connects to a shared staging database other tests also write to. What is the cleanest fix?',
    correct: 'Isolate the test from shared state by mocking or using a dedicated ephemeral fixture so it is deterministic',
    wrong: [
      miss('Add a sleep before the test so the database settles', 'Sleeps hide the race without removing it; the shared-state collision can still happen and the suite gets slower.', 'Ask whether timing or shared mutable state is the actual cause.'),
      miss('Mark the test to always pass and revisit later', 'Forcing a pass deletes the signal; the test now lies about correctness instead of flaking honestly.', 'Think about what a test that cannot fail is worth.'),
      miss('Run the whole suite single-threaded forever', 'Serializing everything papers over one test’s isolation bug at the cost of CI speed for all tests.', 'Prefer fixing the one test’s dependency over slowing the entire pipeline.'),
    ],
    lesson: 'A test that depends on shared, mutable external state is not isolated and will flake. Determinism comes from controlling the test’s inputs: mock the dependency or give each run its own fresh fixture. Sleeps and forced passes hide the problem instead of fixing it.',
    source,
    generated: true,
  },
  {
    id: 7303004,
    chapter: 'Code, Review, and Quality',
    title: 'Review comment that names risk',
    prompt: 'You are reviewing a change to the password-reset flow. Which comment is the strongest review contribution?',
    correct: '"What happens if the reset token is reused or expired — is there a test for that path?"',
    wrong: [
      miss('"Please use single quotes here to match our style."', 'Style nits are real but low-stakes; on a security-sensitive flow, the high-value review attention should go to the risky behavior, not quoting.', 'Ask where the expensive mistakes hide in a password-reset change.'),
      miss('"Looks great, ship it!"', 'Unconditional praise on a security flow provides no risk signal and no verification of the dangerous paths.', 'Consider what a reset-token bug could cost versus what this comment checks.'),
      miss('"This file is getting long, maybe split it someday."', 'A vague future-tense suggestion neither blocks the risk nor creates an actionable change.', 'Prefer a comment that probes a concrete failure mode in this diff.'),
    ],
    lesson: 'Good review comments target risk: edge cases, security boundaries, and missing tests on the dangerous paths. Style preferences are best left to automated linters. On a password-reset flow, token reuse and expiry are exactly where defects become incidents.',
    source,
    generated: true,
  },
  {
    id: 7303005,
    chapter: 'Code, Review, and Quality',
    title: 'Refactor mixed into a feature PR',
    prompt: 'A PR adds a new feature and also reformats and restructures 40 unrelated files. Why do reviewers usually ask to split it?',
    correct: 'Mixing behavior changes with large mechanical changes hides the risky diff in the noise',
    wrong: [
      miss('Refactoring is never allowed in the same repository as features', 'Refactoring is encouraged; the issue is bundling it with behavior changes in one unreviewable diff, not refactoring itself.', 'Ask what makes the feature change hard to see, not whether refactors are banned.'),
      miss('Large PRs deploy faster, so reviewers want them slower', 'PR size does not control deploy speed, and the concern is reviewability, not pace.', 'Think about what a reviewer can and cannot verify in a 40-file diff.'),
      miss('Reformatting always breaks the build', 'Pure reformatting usually does not break the build; the problem is that it masks the behavioral change next to it.', 'Separate "does it compile" from "can a human spot the real change."'),
    ],
    lesson: 'A clean refactor and a behavior change are both fine; combined, they create a diff where the one line that matters drowns in a thousand that do not. Splitting them lets reviewers focus scrutiny on the change that can actually break something.',
    source,
    generated: true,
  },
  {
    id: 7303006,
    chapter: 'Code, Review, and Quality',
    title: 'Static analysis flags, none confirmed',
    prompt: 'A linter reports 200 warnings on a legacy module; the team has ignored them for a year. What is the most useful response?',
    correct: 'Triage them: fix or suppress with rationale, then fail the build only on the rules the team actually enforces',
    wrong: [
      miss('Disable the linter entirely so the noise stops', 'Turning it off removes a useful signal source along with the noise; the fix is curating which rules matter, not blinding the team.', 'Ask how to keep the valuable warnings while silencing the rest.'),
      miss('Fix all 200 immediately regardless of value', 'A blanket cleanup risks churn and regressions on stable legacy code for warnings that may not matter.', 'Weigh the cost of touching stable code against each warning’s real risk.'),
      miss('Leave them; warnings are not errors so they are harmless', 'A perpetually noisy warning list trains everyone to ignore all of them, including the one real bug among the 200.', 'Think about what 200 ignored warnings do to the next real warning.'),
    ],
    lesson: 'Unmanaged warnings become noise everyone learns to ignore, which is how a real defect hides in plain sight. The discipline is to curate the rule set, enforce what matters in CI, and explicitly suppress the rest with a reason.',
    source,
    generated: true,
  },

  // ===== Chapter 2: Architecture and Tradeoffs =====
  {
    id: 7303007,
    chapter: 'Architecture and Tradeoffs',
    title: 'CAP during a network partition',
    prompt: 'A distributed datastore experiences a network partition between regions. Per the CAP theorem, what choice must the system make?',
    correct: 'Between consistency and availability — it cannot guarantee both while the partition lasts',
    wrong: [
      miss('It can keep all three of consistency, availability, and partition tolerance', 'The whole point of CAP is that during a partition you cannot have both consistency and availability; partition tolerance is the condition, not a free third property.', 'Re-read which two of the three are actually in tension when the network splits.'),
      miss('It must sacrifice partition tolerance to stay consistent and available', 'You cannot opt out of partitions on a real network; partition tolerance is a given, so the tradeoff is the other two.', 'Ask whether a system can simply decide that network failures will not occur.'),
      miss('Latency, not consistency or availability, is the only thing affected', 'CAP is about consistency vs. availability under partition, not about latency; latency is a separate (PACELC) concern.', 'Distinguish "what happens during a partition" from "what happens normally."'),
    ],
    lesson: 'CAP says that when a network partition happens, a distributed system can be consistent or available, but not both, for the duration. Partition tolerance is not optional on a real network, so the real design choice is which of C or A you give up when things split.',
    source,
    generated: true,
  },
  {
    id: 7303008,
    chapter: 'Architecture and Tradeoffs',
    title: 'Synchronous chain of five services',
    prompt: 'A request flows synchronously through five services, each 99.9% available. Roughly what is the end-to-end availability, and what is the lesson?',
    correct: 'About 99.5% — chained synchronous dependencies multiply, so each added hop lowers reliability',
    wrong: [
      miss('Still 99.9%, because the weakest service sets the ceiling', 'Availability of a serial chain multiplies (0.999^5), it is not capped at one link; five hops compound the failure probability.', 'Multiply the per-service availabilities rather than taking the minimum.'),
      miss('99.99%, because more services add redundancy', 'These services are in series, not redundant copies; adding serial dependencies reduces availability, it does not add redundancy.', 'Ask whether the services back each other up or all must succeed.'),
      miss('Exactly 95%, because each hop loses 1%', 'Each hop loses about 0.1%, not 1%; 0.999^5 is roughly 0.995, i.e. ~99.5%.', 'Use the right per-hop loss: 99.9% available means 0.1% unavailable.'),
    ],
    lesson: 'In a synchronous chain, availabilities multiply: 0.999^5 ≈ 0.995, or about 99.5%. Every required hop lowers the whole. That is why teams reduce hard synchronous dependencies, add caching/async paths, or make degraded responses acceptable.',
    source,
    generated: true,
  },
  {
    id: 7303009,
    chapter: 'Architecture and Tradeoffs',
    title: 'Shared mutable database between teams',
    prompt: 'Two teams write to the same table with no agreed contract, and a schema change by one team silently breaks the other. What architectural concept best names this failure?',
    correct: 'Tight coupling through a shared data store, with no contract to protect either consumer',
    wrong: [
      miss('Healthy code reuse, since both teams use one source of truth', 'Sharing a table is not reuse of a contract; it is sharing implementation details, which is the coupling that caused the break.', 'Distinguish a stable interface both teams agreed on from a shared internal table.'),
      miss('A caching problem that a cache invalidation fixes', 'Nothing here is about stale cache; the break came from an unguarded schema change to shared state.', 'Ask whether the data was stale or whether the structure changed underneath a consumer.'),
      miss('High cohesion, which is always desirable', 'Cohesion is about a module doing one thing well; this is cross-team coupling, the opposite of a clean boundary.', 'Separate "one module focused" from "two teams entangled."'),
    ],
    lesson: 'When teams share a table with no contract, each becomes coupled to the other’s internal schema, and a change on one side breaks the other invisibly. Boundaries (an API or an event contract) exist precisely so a team can change its internals safely.',
    source,
    generated: true,
  },
  {
    id: 7303010,
    chapter: 'Architecture and Tradeoffs',
    title: 'What an ADR must capture',
    prompt: 'Your team adopts architecture decision records (ADRs). What is the single most valuable thing an ADR preserves that code and tickets usually do not?',
    correct: 'The context and tradeoffs that made the decision reasonable at the time',
    wrong: [
      miss('The exact line numbers the decision touched', 'Line numbers drift with every edit and capture none of the reasoning a future team needs to revisit the choice.', 'Ask what a future engineer needs to know to safely change the decision.'),
      miss('A guarantee that the decision can never be revisited', 'ADRs are explicitly revisable; recording context makes change easier, not forbidden.', 'Think about whether documenting a decision locks it or unlocks it.'),
      miss('The names of everyone who attended the meeting', 'Attendee lists are trivia; without the options and tradeoffs, the decision is still folklore.', 'Prefer the rationale over the roster when judging an ADR’s value.'),
    ],
    lesson: 'An ADR’s durable value is the why: the context, the options considered, the decision, and the tradeoffs accepted. Code shows what was built; an ADR lets a future team revisit the choice without rediscovering the constraints from scratch.',
    source,
    generated: true,
  },
  {
    id: 7303011,
    chapter: 'Architecture and Tradeoffs',
    title: 'Single point of failure in the design',
    prompt: 'A new design routes every request through one shared in-memory cache node with no fallback. What is the primary architectural risk?',
    correct: 'A single point of failure: if that node dies, every request that depends on it fails',
    wrong: [
      miss('Excessive redundancy that wastes money', 'One node with no fallback is the opposite of redundant; the risk is too little redundancy, not too much.', 'Count how many independent copies of this critical component exist.'),
      miss('Too much consistency, since one cache is always consistent', 'A single cache may be internally consistent, but consistency is not the failure here — total dependence on one node is.', 'Ask what happens to availability when that one node is gone.'),
      miss('Nothing, because caches are not load-bearing', 'When every request depends on the cache, it is load-bearing by definition; its failure is a system failure.', 'Trace what fails downstream when this "non-critical" component goes down.'),
    ],
    lesson: 'A single point of failure is any component whose failure takes down the whole system. Routing all traffic through one node with no fallback creates exactly that. Mitigations include replication, fallback to the source of truth, and graceful degradation.',
    source,
    generated: true,
  },
  {
    id: 7303012,
    chapter: 'Architecture and Tradeoffs',
    title: 'Premature microservices',
    prompt: 'A three-person startup with one product splits its small app into twelve microservices "to scale later." What is the most likely near-term consequence?',
    correct: 'Operational and coordination overhead that slows a small team without delivering real scaling benefit yet',
    wrong: [
      miss('Immediate and guaranteed performance gains', 'Splitting into services adds network hops and serialization; it does not automatically make a small app faster.', 'Ask whether the bottleneck was actually the monolith or the team’s capacity.'),
      miss('Lower complexity, since each service is small', 'Each service may be small, but the system now has twelve deploys, twelve failure modes, and inter-service contracts to manage.', 'Count total system complexity, not the size of one piece.'),
      miss('Reduced need for testing across boundaries', 'More boundaries mean more integration surfaces to test, not fewer.', 'Think about where new bugs appear when components talk over the network.'),
    ],
    lesson: 'Microservices trade in-process simplicity for operational and coordination cost: more deploys, more failure modes, more contracts. For a tiny team, that overhead usually outweighs scaling benefits they do not yet need. Architecture should match current constraints, not imagined ones.',
    source,
    generated: true,
  },
  {
    id: 7303013,
    chapter: 'Architecture and Tradeoffs',
    title: 'Latency vs throughput confusion',
    prompt: 'A stakeholder says "our system is high-throughput, so individual requests must be fast." Why is that not necessarily true?',
    correct: 'Throughput measures total requests handled per unit time; a single request can still be slow even when many run in parallel',
    wrong: [
      miss('It is true — throughput and latency are the same metric', 'They are distinct: latency is per-request time, throughput is volume over time, and a batch system can have high throughput with high per-request latency.', 'Define each metric separately before assuming one implies the other.'),
      miss('High throughput always means low latency by definition', 'A system can process huge volume by batching, which often raises per-request latency rather than lowering it.', 'Ask whether handling more total work guarantees each item is handled quickly.'),
      miss('Latency only matters for databases, not services', 'Latency matters anywhere a user or caller waits for a response, not only at the database layer.', 'Think about who is waiting on each individual request.'),
    ],
    lesson: 'Latency is how long one request takes; throughput is how many requests complete per unit time. A system can hit high throughput through parallelism or batching while any single request is slow. They are different dimensions that you tune separately.',
    source,
    generated: true,
  },

  // ===== Chapter 3: Releases and Migrations =====
  {
    id: 7303014,
    chapter: 'Releases and Migrations',
    title: 'Canary vs blue-green',
    prompt: 'You want to expose a risky change to 1% of real users first, then ramp to 100% while watching error rates. Which strategy is this, and how does it differ from blue-green?',
    correct: 'Canary: it routes a small, growing percentage of traffic to the new version, whereas blue-green swaps all traffic between two full environments at once',
    wrong: [
      miss('Blue-green: it sends 1% of users to the green environment first', 'Blue-green flips all traffic from blue to green at the cutover; it is not a gradual percentage ramp, which is what canary does.', 'Ask whether traffic moves in slices over time or all at once at the switch.'),
      miss('Rollback: gradually exposing users is a form of rollback', 'Rollback reverts to a previous version after a problem; gradually exposing a new version forward is a release strategy, not a revert.', 'Distinguish moving forward carefully from undoing a release.'),
      miss('Feature flag: canary and feature flags are identical', 'A feature flag toggles a code path, often per-user; canary is about traffic routing to a new deployment. They overlap but are not the same mechanism.', 'Separate "which deployment serves you" from "which code branch you hit."'),
    ],
    lesson: 'A canary deployment routes a small, increasing share of real traffic to the new version while you watch metrics, so blast radius stays tiny if it breaks. Blue-green keeps two full environments and switches all traffic at cutover (fast rollback, but the whole user base flips together).',
    source,
    generated: true,
  },
  {
    id: 7303015,
    chapter: 'Releases and Migrations',
    title: 'Semantic versioning of a breaking change',
    prompt: 'A library at version 2.4.1 removes a public function that callers depend on. Under semantic versioning, what should the next version be?',
    correct: '3.0.0 — a backward-incompatible change to the public API requires a MAJOR bump',
    wrong: [
      miss('2.5.0 — a new feature warrants a MINOR bump', 'MINOR (the middle number) is for backward-compatible additions; removing a public function is incompatible, which is MAJOR, not MINOR.', 'Ask whether existing callers can upgrade without changing their code.'),
      miss('2.4.2 — it is a small change, so bump PATCH', 'PATCH is for backward-compatible bug fixes; removing a public function breaks callers, so PATCH is wrong.', 'Map the smallest number to fixes that change nothing for callers.'),
      miss('2.4.1 stays — only the changelog needs updating', 'A code change that breaks the public API must change the version; leaving it at 2.4.1 lies to consumers about compatibility.', 'Think about how a consumer’s tooling would even detect the break.'),
    ],
    lesson: 'Semantic versioning (MAJOR.MINOR.PATCH): bump MAJOR for backward-incompatible API changes, MINOR for backward-compatible additions, PATCH for backward-compatible fixes. Removing a public function breaks callers, so 2.4.1 becomes 3.0.0.',
    source,
    generated: true,
  },
  {
    id: 7303016,
    chapter: 'Releases and Migrations',
    title: 'Expand/contract column rename',
    prompt: 'You need to rename a heavily-used database column with zero downtime. What is the safe expand/contract sequence?',
    correct: 'Add the new column, write to both, backfill, switch reads to the new column, then drop the old column once nothing uses it',
    wrong: [
      miss('Rename the column in one migration and deploy the code at the same time', 'A simultaneous rename plus deploy has no safe ordering: old code expects the old name during rollout, so requests fail in the gap.', 'Ask what the currently-running old code reads during the moment of the rename.'),
      miss('Drop the old column first, then add the new one and backfill', 'Dropping first destroys the data and breaks every running reader before the replacement exists.', 'Never remove the thing in use before its replacement is live and populated.'),
      miss('Keep both columns forever and let the app guess which to read', 'Leaving an ambiguous dual-source permanently is unfinished migration and a future correctness bug, not a plan.', 'A migration has an end state; ask what step retires the old column.'),
    ],
    lesson: 'Expand/contract makes schema changes reversible and online: expand by adding the new column and writing to both, backfill old data, migrate reads, then contract by dropping the old column once unused. Each step is independently safe and deployable.',
    source,
    generated: true,
  },
  {
    id: 7303017,
    chapter: 'Releases and Migrations',
    title: 'Feature flag left on forever',
    prompt: 'A feature flag added six months ago is now 100% on for everyone, but the code still branches on it everywhere. What is the right hygiene step?',
    correct: 'Remove the flag and the dead branch once the feature is fully launched and stable',
    wrong: [
      miss('Leave it; flags are free and might be useful someday', 'Stale flags accumulate as conditional complexity and untested dead branches, which is technical debt, not free optionality.', 'Ask what the never-taken branch costs in readability and test surface.'),
      miss('Set it to 50% to keep options open', 'Re-splitting traffic on a launched, stable feature reintroduces inconsistency for no reason and confuses users.', 'Think about whether there is any reason to send half of users to the old path now.'),
      miss('Rename the flag so it looks intentional', 'Renaming does not remove the dead branch or the complexity; it just disguises the debt.', 'Prefer deleting the unused path over relabeling it.'),
    ],
    lesson: 'Feature flags are temporary by design. Once a feature is fully rolled out and stable, the flag and its now-dead alternate branch should be removed. Long-lived flags pile up as conditional complexity and untested code paths.',
    source,
    generated: true,
  },
  {
    id: 7303018,
    chapter: 'Releases and Migrations',
    title: 'Which HTTP method is safe to retry',
    prompt: 'A client times out and wants to safely retry the request automatically. For which method is automatic retry safe by definition because it is idempotent?',
    correct: 'PUT — repeating it yields the same final state, so a retry after a timeout is safe',
    wrong: [
      miss('POST — it is the standard method for retries', 'POST is not idempotent: a retry can create a second resource (e.g., a duplicate order), which is exactly the danger of blind retries.', 'Ask whether sending the request twice could create two of something.'),
      miss('No method is idempotent over HTTP', 'HTTP defines GET, HEAD, PUT, and DELETE as idempotent; the claim that none are is simply incorrect.', 'Recall which methods produce the same end state when repeated.'),
      miss('Only GET, and never any method that writes data', 'PUT and DELETE write data yet are still idempotent because repeating them lands on the same final state.', 'Separate "reads only" (safe) from "same final state when repeated" (idempotent).'),
    ],
    lesson: 'Per HTTP semantics, GET, HEAD, PUT, and DELETE are idempotent: repeating them produces the same final state, so retries are safe. POST is not idempotent and a retry can duplicate work, which is why POST endpoints need idempotency keys to be retry-safe.',
    source,
    generated: true,
  },
  {
    id: 7303019,
    chapter: 'Releases and Migrations',
    title: 'Retry storm during a dependency blip',
    prompt: 'A downstream service has a brief slowdown; every client retries immediately and in lockstep, and the service collapses entirely. What is the standard mitigation?',
    correct: 'Exponential backoff with jitter, so retries spread out instead of arriving in synchronized waves',
    wrong: [
      miss('Retry faster and more often to push through the slowdown', 'Faster, more frequent retries add load to an already-struggling service, turning a blip into a full outage — the retry storm itself.', 'Ask whether piling on more requests helps or hurts a service that is already overloaded.'),
      miss('Remove all retries so clients never reconnect', 'Eliminating retries trades resilience for fragility; transient blips should be survivable, just not via synchronized hammering.', 'Keep retries but change their timing, rather than abandoning them.'),
      miss('Add a fixed 1-second delay to every retry', 'A fixed delay still synchronizes all clients to retry at the same instant; without randomness you recreate the thundering herd.', 'Think about why identical delays make every client fire at once.'),
    ],
    lesson: 'When many clients retry in sync, they create a thundering herd / retry storm that can keep a recovering service down. Exponential backoff increases the wait after each failure, and jitter randomizes it so clients stop firing in unison.',
    source,
    generated: true,
  },
  {
    id: 7303020,
    chapter: 'Releases and Migrations',
    title: 'Deprecating an API endpoint responsibly',
    prompt: 'You must retire a public API endpoint that external customers still call. What is the responsible deprecation path?',
    correct: 'Announce a timeline, document the replacement, monitor remaining callers, and remove only after the window with adequate notice',
    wrong: [
      miss('Delete it now; the new endpoint already exists', 'Removing it before consumers migrate breaks live integrations with no warning, regardless of the replacement existing.', 'Ask what happens to a customer still calling the old endpoint tomorrow morning.'),
      miss('Return a 200 with empty data so nothing "breaks"', 'Silently returning empty data is a worse failure than an error: customers get wrong results with no signal to fix anything.', 'Think about whether silence or a clear deprecation signal is safer for callers.'),
      miss('Leave it forever and never communicate the new one', 'Never retiring anything turns the API into permanent maintenance debt and hides the better replacement from users.', 'Balance not breaking callers against not maintaining dead surface forever.'),
    ],
    lesson: 'Responsible deprecation means giving consumers a clear timeline, a documented migration path, and monitoring to see who is still calling, before removal. APIs have consumers you cannot see, so silent removal or silent behavior changes are the worst options.',
    source,
    generated: true,
  },

  // ===== Chapter 4: Incidents and Reliability =====
  {
    id: 7303021,
    chapter: 'Incidents and Reliability',
    title: 'Error budget arithmetic',
    prompt: 'A service has a 99.9% availability SLO over a 30-day month. Roughly how much total downtime does its error budget allow?',
    correct: 'About 43 minutes — 0.1% of the month',
    wrong: [
      miss('About 7 hours — 1% of the month', 'That is the budget for a 99% SLO; a 99.9% SLO allows ten times less, around 43 minutes, not ~7 hours.', 'Use 0.1% of the month, not 1%, for a "three nines" target.'),
      miss('Zero downtime — 99.9% means it must never go down', '99.9% explicitly permits 0.1% unavailability; the error budget is exactly that allowed downtime, not zero.', 'Read the SLO as "allowed to be down 0.1% of the time," not "always up."'),
      miss('About 4 minutes — 0.01% of the month', 'That is the budget for a 99.99% ("four nines") SLO; 99.9% allows ten times more, about 43 minutes.', 'Match the number of nines to the right fraction: three nines is 0.1%.'),
    ],
    lesson: 'Error budget = 100% minus the SLO. For 99.9% over ~30 days (43,200 minutes), 0.1% is about 43 minutes of allowed downtime. The budget turns reliability into a number teams can spend, balancing release velocity against staying within target.',
    source,
    generated: true,
  },
  {
    id: 7303022,
    chapter: 'Incidents and Reliability',
    title: 'SLA vs SLO vs SLI',
    prompt: 'In reliability terms, which statement correctly distinguishes SLI, SLO, and SLA?',
    correct: 'An SLI is the measured value, an SLO is the internal target for it, and an SLA is the external contract with consequences if breached',
    wrong: [
      miss('They are three names for the same uptime percentage', 'They are distinct: a measurement, a target, and a contract; collapsing them loses the difference between an internal goal and a customer promise.', 'Separate what you measure, what you aim for, and what you promise a customer.'),
      miss('An SLA is the internal target and an SLO is the customer contract', 'It is reversed: the SLA is the external contract with penalties; the SLO is the internal target, usually stricter than the SLA.', 'Ask which one carries financial or contractual consequences — that is the SLA.'),
      miss('An SLI is the contract and an SLO is the raw measurement', 'The SLI is the raw measurement (e.g., success rate); the contract is the SLA, and the SLO is the target derived from SLIs.', 'Map "Indicator" to the metric you actually measure.'),
    ],
    lesson: 'SLI (Indicator) is the measured signal, like request success rate. SLO (Objective) is the internal target for that signal, e.g., 99.9%. SLA (Agreement) is the customer-facing contract with penalties. Teams usually set the SLO stricter than the SLA to leave headroom.',
    source,
    generated: true,
  },
  {
    id: 7303023,
    chapter: 'Incidents and Reliability',
    title: 'Alert on symptoms, not causes',
    prompt: 'On-call is paged constantly for "CPU above 70%" but most pages require no action. What is the better alerting principle?',
    correct: 'Alert on user-visible symptoms (errors, latency) tied to the SLO, not on internal causes that may be harmless',
    wrong: [
      miss('Lower the CPU threshold to 50% so problems are caught earlier', 'A lower threshold pages even more often for an even more harmless condition, worsening the alert fatigue.', 'Ask whether high CPU by itself is actually hurting users.'),
      miss('Page two on-call engineers for every CPU alert for safety', 'Doubling who gets woken for non-actionable pages multiplies fatigue without improving signal.', 'Fix what triggers the page before deciding how many people it wakes.'),
      miss('Disable alerting overnight to let people sleep', 'Blanket suppression risks missing a real user-impacting outage; the goal is fewer false pages, not no coverage.', 'Distinguish "stop the noise" from "stop watching for real failures."'),
    ],
    lesson: 'Good alerts fire on things that hurt users — elevated error rates or latency against the SLO — not on internal metrics like CPU that may be fine. Cause-based alerts create fatigue, and fatigue is how the one real page gets ignored.',
    source,
    generated: true,
  },
  {
    id: 7303024,
    chapter: 'Incidents and Reliability',
    title: 'The three pillars of observability',
    prompt: 'An on-call engineer can see that errors are up (a metric) but cannot tell which user request path caused them. Which observability signal most directly fills that gap?',
    correct: 'Distributed traces, which follow a single request across services to show where it failed',
    wrong: [
      miss('More dashboards of the same aggregate metrics', 'Aggregate metrics tell you something is wrong but not which request or path; adding more aggregate charts does not localize the failure.', 'Ask what signal follows one request end to end rather than summarizing all of them.'),
      miss('A louder alert threshold on the error metric', 'Tuning the alert changes when you are paged, not your ability to see the failing request path.', 'Separate "knowing it is broken" from "seeing where it broke."'),
      miss('Disabling logging to reduce noise', 'Removing logs deletes the most detailed per-event evidence; that makes diagnosis harder, not easier.', 'Think about which signal gives per-request detail, then keep it.'),
    ],
    lesson: 'Observability rests on three pillars: metrics (aggregate trends), logs (discrete events), and traces (one request’s journey across services). Metrics tell you something is wrong; traces and logs tell you where and why. Each answers a different question.',
    source,
    generated: true,
  },
  {
    id: 7303025,
    chapter: 'Incidents and Reliability',
    title: 'Mitigation before root cause',
    prompt: 'Error rates jumped sharply right after a deploy. The team is split between investigating the cause and rolling back. What is the right first move?',
    correct: 'Roll back to mitigate user impact now, then investigate the root cause with the system restored',
    wrong: [
      miss('Keep the deploy live and debug in production until the cause is found', 'Leaving the broken version up makes users absorb the cost of the investigation; mitigation should not wait on diagnosis.', 'Ask who is paying for the time spent finding root cause while the bad version serves traffic.'),
      miss('Push a forward fix immediately without testing it', 'An untested forward fix during an active incident can add a second failure on top of the first; rollback is the known-good state.', 'Prefer returning to a state you know worked over shipping an unverified change live.'),
      miss('Write the postmortem first so the cause is documented', 'A postmortem documents what happened after; it does not restore service and should not precede mitigation.', 'Restore first, explain second; ask what stops the user-facing bleeding now.'),
    ],
    lesson: 'When timing strongly implicates a release, rollback is often the fastest mitigation and returns the system to a known-good state. Restore service first, then investigate root cause. Forward fixes are tempting but risk compounding the incident.',
    source,
    generated: true,
  },
  {
    id: 7303026,
    chapter: 'Incidents and Reliability',
    title: 'Severity level judgment',
    prompt: 'Checkout is completely down for all users during peak shopping hours. How should this incident be classified, and why does it matter?',
    correct: 'High severity (e.g., SEV1) because of total user impact on a revenue-critical path, which sets response urgency and who is paged',
    wrong: [
      miss('Low severity, since no data was corrupted', 'Severity tracks user and business impact, not only data integrity; a fully-down revenue path is severe even with intact data.', 'Ask what the impact on users and revenue is, not just whether data is safe.'),
      miss('Severity does not matter as long as someone is looking at it', 'Severity drives escalation, staffing, and communication cadence; without it, the wrong people respond at the wrong urgency.', 'Think about what the SEV label actually triggers in the response process.'),
      miss('Medium severity, because checkout has a retry button', 'A retry button on a fully-down checkout does not restore the path; the impact is still total for users trying to buy.', 'Judge severity by whether users can actually complete the action, not by UI affordances.'),
    ],
    lesson: 'Severity levels classify incidents by user and business impact, and they drive who responds, how fast, and how often you communicate. Total loss of a revenue-critical path at peak is top severity. Right-sizing severity is what gets the right response moving.',
    source,
    generated: true,
  },
  {
    id: 7303027,
    chapter: 'Incidents and Reliability',
    title: 'Postmortem action items that stick',
    prompt: 'A blameless postmortem ends with a long list of vague suggestions like "be more careful" and "improve testing." Why is this weak, and what fixes it?',
    correct: 'Vague items never get done; each action needs a specific change, a named owner, and a due date',
    wrong: [
      miss('It is fine — listing intentions is the point of a postmortem', 'A postmortem exists to drive concrete change; intentions without owners and specifics produce no improvement and the incident recurs.', 'Ask what would actually change in the system because of this list.'),
      miss('The fix is to assign blame so someone feels responsible', 'Blameless postmortems deliberately avoid scapegoating; the failure here is vagueness, not a missing person to punish.', 'Separate "who do we blame" from "what concrete change prevents recurrence."'),
      miss('Add more suggestions so the list looks thorough', 'More vague items make the list longer, not more actionable; thoroughness is not the same as follow-through.', 'Prefer a few owned, dated, specific actions over many fuzzy ones.'),
    ],
    lesson: 'Blameless does not mean vague. A postmortem earns its keep through action items that are specific, owned by a named person, and dated. "Be more careful" changes nothing; "add a contract test for the payment webhook by Friday, owned by Priya" does.',
    source,
    generated: true,
  },

  // ===== Chapter 5: Technical Debt and Team Communication =====
  {
    id: 7303028,
    chapter: 'Technical Debt and Team Communication',
    title: 'Bus factor of one',
    prompt: 'Only one engineer understands the billing pipeline; nothing is documented. Leadership asks what the risk is called and how to reduce it. Best answer?',
    correct: 'A bus factor of one — reduce it by documenting, pairing, and spreading knowledge so the system survives that person being unavailable',
    wrong: [
      miss('It is job security for that engineer and not a team problem', 'Concentrating critical knowledge in one person is an organizational risk: vacations, illness, or departure can stall billing entirely.', 'Ask what happens to the system the week that person is unreachable.'),
      miss('A scalability problem solved by adding more servers', 'This is a human knowledge concentration risk; more servers do nothing to spread understanding of the pipeline.', 'Distinguish a capacity problem from a knowledge-distribution problem.'),
      miss('A latency problem in the billing path', 'Nothing here concerns request timing; the risk is that one person is the only one who understands the system.', 'Separate how fast the pipeline runs from who can maintain it.'),
    ],
    lesson: 'Bus factor is how many people would need to be hit by a bus before a system becomes unmaintainable. A bus factor of one is a serious operational risk. You raise it by documenting, pairing, runbooks, and deliberately rotating who touches critical systems.',
    source,
    generated: true,
  },
  {
    id: 7303029,
    chapter: 'Technical Debt and Team Communication',
    title: 'Framing debt for a non-technical leader',
    prompt: 'You want a quarter of cleanup time for a fragile legacy module. Which pitch is most likely to win prioritization from a product leader?',
    correct: '"This module causes one customer-facing outage a month and slows every related feature by weeks; cleanup cuts both."',
    wrong: [
      miss('"The code is ugly and uses an old pattern none of us like."', 'Aesthetics and team preference are not business consequences; leaders allocate against impact, not taste.', 'Translate the pain into outcomes the leader already cares about: outages, speed, cost.'),
      miss('"The library is five years old, so it must be replaced."', 'Age alone does not establish risk; many old libraries are stable. Without a consequence, "old" is not a case.', 'Ask what concrete harm the current state causes, beyond its age.'),
      miss('"Trust us, engineers know this needs doing."', 'An appeal to authority gives the leader nothing to weigh against other priorities competing for the same quarter.', 'Give the decision-maker a comparable cost/benefit, not a request to defer to you.'),
    ],
    lesson: 'Technical debt wins priority when it is framed as business consequence: outages, slowed delivery, security exposure, or cost. "Ugly" and "old" are not cases. Quantify the carrying cost and the upside of paying it down in the leader’s terms.',
    source,
    generated: true,
  },
  {
    id: 7303030,
    chapter: 'Technical Debt and Team Communication',
    title: 'Breaking a design deadlock',
    prompt: 'Two senior engineers have argued in PR comments for a week with no resolution and the project is blocked. What unblocks it?',
    correct: 'Agree on explicit decision criteria, weigh the tradeoffs against them, and name one accountable owner to decide; then document it',
    wrong: [
      miss('Let the more senior engineer win automatically', 'Seniority is not a decision criterion; defaulting to rank ignores the actual tradeoffs and breeds resentment without finding the better design.', 'Ask what objective criteria should decide, not whose title is bigger.'),
      miss('Merge both proposed designs to satisfy everyone', 'A blended design that satisfies neither architecture usually combines the downsides of both and pleases no one.', 'Consider whether two conflicting designs can coexist coherently or just create a worse third one.'),
      miss('Keep debating, since thorough discussion is always good', 'Past a point, debate becomes the most expensive option: the project burns a week with no direction and the cost compounds.', 'Recognize when more discussion stops adding information and starts costing delivery.'),
    ],
    lesson: 'Deadlocks break when the team makes the decision process explicit: agree on what criteria matter, compare options against them, and give one person the authority to decide. Then record the decision so it stays decided. "Disagree and commit" beats endless threads.',
    source,
    generated: true,
  },
  {
    id: 7303031,
    chapter: 'Technical Debt and Team Communication',
    title: 'Too much work in progress',
    prompt: 'A six-person team has fourteen tickets "in progress" and ships almost nothing each sprint. Which intervention most directly improves delivery?',
    correct: 'Set a WIP limit and finish the highest-value items before starting new ones',
    wrong: [
      miss('Assign more tickets so everyone always has plenty to do', 'More parallel work increases context-switching, which is the cause of the low completion rate, not a cure for it.', 'Ask whether the team’s problem is too little work or too much half-finished work.'),
      miss('Mark every ticket as highest priority', 'If everything is top priority, nothing is; the team still chooses by preference and the bottleneck remains.', 'Think about whether universal priority actually sequences the work.'),
      miss('Measure success by how many tickets are started each sprint', 'Optimizing for starts rewards the exact behavior causing the problem; the goal is finished, shipped work.', 'Distinguish the input (tickets opened) from the output (value delivered).'),
    ],
    lesson: 'High work-in-progress fragments attention and stalls throughput: everything is started, nothing finishes. Limiting WIP forces the team to complete and ship before opening new fronts, which exposes priorities and increases real delivery.',
    source,
    generated: true,
  },
])
