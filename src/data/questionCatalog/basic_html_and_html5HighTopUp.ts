import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe basic_html_and_html5 high top-up'

export const basic_html_and_html5HighTopUpQuestions: Question[] = makeQuestionBank('AP', [
  // ---------- Chapter 1: How the Web Works ----------
  {
    id: 7576000,
    chapter: 'How the Web Works',
    title: 'Client and server roles',
    prompt: 'You open a web page in a browser. In the request-response cycle, which device sends the HTTP request and which sends the response?',
    correct: 'The browser (client) sends the request; the server sends the response.',
    wrong: [
      miss('The server sends the request; the browser sends the response.', 'This reverses the roles. The browser initiates by asking for a resource; the server answers.', 'Think about who acts first: the user clicks, the browser asks, the server replies.'),
      miss('The browser both sends the request and sends the response to itself.', 'A page request involves a remote server; the browser does not generate the response content on its own.', 'Two machines are involved over the network — name the one that hosts the files.'),
      miss('The DNS server sends both the request and the response.', 'DNS only translates a domain name into an IP address; it does not deliver the page content.', 'DNS is a lookup step, not the source of the HTML.'),
    ],
    lesson: 'In the request-response cycle the client (browser) sends an HTTP request for a resource, and the web server returns an HTTP response. DNS is a separate lookup that maps the domain name to an IP address before the request is sent.',
    source,
    generated: true,
  },
  {
    id: 7576001,
    chapter: 'How the Web Works',
    title: 'HTTP 404 status code',
    prompt: 'A browser requests a page and the server replies with status code 404. What does this mean?',
    correct: 'The requested resource was not found on the server.',
    wrong: [
      miss('The page loaded successfully.', 'A successful request returns a 200 status, not 404. 404 signals failure to find the resource.', 'Recall that 200 means OK; 404 is the famous "missing page" code.'),
      miss('The server is permanently moved to a new address.', 'A permanent move is signalled by 301, not 404.', 'Redirect codes are in the 3xx range; 404 is a 4xx client error.'),
      miss('The user is not allowed to access the resource.', 'Forbidden access is 403; "not found" (404) means the resource does not exist at that path.', 'Distinguish "you can\'t" (403) from "it isn\'t there" (404).'),
    ],
    lesson: 'HTTP status codes report the outcome of a request. 2xx means success (200 OK), 3xx means redirection (301 moved), 4xx means a client error (404 Not Found, 403 Forbidden), and 5xx means a server error.',
    source,
    generated: true,
  },
  {
    id: 7576002,
    chapter: 'How the Web Works',
    title: 'What DNS does',
    prompt: 'Before your browser can connect to example.com, what does the Domain Name System (DNS) provide?',
    correct: 'The IP address that the domain name points to.',
    wrong: [
      miss('The HTML content of the home page.', 'DNS only resolves names to addresses; the web server delivers the HTML afterward.', 'DNS is a phone book, not the page itself.'),
      miss('The CSS stylesheet for the site.', 'Stylesheets are fetched from the web server after the connection is made, not from DNS.', 'DNS returns an address, not site files.'),
      miss('The encryption certificate for HTTPS.', 'TLS certificates come from the server during the secure handshake, not from DNS.', 'DNS maps name to address; certificates are exchanged later.'),
    ],
    lesson: 'DNS works like a phone book for the internet: it translates a human-readable domain name (example.com) into the numeric IP address of the server. Only after the address is known can the browser open a connection and request the page.',
    source,
    generated: true,
  },
  {
    id: 7576003,
    chapter: 'How the Web Works',
    title: 'Reading the Network panel',
    prompt: 'In browser DevTools, which panel would you open to see every file the page requested, each file\'s status code, and how long each took to download?',
    correct: 'The Network panel',
    wrong: [
      miss('The Elements panel', 'The Elements panel shows the live DOM and applied CSS, not the list of network requests and timings.', 'You want requests and timings, not the rendered tree.'),
      miss('The Console panel', 'The Console shows logged messages and errors, not a per-request breakdown of downloads.', 'Logs live here; request waterfalls live elsewhere.'),
      miss('The Sources panel', 'Sources lets you view and debug script files, but it does not list all requests with status and timing.', 'Think about which panel literally tracks the network.'),
    ],
    lesson: 'The Network panel in DevTools lists every request the page makes (HTML, CSS, JS, images), with each one\'s status code, size, and load time. It is the first place to look when an asset fails to load or the page is slow.',
    source,
    generated: true,
  },
  {
    id: 7576004,
    chapter: 'How the Web Works',
    title: 'HTTPS versus HTTP',
    prompt: 'What is the main practical difference between a page served over HTTPS and the same page served over HTTP?',
    correct: 'HTTPS encrypts the data in transit so it cannot be easily read or tampered with.',
    wrong: [
      miss('HTTPS makes the page load faster than HTTP in all cases.', 'HTTPS adds a security layer; speed depends on many factors and is not its defining purpose.', 'The extra "S" stands for security, not speed.'),
      miss('HTTPS changes HTML into a different programming language.', 'Both serve the same HTML; HTTPS only secures the transport, it does not change the content language.', 'The protocol wraps the same content; the HTML is unchanged.'),
      miss('HTTPS removes the need for a server entirely.', 'A server is still required; HTTPS just secures the connection to that server.', 'You still need something to host and answer requests.'),
    ],
    lesson: 'HTTPS is HTTP layered over TLS encryption. It protects data exchanged between browser and server from eavesdropping and tampering, and is required for many modern browser features. The page content (HTML/CSS/JS) is the same either way.',
    source,
    generated: true,
  },
  // ---------- Chapter 2: HTML Document Structure ----------
  {
    id: 7576005,
    chapter: 'HTML Document Structure',
    title: 'Purpose of the doctype',
    prompt: 'Why should an HTML document begin with <!DOCTYPE html>?',
    correct: 'It tells the browser to render the page using standards mode.',
    wrong: [
      miss('It imports the CSS stylesheet for the page.', 'Stylesheets are linked with <link rel="stylesheet">; the doctype does not load CSS.', 'The doctype is a rendering instruction, not a resource import.'),
      miss('It is a comment that the browser ignores.', 'It is a required declaration, not a comment, and it affects how the browser renders.', 'It changes behavior, so it cannot be merely ignored.'),
      miss('It defines the page title shown in the browser tab.', 'The tab title comes from the <title> element, not the doctype.', 'Confusing the doctype with <title> — they do different jobs.'),
    ],
    lesson: 'The <!DOCTYPE html> declaration must be the first line of an HTML document. It switches the browser into standards mode so the page follows modern, predictable rendering rules instead of legacy "quirks mode".',
    source,
    generated: true,
  },
  {
    id: 7576006,
    chapter: 'HTML Document Structure',
    title: 'The viewport meta tag',
    prompt: 'Which element in the <head> tells mobile browsers to size the page to the device width so a responsive layout displays correctly?',
    correct: '<meta name="viewport" content="width=device-width, initial-scale=1">',
    wrong: [
      miss('<meta charset="utf-8">', 'The charset meta declares the character encoding; it does not control viewport scaling.', 'You want the tag that mentions device width, not encoding.'),
      miss('<link rel="viewport" href="mobile.css">', 'There is no rel="viewport"; viewport behavior is set with a <meta> tag, not a stylesheet link.', 'Viewport is a meta directive, not a linked file.'),
      miss('<title>width=device-width</title>', 'The title sets the tab text; it cannot configure how the page scales on phones.', 'Title is for the tab, not for layout scaling.'),
    ],
    lesson: 'The viewport meta tag, <meta name="viewport" content="width=device-width, initial-scale=1">, tells mobile browsers to match the layout width to the device width instead of pretending to be a wide desktop. Without it, responsive CSS often appears zoomed out on phones.',
    source,
    generated: true,
  },
  {
    id: 7576007,
    chapter: 'HTML Document Structure',
    title: 'Declaring page language',
    prompt: 'To help screen readers and translation tools, which attribute should be added to the <html> element of an English page?',
    correct: 'lang="en"',
    wrong: [
      miss('charset="en"', 'charset declares character encoding (like utf-8) and belongs on a <meta> tag, not language.', 'Language and character encoding are different settings.'),
      miss('dir="en"', 'The dir attribute sets text direction (ltr or rtl), not which human language the page uses.', 'dir controls direction; language uses a different attribute.'),
      miss('type="en"', 'There is no type attribute for language on <html>; the correct attribute is lang.', 'Look for the attribute literally named for language.'),
    ],
    lesson: 'The lang attribute on <html> (for example lang="en") declares the document\'s primary language. Screen readers use it to choose the correct pronunciation, and tools use it to offer translation and proper hyphenation.',
    source,
    generated: true,
  },
  {
    id: 7576008,
    chapter: 'HTML Document Structure',
    title: 'Character encoding meta',
    prompt: 'Which element ensures that special characters like é and emoji render correctly across browsers?',
    correct: '<meta charset="utf-8">',
    wrong: [
      miss('<meta name="keywords" content="utf-8">', 'The keywords meta is for (largely deprecated) search metadata, not character encoding.', 'You want the charset meta, not a keywords meta.'),
      miss('<encoding>utf-8</encoding>', 'There is no <encoding> element in HTML; encoding is declared with a <meta charset> tag.', 'Encoding is an attribute on meta, not its own element.'),
      miss('<style charset="utf-8"></style>', 'A <style> block contains CSS rules; it does not set the document\'s character encoding.', 'Encoding is a head meta directive, not CSS.'),
    ],
    lesson: 'Placing <meta charset="utf-8"> early in the <head> declares UTF-8 encoding, which can represent virtually all characters and emoji. Without a correct charset declaration, accented letters and symbols may display as garbled "mojibake".',
    source,
    generated: true,
  },
  // ---------- Chapter 3: Semantic Content ----------
  {
    id: 7576009,
    chapter: 'Semantic Content',
    title: 'Ordered versus unordered lists',
    prompt: 'You are marking up the numbered steps of a recipe where the order matters. Which list element is most appropriate?',
    correct: '<ol> with <li> items',
    wrong: [
      miss('<ul> with <li> items', '<ul> is an unordered (bulleted) list, which signals that sequence does not matter. Recipe steps are sequential.', 'Steps that must happen in order call for an ordered list.'),
      miss('A series of <p> elements', 'Paragraphs do not convey that the items form a numbered, ordered list to assistive technology.', 'You lose the list and the numbering meaning with paragraphs.'),
      miss('A <table> with one row per step', 'A table implies relationships between rows and columns of data, not a simple ordered sequence of steps.', 'Reserve tables for genuine tabular data, not step lists.'),
    ],
    lesson: 'Use <ol> when the sequence is meaningful (steps, rankings) and <ul> when order does not matter. Both use <li> for each item. Choosing the right list element communicates structure to screen readers and other tools.',
    source,
    generated: true,
  },
  {
    id: 7576010,
    chapter: 'Semantic Content',
    title: 'Decorative image alt text',
    prompt: 'A purely decorative divider image adds no information to the page. What is the recommended alt attribute?',
    correct: 'alt="" (an empty alt value)',
    wrong: [
      miss('Omit the alt attribute entirely.', 'A missing alt makes some screen readers announce the file name; an explicit empty alt tells them to skip it.', 'Empty is intentional and skippable; missing is ambiguous.'),
      miss('alt="decorative divider image"', 'Describing a decoration adds noise for screen reader users without giving useful information.', 'If it carries no meaning, do not narrate it.'),
      miss('alt="image"', 'The word "image" is redundant and unhelpful; it tells the user nothing about content or purpose.', 'Generic words like "image" add clutter, not meaning.'),
    ],
    lesson: 'For images that are purely decorative, use an explicit empty alt (alt="") so assistive technology skips them entirely. Reserve descriptive alt text for images that convey information the user would otherwise miss.',
    source,
    generated: true,
  },
  {
    id: 7576011,
    chapter: 'Semantic Content',
    title: 'When a table is correct',
    prompt: 'You need to present each month next to its average rainfall. Which markup is the most semantically correct?',
    correct: 'A <table> with header cells (<th>) for the columns and a row per month',
    wrong: [
      miss('Two side-by-side <ul> lists, one for months and one for rainfall', 'Separate lists break the row relationship between a month and its value, which a table preserves.', 'Paired data across columns is exactly what tables are for.'),
      miss('A grid of <div> elements styled with CSS', 'CSS-styled divs may look like a table but expose no row/column relationships to assistive technology.', 'Visual grid is not the same as semantic table data.'),
      miss('A series of <p> elements like "January: 80mm"', 'Paragraphs lose the tabular structure, making it hard to compare values by column.', 'Genuine row-and-column data deserves a real table.'),
    ],
    lesson: 'Tables are the correct semantic choice for genuine tabular data, where each row and column has meaning. Use <th> for header cells so screen readers can announce which header a data cell belongs to. Do not use tables purely for page layout.',
    source,
    generated: true,
  },
  {
    id: 7576012,
    chapter: 'Semantic Content',
    title: 'Linking to another page',
    prompt: 'Which element and attribute create a clickable link to https://example.com?',
    correct: '<a href="https://example.com">Visit example</a>',
    wrong: [
      miss('<link href="https://example.com">Visit example</link>', 'The <link> element is for relationships in the <head> (like stylesheets); it does not create an in-page clickable link.', 'In-page links use the anchor element, not <link>.'),
      miss('<a src="https://example.com">Visit example</a>', 'Anchors use href, not src; src is for embedded resources like images and scripts.', 'Anchors point with href; src is for media.'),
      miss('<button href="https://example.com">Visit example</button>', 'A <button> triggers actions and does not navigate via href; use an anchor for navigation.', 'Links navigate (a); buttons act (button).'),
    ],
    lesson: 'The anchor element <a> with an href attribute creates a hyperlink. Use links (with href) for navigation to other pages or sections, and reserve <button> for in-page actions. The visible text between the tags should describe the destination.',
    source,
    generated: true,
  },
  {
    id: 7576013,
    chapter: 'Semantic Content',
    title: 'Emphasis versus bold styling',
    prompt: 'You want a word to be read with vocal stress by a screen reader, not just appear visually heavier. Which element best conveys that meaning?',
    correct: '<em>',
    wrong: [
      miss('<b>', '<b> draws visual attention (bold) without conveying added emphasis or stress in meaning.', 'You want meaning (stress), not just visual weight.'),
      miss('<i>', '<i> renders italics for things like terms or names but does not signal vocal emphasis.', 'Italic styling alone is not semantic emphasis.'),
      miss('<strong>', '<strong> marks strong importance, not the spoken stress that <em> represents.', 'Importance and vocal stress are different semantics.'),
    ],
    lesson: 'Use <em> for emphasis that changes the meaning of a sentence (screen readers may alter intonation) and <strong> for strong importance. <b> and <i> are purely presentational and carry no extra meaning.',
    source,
    generated: true,
  },
  // ---------- Chapter 4: CSS Basics and Layout ----------
  {
    id: 7576014,
    chapter: 'CSS Basics and Layout',
    title: 'Rendered width with content-box',
    prompt: 'A div has width: 200px; padding: 20px; border: 5px solid black; and the default box-sizing. What is its total rendered width on screen?',
    correct: '250px',
    wrong: [
      miss('200px', 'Under the default content-box, padding and border are added to the declared width, so it is more than 200px.', 'Default box-sizing adds padding and border on top of width.'),
      miss('225px', 'This counts padding on one side only; padding (20px) applies on both left and right, and border on both sides too.', 'Remember both sides: 20+20 padding and 5+5 border.'),
      miss('230px', 'This adds both paddings but only one border; the 5px border applies to both the left and right edges.', 'Border counts twice as well — left and right.'),
    ],
    lesson: 'With the default content-box, total width = width + left/right padding + left/right border. Here 200 + 20 + 20 + 5 + 5 = 250px. Setting box-sizing: border-box would instead keep the rendered width at 200px and shrink the content area.',
    source,
    generated: true,
  },
  {
    id: 7576015,
    chapter: 'CSS Basics and Layout',
    title: 'What border-box does',
    prompt: 'You set box-sizing: border-box on an element with width: 300px, padding: 10px, and a 2px border. What is the rendered width?',
    correct: '300px (padding and border are included inside the 300px)',
    wrong: [
      miss('324px (padding and border are added on top)', 'Adding padding and border on top is content-box behavior; border-box includes them inside the declared width.', 'border-box pulls padding and border inward, not outward.'),
      miss('322px', 'This adds only one border edge on top of 320; under border-box nothing is added at all.', 'With border-box the total stays exactly the declared width.'),
      miss('280px', 'border-box keeps the box at the declared 300px; it shrinks the content area, not the box.', 'The outer box stays 300px; only the content area inside shrinks.'),
    ],
    lesson: 'box-sizing: border-box makes the declared width include padding and border, so the rendered box stays exactly the width you set and the content area shrinks to absorb them. Many developers apply it globally to make layouts more predictable.',
    source,
    generated: true,
  },
  {
    id: 7576016,
    chapter: 'CSS Basics and Layout',
    title: 'Flexbox main-axis alignment',
    prompt: 'In a flex container with the default flex-direction: row, which property distributes the items along the horizontal (main) axis?',
    correct: 'justify-content',
    wrong: [
      miss('align-items', 'align-items controls alignment on the cross axis (vertical when the direction is row), not the main axis.', 'align-items handles the perpendicular axis, not the main one.'),
      miss('text-align', 'text-align aligns inline text inside a box; it does not position flex items along the main axis.', 'That is for text inside an element, not flex item layout.'),
      miss('flex-wrap', 'flex-wrap decides whether items move to new lines, not how they are distributed along the main axis.', 'Wrapping is about line breaks, not spacing along the axis.'),
    ],
    lesson: 'In flexbox, justify-content aligns and distributes items along the main axis (horizontal when flex-direction is row), while align-items aligns them along the cross axis (vertical in that case). Switching flex-direction to column swaps which axis is which.',
    source,
    generated: true,
  },
  {
    id: 7576017,
    chapter: 'CSS Basics and Layout',
    title: 'Flexbox cross-axis alignment',
    prompt: 'With flex-direction: row, which property vertically centers the items within the flex container?',
    correct: 'align-items: center',
    wrong: [
      miss('justify-content: center', 'justify-content centers along the main (horizontal) axis when direction is row, not vertically.', 'That centers left-to-right, not top-to-bottom.'),
      miss('vertical-align: middle', 'vertical-align affects inline and table-cell content, not flex items inside a flex container.', 'vertical-align does not control flex item positioning.'),
      miss('margin: auto top', 'There is no "auto top" margin value; vertical centering of flex items is done with align-items.', 'Use the flex cross-axis property, not an invalid margin.'),
    ],
    lesson: 'When flex-direction is row, the cross axis is vertical, so align-items: center centers items top-to-bottom. Combining justify-content: center with align-items: center centers items both horizontally and vertically.',
    source,
    generated: true,
  },
  {
    id: 7576018,
    chapter: 'CSS Basics and Layout',
    title: 'CSS specificity contest',
    prompt: 'A paragraph is targeted by both an element rule (p { color: blue }) and a class rule (.note { color: green }). The paragraph has class="note". What color is the text?',
    correct: 'Green, because a class selector is more specific than an element selector.',
    wrong: [
      miss('Blue, because the element rule is written for paragraphs specifically.', 'A class selector outranks an element selector in specificity, so the class rule wins.', 'Specificity, not "how specific it sounds", decides the winner.'),
      miss('Blue, because element rules always override class rules.', 'This is backwards; classes have higher specificity than element selectors.', 'Recall the specificity order: id > class > element.'),
      miss('Whichever rule appears last in the file, regardless of selector type.', 'Source order only breaks ties between rules of equal specificity; here specificity differs.', 'Source order is the tiebreaker, not the first test.'),
    ],
    lesson: 'CSS resolves conflicts by specificity first: inline styles beat IDs, IDs beat classes, classes beat element selectors. Only when specificity is equal does source order (later wins) decide. The class .note therefore overrides the bare p rule.',
    source,
    generated: true,
  },
  {
    id: 7576019,
    chapter: 'CSS Basics and Layout',
    title: 'Linking an external stylesheet',
    prompt: 'Which line, placed in the <head>, correctly links an external stylesheet named styles.css?',
    correct: '<link rel="stylesheet" href="styles.css">',
    wrong: [
      miss('<style src="styles.css"></style>', 'The <style> element holds inline CSS rules and has no src attribute; external files are linked with <link>.', 'External CSS uses <link>, not a <style> with src.'),
      miss('<script rel="stylesheet" href="styles.css"></script>', '<script> is for JavaScript; CSS is connected with a <link> element, not a script tag.', 'Scripts load JS; link loads stylesheets.'),
      miss('<css href="styles.css">', 'There is no <css> element in HTML; stylesheets are linked with <link rel="stylesheet">.', 'No such tag — use the standard link element.'),
    ],
    lesson: 'External stylesheets are attached with <link rel="stylesheet" href="styles.css"> inside the <head>. The <style> element is for CSS written directly in the page, and <script> is only for JavaScript.',
    source,
    generated: true,
  },
  {
    id: 7576020,
    chapter: 'CSS Basics and Layout',
    title: 'Responsive units for type',
    prompt: 'Which length unit scales with the user\'s base font size, making text easier to resize for readability?',
    correct: 'rem',
    wrong: [
      miss('px', 'Pixels are fixed and do not scale with the user\'s preferred base font size.', 'You want a unit relative to font size, not an absolute one.'),
      miss('vw', 'vw is relative to viewport width, not to the base font size, so it can produce extreme sizes.', 'Viewport-relative is not the same as font-relative.'),
      miss('deg', 'deg is an angle unit used for rotation and gradients, not for sizing text.', 'Degrees measure angles, not lengths.'),
    ],
    lesson: 'rem is relative to the root element\'s font size, so type sized in rem scales when users change their base font preference. px is fixed; vw is relative to viewport width. Using rem for type supports accessibility and resizing.',
    source,
    generated: true,
  },
  // ---------- Chapter 5: Forms and User Input ----------
  {
    id: 7576021,
    chapter: 'Forms and User Input',
    title: 'Email input type',
    prompt: 'You want the browser to check that a field contains a properly formatted email address before the form submits. Which input type should you use?',
    correct: '<input type="email">',
    wrong: [
      miss('<input type="text">', 'A text input accepts any characters and performs no built-in email format check.', 'Plain text does no validation; pick the type named for email.'),
      miss('<input type="mail">', 'There is no "mail" input type; the correct value is "email".', 'Close, but the standard type value is email, not mail.'),
      miss('<input type="address">', 'There is no "address" input type for emails; use type="email".', 'No such type — the email type is the one with validation.'),
    ],
    lesson: 'type="email" prompts the browser to validate that the value is a correctly formatted email address and shows an appropriate keyboard on mobile. Remember that client-side validation is a convenience, not security: always validate on the server too.',
    source,
    generated: true,
  },
  {
    id: 7576022,
    chapter: 'Forms and User Input',
    title: 'Marking a field required',
    prompt: 'Which attribute on an <input> prevents the form from submitting until the field is filled in?',
    correct: 'required',
    wrong: [
      miss('mandatory', 'There is no "mandatory" attribute in HTML; the standard attribute is required.', 'The HTML keyword is required, not mandatory.'),
      miss('validate', 'There is no "validate" attribute; native validation is triggered by attributes like required and type.', 'Validation comes from required and type, not a validate flag.'),
      miss('placeholder', 'placeholder shows hint text inside the field but does not make it required.', 'Placeholder is a hint, not a constraint.'),
    ],
    lesson: 'The boolean required attribute makes a field mandatory: the browser blocks submission and shows a message if it is empty. Combine it with the right input type (such as email) for richer native validation.',
    source,
    generated: true,
  },
  {
    id: 7576023,
    chapter: 'Forms and User Input',
    title: 'Grouping radio options',
    prompt: 'You have three radio buttons for choosing one shipping speed. Which element should wrap them, with a caption describing the group?',
    correct: '<fieldset> with a <legend>',
    wrong: [
      miss('<div> with a <label>', 'A div with a single label does not group the controls or expose a group caption to assistive technology.', 'Grouped controls need fieldset/legend, not a plain div.'),
      miss('<section> with an <h3>', 'A section heading does not establish a programmatic grouping of form controls the way fieldset/legend does.', 'Use the dedicated grouping elements for form controls.'),
      miss('<table> with a caption', 'A table is for tabular data; radio groups are grouped semantically with fieldset and legend.', 'Don\'t wrap form controls in a data table.'),
    ],
    lesson: 'Related controls such as a set of radio buttons should be wrapped in a <fieldset>, with a <legend> giving the group a caption. Screen readers announce the legend with each option so users understand what the choices belong to.',
    source,
    generated: true,
  },
  {
    id: 7576024,
    chapter: 'Forms and User Input',
    title: 'Why client validation is not security',
    prompt: 'Your form uses required and type="email" so bad data cannot get through. Why must the server still validate the submitted data?',
    correct: 'A user can bypass or disable client-side checks, so the server is the only trustworthy place to enforce rules.',
    wrong: [
      miss('Server validation makes the page load faster.', 'Server validation is about trust and data integrity, not page speed.', 'The reason is security and integrity, not performance.'),
      miss('Client-side validation does not work in any modern browser.', 'Native client validation does work; it just cannot be trusted because it can be turned off or bypassed.', 'It works, but it is not trustworthy on its own.'),
      miss('The server validation replaces the need for any HTML attributes.', 'Both layers are useful: client checks improve UX, server checks enforce security.', 'It is both-and, not either-or.'),
    ],
    lesson: 'Client-side validation improves the user experience by catching mistakes early, but it can be disabled, edited, or bypassed (for example via direct requests). The server must independently validate all input, since it is the only place an attacker cannot control.',
    source,
    generated: true,
  },
  {
    id: 7576025,
    chapter: 'Forms and User Input',
    title: 'Submit versus plain button',
    prompt: 'Inside a <form>, which button type triggers the form to submit when clicked?',
    correct: '<button type="submit">',
    wrong: [
      miss('<button type="button">', 'type="button" creates a generic button that does nothing by default; it does not submit the form.', 'A plain button needs JavaScript; submit is the type that sends the form.'),
      miss('<button type="reset">', 'type="reset" clears the form fields rather than submitting them.', 'Reset wipes the form; it does not send it.'),
      miss('<input type="text" value="Submit">', 'A text input is a field for typing, not a control that submits the form.', 'A text field accepts input; it does not submit.'),
    ],
    lesson: 'Within a form, <button type="submit"> (the default type for a button inside a form) sends the form data. type="reset" clears the fields, and type="button" does nothing on its own and is meant to be wired up with JavaScript.',
    source,
    generated: true,
  },
  // ---------- Chapter 6: Accessibility From the Start ----------
  {
    id: 7576026,
    chapter: 'Accessibility From the Start',
    title: 'WCAG AA text contrast',
    prompt: 'Under WCAG 2.1 Level AA, what is the minimum contrast ratio between normal-size body text and its background?',
    correct: '4.5 to 1',
    wrong: [
      miss('3 to 1', '3:1 is the AA minimum for large text (and for some non-text elements), not for normal body text.', 'That ratio is for large text; normal text needs a higher one.'),
      miss('7 to 1', '7:1 is the stricter AAA requirement for normal text, not the AA minimum.', 'That is the AAA bar, not AA.'),
      miss('2 to 1', '2:1 is below every WCAG text requirement and would fail for any text size.', 'Too low — even large text needs 3:1.'),
    ],
    lesson: 'WCAG 2.1 Level AA requires a contrast ratio of at least 4.5:1 for normal text and 3:1 for large text (roughly 18pt, or 14pt bold). The stricter AAA level requires 7:1 for normal text. Adequate contrast is essential for low-vision users.',
    source,
    generated: true,
  },
  {
    id: 7576027,
    chapter: 'Accessibility From the Start',
    title: 'First rule of ARIA',
    prompt: 'You need a clickable control that runs JavaScript. Which approach best follows accessibility best practice?',
    correct: 'Use a native <button>, which is focusable and announced as a button automatically.',
    wrong: [
      miss('Use a <div> with role="button" and a click handler.', 'This recreates button behavior manually and easily misses keyboard support; a native button is preferred.', 'The first rule of ARIA: prefer a native element when one exists.'),
      miss('Use a <span> and rely on a tooltip to explain it is clickable.', 'A span is not focusable or announced as interactive, so keyboard and screen reader users are excluded.', 'A span carries no interactive semantics on its own.'),
      miss('Use an <a> with no href and a click handler.', 'An anchor without href is not keyboard-focusable and is semantically a link, not an action control.', 'No-href anchors lose keyboard access; use a real button.'),
    ],
    lesson: 'The first rule of ARIA is to use a native HTML element with the semantics and behavior you need rather than re-creating them with ARIA. A <button> is focusable, keyboard-operable, and announced correctly with no extra work.',
    source,
    generated: true,
  },
  {
    id: 7576028,
    chapter: 'Accessibility From the Start',
    title: 'Not relying on color alone',
    prompt: 'A form shows invalid fields only by turning their borders red. Why can this fail accessibility?',
    correct: 'Users who cannot distinguish red (color blindness) get no indication of the error.',
    wrong: [
      miss('Red borders slow down the browser\'s rendering.', 'The issue is perceivability for color-blind users, not rendering speed.', 'It is an accessibility problem, not a performance one.'),
      miss('Red is never allowed in accessible designs.', 'Red is fine; the problem is using color as the only signal, not the color itself.', 'Color is allowed; relying on it alone is the issue.'),
      miss('Screen readers cannot read any colored elements.', 'Screen readers read content regardless of color; the issue is that color alone conveys no text for them either.', 'The flaw is the missing non-color cue, for sighted and non-sighted users alike.'),
    ],
    lesson: 'WCAG requires that color not be the only means of conveying information. Pair a red border with an icon, text message, or label change so users with color vision deficiencies (and screen reader users) still perceive the error.',
    source,
    generated: true,
  },
  {
    id: 7576029,
    chapter: 'Accessibility From the Start',
    title: 'Keyboard operability',
    prompt: 'What does it mean for an interactive page to be "keyboard operable"?',
    correct: 'Every interactive control can be reached and activated using only the keyboard.',
    wrong: [
      miss('The page includes keyboard shortcuts for power users.', 'Optional shortcuts are a bonus, but basic keyboard operability means all controls work via Tab and Enter without a mouse.', 'It is about reaching every control, not extra shortcuts.'),
      miss('The page disables the mouse so only the keyboard works.', 'Operability means supporting the keyboard, not removing mouse support.', 'You add keyboard access; you do not remove the mouse.'),
      miss('The page can be edited only by typing in a code editor.', 'This confuses authoring with using the page; operability is about end users navigating the page.', 'It concerns using the page, not editing its code.'),
    ],
    lesson: 'Keyboard operability means a user can Tab to every interactive element in a logical order and activate it with the keyboard (Enter or Space). Many disabled users rely on the keyboard, so anything that only works on mouse hover or click is an accessibility barrier.',
    source,
    generated: true,
  },
  // ---------- Chapter 7: Debugging and Quality ----------
  {
    id: 7576030,
    chapter: 'Debugging and Quality',
    title: 'Fixing a relative path',
    prompt: 'A page at /pages/about.html references <img src="logo.png">, but the image lives in an /images folder at the site root. The image is broken. Which src fixes it?',
    correct: '<img src="../images/logo.png">',
    wrong: [
      miss('<img src="images/logo.png">', 'From /pages/, this looks for /pages/images/logo.png, which does not exist; you must go up a level first.', 'You are one folder too deep — step up before going into images.'),
      miss('<img src="logo.png">', 'This is the original broken path; it looks in /pages/ where the image is not located.', 'That is the path that is already failing.'),
      miss('<img src="/pages/images/logo.png">', 'The images folder is at the site root, not inside /pages/, so this absolute path points to a nonexistent folder.', 'The images folder is at the root, not under /pages/.'),
    ],
    lesson: 'Relative paths are resolved from the current file\'s location. From /pages/about.html, ../ moves up to the site root, then images/logo.png reaches the image. Knowing how ./, ../, and leading / behave is essential for fixing broken assets.',
    source,
    generated: true,
  },
  {
    id: 7576031,
    chapter: 'Debugging and Quality',
    title: 'Why a CSS rule is not applying',
    prompt: 'You added .btn { color: red } but the button stays blue. In DevTools you see the .btn rule listed but with a strikethrough. What is the most likely cause?',
    correct: 'A more specific or later rule is overriding the color, so the .btn rule is overridden.',
    wrong: [
      miss('The browser does not support the color property.', 'color is universally supported; a struck-through rule means it was overridden, not unsupported.', 'Strikethrough in DevTools signals an override, not lack of support.'),
      miss('The CSS file failed to load entirely.', 'If the file had not loaded, the .btn rule would not appear in DevTools at all.', 'The rule is visible, so the file did load.'),
      miss('color: red is invalid CSS syntax.', 'color: red is valid; the strikethrough indicates it lost to a higher-priority rule.', 'Valid syntax that is struck through lost the cascade, not a parse error.'),
    ],
    lesson: 'In the DevTools Elements panel, a struck-through declaration means another rule won the cascade by higher specificity or later source order. Inspect which rule is applying, then raise specificity or adjust order rather than guessing.',
    source,
    generated: true,
  },
])
