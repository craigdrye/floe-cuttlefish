import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe introCS top-up'

export const introCSUniv103TopUpQuestions: Question[] = makeQuestionBank('University', [
  // ---------------- Chapter 1: Variables and Data Types ----------------
  {
    id: 7726000,
    chapter: 'Variables and Data Types',
    title: 'Static vs dynamic typing',
    prompt: 'In Python you can write x = 5 and later x = "hello" without an error, while in Java int x = 5; x = "hello"; fails to compile. What language feature explains this difference?',
    correct: 'Python uses dynamic typing where types attach to values at runtime, while Java uses static typing where a variable has a fixed declared type',
    wrong: [
      miss('Python has no types at all, so any value can be stored anywhere', 'Python values still have types (int, str, etc.); the difference is when type checking happens, not whether types exist.', 'Ask whether values have types versus when the language checks them.'),
      miss('Java is interpreted while Python is compiled, so Java catches more errors', 'It is the reverse: Java is compiled to bytecode and Python is typically interpreted, and that is not what causes this difference anyway.', 'Focus on static versus dynamic typing, not compilation.'),
      miss('Java variables are constants and cannot be reassigned', 'Java variables can be reassigned to other ints; only the type, not the value, is fixed.', 'Reassignment to the same type is allowed in Java.'),
    ],
    lesson: 'In statically typed languages each variable has a declared type fixed at compile time, so assigning an incompatible value is a compile error. In dynamically typed languages a name is just a label that can be rebound to a value of any type at runtime, and type errors surface only when an operation is actually attempted.',
    source,
    generated: true,
  },
  {
    id: 7726001,
    chapter: 'Variables and Data Types',
    title: 'Floating-point representation',
    prompt: 'In most languages, evaluating 0.1 + 0.2 == 0.3 returns false. What is the underlying cause?',
    correct: 'Values like 0.1 and 0.2 cannot be represented exactly in binary floating point, so tiny rounding errors accumulate',
    wrong: [
      miss('The == operator never works on numbers and always returns false', 'Equality works fine on integers and on floats that happen to be exactly representable; the issue is specific to these decimal fractions.', 'Try comparing 0.5 + 0.25 == 0.75 and see what happens.'),
      miss('Addition of decimals is computed in a random order, giving random results', 'Floating-point addition is deterministic, not random; the result is wrong but always the same.', 'The error is consistent, so randomness is not the cause.'),
      miss('The literal 0.3 is silently converted to an integer 0', 'No integer truncation occurs; 0.3 remains a float and is simply not exactly equal to the computed sum.', 'Both sides stay floating point.'),
    ],
    lesson: 'Binary floating point (IEEE 754) can only represent fractions whose denominators are powers of two exactly. Decimal values like 0.1, 0.2, and 0.3 become repeating binary fractions that get rounded to the nearest representable value, so their sums differ by a tiny epsilon. Compare floats with a tolerance rather than exact equality.',
    source,
    generated: true,
  },
  {
    id: 7726002,
    chapter: 'Variables and Data Types',
    title: 'Integer overflow behavior',
    prompt: 'A signed 8-bit integer can hold values from -128 to 127. In a language with fixed-width two’s-complement integers that wrap around, what is the result of 127 + 1?',
    correct: '-128, because the value wraps around to the most negative representable number',
    wrong: [
      miss('128, because the integer simply grows to hold the larger value', 'A fixed-width 8-bit type cannot hold 128; that is exactly why overflow occurs.', 'Recall the maximum value an 8-bit signed integer can hold.'),
      miss('0, because overflow resets the value to its default', 'Two’s-complement overflow does not reset to zero; it continues counting past the maximum into the negative range.', 'Think about what bit pattern follows 0111 1111.'),
      miss('127, because the value saturates and stops at the maximum', 'Saturation is a different behavior used in some DSP contexts; standard two’s-complement wraps rather than clamps.', 'Wrapping and saturation are distinct overflow policies.'),
    ],
    lesson: 'In two’s-complement representation the bit pattern 0111 1111 is 127, and adding one yields 1000 0000, which is interpreted as -128. Fixed-width integer types wrap around modulo 2^n, so exceeding the maximum jumps to the minimum. Languages with arbitrary-precision integers (like Python) avoid this entirely.',
    source,
    generated: true,
  },
  {
    id: 7726003,
    chapter: 'Variables and Data Types',
    title: 'Booleans and truthiness',
    prompt: 'In Python, which of the following values is considered falsy (treated as False in a boolean context)?',
    correct: 'An empty list []',
    wrong: [
      miss('The string "False"', 'Any non-empty string, including "False" and "0", is truthy in Python; only the empty string is falsy.', 'Truthiness depends on emptiness, not on the text content.'),
      miss('The integer -1', 'Only the number zero is falsy; any non-zero number, positive or negative, is truthy.', 'Which single numeric value is falsy?'),
      miss('A list containing one zero, [0]', 'A list with any element is non-empty and therefore truthy, even if the element itself is falsy.', 'Container truthiness depends on length, not contents.'),
    ],
    lesson: 'Python treats certain values as falsy: None, False, zero of any numeric type, and empty containers (empty string, list, tuple, dict, set). Everything else is truthy. A non-empty container is truthy regardless of what it contains, which is why [0] is true but [] is false.',
    source,
    generated: true,
  },
  {
    id: 7726004,
    chapter: 'Variables and Data Types',
    title: 'Constants and immutability',
    prompt: 'A program declares a value that should never change after initialization. Which concept best supports this intent?',
    correct: 'Declaring it as a constant (e.g. final in Java, const in C++), preventing reassignment',
    wrong: [
      miss('Storing it in a global variable so every function can see it', 'Global scope controls visibility, not mutability; a global can still be reassigned freely.', 'Visibility and changeability are separate concerns.'),
      miss('Giving the variable an uppercase name, which makes it read-only', 'Uppercase naming is a convention signaling intent but does not enforce immutability in most languages.', 'A naming convention is advisory, not enforced.'),
      miss('Using a longer variable name so it is harder to overwrite by accident', 'Name length has no effect on whether reassignment is permitted by the compiler or interpreter.', 'Think about a language feature that the compiler enforces.'),
    ],
    lesson: 'A constant binding is enforced by the language so that any attempt to reassign it is rejected, often at compile time. This differs from naming conventions (like ALL_CAPS in Python) that only communicate intent. Constants make code safer and let compilers optimize, since the value is guaranteed not to change.',
    source,
    generated: true,
  },

  // ---------------- Chapter 2: Control Flow ----------------
  {
    id: 7726005,
    chapter: 'Control Flow',
    title: 'Short-circuit evaluation',
    prompt: 'In the expression (x != 0) and (10 / x > 2), why does this avoid a division-by-zero error when x is 0?',
    correct: 'The and operator short-circuits: if the left operand is false, the right operand is never evaluated',
    wrong: [
      miss('Python automatically returns infinity instead of dividing by zero', 'Dividing by zero raises a ZeroDivisionError in Python; it does not silently return infinity for integer or normal division.', 'The protection comes from not evaluating the division at all.'),
      miss('The and operator evaluates both sides and then discards the error', 'Errors are not discarded; short-circuiting prevents the second operand from running in the first place.', 'Ask whether the right side even executes.'),
      miss('Parentheses force the right side to run first, catching the zero', 'Parentheses group sub-expressions but do not reorder the left-to-right short-circuit behavior of and.', 'and always tests the left operand first.'),
    ],
    lesson: 'Logical and and or use short-circuit evaluation: and stops as soon as it finds a false operand, and or stops at the first true operand. This both improves performance and lets programmers use the first condition as a guard for the second, a common idiom for avoiding errors like division by zero or dereferencing a null value.',
    source,
    generated: true,
  },
  {
    id: 7726006,
    chapter: 'Control Flow',
    title: 'Switch statement fall-through',
    prompt: 'In C or Java, a switch statement has cases without break statements between them. What happens after a matching case executes?',
    correct: 'Execution falls through and runs the code of subsequent cases until a break or the end of the switch',
    wrong: [
      miss('Only the matching case runs, then control automatically exits the switch', 'C-style switches do not auto-exit after a case; without break, execution continues into the next case.', 'Recall why break is needed in C/Java switches.'),
      miss('A compile error occurs because every case must have a break', 'Missing break is legal (and sometimes intentional); the compiler allows fall-through.', 'Fall-through is permitted, not forbidden.'),
      miss('All cases run from top to bottom regardless of which one matched', 'Execution begins at the matched case, not the top; cases before the match are skipped.', 'Matching jumps to the right case, then falls through forward.'),
    ],
    lesson: 'In C and Java a switch jumps to the matching case label and then executes sequentially through any following cases until it hits a break statement or the closing brace. This fall-through behavior is a frequent source of bugs, which is why each case typically ends with break. Some newer languages and the Java switch expression avoid implicit fall-through.',
    source,
    generated: true,
  },
  {
    id: 7726007,
    chapter: 'Control Flow',
    title: 'Dangling else association',
    prompt: 'In a language with C-like syntax, an if-else with no braces is written as: if (a) if (b) f(); else g();. With which if does the else associate?',
    correct: 'The inner if (the one testing b), because else binds to the nearest preceding unmatched if',
    wrong: [
      miss('The outer if (the one testing a), because it appears first', 'The dangling-else rule binds else to the nearest if, which is the inner one, not the first one written.', 'Recall the nearest-if rule for ambiguous else.'),
      miss('Both ifs, so g() runs whenever either a or b is false', 'An else clause belongs to exactly one if and runs at most once, not for both.', 'Each else pairs with a single if.'),
      miss('Neither, because the code is a syntax error without braces', 'The grammar resolves the ambiguity rather than rejecting the code; it compiles and runs.', 'The language has a rule to disambiguate, not an error.'),
    ],
    lesson: 'The classic dangling-else ambiguity is resolved by the rule that an else clause associates with the nearest preceding if that does not already have an else. So g() runs only when a is true and b is false. To make associations explicit and avoid bugs, use braces around nested if statements.',
    source,
    generated: true,
  },
  {
    id: 7726008,
    chapter: 'Control Flow',
    title: 'Guard clauses and early return',
    prompt: 'What is the primary benefit of using a guard clause (an early return for invalid input) at the top of a function?',
    correct: 'It handles edge cases up front and reduces deep nesting in the main logic',
    wrong: [
      miss('It makes the function run faster by skipping the compiler', 'Early returns do not bypass compilation; their benefit is readability and structure, not raw speed.', 'Think about code structure rather than performance.'),
      miss('It guarantees the function can never throw any exception', 'A guard clause checks specific conditions but does not eliminate all possible exceptions elsewhere.', 'Guards handle the cases you write, not every possible error.'),
      miss('It allows the function to return multiple values at once', 'Returning multiple values is unrelated to guard clauses; that depends on tuples or output parameters.', 'Guards are about control flow, not return arity.'),
    ],
    lesson: 'A guard clause checks for invalid or trivial cases at the start of a function and returns immediately, so the rest of the body can assume valid input. This flattens the code by removing the need to wrap the main logic in nested if blocks, improving readability and making the happy path clearer.',
    source,
    generated: true,
  },

  // ---------------- Chapter 3: Loops and Iteration ----------------
  {
    id: 7726009,
    chapter: 'Loops and Iteration',
    title: 'Off-by-one in loop bounds',
    prompt: 'You want to print the numbers 1 through 10 inclusive. Which Python loop header is correct?',
    correct: 'for i in range(1, 11)',
    wrong: [
      miss('for i in range(1, 10)', 'range(1, 10) stops at 9 because the stop value is exclusive, so 10 is never printed.', 'Recall that range excludes its stop value.'),
      miss('for i in range(0, 10)', 'This produces 0 through 9, which is both off by one at the start and misses 10.', 'Check the first and last values this would generate.'),
      miss('for i in range(11)', 'range(11) starts at 0 and yields 0 through 10, including the unwanted 0.', 'A single argument starts at zero by default.'),
    ],
    lesson: 'Python’s range(start, stop) is half-open: it includes start but excludes stop. To produce 1 through 10 inclusive you need range(1, 11). Off-by-one errors at loop boundaries are among the most common bugs, so always check whether the endpoint should be included.',
    source,
    generated: true,
  },
  {
    id: 7726010,
    chapter: 'Loops and Iteration',
    title: 'while loop termination',
    prompt: 'A while loop is written as: while n > 0: print(n). The variable n starts at 5 and is never modified inside the loop. What happens?',
    correct: 'It is an infinite loop, because the condition n > 0 stays true forever',
    wrong: [
      miss('It prints 5 once and then stops automatically', 'A while loop repeats as long as its condition is true; nothing makes it stop after one iteration here.', 'What changes n between iterations?'),
      miss('It prints 5 four more times, counting down to 1', 'Nothing decrements n, so it never counts down; it stays at 5 indefinitely.', 'Look for code that updates the loop variable.'),
      miss('It raises an error because the loop body does not change n', 'Failing to update the loop variable is a logic bug, not a syntax or runtime error.', 'The interpreter does not require you to modify n.'),
    ],
    lesson: 'A while loop continues as long as its condition evaluates to true. If nothing in the loop body changes the variables that the condition depends on, the condition never becomes false and the loop runs forever. Every while loop needs to make progress toward its termination condition.',
    source,
    generated: true,
  },
  {
    id: 7726011,
    chapter: 'Loops and Iteration',
    title: 'break vs continue',
    prompt: 'Inside a loop, what is the difference between break and continue?',
    correct: 'break exits the loop entirely, while continue skips to the next iteration',
    wrong: [
      miss('break skips one iteration, while continue exits the loop', 'This reverses the two: break is the one that exits, and continue is the one that skips an iteration.', 'Match each keyword to its effect carefully.'),
      miss('Both exit the loop, but break also returns from the function', 'break only leaves the loop; it does not return from the enclosing function, and continue does not exit the loop at all.', 'Neither keyword returns from the function.'),
      miss('continue restarts the loop from the very first iteration', 'continue moves on to the next iteration, not back to the beginning; the loop counter is not reset.', 'continue advances forward, it does not rewind.'),
    ],
    lesson: 'break immediately terminates the nearest enclosing loop and transfers control to the statement after it. continue skips the rest of the current iteration and jumps to the next one (re-evaluating the loop condition or advancing the iterator). Both affect only the innermost loop unless labeled loops are supported.',
    source,
    generated: true,
  },
  {
    id: 7726012,
    chapter: 'Loops and Iteration',
    title: 'Nested loop iteration count',
    prompt: 'A loop runs i from 0 to n-1, and for each i an inner loop runs j from 0 to n-1. How many times does the innermost statement execute?',
    correct: 'n squared (n^2) times',
    wrong: [
      miss('2n times, because there are two loops added together', 'Nested loops multiply rather than add their counts; the inner loop runs fully for each outer iteration.', 'For each outer step, how many inner steps occur?'),
      miss('n times, because both loops share the same bound n', 'Sharing a bound does not merge the loops; the inner loop still runs n times per outer iteration.', 'The loops are nested, not parallel.'),
      miss('n^n times, because each loop multiplies the other exponentially', 'Two nested linear loops give n times n, which is n^2, not n raised to the n.', 'Multiplication of counts gives a product, not an exponent tower.'),
    ],
    lesson: 'When loops are nested, the inner loop executes completely for every single iteration of the outer loop, so the total count is the product of the iteration counts. Two loops each running n times yield n^2 total executions, which is why doubly nested loops over the same data typically run in O(n^2) time.',
    source,
    generated: true,
  },
  {
    id: 7726013,
    chapter: 'Loops and Iteration',
    title: 'Iterating with enumerate',
    prompt: 'In Python, you need both the index and the value while iterating over a list named items. Which is the idiomatic approach?',
    correct: 'for i, value in enumerate(items)',
    wrong: [
      miss('for value in items, then use items.index(value) to get the index', 'index() searches the list each time (O(n)) and returns the first match, giving wrong indices for duplicate values.', 'Searching for the value defeats the purpose and breaks on duplicates.'),
      miss('for i in items, treating each element as if it were an index', 'This binds i to the elements themselves, not to integer positions, so using it as an index is wrong.', 'Iterating a list yields values, not indices.'),
      miss('for i in len(items), which gives index and value together', 'len(items) is an integer and is not iterable, so this raises a TypeError, and it would not yield values anyway.', 'You cannot iterate over a single integer.'),
    ],
    lesson: 'enumerate(iterable) yields pairs of (index, element) as you iterate, which is the clean, Pythonic way to access both. It avoids the bugs of using list.index() (slow and wrong for duplicates) and the awkwardness of manually iterating over range(len(items)). You can also pass a start value, e.g. enumerate(items, 1).',
    source,
    generated: true,
  },

  // ---------------- Chapter 4: Functions ----------------
  {
    id: 7726014,
    chapter: 'Functions',
    title: 'Pass by value vs reference semantics',
    prompt: 'In Python, a function does my_list.append(4) on a list argument and the change is visible to the caller, but doing x = x + 1 on an integer argument is not. Why?',
    correct: 'Python passes object references; mutating a mutable object affects the caller, but rebinding a name to a new immutable object does not',
    wrong: [
      miss('Lists are passed by reference and integers are passed by value, by language rule', 'Python uses the same mechanism for all arguments (pass by object reference); the difference is mutability, not two separate passing modes.', 'Both use the same passing model; mutability is the real distinction.'),
      miss('append modifies a global copy of the list that all functions share', 'There is no hidden global copy; append mutates the very object the caller passed in.', 'The same object is shared, not a global duplicate.'),
      miss('Integers are immutable, so adding to one secretly updates the original', 'Because integers are immutable, x = x + 1 creates a new integer and rebinds the local name, leaving the caller unaffected.', 'Immutability prevents in-place change, so the original is untouched.'),
    ],
    lesson: 'Python passes references to objects by value: the function receives a reference to the same object. Calling a method that mutates a mutable object (like list.append) is visible everywhere the object is referenced. Reassigning the parameter name to a new object only changes the local binding, so immutable arguments appear to be passed by value.',
    source,
    generated: true,
  },
  {
    id: 7726015,
    chapter: 'Functions',
    title: 'Mutable default argument pitfall',
    prompt: 'A function is defined as def add(item, target=[]): target.append(item); return target. It is called three times with only the item argument. What is the bug?',
    correct: 'The default list is created once at definition time and shared across calls, so it accumulates items',
    wrong: [
      miss('A fresh empty list is created on every call, so the function always returns one item', 'That is the intended-but-wrong mental model; the default is actually created once and reused, causing accumulation.', 'Recall when default argument values are evaluated.'),
      miss('Using a list as a default is a syntax error in Python', 'It is syntactically valid; the problem is semantic, not a syntax error.', 'The code runs; the behavior is just surprising.'),
      miss('The list is garbage collected between calls, losing previous items', 'The default persists because the function object holds a reference to it, so it is not collected between calls.', 'Something keeps the list alive across calls.'),
    ],
    lesson: 'In Python, default argument values are evaluated once when the function is defined, not on each call. A mutable default like [] is created a single time and shared by every call that uses the default, so mutations persist and accumulate. The fix is to default to None and create a new list inside the function body.',
    source,
    generated: true,
  },
  {
    id: 7726016,
    chapter: 'Functions',
    title: 'Variable scope and shadowing',
    prompt: 'A global variable x = 10 exists. Inside a function, the line x = 5 appears followed by print(x). What does the function print, and what is x globally afterward?',
    correct: 'It prints 5, and the global x remains 10, because x = 5 creates a local variable',
    wrong: [
      miss('It prints 5, and the global x becomes 5 as well', 'Without the global keyword, assignment creates a local variable that shadows the global; the global is unchanged.', 'Recall what plain assignment does to scope.'),
      miss('It prints 10, because the global value takes priority inside functions', 'The local assignment shadows the global, so the local value 5 is what print sees.', 'A local name hides the global of the same name.'),
      miss('It raises an error because x is defined in two scopes', 'Defining the same name in different scopes is allowed; shadowing is legal, not an error.', 'Two scopes can use the same name safely.'),
    ],
    lesson: 'Assigning to a name inside a function creates a local variable in that function’s scope, shadowing any global of the same name. The local assignment does not affect the global. To modify the global from within the function you must declare global x first; otherwise the names are independent.',
    source,
    generated: true,
  },
  {
    id: 7726017,
    chapter: 'Functions',
    title: 'Return value vs printing',
    prompt: 'A function ends with print(result) instead of return result. A caller writes total = my_func(...). What is the value of total?',
    correct: 'None, because a function with no return statement returns None even though it printed output',
    wrong: [
      miss('The printed value, because print sends the result back to the caller', 'print only writes to standard output; it does not provide a value to the caller. Printing and returning are different.', 'Distinguish what is displayed from what is returned.'),
      miss('An empty string, because print produces text output', 'The function returns None, not an empty string; print’s own return value (None) is also not what total receives.', 'A function with no return yields None, not "".'),
      miss('It raises an error because the function did not return anything', 'A missing return is not an error; the function simply returns None implicitly.', 'Falling off the end of a function is legal in Python.'),
    ],
    lesson: 'Printing and returning are distinct operations. print writes characters to output and yields None; it does not hand a value back to the calling code. A function that never executes a return statement returns None. To make a value usable by callers you must return it, then optionally print it where it is needed.',
    source,
    generated: true,
  },
  {
    id: 7726018,
    chapter: 'Functions',
    title: 'Keyword and positional arguments',
    prompt: 'A function is def greet(name, greeting="Hi"). Which call correctly uses a keyword argument to override only the greeting while passing the name positionally?',
    correct: 'greet("Sam", greeting="Hello")',
    wrong: [
      miss('greet(greeting="Hello", "Sam")', 'A positional argument cannot follow a keyword argument; this is a syntax error in Python.', 'Positional arguments must come before keyword ones.'),
      miss('greet("Hello")', 'This passes "Hello" as the name, leaving greeting at its default; it does not override the greeting.', 'The first positional argument fills the first parameter, name.'),
      miss('greet(name="Sam", "Hello")', 'Once a keyword argument is used, later positional arguments are not allowed, so this is a syntax error.', 'You cannot place a positional argument after a keyword one.'),
    ],
    lesson: 'Positional arguments are matched to parameters by order; keyword arguments are matched by name. Python requires all positional arguments to appear before any keyword arguments in a call. So greet("Sam", greeting="Hello") passes name positionally and overrides the greeting by name, which is both valid and clear.',
    source,
    generated: true,
  },

  // ---------------- Chapter 5: Arrays and Lists ----------------
  {
    id: 7726019,
    chapter: 'Arrays and Lists',
    title: 'Zero-based indexing',
    prompt: 'For a list nums = [10, 20, 30, 40, 50], what does nums[2] return?',
    correct: '30, because indexing starts at 0 so index 2 is the third element',
    wrong: [
      miss('20, because index 2 means the second element', 'Indexing is zero-based, so index 0 is the first element and index 2 is the third, which is 30.', 'Count starting from 0: positions 0, 1, 2.'),
      miss('40, because you count from 1 and index 2 is the second-to-last counted', 'Lists are not 1-based here; index 2 is simply the element at position 2 counting from zero.', 'Indices begin at zero, not one.'),
      miss('50, because index 2 counts backward from the end', 'Positive indices count from the front, not the back; negative indices like -2 count from the end.', 'Positive indices go front to back.'),
    ],
    lesson: 'Most programming languages use zero-based indexing, where the first element is at index 0. For nums = [10, 20, 30, 40, 50], index 0 is 10 and index 2 is 30. Understanding this is essential to avoid off-by-one errors; negative indices in Python count from the end, with -1 being the last element.',
    source,
    generated: true,
  },
  {
    id: 7726020,
    chapter: 'Arrays and Lists',
    title: 'List slicing semantics',
    prompt: 'For lst = [0, 1, 2, 3, 4, 5], what does the slice lst[1:4] produce?',
    correct: '[1, 2, 3], because slicing includes the start index but excludes the stop index',
    wrong: [
      miss('[1, 2, 3, 4], because both endpoints are included', 'Python slicing is half-open: the stop index 4 is excluded, so element 4 is not part of the result.', 'Recall whether the stop index is included.'),
      miss('[2, 3, 4], because the start index is excluded and stop is included', 'It is the reverse: the start index is included and the stop index is excluded.', 'Start is inclusive, stop is exclusive.'),
      miss('[0, 1, 2, 3], because slicing always begins at index 0', 'Slicing begins at the given start index (1 here), not always at 0.', 'The slice starts where you tell it to.'),
    ],
    lesson: 'A Python slice lst[start:stop] includes elements from index start up to but not including stop, producing stop - start elements when in range. So lst[1:4] gives indices 1, 2, and 3. This half-open convention makes lengths easy to compute and slices easy to concatenate without overlap.',
    source,
    generated: true,
  },
  {
    id: 7726021,
    chapter: 'Arrays and Lists',
    title: 'Array vs linked list random access',
    prompt: 'Compared to a singly linked list, what is the key performance advantage of a contiguous array for accessing the element at index i?',
    correct: 'Arrays support O(1) random access by computing the element’s address directly from the index',
    wrong: [
      miss('Arrays let you insert at the front in O(1) by repointing a node', 'O(1) front insertion is a linked-list strength; arrays require shifting elements, which is O(n).', 'Front insertion favors linked lists, not arrays.'),
      miss('Arrays never need to store the data type, saving memory per element', 'Arrays do store typed data; the advantage in question is access speed, not type-free storage.', 'Focus on how the element is located.'),
      miss('Arrays can grow without bound at no cost, unlike linked lists', 'Fixed arrays cannot grow at all, and dynamic arrays occasionally pay O(n) to resize; growth is not free.', 'Growth is a weakness of arrays, not a strength.'),
    ],
    lesson: 'Because array elements are stored contiguously, the address of element i is base + i times the element size, allowing constant-time random access. A linked list must follow pointers from the head, taking O(n) to reach index i. The trade-off is that arrays pay O(n) for insertions and deletions in the middle, where linked lists can be cheaper.',
    source,
    generated: true,
  },
  {
    id: 7726022,
    chapter: 'Arrays and Lists',
    title: 'Aliasing and shallow copy',
    prompt: 'In Python, a = [1, 2, 3] and then b = a. After b.append(4), what does a contain?',
    correct: '[1, 2, 3, 4], because b is another reference to the same list object',
    wrong: [
      miss('[1, 2, 3], because b = a makes an independent copy of the list', 'Plain assignment copies the reference, not the list, so a and b point at the same object.', 'Assignment binds a name; it does not duplicate the object.'),
      miss('[1, 2, 3, 4] only if you also reassign a, otherwise [1, 2, 3]', 'a already refers to the mutated object, so no reassignment is needed to see the change.', 'Both names already see the same underlying list.'),
      miss('An error, because two names cannot point to one list', 'Multiple names referring to one object (aliasing) is normal and allowed in Python.', 'Aliasing is a standard feature, not an error.'),
    ],
    lesson: 'Assigning b = a copies the reference, so both names refer to the same list object (aliasing). Mutating through either name is visible through the other. To get an independent copy use b = a.copy() or b = a[:], and note that those are shallow copies that still share any nested objects.',
    source,
    generated: true,
  },
  {
    id: 7726023,
    chapter: 'Arrays and Lists',
    title: 'Dynamic array amortized append',
    prompt: 'Appending n elements one at a time to a dynamic array (like a Python list) takes what total time, despite occasional costly resizes?',
    correct: 'O(n) total, giving amortized O(1) per append because resizes double the capacity',
    wrong: [
      miss('O(n^2) total, because each append copies the whole array', 'Most appends do not copy; copies happen only on resize, and doubling makes the total copying cost linear overall.', 'Count how often a resize actually occurs.'),
      miss('O(n log n) total, because of the logarithmic number of doublings', 'The number of doublings is logarithmic, but the total copy work sums to a geometric series that is linear, not n log n.', 'Sum the cost of all the doublings, not just count them.'),
      miss('O(1) total, because appends are always constant time', 'Individual appends are amortized O(1), but n of them take O(n) total, not O(1).', 'Per-operation cost differs from total cost.'),
    ],
    lesson: 'Dynamic arrays grow by doubling capacity when full, so the total copying work over n appends forms a geometric series (1 + 2 + 4 + ... + n) that sums to less than 2n, i.e. O(n). Dividing by n appends gives amortized O(1) per append. Any single append may be O(n) during a resize, but the average is constant.',
    source,
    generated: true,
  },

  // ---------------- Chapter 6: Strings ----------------
  {
    id: 7726024,
    chapter: 'Strings',
    title: 'String immutability in Python',
    prompt: 'In Python, what happens when you try s[0] = "H" on a string s = "hello"?',
    correct: 'It raises a TypeError, because strings are immutable and cannot be modified in place',
    wrong: [
      miss('It changes the first character to H, giving "Hello"', 'Strings cannot be modified in place; this assignment is not allowed and raises an error.', 'Recall whether Python strings are mutable.'),
      miss('It silently does nothing and leaves the string unchanged', 'The operation does not fail silently; it raises a TypeError rather than being ignored.', 'An illegal operation raises, it does not no-op.'),
      miss('It appends "H" to the front, giving "Hhello"', 'Item assignment does not insert; and in any case strings do not support item assignment at all.', 'There is no in-place mutation of strings.'),
    ],
    lesson: 'Python strings are immutable: once created, their characters cannot be changed. Attempting item assignment raises a TypeError. To produce a modified string you build a new one, for example "H" + s[1:]. Immutability lets strings be hashable (usable as dict keys) and safely shared.',
    source,
    generated: true,
  },
  {
    id: 7726025,
    chapter: 'Strings',
    title: 'String concatenation cost in a loop',
    prompt: 'Building a long string by repeatedly doing result = result + word inside a loop over n words can be slow. What is the usual recommended alternative in Python?',
    correct: 'Collect the pieces in a list and call "".join(list) once at the end',
    wrong: [
      miss('Use a tuple instead of a string, since tuples are faster to concatenate', 'Tuple concatenation has the same copying cost as strings; the fix is to batch the join, not change container.', 'The issue is repeated copying, not the container type.'),
      miss('Convert each word to bytes first, which avoids the copying overhead', 'Concatenating bytes objects repeatedly has the same quadratic pitfall as strings; bytes are also immutable.', 'Bytes are immutable too, so the same problem applies.'),
      miss('Add the words in reverse order, which avoids reallocations', 'Order does not change the number or cost of allocations; each concatenation still builds a new string.', 'Reordering does not reduce the copying work.'),
    ],
    lesson: 'Because strings are immutable, result = result + word creates a brand-new string each time, copying all characters so far; over n iterations this is O(n^2). Accumulating substrings in a list and calling "".join() once is O(n) total, because join computes the final size and copies each piece a single time.',
    source,
    generated: true,
  },
  {
    id: 7726026,
    chapter: 'Strings',
    title: 'Character encoding vs character',
    prompt: 'In Python, what does ord("A") return, and what does it represent?',
    correct: '65, the Unicode code point (which matches the ASCII value) for the character A',
    wrong: [
      miss('1, because A is the first letter of the alphabet', 'ord returns the character’s code point, not its position in the alphabet; A maps to 65, not 1.', 'ord gives an encoding value, not an alphabet rank.'),
      miss('"A", because ord just returns the character unchanged', 'ord returns an integer code point; the inverse function chr maps an integer back to a character.', 'ord and chr are inverses; ord yields a number.'),
      miss('97, the code point for lowercase a', '97 is ord("a"); uppercase A is 65. Case matters for code points.', 'Lowercase and uppercase letters have different code points.'),
    ],
    lesson: 'ord(c) returns the integer Unicode code point of a single character, and chr(n) does the reverse. For the basic Latin letters these match the historical ASCII values: A is 65, a is 97, and digit 0 is 48. Knowing these mappings is useful for character arithmetic and simple ciphers.',
    source,
    generated: true,
  },
  {
    id: 7726027,
    chapter: 'Strings',
    title: 'Splitting and joining',
    prompt: 'Given csv = "a,b,c", what does csv.split(",") return?',
    correct: '["a", "b", "c"], a list of the substrings between commas',
    wrong: [
      miss('"abc", a single string with the commas removed', 'split returns a list of pieces, not a single joined string; removing characters without splitting would be replace.', 'split produces a list, not a string.'),
      miss('["a,b,c"], the original string as the only element', 'When the separator is present, split breaks the string at each occurrence rather than returning it whole.', 'The comma separator is found and used to split.'),
      miss('["a", ",", "b", ",", "c"], including the separators as elements', 'split discards the separator; it does not include the delimiters in the result list.', 'The delimiter is consumed, not kept.'),
    ],
    lesson: 'str.split(sep) divides a string at each occurrence of sep and returns a list of the pieces, discarding the separators. "a,b,c".split(",") gives ["a", "b", "c"]. The inverse is sep.join(list), so ",".join(["a", "b", "c"]) reconstructs "a,b,c". Calling split() with no argument splits on runs of whitespace.',
    source,
    generated: true,
  },
  {
    id: 7726028,
    chapter: 'Strings',
    title: 'Negative indexing on strings',
    prompt: 'For s = "Python", what does s[-1] return?',
    correct: '"n", because negative indices count from the end with -1 being the last character',
    wrong: [
      miss('"P", because -1 wraps to the first character', 'Negative indexing counts from the end, so -1 is the last character, not the first.', 'Index -1 refers to the final element.'),
      miss('An error, because string indices cannot be negative', 'Python explicitly supports negative indices; they are not an error.', 'Negative indices are a valid feature.'),
      miss('"", an empty string, because there is no index -1', 'Index -1 is valid for any non-empty sequence and returns the last element.', 'A non-empty string always has a valid -1 index.'),
    ],
    lesson: 'In Python, negative indices count backward from the end of a sequence: -1 is the last element, -2 the second to last, and so on. For s = "Python", s[-1] is "n" and s[-6] is "P". This is a convenient way to access the tail of a string or list without computing its length.',
    source,
    generated: true,
  },

  // ---------------- Chapter 7: Recursion ----------------
  {
    id: 7726029,
    chapter: 'Recursion',
    title: 'Base case necessity',
    prompt: 'Why must every correct recursive function include at least one base case?',
    correct: 'The base case stops the recursion; without it the function calls itself forever and overflows the call stack',
    wrong: [
      miss('The base case is optional and only improves readability', 'Without a base case the recursion never terminates, so it is required for correctness, not just style.', 'What stops the chain of calls?'),
      miss('The base case is what makes the function return a list instead of a single value', 'The base case controls termination, not the return type; either kind of function can return any type.', 'Termination is the issue, not return type.'),
      miss('The base case ensures the function runs in constant time', 'A base case does not bound the running time; recursive functions can still take linear or exponential time.', 'A base case prevents infinite recursion, not slow recursion.'),
    ],
    lesson: 'Recursion solves a problem by reducing it to smaller subproblems and combining the results. The base case handles the smallest input directly, without further recursion, giving the chain of calls somewhere to stop. Omitting or never reaching a base case leads to infinite recursion that exhausts the call stack and crashes (a stack overflow).',
    source,
    generated: true,
  },
  {
    id: 7726030,
    chapter: 'Recursion',
    title: 'Tracing factorial recursion',
    prompt: 'Given def fact(n): return 1 if n <= 1 else n * fact(n - 1), what is the return value of fact(4)?',
    correct: '24, computed as 4 * 3 * 2 * 1',
    wrong: [
      miss('10, computed as 4 + 3 + 2 + 1', 'Factorial multiplies the values; the code uses n * fact(n - 1), not addition.', 'Check the operator in the recursive step.'),
      miss('4, because the function returns n directly at the base case', 'The base case returns 1 (for n <= 1), and each level multiplies, so the result accumulates to 24.', 'Trace the multiplications back up the call chain.'),
      miss('120, computed as 5 factorial', 'fact(4) multiplies 4 down to 1, giving 24; 120 is 5 factorial, one level too far.', 'Start the multiplication at n = 4, not 5.'),
    ],
    lesson: 'fact(4) expands to 4 * fact(3) = 4 * 3 * fact(2) = 4 * 3 * 2 * fact(1), and fact(1) hits the base case returning 1. Multiplying back up gives 4 * 3 * 2 * 1 = 24. Tracing recursion by expanding the calls and then collapsing the returns is the most reliable way to predict the result.',
    source,
    generated: true,
  },
  {
    id: 7726031,
    chapter: 'Recursion',
    title: 'Naive recursive Fibonacci cost',
    prompt: 'The naive recursion fib(n) = fib(n-1) + fib(n-2) (with base cases) is slow for large n. Why?',
    correct: 'It recomputes the same subproblems many times, leading to exponential time roughly O(2^n)',
    wrong: [
      miss('Each call uses too much memory, making it O(n!) in space', 'The depth of the call stack is O(n), so space is linear, not factorial; the problem is repeated work in time.', 'The issue is duplicated computation, not space.'),
      miss('Recursion is always slower than loops by a constant factor', 'The slowness here is algorithmic (exponential blowup), not a mere constant overhead of recursion.', 'A constant factor would not explain the dramatic slowdown.'),
      miss('Python cannot handle additions of large numbers efficiently', 'The slowness comes from the number of calls, not big-integer arithmetic; the same recurrence is slow in any language.', 'Count how many times fib is called.'),
    ],
    lesson: 'Naive Fibonacci recursion branches into two calls that overlap heavily, recomputing the same fib(k) values an exponential number of times, giving roughly O(2^n) time. Memoization (caching results) or an iterative bottom-up approach reduces this to O(n) by computing each subproblem only once.',
    source,
    generated: true,
  },
  {
    id: 7726032,
    chapter: 'Recursion',
    title: 'Recursion and the call stack',
    prompt: 'What runtime resource is consumed by deeply nested recursive calls, and what error results when it is exhausted?',
    correct: 'The call stack; exhausting it causes a stack overflow (in Python, a RecursionError)',
    wrong: [
      miss('The heap; exhausting it causes a stack overflow', 'Function call frames live on the call stack, not the heap; running out of stack space, not heap, causes the overflow.', 'Which memory region holds call frames?'),
      miss('The CPU cache; exhausting it causes a cache miss error', 'There is no cache exhaustion error; recursion depth is limited by stack frames, and cache misses are not errors.', 'Cache misses slow things but are not errors.'),
      miss('The network buffer; exhausting it causes a timeout', 'Recursion is purely local computation and does not use network buffers.', 'Recursion does not involve the network.'),
    ],
    lesson: 'Each recursive call pushes a new frame onto the call stack to hold its parameters and local state. Very deep recursion can exhaust the stack, producing a stack overflow; Python guards against this with a recursion limit and raises RecursionError. Iterative solutions or tail-call-friendly languages avoid deep stacks.',
    source,
    generated: true,
  },

  // ---------------- Chapter 8: Time Complexity ----------------
  {
    id: 7726033,
    chapter: 'Time Complexity',
    title: 'Big-O of binary search',
    prompt: 'What is the worst-case time complexity of binary search on a sorted array of n elements?',
    correct: 'O(log n), because each comparison halves the remaining search space',
    wrong: [
      miss('O(n), because in the worst case you might examine every element', 'Binary search discards half the remaining range each step, so it examines only about log2(n) elements, not all n.', 'How many times can you halve n before reaching 1?'),
      miss('O(n log n), the same as efficient sorting', 'O(n log n) is the cost of comparison-based sorting; a single binary search is much cheaper at O(log n).', 'Searching once is far cheaper than sorting.'),
      miss('O(1), because the array is already sorted', 'Sorting helps, but locating a target still requires repeated halving, which is logarithmic, not constant.', 'A sorted array still needs several comparisons.'),
    ],
    lesson: 'Binary search repeatedly compares the target to the middle element and discards the half that cannot contain it, cutting the search space in half each step. After about log2(n) steps the range shrinks to a single element, giving O(log n) worst-case time. This requires the input to be sorted.',
    source,
    generated: true,
  },
  {
    id: 7726034,
    chapter: 'Time Complexity',
    title: 'Dropping constants and lower-order terms',
    prompt: 'An algorithm performs 3n^2 + 5n + 100 operations. What is its Big-O time complexity?',
    correct: 'O(n^2), because Big-O keeps only the dominant term and drops constant factors',
    wrong: [
      miss('O(3n^2), because the leading coefficient must be preserved', 'Big-O ignores constant factors, so 3n^2 is written O(n^2); the coefficient is dropped.', 'Constant multipliers are not part of Big-O.'),
      miss('O(n^2 + n + 1), because every term contributes', 'Big-O keeps only the fastest-growing term; lower-order terms are dropped as n grows large.', 'Only the dominant term survives.'),
      miss('O(100), because the constant 100 is the largest number shown', 'For large n the n^2 term dwarfs the constant 100; Big-O describes growth as n increases, not the value at small n.', 'Asymptotic growth, not the biggest constant, matters.'),
    ],
    lesson: 'Big-O notation describes how running time grows as the input size increases, so it keeps only the dominant (fastest-growing) term and discards constant factors and lower-order terms. 3n^2 + 5n + 100 is therefore O(n^2). This abstraction lets us compare algorithms by their scaling behavior rather than machine-specific constants.',
    source,
    generated: true,
  },
  {
    id: 7726035,
    chapter: 'Time Complexity',
    title: 'Linear vs constant time',
    prompt: 'Looking up a value by key in a well-implemented hash table is typically O(1), while finding a value in an unsorted list is O(n). What does this difference describe?',
    correct: 'How the operation’s cost scales with the number of elements n',
    wrong: [
      miss('How many bytes of memory each structure uses per element', 'Big-O of these operations describes time scaling, not per-element memory usage.', 'These complexities are about time, not space.'),
      miss('The exact number of nanoseconds each lookup takes', 'Big-O gives asymptotic scaling, not concrete wall-clock times, which depend on hardware.', 'Big-O is about growth rate, not literal timings.'),
      miss('Which structure is easier for a programmer to write', 'Complexity classes describe performance scaling, not coding difficulty.', 'Ease of coding is unrelated to Big-O.'),
    ],
    lesson: 'Time complexity describes how an operation’s cost grows with input size n. O(1) means the cost stays roughly constant regardless of n (as with hash-table lookups), while O(n) means cost grows proportionally to n (as with scanning an unsorted list). This scaling, not any specific runtime, is what Big-O captures.',
    source,
    generated: true,
  },
  {
    id: 7726036,
    chapter: 'Time Complexity',
    title: 'Identifying nested-loop complexity',
    prompt: 'A function compares every pair of elements in a list of n items using two nested loops over the list. What is its time complexity?',
    correct: 'O(n^2), because each of the n elements is paired with each of the n elements',
    wrong: [
      miss('O(n), because it loops over the list', 'A single pass would be O(n), but two nested passes multiply to n times n, which is O(n^2).', 'Count how the inner loop multiplies the work.'),
      miss('O(log n), because comparing pairs narrows things down', 'Comparing all pairs does not halve the work; it examines every combination, giving quadratic time.', 'Nothing here halves the search space.'),
      miss('O(2n), because there are two loops', 'Nested loops multiply rather than add; two nested loops give a product, not a sum.', 'Nesting multiplies the counts.'),
    ],
    lesson: 'When an algorithm pairs every element with every other element using nested loops, the inner loop runs n times for each of the n outer iterations, yielding n^2 total comparisons and O(n^2) time. Recognizing nested iteration over the same data is a quick way to spot quadratic complexity.',
    source,
    generated: true,
  },
  {
    id: 7726037,
    chapter: 'Time Complexity',
    title: 'Space complexity vs time complexity',
    prompt: 'An algorithm runs in O(n) time but creates a new list holding a copy of all n inputs. What is its space complexity?',
    correct: 'O(n), because it allocates additional memory proportional to the input size',
    wrong: [
      miss('O(1), because space complexity always equals the number of variables', 'Space complexity counts memory that grows with input; copying n items uses O(n) extra space, not constant.', 'A copy of n items takes memory proportional to n.'),
      miss('O(n) only if the input is also O(n) in time, otherwise O(1)', 'Space and time complexities are measured independently; the time class does not set the space class.', 'Time and space are analyzed separately.'),
      miss('O(log n), because lists are stored efficiently', 'Storing n elements requires at least O(n) space regardless of efficiency; logarithmic space would not fit n copies.', 'You cannot store n items in log n space.'),
    ],
    lesson: 'Space complexity measures how the extra memory an algorithm uses grows with input size, separate from time complexity. Building a structure that holds all n inputs requires O(n) auxiliary space. An in-place algorithm that uses only a few variables would be O(1) space even if it runs in O(n) time.',
    source,
    generated: true,
  },

  // ---------------- Chapter 9: Debugging ----------------
  {
    id: 7726038,
    chapter: 'Debugging',
    title: 'Syntax error vs runtime error',
    prompt: 'A Python program fails before running any code, reporting an unexpected token. Which category does this error belong to?',
    correct: 'A syntax error, detected when the source is parsed, before execution begins',
    wrong: [
      miss('A runtime error, because the program tried to run and failed', 'Syntax errors are caught during parsing, before any statement executes; the program never starts running.', 'Did any code actually execute?'),
      miss('A logic error, because the output is wrong', 'A logic error produces wrong results from code that runs fine; here the code does not run at all.', 'Logic errors run but give wrong answers.'),
      miss('A linker error, because modules failed to connect', 'Python is not statically linked like C; an unexpected token is a parsing problem, not linking.', 'This is about parsing the source, not linking.'),
    ],
    lesson: 'Errors fall into broad categories: syntax errors violate the grammar and are caught when the source is parsed, before execution. Runtime errors occur while the program runs (like dividing by zero). Logic errors let the program run to completion but produce incorrect results. Identifying the category narrows down where to look.',
    source,
    generated: true,
  },
  {
    id: 7726039,
    chapter: 'Debugging',
    title: 'Reading a stack trace',
    prompt: 'When a Python exception prints a traceback, where in the listing is the line that actually triggered the error usually found?',
    correct: 'At the bottom of the traceback, the innermost call closest to where the exception was raised',
    wrong: [
      miss('At the top, which always shows the exact failing line', 'The top of a Python traceback is the outermost call (often the program entry point), not the line that raised the error.', 'Python prints most-recent call last.'),
      miss('In the middle, halfway down the list of calls', 'There is no fixed middle position; the failing line is the last frame shown, not a midpoint.', 'Look at the final frame, not a middle one.'),
      miss('Tracebacks do not indicate which line failed, only the error type', 'Tracebacks list file names and line numbers for each frame, pinpointing where the error occurred.', 'Each frame names a file and line number.'),
    ],
    lesson: 'A Python traceback lists the chain of function calls leading to an exception, with the note "most recent call last," so the innermost frame where the error was actually raised appears at the bottom, just above the error message. Reading from the bottom up tells you where to start investigating and how you got there.',
    source,
    generated: true,
  },
  {
    id: 7726040,
    chapter: 'Debugging',
    title: 'Rubber-duck and isolating bugs',
    prompt: 'A function gives wrong output. Which debugging strategy most directly helps locate which line introduces the bad value?',
    correct: 'Add print statements (or use a debugger breakpoint) to inspect variable values at intermediate steps',
    wrong: [
      miss('Rewrite the entire function from scratch immediately', 'Rewriting without understanding the cause may reintroduce the bug and discards working code; first locate the fault.', 'Find the cause before rewriting.'),
      miss('Run the program many more times until it eventually works', 'A deterministic logic bug will not fix itself by rerunning; the same input yields the same wrong output.', 'Repetition does not change deterministic behavior.'),
      miss('Delete the failing test so the program reports success', 'Removing the test hides the bug rather than fixing it, leaving the defect in the code.', 'Suppressing the symptom is not a fix.'),
    ],
    lesson: 'To isolate where a value goes wrong, inspect intermediate state by printing variables or stepping through with a debugger, comparing actual values against what you expect at each point. This binary-search-like narrowing pinpoints the offending line. Rewriting blindly or rerunning a deterministic program does not reveal the cause.',
    source,
    generated: true,
  },
  {
    id: 7726041,
    chapter: 'Debugging',
    title: 'Reproducing a bug',
    prompt: 'Why is reliably reproducing a bug usually the first step before attempting to fix it?',
    correct: 'A reproducible case lets you observe the failure consistently and confirm whether a fix actually works',
    wrong: [
      miss('Reproducing the bug automatically reveals the fix in the error message', 'Reproducing shows the failure but rarely states the fix; the message just helps you investigate.', 'Reproduction enables diagnosis, it does not hand you the fix.'),
      miss('You cannot read the source code until the bug is reproduced', 'Source code is readable at any time; reproduction is about observing and verifying behavior, not access.', 'Reading code does not depend on reproduction.'),
      miss('Reproducing a bug is only necessary for hardware faults, not software', 'Reproducibility is central to software debugging too, especially for intermittent issues.', 'Software bugs also need a reliable repro.'),
    ],
    lesson: 'Reliable reproduction is foundational to debugging: if you can trigger the failure on demand, you can study it under controlled conditions, form and test hypotheses, and verify that a proposed fix actually eliminates it. Intermittent, hard-to-reproduce bugs are notoriously difficult precisely because you cannot confirm a fix.',
    source,
    generated: true,
  },

  // ---------------- Chapter 10: Data Structures Basics ----------------
  {
    id: 7726042,
    chapter: 'Data Structures Basics',
    title: 'Stack LIFO behavior',
    prompt: 'A stack supports push and pop. If you push 1, then 2, then 3, and then pop once, which value is removed?',
    correct: '3, because a stack is last-in, first-out (LIFO)',
    wrong: [
      miss('1, because the first item pushed is removed first', 'That describes a queue (FIFO); a stack removes the most recently pushed item.', 'Stacks return the most recent item, not the oldest.'),
      miss('2, because pop removes the middle element', 'pop always removes the top (most recent) element, not an item from the middle.', 'pop operates on the top only.'),
      miss('All three, because pop empties the stack', 'A single pop removes exactly one element, the top one.', 'One pop removes one item.'),
    ],
    lesson: 'A stack is a last-in, first-out structure: the most recently pushed element is the first one popped. After pushing 1, 2, 3, the top is 3, so a single pop returns 3. Stacks model function call frames, undo history, and expression evaluation. A queue, by contrast, is first-in, first-out.',
    source,
    generated: true,
  },
  {
    id: 7726043,
    chapter: 'Data Structures Basics',
    title: 'Queue FIFO behavior',
    prompt: 'Items arrive at a queue in the order A, B, C using enqueue. In what order will three dequeue operations return them?',
    correct: 'A, B, C, because a queue is first-in, first-out (FIFO)',
    wrong: [
      miss('C, B, A, because the last item in comes out first', 'That is stack (LIFO) behavior; a queue returns items in the order they arrived.', 'A queue preserves arrival order.'),
      miss('B, A, C, because the middle item has priority', 'A plain queue has no priority; it strictly follows arrival order.', 'Ordinary queues do not reorder by priority.'),
      miss('A, A, A, because dequeue keeps returning the front item', 'Each dequeue removes the front item, so subsequent dequeues return the next items, not the same one repeatedly.', 'Dequeue removes the item it returns.'),
    ],
    lesson: 'A queue is first-in, first-out: elements are removed in the same order they were added. Enqueuing A, B, C and then dequeuing three times returns A, B, C. Queues model waiting lines, task scheduling, and breadth-first traversal. This contrasts with a stack, which reverses the order (LIFO).',
    source,
    generated: true,
  },
  {
    id: 7726044,
    chapter: 'Data Structures Basics',
    title: 'Dictionary/hash map lookup',
    prompt: 'Why can a Python dictionary look up a value by its key in average-case O(1) time?',
    correct: 'It hashes the key to compute an index into an internal table, jumping directly to the slot',
    wrong: [
      miss('It keeps keys sorted and uses binary search, which is O(1)', 'Sorted keys with binary search would be O(log n), and Python dicts do not keep keys sorted for lookup.', 'Hashing, not sorting, drives dict lookup.'),
      miss('It scans every key until it finds a match, which is fast for small dicts', 'A linear scan is O(n); dicts avoid scanning by computing the slot directly from the hash.', 'Direct addressing beats scanning.'),
      miss('It stores values in a contiguous array indexed by insertion order', 'Insertion order is preserved for iteration, but lookups are by hashed key, not by an integer position.', 'Lookup uses the key’s hash, not its insertion index.'),
    ],
    lesson: 'A hash map applies a hash function to the key to compute a table index, so it can go straight to the slot holding the value, giving average-case O(1) lookup, insertion, and deletion. Collisions are resolved by probing or chaining. Worst-case behavior degrades to O(n) if many keys collide, but good hashing makes that rare.',
    source,
    generated: true,
  },
  {
    id: 7726045,
    chapter: 'Data Structures Basics',
    title: 'Set membership and uniqueness',
    prompt: 'What two properties make a set the right choice for checking whether an item has already been seen while scanning a list?',
    correct: 'Sets store only unique elements and support average O(1) membership testing',
    wrong: [
      miss('Sets keep elements in sorted order and allow indexing by position', 'Standard hash sets are unordered and do not support positional indexing; their strength is fast membership.', 'Sets are unordered and not indexable.'),
      miss('Sets allow duplicate elements and preserve insertion order', 'A defining property of a set is that it discards duplicates, so it does not allow them.', 'Uniqueness is the whole point of a set.'),
      miss('Sets store key-value pairs like a dictionary for fast lookup', 'A set stores standalone elements, not key-value pairs; that describes a dictionary.', 'Sets hold elements, not pairs.'),
    ],
    lesson: 'A set holds only distinct elements and, being backed by a hash table, tests membership in average O(1) time. These two properties make it ideal for deduplication and for tracking which items have already been seen, replacing a slow O(n) scan of a list with a fast hash lookup.',
    source,
    generated: true,
  },

  // ---------------- Chapter 11: Boolean Logic and Operators ----------------
  {
    id: 7726046,
    chapter: 'Boolean Logic and Operators',
    title: 'De Morgan’s law',
    prompt: 'Which expression is equivalent to not (a and b) by De Morgan’s law?',
    correct: '(not a) or (not b)',
    wrong: [
      miss('(not a) and (not b)', 'De Morgan’s law flips and to or when distributing the negation; this keeps and, which is the negation of (a or b) instead.', 'Negating an and should produce an or.'),
      miss('not a or b', 'Operator precedence makes this (not a) or b, which is not the correct transformation and changes the meaning.', 'Both operands must be negated, with parentheses.'),
      miss('a or b', 'Dropping the negation entirely changes the logic; the negation must be distributed, not removed.', 'You cannot simply delete the not.'),
    ],
    lesson: 'De Morgan’s laws state that not (a and b) equals (not a) or (not b), and not (a or b) equals (not a) and (not b). Negation distributes across the operands while flipping and to or (and vice versa). These identities are essential for simplifying conditions and reasoning about negated boolean expressions.',
    source,
    generated: true,
  },
  {
    id: 7726047,
    chapter: 'Boolean Logic and Operators',
    title: 'Integer division vs true division',
    prompt: 'In Python 3, what is the difference between 7 / 2 and 7 // 2?',
    correct: '7 / 2 gives 3.5 (float true division) and 7 // 2 gives 3 (floor division)',
    wrong: [
      miss('Both give 3.5, because division always produces a float', 'The // operator performs floor division, discarding the fractional part to yield 3, not 3.5.', 'One operator floors the result.'),
      miss('Both give 3, because Python truncates all division to integers', 'The single-slash / always returns a float in Python 3, so 7 / 2 is 3.5, not 3.', 'Single-slash division keeps the decimal part.'),
      miss('7 / 2 gives 3 and 7 // 2 gives 3.5', 'This swaps the two operators; / is true division (3.5) and // is floor division (3).', 'Match each operator to the correct result.'),
    ],
    lesson: 'In Python 3 the / operator performs true division and always returns a float (7 / 2 is 3.5), while // performs floor division, rounding toward negative infinity to give an integer-valued result (7 // 2 is 3). For negative operands, floor division rounds down, so -7 // 2 is -4, not -3.',
    source,
    generated: true,
  },
  {
    id: 7726048,
    chapter: 'Boolean Logic and Operators',
    title: 'Modulo operator use',
    prompt: 'Which condition correctly tests whether an integer n is even using the modulo operator?',
    correct: 'n % 2 == 0',
    wrong: [
      miss('n % 2 == 1', 'A remainder of 1 when divided by 2 indicates an odd number, not an even one.', 'Even numbers leave remainder 0.'),
      miss('n / 2 == 0', 'This is only true when n is 0; dividing does not test parity the way the remainder does.', 'Use the remainder, not the quotient.'),
      miss('n // 2 == 0', 'Floor division equals 0 only for n in the range 0 to 1; it does not test evenness.', 'Floor division gives the quotient, not parity.'),
    ],
    lesson: 'The modulo operator % returns the remainder of a division. An integer n is even exactly when n % 2 == 0, since even numbers divide evenly by 2. Modulo is widely used for parity checks, wrapping indices around an array (i % length), and grouping by cycles.',
    source,
    generated: true,
  },
  {
    id: 7726049,
    chapter: 'Boolean Logic and Operators',
    title: 'Operator precedence',
    prompt: 'In most languages, how is the expression 2 + 3 * 4 evaluated?',
    correct: '14, because multiplication has higher precedence than addition and is done first',
    wrong: [
      miss('20, because operators are applied strictly left to right', 'Left-to-right order is overridden by precedence: multiplication binds tighter than addition, so 3 * 4 happens first.', 'Precedence overrides simple left-to-right.'),
      miss('24, because the whole expression is multiplied together', 'Only 3 * 4 is multiplied; the result is then added to 2, giving 14, not 24.', 'Addition is not turned into multiplication.'),
      miss('14, but only if you add parentheses around 3 * 4', 'No parentheses are needed; precedence rules already cause 3 * 4 to be evaluated first.', 'Precedence applies automatically.'),
    ],
    lesson: 'Operator precedence determines the order in which operations are applied when parentheses are absent. Multiplication and division have higher precedence than addition and subtraction, so 2 + 3 * 4 evaluates 3 * 4 first, then adds 2, giving 14. Use parentheses when you want a different order or to make intent explicit.',
    source,
    generated: true,
  },

  // ---------------- Chapter 12: Algorithms and Problem Solving ----------------
  {
    id: 7726050,
    chapter: 'Algorithms and Problem Solving',
    title: 'Linear search applicability',
    prompt: 'You must find a target in an unsorted array of n elements. Which search approach is appropriate, and what is its worst-case cost?',
    correct: 'Linear search, scanning each element, with worst-case O(n)',
    wrong: [
      miss('Binary search, with worst-case O(log n)', 'Binary search requires sorted data; on an unsorted array it can miss the target and is not valid.', 'Binary search has a precondition.'),
      miss('Hashing the array once, giving O(1) search with no extra cost', 'Building a hash structure first costs O(n) and extra memory; a one-off search of an unsorted array is just linear.', 'Setting up a hash is not free.'),
      miss('Linear search, but with worst-case O(log n)', 'Linear search may inspect every element, so its worst case is O(n), not logarithmic.', 'Scanning all n elements is linear.'),
    ],
    lesson: 'Binary search only works on sorted data because it relies on discarding half the range based on ordering. For an unsorted array you must use linear search, examining elements one by one, which is O(n) in the worst case (the target is last or absent). Sorting first to enable binary search costs O(n log n), worthwhile only for many searches.',
    source,
    generated: true,
  },
  {
    id: 7726051,
    chapter: 'Algorithms and Problem Solving',
    title: 'Comparison-based sorting lower bound',
    prompt: 'Efficient general-purpose sorting algorithms like merge sort and heapsort run in what worst-case time on n elements?',
    correct: 'O(n log n), which is the best achievable for comparison-based sorting',
    wrong: [
      miss('O(n), because each element is placed in one pass', 'No comparison-based sort can beat O(n log n) in the worst case; a single linear pass cannot fully order arbitrary data.', 'There is a proven lower bound for comparison sorts.'),
      miss('O(n^2), the same as bubble sort', 'O(n^2) is the cost of simple sorts like bubble or insertion sort; merge sort and heapsort are faster at O(n log n).', 'Efficient sorts beat the quadratic ones.'),
      miss('O(log n), because the work halves each level', 'The recursion has log n levels but each level does O(n) work, multiplying to O(n log n), not O(log n).', 'Multiply the per-level work by the number of levels.'),
    ],
    lesson: 'Any sorting algorithm that orders elements solely by pairwise comparisons needs at least O(n log n) comparisons in the worst case, a proven lower bound. Merge sort and heapsort achieve this bound. Simpler sorts like bubble and insertion sort are O(n^2). Non-comparison sorts (like counting or radix sort) can beat O(n log n) under special conditions.',
    source,
    generated: true,
  },
  {
    id: 7726052,
    chapter: 'Algorithms and Problem Solving',
    title: 'Pseudocode and algorithm design',
    prompt: 'What is the main purpose of writing pseudocode before implementing an algorithm in a specific language?',
    correct: 'To express the logic and steps clearly without being constrained by a particular language’s syntax',
    wrong: [
      miss('To produce executable code that runs directly on any computer', 'Pseudocode is not executable; it is an informal description meant to be read by humans, then translated to real code.', 'Can a computer run pseudocode as written?'),
      miss('To make the program run faster than writing it directly', 'Pseudocode does not affect runtime performance; it is a planning and communication tool.', 'Planning aids clarity, not speed.'),
      miss('To automatically detect all bugs before coding', 'Pseudocode helps reason about logic but does not automatically find every bug; testing the real code is still needed.', 'It clarifies design, it does not verify correctness.'),
    ],
    lesson: 'Pseudocode is an informal, language-agnostic description of an algorithm’s steps, written to clarify the logic and communicate the design to humans. By ignoring syntactic details, it lets you focus on correctness and structure before committing to a specific language. It is then translated into real, executable code.',
    source,
    generated: true,
  },
])
