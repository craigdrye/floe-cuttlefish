import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe col-ap-computer-science top-up'

export const colApComputerScienceHigh103TopUpQuestions: Question[] = makeQuestionBank('High', [
  // ---- Unit 1: Primitive Types ----
  {
    id: 7991000,
    chapter: 'Primitive Types',
    title: 'Integer division result',
    prompt: 'In Java, what value does the expression 17 / 5 evaluate to?',
    mentorHint: 'Check the data types of both operands before deciding whether division is integer or floating point.',
    correct: '3',
    wrong: [
      miss('3.4', 'Both operands are int, so Java performs integer division and discards the fractional part rather than producing a double.', 'Check the data types of both operands before deciding whether division is integer or floating point.'),
      miss('4', 'Integer division truncates toward zero; it does not round the quotient up.', 'Truncation drops the decimal portion, it does not round.'),
      miss('2', 'This underestimates the quotient; 5 goes into 17 three whole times, not two.', 'Count how many whole times 5 fits into 17.'),
    ],
    lesson: 'When both operands of / are of type int, Java performs integer division, keeping only the whole-number part of the quotient and discarding any remainder. So 17 / 5 is 3, while the remainder 2 would be produced by 17 % 5. To get 3.4 you would need at least one operand to be a double.',
    source,
    generated: true,
  },
  {
    id: 7991001,
    chapter: 'Primitive Types',
    title: 'Modulo with operands',
    prompt: 'What is the value of the expression 23 % 4 in Java?',
    mentorHint: 'The % operator returns the remainder, not the quotient.',
    correct: '3',
    wrong: [
      miss('5', 'This is the integer quotient 23 / 4, not the remainder that % returns.', 'The % operator returns the remainder, not the quotient.'),
      miss('5.75', 'The % operator returns an integer remainder for int operands, not a decimal result.', 'Modulo produces the leftover after whole division.'),
      miss('0', 'A remainder of 0 would only occur if 4 divided 23 exactly, which it does not.', 'Find what is left over after subtracting multiples of 4.'),
    ],
    lesson: 'The modulo operator % returns the remainder after integer division. Since 4 goes into 23 five times (giving 20), the remainder is 23 - 20 = 3. The modulo operator is heavily used in AP CS A for tasks like testing evenness (n % 2 == 0) and extracting digits.',
    source,
    generated: true,
  },
  {
    id: 7991002,
    chapter: 'Primitive Types',
    title: 'Implicit type casting',
    prompt: 'Given int x = 7; double y = x / 2;, what value is stored in y?',
    mentorHint: 'Evaluate the right side fully before the assignment widens it.',
    correct: '3.0',
    wrong: [
      miss('3.5', 'The division x / 2 happens first using int arithmetic (giving 3); only afterward is the result widened to double.', 'Evaluate the right side fully before the assignment widens it.'),
      miss('3', 'y is declared as a double, so its stored value is 3.0, not the int 3.', 'A double variable always stores a value with a decimal component.'),
      miss('4.0', 'Integer division truncates rather than rounding, so 7 / 2 is 3, not 4.', 'Integer division drops the fraction; it does not round up.'),
    ],
    lesson: 'In x / 2, both operands are int, so Java computes 3 using integer division before the assignment. That int 3 is then implicitly widened to the double 3.0. To get 3.5 you would cast first, e.g. (double) x / 2, forcing floating-point division.',
    source,
    generated: true,
  },
  {
    id: 7991003,
    chapter: 'Primitive Types',
    title: 'Compound assignment',
    prompt: 'After int n = 10; n += 3 * 2;, what is the value of n?',
    mentorHint: 'Apply operator precedence: multiplication happens before the compound add.',
    correct: '16',
    wrong: [
      miss('26', 'The multiplication 3 * 2 is evaluated before the addition, so 6 is added to 10, not 16.', 'Apply operator precedence: multiplication happens before the compound add.'),
      miss('60', 'n += 3 * 2 adds 3 * 2 to n; it does not multiply n by the whole expression.', 'The += operator adds the right side, it does not multiply.'),
      miss('15', 'This adds only the 3 and ignores the multiplication by 2.', 'Evaluate the full right-hand expression 3 * 2 first.'),
    ],
    lesson: 'The statement n += 3 * 2 is shorthand for n = n + (3 * 2). Multiplication has higher precedence than the addition, so 3 * 2 = 6 is computed first, then added to 10 to give 16. Compound assignment operators always combine the existing value with the full right-hand expression.',
    source,
    generated: true,
  },
  {
    id: 7991004,
    chapter: 'Primitive Types',
    title: 'Overflow of int range',
    prompt: 'Why can a Java int variable not reliably store the value 3,000,000,000?',
    mentorHint: 'Recall the actual order of magnitude of Integer.MAX_VALUE.',
    correct: 'It exceeds the maximum value an int can hold (about 2.1 billion)',
    wrong: [
      miss('int cannot store values above one million', 'The int maximum is roughly 2.147 billion (2^31 - 1), far above one million.', 'Recall the actual order of magnitude of Integer.MAX_VALUE.'),
      miss('int can only store negative numbers', 'int stores both positive and negative whole numbers across its range.', 'int is a signed type covering positives and negatives.'),
      miss('int requires a decimal point to store large numbers', 'int stores whole numbers and never uses a decimal point; size is the issue, not format.', 'The limitation is range, not whether a decimal is present.'),
    ],
    lesson: 'A Java int is a 32-bit signed integer with a range of about -2.147 billion to +2.147 billion (Integer.MIN_VALUE to Integer.MAX_VALUE). Assigning 3,000,000,000 overflows this range, so a long (64-bit) would be needed instead. Recognizing integer overflow is part of understanding primitive type limits.',
    source,
    generated: true,
  },
  // ---- Unit 2: Using Objects ----
  {
    id: 7991005,
    chapter: 'Using Objects',
    title: 'String length method',
    prompt: 'For String s = "Banana";, what does s.length() return?',
    mentorHint: 'Count every character including repeated letters.',
    correct: '6',
    wrong: [
      miss('5', 'This undercounts; "Banana" has six characters: B, a, n, a, n, a.', 'Count every character including repeated letters.'),
      miss('7', 'length() counts characters, not characters plus one; there is no extra slot.', 'There is no off-by-one extra count in length().'),
      miss('"6"', 'length() returns an int value 6, not the String "6".', 'Note the return type of length() is a number, not text.'),
    ],
    lesson: 'The String method length() returns the number of characters in the string as an int. "Banana" contains six characters, so it returns 6. Note that String indices run from 0 to length() - 1, so the last valid index here is 5.',
    source,
    generated: true,
  },
  {
    id: 7991006,
    chapter: 'Using Objects',
    title: 'Substring extraction',
    prompt: 'For String s = "Computer";, what does s.substring(2, 5) return?',
    mentorHint: 'The second argument is exclusive, not inclusive.',
    correct: '"mpu"',
    wrong: [
      miss('"mput"', 'substring(2, 5) stops before index 5, so the character at index 5 is excluded.', 'The second argument is exclusive, not inclusive.'),
      miss('"omp"', 'Indexing starts at 0, so index 2 is the third character m, not the second character o.', 'Remember that the first character is at index 0.'),
      miss('"put"', 'This starts at the wrong index; substring begins at index 2, which is m.', 'Count from index 0 to locate the starting character.'),
    ],
    lesson: 'substring(start, end) returns characters from index start up to but not including index end. For "Computer", index 2 is m, index 3 is p, index 4 is u, and index 5 (t) is excluded, giving "mpu". The end index being exclusive is a common source of off-by-one errors.',
    source,
    generated: true,
  },
  {
    id: 7991007,
    chapter: 'Using Objects',
    title: 'Comparing String contents',
    prompt: 'What is the correct way to test whether two String variables a and b contain the same sequence of characters?',
    mentorHint: 'Reference equality is not the same as content equality for objects.',
    correct: 'a.equals(b)',
    wrong: [
      miss('a == b', 'The == operator compares object references (memory addresses), not the character contents.', 'Reference equality is not the same as content equality for objects.'),
      miss('a.compareTo(b)', 'compareTo returns an int ordering value; it is not a boolean test of equality on its own.', 'Equality tests should return a boolean directly.'),
      miss('a.same(b)', 'There is no same method in the String class; equals is the standard method.', 'Recall the actual method name used for content comparison.'),
    ],
    lesson: 'Strings are objects, so == only checks whether two variables refer to the same object in memory. To compare the actual character content you must use the equals method, e.g. a.equals(b), which returns true when both strings hold identical characters in the same order.',
    source,
    generated: true,
  },
  {
    id: 7991008,
    chapter: 'Using Objects',
    title: 'Wrapper class and Math',
    prompt: 'Which expression returns a random integer from 1 to 6 inclusive, simulating a die roll?',
    mentorHint: 'Add an offset to shift the lower bound up to 1.',
    correct: '(int)(Math.random() * 6) + 1',
    wrong: [
      miss('(int)(Math.random() * 6)', 'Math.random() returns a value in [0.0, 1.0), so this gives 0 to 5, not 1 to 6.', 'Add an offset to shift the lower bound up to 1.'),
      miss('(int)(Math.random() * 7) + 1', 'Multiplying by 7 produces 0 to 6, then +1 yields 1 to 7, exceeding the desired maximum.', 'The multiplier should equal the count of possible values.'),
      miss('(int)(Math.random()) * 6 + 1', 'Casting Math.random() to int first gives 0, so this always evaluates to 1.', 'Cast the whole scaled product, not Math.random() alone.'),
    ],
    lesson: 'Math.random() returns a double in the range [0.0, 1.0). Multiplying by 6 gives [0.0, 6.0), and casting to int truncates to an integer 0 through 5. Adding 1 shifts the range to 1 through 6. The general pattern for a range of size n starting at low is (int)(Math.random() * n) + low.',
    source,
    generated: true,
  },
  {
    id: 7991009,
    chapter: 'Using Objects',
    title: 'indexOf behaviour',
    prompt: 'For String s = "mississippi";, what does s.indexOf("ss") return?',
    mentorHint: 'indexOf finds the earliest match, scanning left to right.',
    correct: '2',
    wrong: [
      miss('5', 'indexOf returns the index of the first occurrence, not a later one; the first "ss" begins at index 2.', 'indexOf finds the earliest match, scanning left to right.'),
      miss('3', 'Indexing starts at 0, so the first "ss" begins at index 2, not 3.', 'Count characters starting from index 0.'),
      miss('-1', 'A return of -1 means the substring was not found, but "ss" does occur in the string.', 'Check whether the substring actually appears before assuming it is absent.'),
    ],
    lesson: 'indexOf(str) returns the index of the first character of the first occurrence of str, or -1 if it is absent. In "mississippi" the characters at indices 2 and 3 are s and s, so the first "ss" begins at index 2. A second "ss" occurs later, but indexOf reports only the earliest one.',
    source,
    generated: true,
  },
  // ---- Unit 3: Boolean Expressions and if Statements ----
  {
    id: 7991010,
    chapter: 'Boolean Expressions and if Statements',
    title: 'Short-circuit evaluation',
    prompt: 'In the expression (x != 0) && (10 / x > 2), why does this avoid a division-by-zero error when x is 0?',
    mentorHint: 'Consider what actually prevents the right side from running.',
    correct: 'The && operator short-circuits: if the left operand is false, the right operand is never evaluated',
    wrong: [
      miss('Java automatically replaces division by zero with 0', 'Java throws an ArithmeticException for integer division by zero; it does not silently substitute a value.', 'Consider what actually prevents the right side from running.'),
      miss('The && operator always evaluates the right side first', '&& evaluates the left operand first; that ordering is what enables the guard.', 'Think about which operand is checked first.'),
      miss('Parentheses force both sides to be evaluated together', 'Parentheses group sub-expressions but do not force both sides of && to evaluate.', 'Grouping does not override short-circuit behaviour.'),
    ],
    lesson: 'The logical AND operator && uses short-circuit evaluation: it evaluates the left operand first, and if that is false the whole expression must be false, so the right operand is skipped. Here, when x is 0 the test x != 0 is false, so 10 / x is never computed, avoiding the division-by-zero error. This guarding idiom is extremely common.',
    source,
    generated: true,
  },
  {
    id: 7991011,
    chapter: 'Boolean Expressions and if Statements',
    title: 'De Morgan\'s law',
    prompt: 'Which expression is equivalent to !(a && b) by De Morgan\'s law?',
    mentorHint: 'Recall that negation flips both the operands and the connective.',
    correct: '!a || !b',
    wrong: [
      miss('!a && !b', 'Negating an AND yields an OR of the negations, not an AND; this is the negation of (a || b) instead.', 'Recall that negation flips both the operands and the connective.'),
      miss('a || b', 'This drops the negations entirely; both operands must be negated when distributing the not.', 'The not must be applied to each individual operand.'),
      miss('!(a || b)', 'This is a different expression; De Morgan converts the connective, it does not just move the not outside an OR.', 'Negating an AND should produce an OR after distribution.'),
    ],
    lesson: 'De Morgan\'s laws state that !(a && b) is equivalent to !a || !b, and !(a || b) is equivalent to !a && !b. When you push a negation inside, you negate each operand and switch AND to OR (or OR to AND). This is essential for simplifying and correctly rewriting boolean conditions.',
    source,
    generated: true,
  },
  {
    id: 7991012,
    chapter: 'Boolean Expressions and if Statements',
    title: 'Nested if-else flow',
    prompt: 'Given if (n > 0) { if (n > 100) print("big"); } else print("nonpositive");, what is printed when n is 50?',
    mentorHint: 'Check whether the inner condition is satisfied.',
    correct: 'Nothing is printed',
    wrong: [
      miss('"big"', '"big" prints only when n > 100; 50 does not exceed 100, so the inner if is false.', 'Check whether the inner condition is satisfied.'),
      miss('"nonpositive"', 'The else belongs to the outer if; since n > 0 is true for 50, the else branch is skipped.', 'Match the else to the if it pairs with.'),
      miss('"big" and "nonpositive"', 'Only one branch of an if-else can execute, and here the inner if also fails.', 'An if-else executes at most one of its two branches.'),
    ],
    lesson: 'For n = 50, the outer condition n > 0 is true, so the else (nonpositive) is skipped. Inside, n > 100 is false, so "big" is not printed. With no statement for the case 0 < n <= 100, nothing is printed. The else binds to the nearest unmatched if, which here is the outer one.',
    source,
    generated: true,
  },
  {
    id: 7991013,
    chapter: 'Boolean Expressions and if Statements',
    title: 'Equivalent boolean conditions',
    prompt: 'Which condition is logically equivalent to !(x >= 5)?',
    mentorHint: 'Determine whether the boundary value 5 satisfies the original condition.',
    correct: 'x < 5',
    wrong: [
      miss('x <= 5', 'This includes the value 5, but !(x >= 5) excludes 5 since 5 >= 5 is true.', 'Determine whether the boundary value 5 satisfies the original condition.'),
      miss('x > 5', 'This excludes values below 5, which the original condition includes.', 'Negating >= should produce the opposite ordering relation.'),
      miss('x != 5', 'This is true for many values at or above 5 (like 6), which the original excludes.', 'Negation of a comparison is another single comparison, not an inequality test.'),
    ],
    lesson: 'The negation of x >= 5 is x < 5, because the complement of greater-than-or-equal is strictly less-than. The boundary value 5 satisfies x >= 5, so it must NOT satisfy the negation, confirming x < 5 (which excludes 5) rather than x <= 5.',
    source,
    generated: true,
  },
  {
    id: 7991014,
    chapter: 'Boolean Expressions and if Statements',
    title: 'Comparing objects vs primitives',
    prompt: 'When comparing two int variables for equality in an if statement, which operator should be used?',
    mentorHint: 'Recall that primitives and objects compare differently.',
    correct: 'The == operator',
    wrong: [
      miss('The equals method', 'equals is used for objects like String; primitives such as int use ==.', 'Recall that primitives and objects compare differently.'),
      miss('The = operator', 'A single = is assignment, not comparison, and is generally illegal in an int boolean test.', 'Equality testing uses a double equals sign.'),
      miss('The compareTo method', 'compareTo applies to objects implementing Comparable, not to primitive int values.', 'Primitives do not have methods.'),
    ],
    lesson: 'For primitive types such as int, double, char, and boolean, the == operator correctly compares values. The equals method and compareTo are for objects (like String). Be careful not to confuse = (assignment) with == (equality test); the single = does not compare.',
    source,
    generated: true,
  },
  // ---- Unit 4: Iteration ----
  {
    id: 7991015,
    chapter: 'Iteration',
    title: 'Counting loop iterations',
    prompt: 'How many times does the body of for (int i = 3; i <= 12; i++) execute?',
    mentorHint: 'Check whether the final value 12 is included by the condition.',
    correct: '10',
    wrong: [
      miss('9', 'This treats the loop as exclusive of 12, but i <= 12 includes the iteration where i is 12.', 'Check whether the final value 12 is included by the condition.'),
      miss('12', 'The loop starts at 3, not 0 or 1, so it runs fewer than 12 times.', 'Count from the starting value, not from zero.'),
      miss('11', 'The count is end minus start plus one: 12 - 3 + 1 = 10, not 11.', 'Use the formula end - start + 1 for an inclusive range.'),
    ],
    lesson: 'For a loop running while i goes from a starting value to an inclusive ending value with step 1, the number of iterations is end - start + 1. Here that is 12 - 3 + 1 = 10. Because the condition is i <= 12, the iteration with i = 12 does execute.',
    source,
    generated: true,
  },
  {
    id: 7991016,
    chapter: 'Iteration',
    title: 'Accumulation in a loop',
    prompt: 'What is the final value of sum after int sum = 0; for (int i = 1; i <= 4; i++) sum += i;?',
    mentorHint: 'The loop adds every value of i, not just the final one.',
    correct: '10',
    wrong: [
      miss('4', 'This is only the last value of i added once; sum accumulates 1 + 2 + 3 + 4.', 'The loop adds every value of i, not just the final one.'),
      miss('24', '24 is the product 1*2*3*4; the loop adds the values rather than multiplying them.', 'The += operator accumulates a sum, not a product.'),
      miss('6', '6 is 1 + 2 + 3, but the loop runs through i = 4 inclusive.', 'Include the i = 4 iteration in the total.'),
    ],
    lesson: 'This loop accumulates a running total. With i taking values 1, 2, 3, and 4, the statement sum += i adds each in turn: 0 + 1 + 2 + 3 + 4 = 10. Accumulator patterns initialize a variable before the loop and update it on each iteration.',
    source,
    generated: true,
  },
  {
    id: 7991017,
    chapter: 'Iteration',
    title: 'Nested loop total iterations',
    prompt: 'How many times does the innermost statement run in for (int i = 0; i < 3; i++) for (int j = 0; j < 4; j++) print(i + j);?',
    mentorHint: 'Nested loops multiply their iteration counts.',
    correct: '12',
    wrong: [
      miss('7', 'The loops are nested (multiplied), not run sequentially (added), so it is 3 * 4 rather than 3 + 4.', 'Nested loops multiply their iteration counts.'),
      miss('3', 'This counts only the outer loop and ignores the four inner iterations per pass.', 'Account for the inner loop running fully each outer pass.'),
      miss('16', 'The outer loop runs 3 times, not 4; the product is 3 * 4 = 12.', 'Recheck each loop bound carefully.'),
    ],
    lesson: 'In nested loops, the inner loop completes fully for each iteration of the outer loop, so the total number of inner executions is the product of the iteration counts. Here the outer loop runs 3 times and the inner loop runs 4 times each pass, giving 3 * 4 = 12 executions.',
    source,
    generated: true,
  },
  {
    id: 7991018,
    chapter: 'Iteration',
    title: 'while loop termination',
    prompt: 'What is the value of n after int n = 1; while (n < 100) n *= 2;?',
    mentorHint: 'Continue doubling until the condition becomes false.',
    correct: '128',
    wrong: [
      miss('64', '64 satisfies n < 100, so the loop runs once more and doubles it to 128 before stopping.', 'Continue doubling until the condition becomes false.'),
      miss('100', 'The loop doubles n by powers of 2 and never lands exactly on 100.', 'Track the actual sequence of values n takes.'),
      miss('256', 'After n reaches 128 the condition n < 100 is false, so the loop stops before doubling again.', 'Stop as soon as the condition fails.'),
    ],
    lesson: 'Starting at 1, n doubles each pass: 1, 2, 4, 8, 16, 32, 64, then 128. After n becomes 128 the condition 128 < 100 is false, so the loop ends with n = 128. The final doubling that exceeds the limit still happens because the condition is checked before, not during, the body.',
    source,
    generated: true,
  },
  {
    id: 7991019,
    chapter: 'Iteration',
    title: 'Reversing a string with a loop',
    prompt: 'To build the reverse of a String s using a loop and accumulating into a result variable, which traversal correctly produces the reversed string?',
    mentorHint: 'Decide which direction of traversal reverses the order.',
    correct: 'Loop i from s.length() - 1 down to 0, appending s.substring(i, i + 1) each time',
    wrong: [
      miss('Loop i from 0 up to s.length() - 1, appending s.substring(i, i + 1)', 'Traversing front to back appends characters in original order, producing the same string, not its reverse.', 'Decide which direction of traversal reverses the order.'),
      miss('Loop i from s.length() down to 1, appending s.substring(i, i + 1)', 'Starting at index s.length() is out of bounds; the last valid index is s.length() - 1.', 'Recall the maximum valid index of a string.'),
      miss('Loop i from 0 to s.length(), appending s.charAt(i)', 'Accessing index s.length() throws an out-of-bounds error, and forward order does not reverse.', 'The valid indices stop at length minus one.'),
    ],
    lesson: 'To reverse a string by accumulation, start at the last index (length - 1) and move down to 0, appending each character to a growing result. Going front-to-back instead keeps the original order. Always remember valid indices run 0 through length() - 1, so starting at length() would be out of bounds.',
    source,
    generated: true,
  },
  {
    id: 7991020,
    chapter: 'Iteration',
    title: 'Off-by-one in loop bound',
    prompt: 'A programmer writes for (int i = 0; i <= arr.length; i++) to traverse an array. What runtime error will occur?',
    mentorHint: 'Identify the error type caused by an invalid index.',
    correct: 'ArrayIndexOutOfBoundsException when i equals arr.length',
    wrong: [
      miss('NullPointerException on the first iteration', 'A NullPointerException arises from using a null reference, not from an index that is one too large.', 'Identify the error type caused by an invalid index.'),
      miss('No error; the extra iteration is harmless', 'Accessing arr[arr.length] is out of bounds and throws an exception, so it is not harmless.', 'Consider what index is reached on the final iteration.'),
      miss('ArithmeticException due to division', 'No division occurs here; the fault is an invalid array index.', 'Match the exception to the actual operation that fails.'),
    ],
    lesson: 'Valid array indices range from 0 to arr.length - 1. The condition i <= arr.length lets i reach arr.length, and accessing arr[arr.length] throws an ArrayIndexOutOfBoundsException. The correct loop bound is i < arr.length. This off-by-one error is one of the most common iteration bugs.',
    source,
    generated: true,
  },
  // ---- Unit 5: Writing Classes ----
  {
    id: 7991021,
    chapter: 'Writing Classes',
    title: 'Purpose of a constructor',
    prompt: 'What is the primary role of a constructor in a Java class?',
    mentorHint: 'Constructors run when an object is made, not removed.',
    correct: 'To initialize the state (instance variables) of a newly created object',
    wrong: [
      miss('To destroy an object and free its memory', 'Java uses garbage collection for cleanup; constructors create and initialize, they do not destroy.', 'Constructors run when an object is made, not removed.'),
      miss('To define the class\'s static methods', 'Static methods are declared separately; a constructor sets up an instance\'s data.', 'Constructors are about per-object initialization.'),
      miss('To return the object\'s data type', 'A constructor has no return type and does not report the class type.', 'Note that constructors do not declare a return type.'),
    ],
    lesson: 'A constructor is a special method, named after the class and with no return type, that runs when an object is created with new. Its job is to initialize the object\'s instance variables to a valid starting state. Java handles memory cleanup automatically through garbage collection, so there is no destructor.',
    source,
    generated: true,
  },
  {
    id: 7991022,
    chapter: 'Writing Classes',
    title: 'Encapsulation with private',
    prompt: 'Why are instance variables typically declared private in a well-designed class?',
    mentorHint: 'Think about the design principle, not performance.',
    correct: 'To enforce encapsulation, controlling access through public methods rather than direct external modification',
    wrong: [
      miss('To make the program run faster at runtime', 'Access modifiers affect visibility and design, not execution speed.', 'Think about the design principle, not performance.'),
      miss('Because private variables use less memory than public ones', 'Visibility does not change how much memory a variable occupies.', 'Memory usage is unrelated to access modifiers.'),
      miss('Because Java requires all instance variables to be private', 'Java permits public instance variables; private is a convention chosen for good design.', 'It is a best practice, not a language requirement.'),
    ],
    lesson: 'Declaring instance variables private enforces encapsulation: external code cannot read or change them directly and must go through public accessor (getter) and mutator (setter) methods. This lets the class validate changes and hide internal representation, making code safer and easier to maintain. It is a design choice, not a language mandate.',
    source,
    generated: true,
  },
  {
    id: 7991023,
    chapter: 'Writing Classes',
    title: 'The this keyword',
    prompt: 'In a constructor with the parameter String name and an instance variable also named name, what does this.name = name; accomplish?',
    mentorHint: 'The left side of = is the destination of the assignment.',
    correct: 'It assigns the parameter value to the instance variable, distinguishing the two same-named variables',
    wrong: [
      miss('It assigns the instance variable to the parameter', 'this.name is the instance variable (the assignment target); name on the right is the parameter (the source).', 'The left side of = is the destination of the assignment.'),
      miss('It creates a new variable called this.name', 'this.name refers to the existing instance variable; it does not create a new one.', 'this references the current object\'s existing field.'),
      miss('It does nothing because the names are identical', 'this distinguishes the field from the parameter, so the assignment is meaningful, not a no-op.', 'this resolves the naming ambiguity between field and parameter.'),
    ],
    lesson: 'When a parameter shadows an instance variable of the same name, the unqualified name refers to the parameter. The keyword this refers to the current object, so this.name is the instance variable. The statement this.name = name copies the parameter\'s value into the object\'s field, resolving the ambiguity.',
    source,
    generated: true,
  },
  {
    id: 7991024,
    chapter: 'Writing Classes',
    title: 'Static versus instance variables',
    prompt: 'A class declares private static int count;. What is true about this variable?',
    mentorHint: 'Recall what the static keyword means for storage.',
    correct: 'There is one shared copy of count for the entire class, regardless of how many objects exist',
    wrong: [
      miss('Each object gets its own separate copy of count', 'A static variable is shared by all instances; only non-static instance variables get per-object copies.', 'Recall what the static keyword means for storage.'),
      miss('count can only be accessed inside constructors', 'A static variable can be accessed in any method of the class (and elsewhere per its modifier), not only constructors.', 'Static does not restrict access to constructors.'),
      miss('count is automatically recalculated each time an object is created', 'Static variables are not auto-recalculated; they change only when code explicitly modifies them.', 'Values change only through explicit assignment.'),
    ],
    lesson: 'A static variable belongs to the class itself rather than to individual objects, so all instances share a single copy. This makes static fields useful for things like counting how many objects have been created. By contrast, each object has its own independent copy of every non-static instance variable.',
    source,
    generated: true,
  },
  {
    id: 7991025,
    chapter: 'Writing Classes',
    title: 'Method return type matching',
    prompt: 'A method is declared as public double average(int total, int n). Which statement could legally appear as its return statement?',
    mentorHint: 'A non-void method must return a value of its declared type.',
    correct: 'return (double) total / n;',
    wrong: [
      miss('return;', 'A method with return type double must return a double value, not nothing.', 'A non-void method must return a value of its declared type.'),
      miss('return "average";', 'A String cannot be returned where a double is expected; the types are incompatible.', 'Match the returned value\'s type to the declared return type.'),
      miss('return total / n without any cast', 'Without a cast both operands are int, so this returns a truncated int value rather than a true average.', 'Force floating-point division to preserve the fractional part.'),
    ],
    lesson: 'A method declared to return double must return a double-compatible expression. Casting total to double forces floating-point division so the fractional part is preserved, e.g. (double) total / n. Returning nothing is only valid for void methods, and returning a String would be a type mismatch.',
    source,
    generated: true,
  },
  {
    id: 7991026,
    chapter: 'Writing Classes',
    title: 'Overriding toString',
    prompt: 'What is the benefit of overriding the toString method in a class?',
    mentorHint: 'toString affects printing, not equality comparison.',
    correct: 'It provides a meaningful String representation used automatically when the object is printed',
    wrong: [
      miss('It allows the object to be compared with the == operator', 'toString controls textual representation, not how == compares references.', 'toString affects printing, not equality comparison.'),
      miss('It makes all the object\'s instance variables public', 'Overriding toString does not change access modifiers of fields.', 'toString does not alter visibility.'),
      miss('It is required for every class to compile', 'Classes inherit a default toString from Object and compile fine without overriding it.', 'Object already provides a default toString.'),
    ],
    lesson: 'Every class inherits a toString method from Object, but the default returns an unhelpful hash-based string. Overriding toString lets you return a meaningful description, which Java calls automatically when you print the object or concatenate it with a String. It does not affect equality comparison or field visibility.',
    source,
    generated: true,
  },
  // ---- Unit 6: Array ----
  {
    id: 7991027,
    chapter: 'Array',
    title: 'Default array element values',
    prompt: 'After int[] data = new int[5];, what is the value of data[0] before any assignment?',
    mentorHint: 'Distinguish primitive defaults from object reference defaults.',
    correct: '0',
    wrong: [
      miss('null', 'null is the default for object reference elements, but int is a primitive whose default is 0.', 'Distinguish primitive defaults from object reference defaults.'),
      miss('It is undefined and contains random memory', 'Java initializes array elements to type defaults rather than leaving them as arbitrary garbage.', 'Java guarantees a defined default value.'),
      miss('1', 'The default value for numeric primitives is 0, not 1.', 'Recall the standard default for int.'),
    ],
    lesson: 'When you create an array with new, Java initializes every element to the default value for its type: 0 for int, 0.0 for double, false for boolean, and null for object references. So data[0] is 0 immediately after creation, before any explicit assignment.',
    source,
    generated: true,
  },
  {
    id: 7991028,
    chapter: 'Array',
    title: 'Enhanced for loop limitation',
    prompt: 'Why can a standard enhanced for loop (for-each) not be used to modify the elements of an int array in place?',
    mentorHint: 'The loop can read values; the issue is writing.',
    correct: 'The loop variable is a copy of each element, so assigning to it does not change the array',
    wrong: [
      miss('Enhanced for loops cannot access array elements at all', 'A for-each loop does read each element; it simply cannot write back through the loop variable.', 'The loop can read values; the issue is writing.'),
      miss('Enhanced for loops only work on ArrayList, not arrays', 'For-each loops work on both arrays and many collections including ArrayList.', 'For-each is valid for arrays.'),
      miss('Enhanced for loops run in reverse order', 'A for-each loop traverses elements in forward order; ordering is not why modification fails.', 'Traversal direction is not the obstacle here.'),
    ],
    lesson: 'In a for-each loop over primitives, the loop variable receives a copy of each element\'s value. Assigning a new value to that copy does not affect the underlying array. To modify array elements in place you must use an indexed for loop and assign through arr[i].',
    source,
    generated: true,
  },
  {
    id: 7991029,
    chapter: 'Array',
    title: 'Finding the maximum',
    prompt: 'What is the standard way to initialize the running maximum when scanning an array of unknown values to find the largest element?',
    mentorHint: 'Consider what happens with all-negative data.',
    correct: 'Set max to the first element, then compare it with each remaining element',
    wrong: [
      miss('Set max to 0, then compare with each element', 'If all elements are negative, 0 wrongly remains the maximum since no element exceeds it.', 'Consider what happens with all-negative data.'),
      miss('Set max to Integer.MAX_VALUE, then take the larger each time', 'No element can exceed Integer.MAX_VALUE, so max never updates and stays incorrect.', 'Starting at the largest possible value defeats the comparison.'),
      miss('Set max to the array length, then compare with each element', 'The array length is unrelated to the element values and is an arbitrary starting point.', 'Initialize from actual data, not the size.'),
    ],
    lesson: 'A robust way to find a maximum is to initialize the running max to the first element (a real value from the data) and then compare it against each remaining element, updating when a larger one is found. Initializing to 0 fails for all-negative arrays, and starting at Integer.MAX_VALUE prevents any update.',
    source,
    generated: true,
  },
  {
    id: 7991030,
    chapter: 'Array',
    title: 'Array length property',
    prompt: 'How do you obtain the number of elements in an array named arr in Java?',
    mentorHint: 'Arrays use a property, not a method call.',
    correct: 'arr.length',
    wrong: [
      miss('arr.length()', 'length for arrays is a field accessed without parentheses, unlike the length() method of String.', 'Arrays use a property, not a method call.'),
      miss('arr.size()', 'size() is the method used by ArrayList; arrays use the length field.', 'Distinguish array syntax from ArrayList syntax.'),
      miss('length(arr)', 'There is no global length function taking the array as an argument in standard Java.', 'Access the size through the array reference itself.'),
    ],
    lesson: 'For arrays, the number of elements is given by the field arr.length, written without parentheses. This differs from String, which uses the method length() with parentheses, and from ArrayList, which uses the method size(). Mixing these three forms is a common syntax error.',
    source,
    generated: true,
  },
  {
    id: 7991031,
    chapter: 'Array',
    title: 'Linear search outcome',
    prompt: 'A linear search loops through an array looking for a target. What should the method return if the target is not present?',
    mentorHint: 'Choose a value that cannot be a valid index.',
    correct: 'A sentinel value such as -1 to indicate not found',
    wrong: [
      miss('The array length', 'Returning the length is indistinguishable from a valid result in some contexts and is not a conventional not-found signal.', 'Choose a value that cannot be a valid index.'),
      miss('0', 'Returning 0 is ambiguous because 0 is a valid index where the target might genuinely be found.', 'Avoid a return value that could be a real index.'),
      miss('The target value itself', 'Returning the target tells the caller nothing about whether or where it was found.', 'The result should communicate presence and position.'),
    ],
    lesson: 'Linear search examines each element in order; if a match is found it typically returns the index. When the target is absent, a sentinel like -1 is conventional because no valid array index is negative, so the caller can unambiguously detect the not-found case. Returning 0 or the length could be confused with legitimate results.',
    source,
    generated: true,
  },
  // ---- Unit 7: ArrayList ----
  {
    id: 7991032,
    chapter: 'ArrayList',
    title: 'ArrayList stores objects',
    prompt: 'Why is ArrayList<Integer> used instead of ArrayList<int> to store integers?',
    mentorHint: 'Consider what types generics are allowed to hold.',
    correct: 'Generic type parameters require object types, so the wrapper class Integer is used instead of the primitive int',
    wrong: [
      miss('Integer can store larger numbers than int', 'Integer wraps an int and has the same value range; the issue is that generics need an object type.', 'Consider what types generics are allowed to hold.'),
      miss('ArrayList<int> is faster but uses more memory', 'ArrayList<int> is not valid Java at all, so the comparison does not apply.', 'Recall whether primitives are permitted as type parameters.'),
      miss('int is deprecated in modern Java', 'The primitive int is fully supported; it simply cannot be a generic type argument.', 'Primitives remain valid outside generics.'),
    ],
    lesson: 'Java generics can only be parameterized with object (reference) types, not primitives, so you must use the wrapper class Integer rather than int. Autoboxing and unboxing automatically convert between int and Integer when adding to or reading from the list, so you can mostly treat the elements like ints.',
    source,
    generated: true,
  },
  {
    id: 7991033,
    chapter: 'ArrayList',
    title: 'Removing during traversal',
    prompt: 'What problem commonly occurs when you remove elements from an ArrayList while traversing it with an increasing index?',
    mentorHint: 'Focus on how indices shift after removal.',
    correct: 'Elements shift left after a removal, so the next element is skipped',
    wrong: [
      miss('The ArrayList automatically resizes to a fixed length', 'ArrayList does resize, but the traversal bug is skipping elements, not a fixed length.', 'Focus on how indices shift after removal.'),
      miss('Removing an element throws a NullPointerException', 'A valid remove does not throw a NullPointerException; the issue is index shifting.', 'Identify the logical, not exceptional, problem.'),
      miss('The removed element is duplicated at the end', 'remove deletes the element entirely; nothing is appended or duplicated.', 'remove shortens the list rather than copying anything.'),
    ],
    lesson: 'When you remove an element at index i, every later element shifts down one position to fill the gap. If your loop then increments i, you skip over the element that moved into position i. Common fixes are to not increment i after a removal, or to traverse the list from the end toward the beginning.',
    source,
    generated: true,
  },
  {
    id: 7991034,
    chapter: 'ArrayList',
    title: 'ArrayList add with index',
    prompt: 'After ArrayList<String> list holds [A, B, C], what does list.add(1, "X") produce?',
    mentorHint: 'Distinguish inserting from replacing.',
    correct: '[A, X, B, C]',
    wrong: [
      miss('[A, X, C]', 'add(index, element) inserts X and shifts existing elements right; it does not replace B.', 'Distinguish inserting from replacing.'),
      miss('[X, A, B, C]', 'Index 1 is the second position, not the front; inserting at the front would use index 0.', 'Recall that indices start at 0.'),
      miss('[A, B, X, C]', 'Inserting at index 1 places X before the element currently at index 1 (B), not after it.', 'The new element takes the specified index, pushing that element later.'),
    ],
    lesson: 'The two-argument add(index, element) inserts the element at the given index and shifts all subsequent elements one position to the right (it does not overwrite). Inserting "X" at index 1 of [A, B, C] places it between A and B, giving [A, X, B, C]. To replace an element instead, use set(index, element).',
    source,
    generated: true,
  },
  {
    id: 7991035,
    chapter: 'ArrayList',
    title: 'set versus add',
    prompt: 'What is the difference between list.set(2, "Q") and list.add(2, "Q") on an ArrayList?',
    mentorHint: 'Compare whether the list size changes.',
    correct: 'set replaces the element at index 2; add inserts a new element at index 2 and shifts others right',
    wrong: [
      miss('They are identical and produce the same result', 'set overwrites while add inserts and shifts, so they differ in effect and in resulting size.', 'Compare whether the list size changes.'),
      miss('set inserts and shifts; add replaces in place', 'This reverses the roles; set replaces and add inserts.', 'Match each method to its true behaviour.'),
      miss('Both increase the size of the list by one', 'Only add increases the size; set leaves the size unchanged.', 'Consider which operation grows the list.'),
    ],
    lesson: 'set(index, element) overwrites the existing element at that index, leaving the list size unchanged. add(index, element) inserts a new element at that index and shifts subsequent elements right, increasing the size by one. Confusing the two leads to either lost data or unexpected growth.',
    source,
    generated: true,
  },
  {
    id: 7991036,
    chapter: 'ArrayList',
    title: 'ArrayList size method',
    prompt: 'To loop over every element of an ArrayList named items by index, what should the loop condition use?',
    mentorHint: 'Recall the correct count syntax for ArrayList.',
    correct: 'i < items.size()',
    wrong: [
      miss('i < items.length', 'length is an array field; ArrayList uses the method size() with parentheses.', 'Recall the correct count syntax for ArrayList.'),
      miss('i <= items.size()', 'Valid indices run 0 to size() - 1, so i <= size() reaches an out-of-bounds index.', 'The last valid index is one less than the size.'),
      miss('i < items.size', 'size is a method that must be called with parentheses, not accessed as a field.', 'ArrayList exposes a method, not a field.'),
    ],
    lesson: 'An ArrayList reports its element count through the method size(), called with parentheses, unlike arrays which use the field length. Valid indices run from 0 to size() - 1, so the standard indexed loop uses i < items.size(). Using <= or omitting the parentheses are common errors.',
    source,
    generated: true,
  },
  // ---- Unit 8: 2D Array ----
  {
    id: 7991037,
    chapter: '2D Array',
    title: 'Element count in a 2D array',
    prompt: 'How many elements does the array declared by int[][] grid = new int[3][5]; contain in total?',
    mentorHint: 'Total elements equal rows times columns.',
    correct: '15',
    wrong: [
      miss('8', 'The dimensions are multiplied (3 * 5), not added (3 + 5), to count elements.', 'Total elements equal rows times columns.'),
      miss('3', 'This counts only the rows, ignoring the 5 columns in each row.', 'Each row contains multiple columns.'),
      miss('5', 'This counts only the columns of one row, ignoring that there are 3 rows.', 'Account for all rows, not just one.'),
    ],
    lesson: 'A 2D array declared as new int[3][5] has 3 rows and 5 columns, for 3 * 5 = 15 total elements. In Java this is really an array of 3 references, each pointing to an array of 5 ints. The number of elements is the product of the dimensions.',
    source,
    generated: true,
  },
  {
    id: 7991038,
    chapter: '2D Array',
    title: 'Row-major traversal',
    prompt: 'In the standard nested loop for (int r = 0; r < grid.length; r++) for (int c = 0; c < grid[0].length; c++), what does grid.length represent?',
    mentorHint: 'The outer length measures rows, not columns.',
    correct: 'The number of rows in the 2D array',
    wrong: [
      miss('The number of columns in the 2D array', 'grid.length is the count of rows; the column count is grid[0].length.', 'The outer length measures rows, not columns.'),
      miss('The total number of elements in the array', 'grid.length is only the row count, not rows times columns.', 'Multiply dimensions to get the total; one length alone does not.'),
      miss('The value stored in the first element', 'grid.length is a size, not a stored data value.', 'length reports a count, never an element\'s contents.'),
    ],
    lesson: 'A 2D array in Java is an array of row arrays. grid.length gives the number of rows, while grid[r].length (often grid[0].length for rectangular grids) gives the number of columns in a row. The outer loop iterates over rows and the inner loop over columns in standard row-major traversal.',
    source,
    generated: true,
  },
  {
    id: 7991039,
    chapter: '2D Array',
    title: 'Accessing a 2D element',
    prompt: 'For a 2D array grid, how do you access the element in the third row and second column?',
    mentorHint: 'Subtract one from each position because indexing is zero-based.',
    correct: 'grid[2][1]',
    wrong: [
      miss('grid[3][2]', 'Indices start at 0, so the third row is index 2 and the second column is index 1.', 'Subtract one from each position because indexing is zero-based.'),
      miss('grid[1][2]', 'This swaps row and column; the third row is index 2 and the second column is index 1.', 'The first index is the row, the second is the column.'),
      miss('grid(2, 1)', 'Java uses square brackets for array access, not parentheses with a comma.', 'Recall the bracket syntax for indexing.'),
    ],
    lesson: 'Java arrays are zero-indexed, so the third row is index 2 and the second column is index 1, accessed as grid[2][1] with the row index first. Parentheses are not used for array access, and the order is always [row][column] for the conventional layout.',
    source,
    generated: true,
  },
  {
    id: 7991040,
    chapter: '2D Array',
    title: 'Summing a 2D array',
    prompt: 'When computing the sum of all elements in a rectangular 2D int array, why is a nested loop required?',
    mentorHint: 'Two coordinates need two loop variables.',
    correct: 'The outer loop selects each row and the inner loop visits every element within that row',
    wrong: [
      miss('A single loop can sum all elements without nesting', 'A single index cannot address both the row and the column dimensions of a 2D array.', 'Two coordinates need two loop variables.'),
      miss('Nesting is only needed for non-rectangular (jagged) arrays', 'Even a perfectly rectangular 2D array needs two loops to cover both dimensions.', 'Rectangular shape still has two dimensions to traverse.'),
      miss('The nested loop reverses the array as it sums', 'Summing with a nested loop does not reorder the array; it only reads each value once.', 'Traversal for a sum does not modify the data.'),
    ],
    lesson: 'A 2D array has two dimensions, so you need two index variables to reach every element. The outer loop iterates over rows and the inner loop iterates over the columns of the current row, accumulating each grid[r][c] into a total. A single loop cannot index both dimensions simultaneously.',
    source,
    generated: true,
  },
  // ---- Unit 9: Inheritance ----
  {
    id: 7991041,
    chapter: 'Inheritance',
    title: 'The extends keyword',
    prompt: 'Which keyword is used to declare that class Dog inherits from class Animal?',
    mentorHint: 'Distinguish inheriting a class from realizing an interface.',
    correct: 'extends, as in class Dog extends Animal',
    wrong: [
      miss('implements, as in class Dog implements Animal', 'implements is used for interfaces; class-to-class inheritance uses extends.', 'Distinguish inheriting a class from realizing an interface.'),
      miss('inherits, as in class Dog inherits Animal', 'inherits is not a Java keyword; the correct keyword is extends.', 'Recall the actual reserved word in Java.'),
      miss('super, as in class Dog super Animal', 'super refers to the parent inside methods; it is not used in the class declaration.', 'super is used within code, not to declare a subclass.'),
    ],
    lesson: 'In Java, a subclass inherits from a superclass using the extends keyword, e.g. class Dog extends Animal. The keyword implements is reserved for interfaces, and super is used inside a subclass to call the parent\'s constructor or methods, not in the class header.',
    source,
    generated: true,
  },
  {
    id: 7991042,
    chapter: 'Inheritance',
    title: 'Calling the superclass constructor',
    prompt: 'Inside a subclass constructor, how do you invoke the constructor of the superclass?',
    mentorHint: 'Use the keyword that refers to the parent class.',
    correct: 'super(arguments); as the first statement of the constructor',
    wrong: [
      miss('this(arguments); as the first statement', 'this(...) calls another constructor in the same class, not the superclass constructor.', 'Use the keyword that refers to the parent class.'),
      miss('Animal(arguments); using the superclass name', 'You cannot call a constructor by class name like a method; use the super keyword.', 'Constructors are invoked with super, not by name.'),
      miss('super.constructor(arguments);', 'There is no super.constructor syntax; the parent constructor is called simply with super(...).', 'The call uses super directly, without a method name.'),
    ],
    lesson: 'A subclass constructor calls its superclass constructor using super(arguments), and this call must be the first statement in the constructor. If you omit it, Java automatically inserts a call to the no-argument super constructor. The form this(...) instead invokes a different constructor within the same class.',
    source,
    generated: true,
  },
  {
    id: 7991043,
    chapter: 'Inheritance',
    title: 'Method overriding',
    prompt: 'A subclass defines a method with the same signature as one in its superclass. What is this called, and what happens at runtime for a subclass object?',
    mentorHint: 'Same signature in a subclass is not overloading.',
    correct: 'Overriding; the subclass version is called for a subclass object',
    wrong: [
      miss('Overloading; the superclass version is always called', 'Overloading means same name but different parameter lists; same signature in a subclass is overriding.', 'Same signature in a subclass is not overloading.'),
      miss('Hiding; both versions run in sequence', 'Overriding replaces the inherited behaviour for instance methods; both do not run together.', 'Only one version executes for an instance method call.'),
      miss('Overriding; but the superclass version still runs', 'Once overridden, the subclass version is the one invoked unless super is explicitly used.', 'The subclass version takes precedence for its objects.'),
    ],
    lesson: 'When a subclass provides a method with the same signature as an inherited method, it overrides that method. For an object of the subclass, the overriding version is invoked. Overloading is different: it means multiple methods sharing a name but with different parameter lists. The subclass can still reach the parent version explicitly via super.methodName().',
    source,
    generated: true,
  },
  {
    id: 7991044,
    chapter: 'Inheritance',
    title: 'Polymorphism and dynamic dispatch',
    prompt: 'Given Animal a = new Dog(); where Dog overrides speak(), which version of speak() runs when a.speak() is called?',
    mentorHint: 'Distinguish the declared type from the runtime object.',
    correct: 'The Dog version, because the actual object type determines the method at runtime',
    wrong: [
      miss('The Animal version, because a is declared as type Animal', 'The declared type controls what methods are visible, but the actual object type determines which overridden version runs.', 'Distinguish the declared type from the runtime object.'),
      miss('Neither, because this assignment causes a compile error', 'Assigning a Dog to an Animal reference is legal upcasting and compiles fine.', 'A subclass object may be referenced by a superclass variable.'),
      miss('Both versions run, parent first then child', 'Only the actual object\'s overriding method runs; the parent version does not run automatically.', 'A single overridden call invokes one version.'),
    ],
    lesson: 'This illustrates polymorphism with dynamic dispatch. Although a is declared as Animal, it refers to a Dog object, so the overridden Dog version of speak() executes at runtime. The declared (reference) type determines which methods can be called, but the actual object type determines which overriding implementation runs.',
    source,
    generated: true,
  },
  {
    id: 7991045,
    chapter: 'Inheritance',
    title: 'Object as universal superclass',
    prompt: 'In Java, what is true about every class that does not explicitly extend another class?',
    mentorHint: 'Java provides a default parent when none is named.',
    correct: 'It implicitly extends the Object class',
    wrong: [
      miss('It has no superclass at all', 'Every class except Object itself has a superclass; unspecified ones default to Object.', 'Java provides a default parent when none is named.'),
      miss('It cannot be used in polymorphic assignments', 'Such classes still participate in polymorphism through their Object superclass and any interfaces.', 'Inheriting from Object enables broad polymorphic use.'),
      miss('It automatically extends the first class defined in the file', 'There is no rule tying a class to the first class in a file; the default parent is Object.', 'The default superclass is always Object.'),
    ],
    lesson: 'Object is the root of Java\'s class hierarchy. Any class that does not specify a superclass with extends implicitly extends Object, inheriting methods such as toString, equals, and hashCode. This is why every object can be assigned to a reference of type Object.',
    source,
    generated: true,
  },
  {
    id: 7991046,
    chapter: 'Inheritance',
    title: 'super to extend behaviour',
    prompt: 'A subclass overrides a method but wants to also run the superclass version inside the override. How is that done?',
    mentorHint: 'this refers to the current (overriding) implementation.',
    correct: 'Call super.methodName(arguments) within the overriding method',
    wrong: [
      miss('Call this.methodName(arguments)', 'this.methodName refers to the overriding version itself and would cause infinite recursion.', 'this refers to the current (overriding) implementation.'),
      miss('Call Superclass.methodName(arguments) using the class name', 'Instance methods are not invoked through the class name like static methods.', 'Use super, not the class name, for inherited instance methods.'),
      miss('It is impossible to reach the superclass version after overriding', 'super provides explicit access to the inherited implementation even after overriding.', 'There is a keyword that reaches the parent version.'),
    ],
    lesson: 'Inside an overriding method, super.methodName(arguments) explicitly calls the superclass\'s version. This lets a subclass extend rather than fully replace inherited behaviour, for example running the parent\'s logic and then adding more. Using this.methodName would call the override itself, risking infinite recursion.',
    source,
    generated: true,
  },
  // ---- Unit 10: Recursion ----
  {
    id: 7991047,
    chapter: 'Recursion',
    title: 'Role of the base case',
    prompt: 'Why must a recursive method include a base case?',
    mentorHint: 'Focus on stopping, not on performance.',
    correct: 'It provides a condition under which the method stops calling itself, preventing infinite recursion',
    wrong: [
      miss('It makes the method run faster than iteration', 'A base case ensures termination; it is not a speed optimization over loops.', 'Focus on stopping, not on performance.'),
      miss('It is where the method\'s parameters are first declared', 'Parameters are declared in the method header, not specifically in the base case.', 'The base case is about ending recursion, not declaring parameters.'),
      miss('It allows the method to call other methods', 'Calling other methods is unrelated to the purpose of a base case.', 'The base case governs self-termination.'),
    ],
    lesson: 'A recursive method calls itself on progressively smaller or simpler input. The base case is the condition for which the method returns directly without recursing, halting the chain of calls. Without it, the recursion would never stop and would eventually cause a StackOverflowError.',
    source,
    generated: true,
  },
  {
    id: 7991048,
    chapter: 'Recursion',
    title: 'Tracing a factorial',
    prompt: 'Given int fact(int n) { if (n <= 1) return 1; return n * fact(n - 1); }, what does fact(4) return?',
    mentorHint: 'Factorial is a product, not a sum.',
    correct: '24',
    wrong: [
      miss('10', '10 is 4 + 3 + 2 + 1; factorial multiplies the values rather than adding them.', 'Factorial is a product, not a sum.'),
      miss('12', '12 is only 4 * 3; the recursion continues multiplying down through fact(2) and fact(1).', 'Follow the recursion all the way to the base case.'),
      miss('4', 'This returns only n without multiplying by the recursive results from smaller inputs.', 'Each call multiplies n by the result of fact(n - 1).'),
    ],
    lesson: 'The method computes n factorial: fact(4) = 4 * fact(3) = 4 * 3 * fact(2) = 4 * 3 * 2 * fact(1) = 4 * 3 * 2 * 1 = 24. The base case n <= 1 returns 1, ending the recursion. Each level multiplies n by the result of the smaller subproblem.',
    source,
    generated: true,
  },
  {
    id: 7991049,
    chapter: 'Recursion',
    title: 'Recursion versus iteration',
    prompt: 'Which statement best describes the relationship between recursion and iteration?',
    mentorHint: 'Consider whether the two approaches differ in raw capability.',
    correct: 'Any problem solvable by iteration can also be solved by recursion, and vice versa',
    wrong: [
      miss('Recursion can solve problems that iteration fundamentally cannot', 'Both are computationally equivalent; either approach can solve the same problems.', 'Consider whether the two approaches differ in raw capability.'),
      miss('Iteration always uses more memory than recursion', 'Recursion typically uses more memory due to the call stack; iteration is often lighter.', 'Recall what recursion adds for each pending call.'),
      miss('Recursion never terminates while iteration always does', 'Both can terminate or fail to terminate depending on whether they are written correctly.', 'Correctness, not the technique itself, governs termination.'),
    ],
    lesson: 'Recursion and iteration are equally powerful; any iterative solution can be rewritten recursively and vice versa. The trade-off is practical: recursion can express certain problems more naturally but consumes stack memory for each pending call, while iteration is often more memory-efficient. Either can loop forever if written incorrectly.',
    source,
    generated: true,
  },
  {
    id: 7991050,
    chapter: 'Recursion',
    title: 'Recursive sum of array',
    prompt: 'A recursive method sums an array from index i to the end via return arr[i] + sumFrom(arr, i + 1);. What should the base case return when i equals arr.length?',
    mentorHint: 'There is no element to add at that index.',
    correct: '0',
    wrong: [
      miss('arr[i]', 'When i equals arr.length there is no valid element at that index, so accessing arr[i] is out of bounds.', 'There is no element to add at that index.'),
      miss('1', 'Adding 1 as the base contributes an extra value, corrupting the sum.', 'The neutral value for addition is 0.'),
      miss('arr.length', 'Returning the length adds an unrelated value into the sum.', 'The base case should not contribute to the total.'),
    ],
    lesson: 'When the index reaches arr.length, every element has already been added, so the base case should contribute nothing further to the sum. The identity element for addition is 0, so returning 0 correctly terminates the recursion without altering the accumulated total. Returning arr[i] would be an out-of-bounds access.',
    source,
    generated: true,
  },
  {
    id: 7991051,
    chapter: 'Recursion',
    title: 'Binary search recursion depth',
    prompt: 'Recursive binary search halves the search interval each call. For a sorted array of 1000 elements, roughly how many comparisons are needed in the worst case?',
    mentorHint: 'The work is logarithmic, not linear in half the size.',
    correct: 'About 10, since log base 2 of 1000 is approximately 10',
    wrong: [
      miss('About 500, half of the array size', 'Binary search halves the remaining interval each step rather than checking half the elements once.', 'The work is logarithmic, not linear in half the size.'),
      miss('About 1000, one comparison per element', 'Checking every element is linear search; binary search examines far fewer by halving.', 'Binary search avoids visiting every element.'),
      miss('About 100, one tenth of the array', 'The count is logarithmic (log2 of 1000), which is about 10, not 100.', 'Use a logarithm base 2, not a fixed fraction.'),
    ],
    lesson: 'Binary search discards half of the remaining elements on each comparison, so the number of steps is about log base 2 of n. For n = 1000, log2(1000) is roughly 10, meaning at most about 10 comparisons. This logarithmic behaviour makes binary search vastly faster than linear search on large sorted data.',
    source,
    generated: true,
  },
  {
    id: 7991052,
    chapter: 'Recursion',
    title: 'StackOverflowError cause',
    prompt: 'What typically causes a StackOverflowError in a recursive method?',
    mentorHint: 'Distinguish value overflow from call-stack overflow.',
    correct: 'The recursion does not reach a base case, so calls keep stacking until memory is exhausted',
    wrong: [
      miss('The method returns a value that is too large for an int', 'An overly large return value causes integer overflow, not a stack overflow of calls.', 'Distinguish value overflow from call-stack overflow.'),
      miss('The method uses too many local variables', 'A few local variables do not cause a stack overflow; uncontrolled recursion depth does.', 'The problem is the depth of pending calls.'),
      miss('The base case returns the wrong data type', 'A type mismatch is a compile error, not a runtime StackOverflowError.', 'Type errors are caught before running.'),
    ],
    lesson: 'Each method call adds a frame to the call stack. If a recursive method fails to reach its base case (for example, because the recursive call does not move toward the base condition), calls accumulate without returning and eventually exhaust the stack, throwing a StackOverflowError. The fix is ensuring the recursion always progresses toward a reachable base case.',
    source,
    generated: true,
  },
  // ---- Cross-cutting: Algorithms and program design ----
  {
    id: 7991053,
    chapter: 'Iteration',
    title: 'Selection sort behaviour',
    prompt: 'In selection sort applied to an array, what happens on each pass through the unsorted portion?',
    mentorHint: 'Identify which algorithm swaps neighbours repeatedly.',
    correct: 'The smallest remaining element is found and swapped into the next sorted position',
    wrong: [
      miss('Adjacent elements are repeatedly swapped until none are out of order', 'That describes bubble sort, which compares neighbours, not selection sort\'s single placement per pass.', 'Identify which algorithm swaps neighbours repeatedly.'),
      miss('Each element is inserted into its correct place among the already-sorted elements', 'That describes insertion sort; selection sort instead selects an extreme element each pass.', 'Distinguish selecting a minimum from inserting into place.'),
      miss('The array is split in half and each half is sorted recursively', 'That describes merge sort; selection sort is an in-place comparison sort, not divide-and-conquer.', 'Recursion-by-halving is a different algorithm.'),
    ],
    lesson: 'Selection sort repeatedly scans the unsorted part of the array to find the minimum element, then swaps it into the first unsorted position, growing the sorted region by one each pass. This contrasts with bubble sort (swapping neighbours), insertion sort (inserting into a sorted prefix), and merge sort (recursive halving).',
    source,
    generated: true,
  },
  {
    id: 7991054,
    chapter: 'Boolean Expressions and if Statements',
    title: 'Testing divisibility',
    prompt: 'Which boolean expression correctly tests whether an int n is evenly divisible by 3?',
    mentorHint: 'Use the remainder, not the quotient, to test divisibility.',
    correct: 'n % 3 == 0',
    wrong: [
      miss('n / 3 == 0', 'n / 3 is the quotient; it equals 0 only for small n, not for divisibility.', 'Use the remainder, not the quotient, to test divisibility.'),
      miss('n % 3 == 1', 'A remainder of 1 means n is one more than a multiple of 3, the opposite of evenly divisible.', 'Even divisibility means a remainder of zero.'),
      miss('n * 3 == 0', 'Multiplying by 3 equals 0 only when n is 0; it does not test divisibility.', 'Divisibility is about the remainder of division.'),
    ],
    lesson: 'A number n is evenly divisible by 3 exactly when dividing n by 3 leaves no remainder. The modulo operator gives that remainder, so n % 3 == 0 is the correct test. Using the quotient (n / 3) or other operators does not capture divisibility.',
    source,
    generated: true,
  },
])
