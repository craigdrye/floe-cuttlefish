import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe col-computers-and-the-internet top-up'

export const colComputersAndTheInternetHigh103TopUpAQuestions: Question[] = makeQuestionBank('High', [
  // ---- Chapter 1: How the Web Works ----
  {
    id: 7917000,
    chapter: 'How the Web Works',
    title: 'Who sends the page',
    prompt: 'When you type a URL and press Enter, which machine sends the HTML document back to your browser?',
    correct: 'The web server that hosts the site',
    wrong: [
      miss('Your own browser generates the HTML locally', 'The browser requests and renders the page; it does not author the site\'s content from nothing.', 'Ask which machine actually stores the website\'s files.'),
      miss('The DNS server delivers the HTML file', 'DNS only translates the domain name into an IP address; it does not serve page content.', 'Separate name lookup from content delivery.'),
      miss('Your internet service provider writes the page on demand', 'The ISP forwards traffic between you and the server; it is not the source of the page.', 'Think about which party owns and hosts the site.'),
    ],
    lesson: 'The web follows a request-response model. Your browser (the client) sends an HTTP request to a web server, and the server responds with the requested resource, typically an HTML document plus its CSS, JavaScript, and images. The browser then renders that text into the visual page you see.',
    source,
    generated: true,
  },
  {
    id: 7917001,
    chapter: 'How the Web Works',
    title: 'Browser receives text, not a picture',
    prompt: 'A common misconception is that the server sends a finished image of the page. What does the server actually send for a typical web page?',
    correct: 'Text-based files (HTML, CSS, JS) that the browser turns into the visual page',
    wrong: [
      miss('A single rendered screenshot of the page', 'If the server sent a flat image, you could not select text, follow links, or have it adapt to your screen.', 'Consider why you can highlight and copy text on a page.'),
      miss('Compiled machine code that the browser executes directly', 'HTML and CSS are not compiled to machine code; they are markup the browser parses.', 'Markup describes content; it is not a CPU program.'),
      miss('Only the images, while the browser invents the text', 'The browser does not invent content; the server supplies the markup and text.', 'The server provides the source the browser renders.'),
    ],
    lesson: 'A server delivers text-based source files. The browser parses the HTML into a structure, applies CSS to style it, and runs any JavaScript, then composites pixels on screen. Because the page is text the browser renders, content can be selected, links are live, and the layout can respond to different devices.',
    source,
    generated: true,
  },
  {
    id: 7917002,
    chapter: 'How the Web Works',
    title: 'HTTP is not HTML',
    prompt: 'What is the difference between HTTP and HTML?',
    correct: 'HTTP is the protocol for transferring resources; HTML is the markup language describing a page',
    wrong: [
      miss('They are two names for the same web technology', 'They operate at different layers: one moves data, the other structures content.', 'One is about transfer, the other about content.'),
      miss('HTTP styles the page and HTML transfers it', 'Styling is CSS, not HTTP, and transfer is HTTP, not HTML.', 'Match each acronym to transfer versus structure.'),
      miss('HTML is the secure version of HTTP', 'The secure version of HTTP is HTTPS, not HTML.', 'Watch the final letter of each acronym.'),
    ],
    lesson: 'HTTP (HyperText Transfer Protocol) is the set of rules governing how a browser and server exchange requests and responses. HTML (HyperText Markup Language) is the language used to write the structure of a page. HTTP is how the page travels; HTML is what the page is made of.',
    source,
    generated: true,
  },
  {
    id: 7917003,
    chapter: 'How the Web Works',
    title: 'Reading a 404',
    prompt: 'A browser receives an HTTP 404 status code. What does this most directly mean?',
    correct: 'The requested resource could not be found on the server',
    wrong: [
      miss('The server is broken and crashed internally', 'A server-side failure returns a 500-range code, not 404.', 'Recall which status range signals server errors versus missing resources.'),
      miss('The page loaded successfully', 'A successful response uses 200; 404 signals a problem.', 'Success codes are in the 200 range.'),
      miss('Your internet connection is offline', 'A 404 is a response from a reachable server, so you are connected.', 'You must reach the server to get any HTTP code back.'),
    ],
    lesson: 'HTTP status codes report the outcome of a request. 200 means success, 3xx codes indicate redirection, 4xx codes are client errors such as 404 (Not Found) where the resource does not exist at that path, and 5xx codes are server errors. A 404 means the server was reached but could not find what you asked for.',
    source,
    generated: true,
  },
  {
    id: 7917004,
    chapter: 'How the Web Works',
    title: 'What DNS does',
    prompt: 'What is the primary job of the Domain Name System (DNS)?',
    correct: 'Translating a human-readable domain name into an IP address',
    wrong: [
      miss('Encrypting the data sent between browser and server', 'Encryption is handled by TLS/HTTPS, not DNS.', 'DNS is about naming, not securing.'),
      miss('Storing the website\'s HTML files for delivery', 'Page files live on a web server, not in the DNS.', 'DNS resolves names; it does not host content.'),
      miss('Choosing which CSS styles to apply to the page', 'Styling is decided by CSS in the browser, unrelated to DNS.', 'DNS works before the page is even fetched.'),
    ],
    lesson: 'Computers route traffic using numeric IP addresses, but people prefer names like example.com. DNS acts like the internet\'s phone book, resolving a domain name into the IP address of the server that hosts it. Only after this lookup can the browser open a connection and request the page.',
    source,
    generated: true,
  },
  {
    id: 7917005,
    chapter: 'How the Web Works',
    title: 'Separation of concerns',
    prompt: 'In the standard separation of web languages, which layer is responsible for the meaning and structure of content?',
    correct: 'HTML',
    wrong: [
      miss('CSS', 'CSS controls presentation such as color and layout, not structure or meaning.', 'CSS describes how things look, not what they are.'),
      miss('JavaScript', 'JavaScript adds behavior and interactivity, not the underlying content structure.', 'Behavior is a separate concern from structure.'),
      miss('HTTP', 'HTTP delivers the resources; it does not define the page\'s structure.', 'Delivery is not the same as structure.'),
    ],
    lesson: 'A core principle of the web is separation of concerns: HTML provides structure and meaning, CSS provides presentation, and JavaScript provides behavior. Keeping these layers distinct makes pages easier to maintain, more accessible, and less confusing to debug than mixing styling and logic into the markup.',
    source,
    generated: true,
  },
  {
    id: 7917006,
    chapter: 'How the Web Works',
    title: 'Classify the code line',
    prompt: 'You see the rule "color: red;" in a project. Which layer of the web does it belong to?',
    correct: 'Presentation (CSS)',
    wrong: [
      miss('Structure (HTML)', 'HTML uses tags to mark up content; "color: red;" is a style declaration, not an element.', 'Property: value syntax is a hallmark of CSS.'),
      miss('Behavior (JavaScript)', 'JavaScript uses statements and functions; this is a static styling rule.', 'No logic or events appear here, just a visual property.'),
      miss('Delivery (HTTP)', 'HTTP carries files; it does not contain style declarations.', 'Delivery has nothing to do with declaring a color.'),
    ],
    lesson: 'You can classify a line of web code by its job. Angle-bracket tags like <p> are HTML structure. Property and value pairs like color: red; are CSS presentation. Statements with logic, functions, or event handling are JavaScript behavior. Recognizing the layer tells you where to look when something needs fixing.',
    source,
    generated: true,
  },
  {
    id: 7917007,
    chapter: 'How the Web Works',
    title: 'Inspecting the network',
    prompt: 'In browser developer tools, which panel would you open to see each file the page requested and how long each took to download?',
    correct: 'The Network panel',
    wrong: [
      miss('The Elements panel', 'Elements shows the live HTML and CSS, not the list of network requests and timings.', 'Think about which panel logs requests over the wire.'),
      miss('The Console panel', 'The Console shows logged messages and errors, not a request-by-request transfer list.', 'Console is for messages, not download timing.'),
      miss('The Sources panel', 'Sources lets you view and debug script files, not monitor request timing.', 'Watching requests load is a separate tool.'),
    ],
    lesson: 'Browser DevTools group functions into panels. The Elements panel inspects the rendered DOM and applied styles, the Console reports logs and errors, and the Network panel records every request the page made along with its status, size, and timing. The Network panel is essential for diagnosing slow loads and broken resources.',
    source,
    generated: true,
  },
  {
    id: 7917008,
    chapter: 'How the Web Works',
    title: 'Client versus server',
    prompt: 'In the client-server model of the web, which device acts as the client?',
    correct: 'The user\'s browser making the request',
    wrong: [
      miss('The machine that stores and sends the website files', 'That machine is the server, which answers requests, not the client.', 'The client is the one asking, not the one answering.'),
      miss('The DNS resolver that maps the name to an address', 'A DNS resolver is a supporting service, not the client in the page request.', 'The client is the browser requesting the page itself.'),
      miss('The router inside your home network', 'A router forwards packets; it is neither the requesting client nor the hosting server.', 'Focus on which program initiates the page request.'),
    ],
    lesson: 'The web is built on a client-server relationship. The client is the program that initiates a request, almost always a web browser. The server is the machine that listens for requests and responds with resources. Understanding which role each device plays clarifies where content, processing, and storage actually live.',
    source,
    generated: true,
  },
  {
    id: 7917009,
    chapter: 'How the Web Works',
    title: 'Parts of a URL',
    prompt: 'In the URL https://example.com/blog/post.html, which part is the path that identifies the specific resource on the host?',
    correct: '/blog/post.html',
    wrong: [
      miss('https', 'That is the scheme (protocol), which says how to communicate, not which resource to fetch.', 'The scheme is the part before the colon-slash-slash.'),
      miss('example.com', 'That is the host (domain), identifying the server, not the specific file on it.', 'The host names the server; the path locates the file.'),
      miss('.com', 'That is the top-level domain, only one piece of the host name.', 'A TLD is part of the domain, not the resource path.'),
    ],
    lesson: 'A URL has distinct parts: the scheme (https) states the protocol, the host (example.com) names the server, and the path (/blog/post.html) identifies the specific resource on that server. Optional pieces include a port, a query string after a ?, and a fragment after a #. Reading these parts helps you reason about exactly what is being requested.',
    source,
    generated: true,
  },

  // ---- Chapter 2: HTML Document Structure ----
  {
    id: 7917010,
    chapter: 'HTML Document Structure',
    title: 'Purpose of the doctype',
    prompt: 'Why does a modern HTML page begin with <!DOCTYPE html>?',
    correct: 'It tells the browser to use standards mode for rendering HTML5',
    wrong: [
      miss('It imports the page\'s CSS stylesheet', 'Stylesheets are linked with <link> in the head, not by the doctype.', 'The doctype is a rendering instruction, not a style import.'),
      miss('It is a comment that browsers ignore', 'It is a real declaration that affects how the page is parsed, not a comment.', 'Its presence changes the rendering mode.'),
      miss('It defines the page title shown in the tab', 'The tab title comes from the <title> element, not the doctype.', 'Title and doctype are unrelated declarations.'),
    ],
    lesson: 'The <!DOCTYPE html> declaration at the top of a document tells the browser to render in standards mode using the HTML5 rules. Without it, browsers may fall back to "quirks mode" and apply inconsistent legacy behavior. The doctype is not an element and carries no content; it is a single instruction about how to interpret the page.',
    source,
    generated: true,
  },
  {
    id: 7917011,
    chapter: 'HTML Document Structure',
    title: 'Head versus body',
    prompt: 'Which of the following correctly describes what belongs in the <head> versus the <body>?',
    correct: 'Metadata and resource links go in the head; visible content goes in the body',
    wrong: [
      miss('Visible paragraphs and headings go in the head', 'Content the user sees belongs in the body, not the head.', 'The head holds information about the page, not the page content.'),
      miss('Both the title and the visible text must go in the body', 'The <title> belongs in the head, not the body.', 'Metadata like the title lives in the head.'),
      miss('CSS links go in the body and content goes in the head', 'This reverses the convention: links and metadata go in the head.', 'Swap your two halves; you have them backwards.'),
    ],
    lesson: 'The <head> contains metadata about the document, such as the <title>, character set, viewport settings, and links to stylesheets and scripts. The <body> contains everything the user actually sees and interacts with: headings, paragraphs, images, links, and forms. Putting visible content in the head is a common structural mistake.',
    source,
    generated: true,
  },
  {
    id: 7917012,
    chapter: 'HTML Document Structure',
    title: 'Why charset matters',
    prompt: 'What is the purpose of <meta charset="UTF-8"> in the head of a page?',
    correct: 'It tells the browser which character encoding to use so text displays correctly',
    wrong: [
      miss('It sets the font family for the whole page', 'Fonts are chosen with CSS, not with a charset declaration.', 'Encoding is about characters, not typefaces.'),
      miss('It encrypts the page so it cannot be read in transit', 'Charset does not encrypt anything; that is the role of HTTPS/TLS.', 'Encoding and encryption are different ideas.'),
      miss('It restricts the page to English-only text', 'UTF-8 actually supports nearly all writing systems, not just English.', 'UTF-8 is the opposite of restrictive.'),
    ],
    lesson: 'Character encoding defines how bytes map to characters. Declaring <meta charset="UTF-8"> early in the head ensures the browser interprets accented letters, symbols, and non-Latin scripts correctly instead of showing garbled characters. UTF-8 is the recommended modern encoding because it can represent virtually every character in use.',
    source,
    generated: true,
  },
  {
    id: 7917013,
    chapter: 'HTML Document Structure',
    title: 'The viewport meta tag',
    prompt: 'On mobile devices, what does <meta name="viewport" content="width=device-width, initial-scale=1"> accomplish?',
    correct: 'It makes the page render at the device\'s real width instead of a zoomed-out desktop width',
    wrong: [
      miss('It blocks the page from loading on small screens', 'It enables proper mobile rendering rather than blocking anything.', 'The tag helps mobile, it does not forbid it.'),
      miss('It downloads a separate mobile-only version of the site', 'It changes how the existing page is scaled, not which file is fetched.', 'No separate file is involved; it adjusts scaling.'),
      miss('It sets the maximum file size of images on the page', 'It has nothing to do with image sizes; it controls layout scaling.', 'Viewport is about width and zoom, not assets.'),
    ],
    lesson: 'Without the viewport meta tag, mobile browsers assume a wide desktop layout and zoom it out, making text tiny. Setting width=device-width tells the browser to match the layout to the device\'s actual pixel width, and initial-scale=1 prevents an automatic zoom. This tag is essential for responsive design to work on phones.',
    source,
    generated: true,
  },
  {
    id: 7917014,
    chapter: 'HTML Document Structure',
    title: 'Spot the missing boilerplate',
    prompt: 'A page has <!DOCTYPE html>, <html lang="en">, a <head> with a <title>, and a <body>, but it has no <meta charset>. What is the most likely visible symptom?',
    correct: 'Special or accented characters may appear as garbled symbols',
    wrong: [
      miss('The page will not load at all', 'A missing charset degrades text display but does not prevent loading.', 'Think about which feature the charset specifically supports.'),
      miss('All CSS styling will be ignored', 'Styling is independent of the charset declaration.', 'Charset affects text encoding, not stylesheets.'),
      miss('Links will stop working', 'Hyperlink behavior does not depend on the charset.', 'Encoding does not control navigation.'),
    ],
    lesson: 'Each boilerplate element has a distinct effect, so missing pieces produce specific symptoms. Omitting the charset typically causes mojibake (garbled accented or special characters) because the browser may guess the wrong encoding. Diagnosing structural omissions means matching the symptom to the element that prevents it.',
    source,
    generated: true,
  },
  {
    id: 7917015,
    chapter: 'HTML Document Structure',
    title: 'Why the lang attribute',
    prompt: 'Why is it good practice to write <html lang="en"> rather than just <html>?',
    correct: 'It tells browsers and screen readers what language the content is in',
    wrong: [
      miss('It automatically translates the page into English', 'The attribute declares the language; it does not translate anything.', 'Declaring a language is not the same as translating.'),
      miss('It is required or the page will not validate as HTML5', 'A page without lang still validates; lang is a strong recommendation, not a hard requirement.', 'Best practice is not always a validation error.'),
      miss('It sets the keyboard layout for forms on the page', 'Keyboard layout is controlled by the operating system and input types, not lang.', 'lang describes content language, not input hardware.'),
    ],
    lesson: 'The lang attribute on the <html> element declares the document\'s primary language. This helps screen readers choose the correct pronunciation rules, lets browsers offer accurate translation, and improves search indexing. It is an inexpensive accessibility win that should appear on every page.',
    source,
    generated: true,
  },
  {
    id: 7917016,
    chapter: 'HTML Document Structure',
    title: 'Correct nesting',
    prompt: 'Which of these is correctly nested HTML?',
    correct: '<p>This is <strong>important</strong> text.</p>',
    wrong: [
      miss('<p>This is <strong>important</p></strong> text.', 'The tags overlap instead of nesting; <strong> must close before <p> does.', 'Inner elements must close before their outer element.'),
      miss('<strong><p>This is important text.</strong></p>', 'The opening order and closing order do not match, so the nesting is invalid.', 'Last opened should be first closed.'),
      miss('<p>This is <strong>important<p> text.</strong>', 'A second <p> is opened without closing the first, breaking the structure.', 'Do not start a new block before closing the current one.'),
    ],
    lesson: 'HTML elements must nest like properly matched brackets: an element opened inside another must be closed before the outer element closes. Overlapping tags, as in <p><strong>...</p></strong>, create invalid structure that browsers try to repair unpredictably. Clean nesting keeps the document tree well formed and reliable to style and script.',
    source,
    generated: true,
  },
  {
    id: 7917017,
    chapter: 'HTML Document Structure',
    title: 'Where the title shows',
    prompt: 'Where does the text inside the <title> element appear?',
    correct: 'In the browser tab and as the default name when bookmarking the page',
    wrong: [
      miss('As the largest heading at the top of the page body', 'The big on-page heading is usually an <h1>, which is separate from the title.', 'The title is page metadata, not body content.'),
      miss('Nowhere; it is purely for internal documentation', 'The title is very visible in tabs, history, and search results.', 'The title has prominent user-facing roles.'),
      miss('Only in the page\'s URL bar as the address', 'The address bar shows the URL, not the title element.', 'URL and title are different things.'),
    ],
    lesson: 'The <title> element lives in the head and provides the page\'s name. Browsers show it in the tab and window, it becomes the default bookmark label, and search engines often use it as the clickable result heading. Because it is highly visible to users and machines, a clear, specific title matters.',
    source,
    generated: true,
  },
  {
    id: 7917018,
    chapter: 'HTML Document Structure',
    title: 'Validation tells you what',
    prompt: 'What does running a page through an HTML validator primarily check?',
    correct: 'Whether the markup follows HTML rules, such as proper nesting and required attributes',
    wrong: [
      miss('Whether the page looks visually attractive', 'Validation checks correctness of markup, not aesthetics or design quality.', 'A validator judges rules, not beauty.'),
      miss('How fast the page loads over the network', 'Load speed is measured by performance tools, not the markup validator.', 'Speed and validity are separate concerns.'),
      miss('Whether the content is factually accurate', 'A validator cannot judge the truth of the text, only the structure of the code.', 'Validation is about syntax, not facts.'),
    ],
    lesson: 'An HTML validator parses your markup against the language specification and reports errors such as unclosed tags, illegal nesting, duplicate ids, or missing required attributes. Valid markup is more predictable across browsers and assistive technologies. Validation does not assess design, performance, or content accuracy; those need separate review.',
    source,
    generated: true,
  },
  {
    id: 7917019,
    chapter: 'HTML Document Structure',
    title: 'Elements, tags, and content',
    prompt: 'In <p>Hello</p>, which statement is correct?',
    correct: '<p> is the opening tag, </p> is the closing tag, and "Hello" is the element\'s content',
    wrong: [
      miss('<p> and </p> are two separate, unrelated elements', 'They are the opening and closing tags of one element, not two elements.', 'A pair of matching tags forms a single element.'),
      miss('"Hello" is an attribute of the paragraph', 'Attributes appear inside the opening tag as name="value"; "Hello" is content.', 'Content sits between the tags, not inside them.'),
      miss('The whole thing is a single self-closing tag', 'A paragraph uses separate opening and closing tags, not a self-closing form.', 'Self-closing tags have no separate closing tag.'),
    ],
    lesson: 'An HTML element is usually built from an opening tag, content, and a closing tag, such as <p>Hello</p>. Attributes, written as name="value", go inside the opening tag to configure the element. Some elements like <img> are void (self-closing) and have no separate closing tag. Using this vocabulary precisely makes it easier to read and debug markup.',
    source,
    generated: true,
  },
  {
    id: 7917020,
    chapter: 'HTML Document Structure',
    title: 'Linking a stylesheet',
    prompt: 'Where and how should an external CSS file usually be connected to an HTML page?',
    correct: 'With a <link rel="stylesheet" href="styles.css"> element inside the <head>',
    wrong: [
      miss('With a <style src="styles.css"> tag in the body', 'The <style> element holds inline CSS and takes no src; external files use <link>.', 'External files are referenced with <link>, not <style>.'),
      miss('By pasting the CSS file path into the <title> element', 'The title names the page; it cannot load a stylesheet.', 'Stylesheets are loaded by <link>, not the title.'),
      miss('With a <script src="styles.css"> tag at the end of the body', 'The <script> element loads JavaScript, not CSS stylesheets.', 'Scripts are for behavior; styles use <link>.'),
    ],
    lesson: 'An external stylesheet is connected using <link rel="stylesheet" href="..."> placed in the head, so the styles are known before the body renders. The <style> element is for CSS written directly in the page, and <script> is for JavaScript. Keeping CSS in an external file and linked from the head supports separation of concerns and caching.',
    source,
    generated: true,
  },

  // ---- Chapter 3: Semantic Content ----
  {
    id: 7917021,
    chapter: 'Semantic Content',
    title: 'Choosing a heading',
    prompt: 'A document has one main title and several major sections, each with a sub-heading. What is the correct way to mark the single main page title?',
    correct: 'A single <h1>, with section sub-headings as <h2>',
    wrong: [
      miss('Several <h1> elements, one per visible bold line', 'Reusing <h1> for every emphasis breaks the document outline; the main title should be one <h1>.', 'A heading level signals rank, not just bold text.'),
      miss('A <p> with large font size set in CSS', 'Styling a paragraph to look big does not make it a heading for the outline or screen readers.', 'Looking like a heading is not the same as being one.'),
      miss('A <div> with a class named "title"', 'A <div> carries no heading meaning; assistive tech cannot treat it as a title.', 'Meaning comes from the element, not the class name.'),
    ],
    lesson: 'Headings h1 to h6 create a document outline that browsers, search engines, and screen readers rely on to convey structure. Use one h1 for the page\'s main title and nest sub-sections with h2, h3, and so on without skipping levels for styling reasons. Faking headings with sized paragraphs or divs loses this machine-readable structure.',
    source,
    generated: true,
  },
  {
    id: 7917022,
    chapter: 'Semantic Content',
    title: 'List or table',
    prompt: 'You need to display a set of names paired with each person\'s phone number and email. Which element best fits this data?',
    correct: 'A <table>, because the data is naturally rows and columns',
    wrong: [
      miss('An unordered list <ul>, with all values run together', 'A flat list loses the relationship between each person and their multiple data fields.', 'Multiple aligned columns of related data point to a table.'),
      miss('A series of <div> elements positioned with CSS', 'Divs positioned visually carry no row/column meaning for assistive tech.', 'Visual alignment is not the same as semantic structure.'),
      miss('A single long <p> with everything separated by commas', 'A paragraph cannot express the two-dimensional relationship between people and fields.', 'Tabular relationships need a table, not prose.'),
    ],
    lesson: 'Choose the element that matches the shape of the data. Genuinely tabular data, where rows and columns both carry meaning, belongs in a <table> with <th> headers so screen readers can announce relationships. Lists suit one-dimensional collections, and paragraphs suit prose. Using a table for layout, or a list for tabular data, misrepresents the content.',
    source,
    generated: true,
  },
  {
    id: 7917023,
    chapter: 'Semantic Content',
    title: 'Ordered versus unordered',
    prompt: 'When should you use an ordered list <ol> instead of an unordered list <ul>?',
    correct: 'When the sequence of items matters, such as steps in a recipe',
    wrong: [
      miss('Whenever you want the items to appear in a vertical column', 'Both list types stack vertically by default; the choice is about meaning, not direction.', 'Layout is not what distinguishes the two list types.'),
      miss('Whenever the list has more than five items', 'List length has nothing to do with whether order is meaningful.', 'Count does not decide ordered versus unordered.'),
      miss('Whenever you want numbers shown, regardless of meaning', 'The numbering should reflect real sequence, not be added just for decoration.', 'Numbers signal that order carries meaning.'),
    ],
    lesson: 'Use <ol> when the order of items conveys meaning, such as ranked results or sequential steps, because the numbering communicates that sequence. Use <ul> when items are a set with no inherent order, like ingredients or features. Both render as lists; the semantic difference is whether sequence matters, and that should drive the choice.',
    source,
    generated: true,
  },
  {
    id: 7917024,
    chapter: 'Semantic Content',
    title: 'Descriptive link text',
    prompt: 'Which link text is most accessible and useful?',
    correct: '<a href="report.pdf">Download the 2025 safety report (PDF)</a>',
    wrong: [
      miss('<a href="report.pdf">Click here</a>', '"Click here" gives no information out of context, which is poor for screen reader users who scan links.', 'Link text should describe the destination on its own.'),
      miss('<a href="report.pdf">report.pdf</a>', 'A raw filename is hard to read aloud and does not describe the content.', 'Prefer human-readable wording over a filename.'),
      miss('<a href="report.pdf">Read more</a>', '"Read more" is vague and unhelpful when links are listed out of context.', 'Generic phrases fail when links are read in isolation.'),
    ],
    lesson: 'Link text should make sense on its own because many people, especially screen reader users, navigate by jumping between links. Descriptive text like "Download the 2025 safety report (PDF)" states the destination and format, while "click here" or "read more" convey nothing without surrounding context. Writing meaningful link text is a core accessibility habit.',
    source,
    generated: true,
  },
  {
    id: 7917025,
    chapter: 'Semantic Content',
    title: 'Writing alt text',
    prompt: 'A photo on a club page shows three students planting a tree at a community event. What is the best alt text?',
    correct: 'alt="Three students planting a tree at the community garden event"',
    wrong: [
      miss('alt="image"', 'Saying "image" adds nothing; the reader already knows it is an image and learns nothing about it.', 'Alt text should describe the content, not the medium.'),
      miss('alt="IMG_4837.jpg"', 'A filename does not convey what the picture shows.', 'Describe what is depicted, not the file name.'),
      miss('alt=""', 'An empty alt is for decorative images; this photo carries meaningful content that should be described.', 'Empty alt hides meaningful images from screen readers.'),
    ],
    lesson: 'Alt text gives a text alternative for an image so people who cannot see it still get the information. Good alt text concisely describes the meaningful content and purpose, like "Three students planting a tree at the community garden event." Use an empty alt ("") only for purely decorative images, and never substitute the word "image" or a filename.',
    source,
    generated: true,
  },
  {
    id: 7917026,
    chapter: 'Semantic Content',
    title: 'Div for everything',
    prompt: 'Why is wrapping all page content in generic <div> elements considered poor practice when semantic elements exist?',
    correct: 'Divs carry no meaning, so assistive tech and search engines cannot understand the page structure',
    wrong: [
      miss('Divs are slower to render than semantic elements', 'Rendering speed is essentially the same; the problem is lost meaning, not performance.', 'The issue is semantics, not speed.'),
      miss('Divs cannot be styled with CSS', 'Divs are perfectly styleable; the drawback is that they convey no meaning.', 'You can style a div; it just means nothing.'),
      miss('Divs are deprecated and removed from HTML5', 'The <div> is still valid in HTML5; it is simply non-semantic.', 'Div is valid, just meaningless on its own.'),
    ],
    lesson: 'A <div> is a generic container with no inherent meaning. Using semantic elements like <nav>, <main>, <article>, and <footer> lets browsers, screen readers, and search engines understand the role of each region. Div-soup pages still display, but they lose this machine-readable structure, hurting accessibility and maintainability.',
    source,
    generated: true,
  },
  {
    id: 7917027,
    chapter: 'Semantic Content',
    title: 'The main landmark',
    prompt: 'Which element should wrap the primary, unique content of a page, distinct from headers, navigation, and footers?',
    correct: '<main>',
    wrong: [
      miss('<section>', 'A <section> groups related content but is not the single landmark for the page\'s primary content.', 'There should be exactly one primary-content landmark.'),
      miss('<div id="main">', 'A div with an id has no built-in landmark meaning for assistive tech.', 'Use the dedicated element, not a named div.'),
      miss('<header>', 'The <header> holds introductory content like logos and titles, not the page\'s core content.', 'The header introduces; another element holds the main content.'),
    ],
    lesson: 'The <main> element identifies the dominant, unique content of a document, and there should be just one per page. Screen reader users can jump straight to it, skipping repeated headers and navigation. Pairing <main> with <header>, <nav>, and <footer> gives the page clear landmark regions that improve navigation for everyone.',
    source,
    generated: true,
  },
  {
    id: 7917028,
    chapter: 'Semantic Content',
    title: 'Article versus section',
    prompt: 'Which content is the clearest fit for an <article> element?',
    correct: 'A single blog post that would still make sense if shown on its own',
    wrong: [
      miss('The site-wide navigation menu', 'Navigation belongs in a <nav>, not an <article>.', 'Articles are self-contained content, not menus.'),
      miss('A thin wrapper used only to apply a background color', 'A purely stylistic wrapper has no semantic meaning and should be a <div>.', 'Styling-only containers are divs, not articles.'),
      miss('The copyright line at the bottom of every page', 'Footer information belongs in a <footer>, not an <article>.', 'Articles hold standalone content, not site footers.'),
    ],
    lesson: 'An <article> represents a self-contained, independently distributable piece of content, such as a blog post, news story, or comment, that would still make sense on its own. A <section> groups thematically related content within a larger whole. Choosing between them depends on whether the content could stand alone or is part of a larger structure.',
    source,
    generated: true,
  },
  {
    id: 7917029,
    chapter: 'Semantic Content',
    title: 'Emphasis with meaning',
    prompt: 'You want to mark a word as having strong importance, conveyed even to screen readers. Which element is most appropriate?',
    correct: '<strong>',
    wrong: [
      miss('<b>', 'The <b> element makes text bold but carries no importance meaning for assistive tech.', 'One adds meaning, the other only adds boldness.'),
      miss('A <span> styled with font-weight: bold', 'Styling a span bold conveys no semantic importance, only a visual change.', 'Visual weight alone does not signal importance.'),
      miss('<em>', '<em> conveys stress emphasis (a change in tone), not strong importance.', 'Emphasis and importance are different semantic ideas.'),
    ],
    lesson: 'Semantic emphasis matters because screen readers can announce it. <strong> marks content with strong importance, seriousness, or urgency, while <em> marks stress emphasis that changes meaning. The presentational <b> and <i> only change appearance without semantics. Choosing the meaningful element ensures importance is communicated, not just shown.',
    source,
    generated: true,
  },
  {
    id: 7917030,
    chapter: 'Semantic Content',
    title: 'Navigation landmark',
    prompt: 'A block of links lets users move between the main pages of a site. Which element best wraps it?',
    correct: '<nav>',
    wrong: [
      miss('<menu> styled to look like a bar', 'While <menu> exists, the standard landmark for site navigation is <nav>.', 'There is a dedicated navigation landmark element.'),
      miss('<aside>', 'An <aside> is for tangential content, not the primary navigation.', 'Asides are side content, not main navigation.'),
      miss('<footer>', 'A <footer> contains closing information; it is not the navigation landmark itself.', 'Footers can contain links but are not the nav region.'),
    ],
    lesson: 'The <nav> element marks a section of major navigation links, creating a landmark that lets assistive technology jump to or skip the menu. Not every group of links needs <nav>; it is for primary navigation blocks. Using it for the main menu improves keyboard and screen reader navigation across the site.',
    source,
    generated: true,
  },
  {
    id: 7917031,
    chapter: 'Semantic Content',
    title: 'Skipping heading levels',
    prompt: 'Why should you avoid jumping from an <h2> straight to an <h4> just because the h4 happens to look the right size?',
    correct: 'Skipping levels breaks the logical outline that screen readers use to convey structure',
    wrong: [
      miss('Browsers refuse to display an h4 that follows an h2', 'Browsers will display it fine; the problem is the broken logical outline.', 'It is a meaning problem, not a rendering one.'),
      miss('h4 text is always smaller than body text', 'Default sizes vary, but size is not the reason to keep levels in order.', 'Order is about hierarchy, not pixel size.'),
      miss('Search engines ignore any page that uses h4', 'Search engines do not blacklist h4; they just rely on a sensible hierarchy.', 'It is hierarchy quality, not the tag itself, that matters.'),
    ],
    lesson: 'Heading levels express a hierarchy, like an outline. Screen reader users often navigate by heading level and infer the structure from it, so skipping from h2 to h4 implies a missing h3 and confuses that mental model. Choose levels by their place in the outline and use CSS to control size, never the reverse.',
    source,
    generated: true,
  },
  {
    id: 7917032,
    chapter: 'Semantic Content',
    title: 'Table headers',
    prompt: 'In a data table, why should the top row of labels use <th> rather than <td>?',
    correct: 'It marks them as headers so screen readers can associate each cell with its column',
    wrong: [
      miss('It makes the text bold and centered automatically', 'The default bold/centered styling is incidental; the real purpose is the header relationship.', 'The semantic association matters more than the styling.'),
      miss('It is the only way to add a border to the row', 'Borders come from CSS and apply to any cell type.', 'Borders are styling, not what <th> provides.'),
      miss('It allows the row to be sorted by clicking', 'Click-to-sort requires JavaScript; <th> alone does not add sorting.', 'Headers add meaning, not built-in interactivity.'),
    ],
    lesson: 'A <th> element declares a header cell, and with scope it tells assistive technology which data cells a header describes. This lets a screen reader announce, for example, "Price: 12" by pairing the header with the value. Using <td> for everything loses these relationships, making data tables hard to understand non-visually.',
    source,
    generated: true,
  },
  {
    id: 7917033,
    chapter: 'Semantic Content',
    title: 'Decorative image alt',
    prompt: 'A purely decorative divider image adds no information to the content. What is the correct alt attribute?',
    correct: 'alt="" (an empty alt)',
    wrong: [
      miss('alt="decorative divider line"', 'Describing a meaningless decoration just adds noise for screen reader users.', 'If it carries no information, it should be hidden, not described.'),
      miss('Omit the alt attribute entirely', 'A missing alt makes some screen readers read the filename; an empty alt is the correct signal.', 'Provide alt="" rather than no alt at all.'),
      miss('alt="line"', 'Even a short description of a decoration is unhelpful clutter.', 'Decorations should be skipped, not labeled.'),
    ],
    lesson: 'Images fall into two groups: informative and decorative. Informative images need descriptive alt text. Purely decorative images should have alt="" so assistive technology skips them entirely; omitting alt is not the same, because some screen readers then read the filename. Matching the alt to the image\'s role keeps the experience clean.',
    source,
    generated: true,
  },
  {
    id: 7917034,
    chapter: 'Semantic Content',
    title: 'Paragraphs versus line breaks',
    prompt: 'You have two distinct paragraphs of text. What is the semantically correct way to separate them?',
    correct: 'Wrap each in its own <p> element',
    wrong: [
      miss('Put a <br><br> between the two blocks of text', 'Line breaks only insert vertical space; they do not mark each block as a paragraph.', 'Use a structural element, not visual spacing.'),
      miss('Separate them with several non-breaking spaces', 'Spaces are presentational and create no paragraph structure.', 'Whitespace tricks do not convey meaning.'),
      miss('Place each in a <div> with margin in CSS', 'A div conveys no paragraph meaning even if it looks spaced apart.', 'Choose the element that means paragraph.'),
    ],
    lesson: 'A paragraph of prose belongs in a <p> element, which gives it semantic meaning and default spacing. Using <br> tags or empty divs to fake paragraph breaks produces the right look but no structure, which harms accessibility and makes styling fragile. Reserve <br> for line breaks within a single block, like a postal address.',
    source,
    generated: true,
  },
  {
    id: 7917035,
    chapter: 'Semantic Content',
    title: 'Footer content',
    prompt: 'Which content is most appropriately placed in a page <footer>?',
    correct: 'Copyright notice, contact info, and links to related documents',
    wrong: [
      miss('The page\'s main heading and primary content', 'Primary content belongs in <main>, not the footer.', 'The footer holds closing information, not the core content.'),
      miss('The site\'s top navigation bar only', 'Primary navigation typically uses <nav> near the top, though a footer may repeat some links.', 'The main menu is a nav, not the defining footer content.'),
      miss('All of the page\'s images', 'Images appear wherever their content lives, not gathered in the footer.', 'The footer is about closing metadata, not media storage.'),
    ],
    lesson: 'A <footer> holds information about its nearest sectioning content or the whole page, typically authorship, copyright, contact details, and related links. It creates a landmark that assistive technology can skip to. Placing primary content or the main navigation in the footer misuses the element and confuses the page\'s structure.',
    source,
    generated: true,
  },

  // ---- Chapter 4: CSS Basics and Layout ----
  {
    id: 7917036,
    chapter: 'CSS Basics and Layout',
    title: 'Anatomy of a rule',
    prompt: 'In the CSS rule p { color: blue; }, what is "color"?',
    correct: 'The property being set',
    wrong: [
      miss('The selector', 'The selector is p, which chooses what to style; color is the property.', 'The selector comes before the curly braces.'),
      miss('The value', 'The value is blue; color is the property whose value is blue.', 'The value comes after the colon.'),
      miss('The declaration', 'The whole "color: blue;" is the declaration; color alone is just the property.', 'A declaration pairs a property with a value.'),
    ],
    lesson: 'A CSS rule has a selector that targets elements and a declaration block in braces. Each declaration is a property (like color) and a value (like blue) separated by a colon and ended with a semicolon. Knowing this vocabulary, selector, property, value, declaration, makes it much easier to read, write, and debug stylesheets.',
    source,
    generated: true,
  },
  {
    id: 7917037,
    chapter: 'CSS Basics and Layout',
    title: 'Box model width',
    prompt: 'An element has width: 200px, padding: 20px on each side, and border: 5px on each side, with the default box-sizing. How wide is the rendered box?',
    correct: '250px',
    wrong: [
      miss('200px', 'Under the default content-box, padding and border add to the declared width, so it is not just 200px.', 'Remember that padding and border extend the box by default.'),
      miss('245px', 'This counts padding on both sides but forgets the border on one side; add 5px twice.', 'Border applies to both sides, like padding.'),
      miss('220px', 'This adds only one side of padding and ignores the border entirely.', 'Both sides of both padding and border count.'),
    ],
    lesson: 'With the default box-sizing of content-box, the declared width covers only the content. Padding and border are added on top of it. Here 200 + 20 + 20 + 5 + 5 equals 250px. This surprise is why many developers set box-sizing: border-box, which makes width include padding and border.',
    source,
    generated: true,
  },
  {
    id: 7917038,
    chapter: 'CSS Basics and Layout',
    title: 'border-box behavior',
    prompt: 'If you set box-sizing: border-box on an element with width: 200px, padding: 20px, and border: 5px, how wide is the rendered box?',
    correct: '200px (padding and border fit inside the declared width)',
    wrong: [
      miss('250px', 'That is the content-box result; border-box keeps the total at the declared 200px.', 'With border-box the declared width is the total.'),
      miss('160px', 'border-box does not subtract padding and border from a smaller number; the total stays 200px.', 'The declared width is the final outer width.'),
      miss('230px', 'border-box does not add only part of the spacing; the outer width is exactly 200px.', 'No addition happens to the declared width under border-box.'),
    ],
    lesson: 'box-sizing: border-box changes how width is interpreted so that padding and border are included within the declared width rather than added to it. With width: 200px, the content area shrinks to fit the padding and border, but the rendered box stays 200px wide. This predictable behavior is why border-box is a popular default.',
    source,
    generated: true,
  },
  {
    id: 7917039,
    chapter: 'CSS Basics and Layout',
    title: 'Specificity contest',
    prompt: 'Two rules target the same element: one uses the selector .button and the other uses #submit. Which declaration wins on the element <button id="submit" class="button">?',
    correct: 'The #submit rule, because an ID selector is more specific than a class selector',
    wrong: [
      miss('The .button rule, because class selectors always override IDs', 'This reverses the cascade; an ID has higher specificity than a class.', 'Rank the selector types by specificity.'),
      miss('Whichever rule appears first in the file', 'Source order only breaks ties between equal specificity; here specificity differs.', 'Specificity is checked before source order.'),
      miss('Neither; conflicting rules cancel out and nothing applies', 'The cascade always resolves a winner; rules do not cancel each other.', 'CSS picks a winner rather than ignoring both.'),
    ],
    lesson: 'When multiple rules set the same property, CSS resolves the conflict by specificity. ID selectors (#submit) outrank class selectors (.button), which outrank element selectors (button). Only when specificity ties does later source order win. Understanding this order is key to predicting which style actually applies.',
    source,
    generated: true,
  },
  {
    id: 7917040,
    chapter: 'CSS Basics and Layout',
    title: 'Cascade source order',
    prompt: 'Two rules have identical specificity and both set the same property on the same element. Which one applies?',
    correct: 'The one that appears later in the source order',
    wrong: [
      miss('The one that appears earlier in the source order', 'Later rules of equal specificity override earlier ones, not the reverse.', 'In a tie, the cascade favors the last rule.'),
      miss('The shorter of the two rules', 'Rule length has no effect on which declaration wins.', 'The cascade does not measure rule length.'),
      miss('Both apply and the values are averaged', 'CSS does not average conflicting values; one declaration wins outright.', 'Only a single value can win for a property.'),
    ],
    lesson: 'The cascade resolves conflicts in order: importance, then specificity, then source order. When two declarations tie on importance and specificity, the one written later in the stylesheet wins. This is why the order of your rules matters and why a later override can silently defeat an earlier rule you expected to apply.',
    source,
    generated: true,
  },
  {
    id: 7917041,
    chapter: 'CSS Basics and Layout',
    title: 'Inheritance',
    prompt: 'Which of these properties is typically inherited by child elements from their parent?',
    correct: 'color',
    wrong: [
      miss('margin', 'Box-model properties like margin are not inherited; each element gets its own.', 'Spacing properties do not pass down to children.'),
      miss('border', 'Border is not inherited; it must be set on each element that needs it.', 'Borders do not flow from parent to child.'),
      miss('padding', 'Padding is not inherited; children do not pick up a parent\'s padding.', 'Inner spacing is set per element, not inherited.'),
    ],
    lesson: 'Some CSS properties are inherited by default, mostly text-related ones like color, font-family, and line-height, so setting them on a parent flows to its children. Box-model properties such as margin, padding, and border are not inherited because each element needs its own layout. Knowing which properties inherit helps you style efficiently and predict results.',
    source,
    generated: true,
  },
  {
    id: 7917042,
    chapter: 'CSS Basics and Layout',
    title: 'Class versus ID in HTML',
    prompt: 'What is the key rule about using an id value within a single HTML page?',
    correct: 'Each id must be unique; only one element on the page may have it',
    wrong: [
      miss('An id can be reused on many elements, like a class', 'Reusing an id is invalid; that is what classes are for.', 'One of these is unique per page, the other is reusable.'),
      miss('An id may only contain numbers', 'ids can contain letters and other characters, not just numbers.', 'There is no digits-only restriction on ids.'),
      miss('An element may have only one class but many ids', 'It is the opposite: many classes are allowed, but only one id.', 'Swap which one is limited to a single value.'),
    ],
    lesson: 'A class is a reusable label that can appear on many elements, useful for styling groups. An id must be unique within the page and identifies a single element, useful for targeting or linking to one specific place. Mixing these up, such as reusing an id, leads to invalid markup and unreliable styling and scripting.',
    source,
    generated: true,
  },
  {
    id: 7917043,
    chapter: 'CSS Basics and Layout',
    title: 'Flexbox main axis',
    prompt: 'In a flex container with the default flex-direction: row, which property aligns items along the horizontal (main) axis?',
    correct: 'justify-content',
    wrong: [
      miss('align-items', 'align-items controls the cross axis (vertical in a row), not the main axis.', 'One property is for the main axis, the other for the cross axis.'),
      miss('text-align', 'text-align affects inline text within a box, not the positioning of flex items.', 'Flex item placement uses flex properties, not text-align.'),
      miss('vertical-align', 'vertical-align is for inline and table-cell content, not flex layout.', 'This property is unrelated to flexbox.'),
    ],
    lesson: 'In flexbox, the main axis runs along the flex-direction. With flex-direction: row, the main axis is horizontal, and justify-content distributes items along it (start, center, space-between, and so on). The cross axis is perpendicular, controlled by align-items. Keeping main versus cross axis straight is essential to predicting flex layouts.',
    source,
    generated: true,
  },
  {
    id: 7917044,
    chapter: 'CSS Basics and Layout',
    title: 'Flexbox cross axis',
    prompt: 'In a flex row, which property vertically centers the items within the container\'s height?',
    correct: 'align-items: center',
    wrong: [
      miss('justify-content: center', 'justify-content centers along the main (horizontal) axis in a row, not vertically.', 'Vertical centering in a row is the cross axis.'),
      miss('margin: auto on the text', 'Auto margins can help in some cases but are not the standard cross-axis alignment property.', 'There is a dedicated flex property for cross-axis alignment.'),
      miss('line-height equal to the container height', 'That old trick centers a single line of text, not flex items generally.', 'Flexbox has a direct property for this.'),
    ],
    lesson: 'In a flex row, align-items controls placement along the cross axis (vertical). Setting align-items: center vertically centers items within the container\'s height, while justify-content handles horizontal distribution along the main axis. Pairing justify-content and align-items lets you center content in both directions with two short declarations.',
    source,
    generated: true,
  },
  {
    id: 7917045,
    chapter: 'CSS Basics and Layout',
    title: 'Media queries',
    prompt: 'What is the main purpose of a CSS media query such as @media (max-width: 600px)?',
    correct: 'To apply styles only when the viewport meets a condition, enabling responsive design',
    wrong: [
      miss('To load a completely different HTML page for phones', 'Media queries switch CSS within one page; they do not swap HTML documents.', 'Responsive design adapts one page, not separate pages.'),
      miss('To compress images automatically on small screens', 'Media queries do not resize or compress assets by themselves.', 'They apply style rules, not asset processing.'),
      miss('To block the page from loading above a certain width', 'Media queries adapt styling; they do not prevent loading.', 'They tailor styles, never block the page.'),
    ],
    lesson: 'A media query applies a block of CSS only when the device or viewport meets a condition, such as a maximum width. This lets one HTML page adapt its layout across phones, tablets, and desktops, the heart of responsive design. Media queries change which styles apply; they do not load different pages or alter the markup.',
    source,
    generated: true,
  },
  {
    id: 7917046,
    chapter: 'CSS Basics and Layout',
    title: 'Relative units',
    prompt: 'Why might a designer choose rem units instead of fixed px for font sizes?',
    correct: 'rem scales relative to the root font size, so text respects the user\'s size settings',
    wrong: [
      miss('rem makes images load faster than px-based layouts', 'Units do not affect image loading speed; rem is about scalable sizing.', 'The benefit is scalability, not download speed.'),
      miss('px is invalid in modern CSS and must be replaced', 'px is perfectly valid; rem is just often preferred for scalable text.', 'Both units are valid; the difference is how they scale.'),
      miss('rem prevents users from zooming the page', 'rem supports user scaling rather than preventing it.', 'rem respects user preferences instead of blocking them.'),
    ],
    lesson: 'Fixed px sizes ignore the user\'s browser font preference, which can hurt accessibility. The rem unit is relative to the root element\'s font size, so when a user increases their default size, rem-based text scales with it. Relative units like rem, em, %, and vw help build flexible, accessible layouts that adapt to users and devices.',
    source,
    generated: true,
  },
  {
    id: 7917047,
    chapter: 'CSS Basics and Layout',
    title: 'Display block versus inline',
    prompt: 'What is a defining difference between a block-level element and an inline element by default?',
    correct: 'A block-level element starts on a new line and takes the full available width; an inline element flows within text',
    wrong: [
      miss('Inline elements always have a larger font than block elements', 'Font size is unrelated to whether an element is block or inline.', 'The difference is about layout flow, not font size.'),
      miss('Block elements cannot contain any text', 'Block elements like <p> are full of text; the difference is layout behavior.', 'Block elements certainly hold text.'),
      miss('Inline elements cannot be styled with CSS at all', 'Inline elements can be styled; some box properties just behave differently.', 'Styling applies to both; flow is what differs.'),
    ],
    lesson: 'By default a block-level element (like <div> or <p>) begins on a new line and stretches to fill the available width, stacking vertically. An inline element (like <span> or <a>) sits within the text flow and is only as wide as its content. The display property can change these behaviors, but knowing the defaults explains most layout surprises.',
    source,
    generated: true,
  },
  {
    id: 7917048,
    chapter: 'CSS Basics and Layout',
    title: 'Class selector syntax',
    prompt: 'How do you write a CSS selector that targets every element with class="card"?',
    correct: '.card',
    wrong: [
      miss('#card', 'A leading # targets an id, not a class; classes use a dot.', 'Match the punctuation to class versus id.'),
      miss('card', 'A bare word targets elements named card (a tag), not the class.', 'Selecting a class needs a leading symbol.'),
      miss('*card', 'The asterisk is the universal selector; it does not combine this way to match a class.', 'Use the dot prefix to select a class.'),
    ],
    lesson: 'CSS selector prefixes encode what you are targeting: a dot (.card) selects elements by class, a hash (#card) selects by id, and a bare name (card) selects by element type. The universal selector * matches everything. Using the correct prefix is fundamental; a missing or wrong symbol is a common reason a rule silently fails to apply.',
    source,
    generated: true,
  },
  {
    id: 7917049,
    chapter: 'CSS Basics and Layout',
    title: 'Margin versus padding',
    prompt: 'What is the difference between margin and padding in the box model?',
    correct: 'Padding is space inside the border; margin is space outside the border',
    wrong: [
      miss('Margin is inside the border and padding is outside it', 'This reverses the two: padding is the inner space, margin the outer.', 'Swap inner and outer.'),
      miss('They are identical and interchangeable', 'They affect different regions of the box and are not interchangeable.', 'One spaces content from the border, one spaces the box from neighbors.'),
      miss('Padding controls font size and margin controls color', 'Both control spacing, not font size or color.', 'These are both spacing properties.'),
    ],
    lesson: 'In the box model, padding is the space between the content and the border, sitting inside the element\'s background. Margin is the space outside the border, separating the element from its neighbors and remaining transparent. Confusing the two leads to layouts where backgrounds extend too far or elements crowd together unexpectedly.',
    source,
    generated: true,
  },
  {
    id: 7917050,
    chapter: 'CSS Basics and Layout',
    title: 'Grid versus flexbox intent',
    prompt: 'CSS Grid is generally the better choice over flexbox when you need to do what?',
    correct: 'Lay out content in two dimensions, controlling both rows and columns together',
    wrong: [
      miss('Lay out a single row or column of items', 'A one-dimensional arrangement is exactly what flexbox handles best.', 'One tool is one-dimensional, the other two-dimensional.'),
      miss('Change the font color of text', 'Layout systems do not set text color; that is a separate property.', 'Grid is about placement, not color.'),
      miss('Make a page load faster over the network', 'Choosing grid does not change download speed; it changes layout structure.', 'Layout choice is unrelated to load performance.'),
    ],
    lesson: 'Flexbox excels at one-dimensional layout, distributing items along a single row or column. CSS Grid is designed for two-dimensional layout, letting you define rows and columns at once and place items into the resulting cells. Many real layouts combine both: grid for the overall page structure and flexbox for arranging items within a region.',
    source,
    generated: true,
  },
])
