import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe softwareEngineeringRoadmap 103-pass'

export const softwareEngineeringRoadmapR2Questions: Question[] = makeQuestionBank('Career Skills', [
  // ============================================================
  // Chapter 1: Code, Review, and Quality
  // (includes Ch.6 Testing & Observability per syllabus routing)
  // ============================================================
  {
    id: 7627000,
    chapter: 'Code, Review, and Quality',
    title: 'Four golden signals coverage',
    prompt: 'You instrument a new service and add a dashboard for CPU and memory only. Per the "four golden signals" of monitoring, which user-facing signals are you still missing?',
    correct: 'Latency, traffic, and errors — CPU/memory only covers saturation, leaving the three signals that reflect what users actually experience',
    wrong: [
      miss('Nothing — CPU and memory are two of the four golden signals, so you are halfway done', 'Only saturation maps to CPU/memory; latency, traffic, and errors are about request behavior, not host resources, so you have one of four, not two.', 'List the four signals and ask which are measured at the request level versus the host level.'),
      miss('Only traffic, since latency and errors are derived from CPU usage', 'Latency and errors are measured directly from requests; they are not computed from CPU and can be bad even when CPU looks fine.', 'A request can be slow or failing while the box is idle — what tells you that?'),
      miss('Cardinality and trace sampling rate, which are the remaining golden signals', 'Cardinality and sampling are observability implementation concerns, not golden signals; the four are latency, traffic, errors, saturation.', 'Recall the canonical four, not adjacent observability vocabulary.'),
    ],
    lesson: 'Google\'s four golden signals are latency, traffic, errors, and saturation. CPU and memory are saturation only. A health view built on saturation alone can show green while users see slow or failing requests, because it never measured the request path itself.',
    source,
    generated: true,
  },
  {
    id: 7627001,
    chapter: 'Code, Review, and Quality',
    title: 'Logs without correlation IDs',
    prompt: 'During an incident, on-call has plenty of logs but cannot follow a single failing request across three services. What instrumentation change fixes this most directly?',
    correct: 'Propagate a correlation/trace ID through every service and include it in each log line so one request can be stitched together end to end',
    wrong: [
      miss('Increase the log level to DEBUG everywhere in production', 'More verbose logs add volume and cost but still cannot link lines across services without a shared key; you get more haystack, not the needle.', 'Ask what lets you join log lines from different services for the same request.'),
      miss('Switch from structured JSON logs back to plain text so they are easier to read', 'Plain text is harder to query and parse at scale; the missing piece is a shared identifier, and removing structure makes correlation worse, not better.', 'The problem is joining records, not the readability of one line.'),
      miss('Add a second logging vendor so you have a backup copy of the logs', 'Duplicating logs to another vendor does nothing to connect a request\'s journey; you would have two disconnected copies instead of one.', 'A backup of disconnected logs is still disconnected.'),
    ],
    lesson: 'A correlation (or trace) ID generated at the edge and propagated through every downstream call lets you filter all log lines for one request, even across services. Without it, distributed logs are isolated islands and reconstructing a request\'s path becomes guesswork.',
    source,
    generated: true,
  },
  {
    id: 7627002,
    chapter: 'Code, Review, and Quality',
    title: 'High-cardinality label blowup',
    prompt: 'Someone adds the raw user ID as a label on a Prometheus-style counter metric, and your monitoring bill and memory usage explode. What concept did they violate?',
    correct: 'Cardinality: each distinct label value creates a separate time series, so a unbounded high-cardinality label like user ID multiplies series counts uncontrollably',
    wrong: [
      miss('Sampling rate, since metrics were being recorded too frequently', 'The blowup is from the number of distinct series, not how often each is scraped; reducing frequency would not address millions of per-user series.', 'Ask whether the cost came from how many series exist or how often each is sampled.'),
      miss('Log retention, because user IDs made the logs too large to store', 'This is a metrics label problem, not log storage; metric series count, not log volume, drove the cost.', 'The label is on a counter metric, not a log line.'),
      miss('Idempotency, since the same user could increment the counter twice', 'Idempotency is about repeated operations producing the same effect; it has nothing to do with how many time series a label creates.', 'The issue is the breadth of label values, not duplicate increments.'),
    ],
    lesson: 'In dimensional metrics systems, every unique combination of label values is its own time series. Putting an unbounded identifier (user ID, request ID, email) on a metric creates a near-infinite number of series, which wrecks memory and cost. Keep metric labels low-cardinality; put high-cardinality detail in logs or traces.',
    source,
    generated: true,
  },
  {
    id: 7627003,
    chapter: 'Code, Review, and Quality',
    title: 'Contract test vs full E2E',
    prompt: 'Service A calls Service B\'s API. You want to catch the case where B\'s team changes its response shape and breaks A, without standing up both full systems on every commit. What test fits best?',
    correct: 'A contract test that pins the agreed request/response shape between A and B, run against each side independently',
    wrong: [
      miss('A full end-to-end test that deploys A, B, and their databases together on every commit', 'That catches the break but is slow, flaky, and expensive to run per commit; contract tests get most of the safety at a fraction of the cost.', 'You wanted to avoid standing up both full systems — which test honors that constraint?'),
      miss('A unit test inside Service A that mocks B with hardcoded responses', 'A hand-written mock can drift from B\'s real behavior; if B changes and your mock does not, the test stays green while production breaks.', 'A mock A controls alone cannot detect that B changed underneath it.'),
      miss('A load test that sends high traffic from A to B', 'Load tests probe performance under volume, not whether the response shape still matches A\'s expectations.', 'The risk is a shape change, not a throughput limit.'),
    ],
    lesson: 'Contract tests verify that a provider and consumer still agree on the interface (fields, types, status codes) by testing each side against a shared contract. They catch integration drift far more cheaply than spinning up every service end to end, which is why they sit between unit and full E2E in the pyramid.',
    source,
    generated: true,
  },
  {
    id: 7627004,
    chapter: 'Code, Review, and Quality',
    title: 'Reviewing an AI-generated diff',
    prompt: 'A teammate opens a PR full of AI-assisted code and says "the tests pass and I don\'t fully follow it, but the model seemed confident." What is the right reviewer stance?',
    correct: 'The author is accountable for code they cannot explain; ask them to walk through behavior and edge cases, and do not approve until someone understands it',
    wrong: [
      miss('Approve it — passing tests plus a capable model is sufficient verification', 'Passing tests prove only the asserted cases; an author who cannot explain the diff cannot maintain or debug it, and "the model was confident" is not a correctness argument.', 'Ask who debugs this at 3 a.m. when the unexplained code fails.'),
      miss('Reject all AI-generated code on principle and ask them to rewrite it by hand', 'A blanket ban throws away a legitimate tool; the issue is unreviewed, unexplained code, not the fact that AI helped write it.', 'The problem is accountability and understanding, not the origin of the keystrokes.'),
      miss('Approve but add a comment noting it was AI-generated so blame is clear later', 'Labeling for blame does nothing to verify correctness now and still merges code nobody understands; review is risk triage, not a paper trail.', 'A blame label is not a behavior check.'),
    ],
    lesson: 'AI assistance does not transfer accountability: the human author owns the diff and must be able to explain its behavior and edge cases. Reviewers should check behavior, not wave code through because tests pass or a model sounded sure. Code no one on the team can explain is a liability regardless of who or what typed it.',
    source,
    generated: true,
  },
  {
    id: 7627005,
    chapter: 'Code, Review, and Quality',
    title: 'Quarantine vs fix a flaky test',
    prompt: 'A critical integration test flakes about 5% of runs and is now blocking unrelated deploys. The owner is out for a week. What is the most defensible immediate move?',
    correct: 'Quarantine the test (move it out of the blocking path) with a tracked ticket to fix it, so deploys unblock without silently losing the signal forever',
    wrong: [
      miss('Delete the test so the pipeline is green again', 'Deletion removes the coverage permanently with no record; quarantine preserves intent and the obligation to fix, deletion just hides the gap.', 'You want to stop the blockage without throwing away what the test checks.'),
      miss('Add automatic reruns so any failure retries up to five times before reporting', 'Auto-rerun-until-green trains the team to ignore real failures hiding inside the flake and masks genuine regressions in that test.', 'Retrying a flaky test until it passes also retries real bugs until they pass.'),
      miss('Leave it blocking and tell everyone to keep clicking re-run until it passes', 'Manual rerun-until-green burns engineer time on every deploy and still risks merging through a real failure; it is the same anti-pattern done by hand.', 'The whole problem is that it is currently blocking everyone.'),
    ],
    lesson: 'Quarantining a flaky test removes it from the path that blocks others while keeping a tracked commitment to fix it. That is honest about the lost coverage. Deleting hides the gap; rerun-until-green normalizes ignoring failures, which is how a real regression eventually sails through.',
    source,
    generated: true,
  },
  {
    id: 7627006,
    chapter: 'Code, Review, and Quality',
    title: 'Mocking the thing under test',
    prompt: 'A unit test for a discount-calculation function mocks out the discount calculator and asserts the mock returned the expected value. Why is this test nearly worthless?',
    correct: 'It tests the mock, not the real logic — by stubbing the function under test it can never catch a bug in the actual calculation',
    wrong: [
      miss('It is fine; mocking makes tests faster and that is the main goal of unit testing', 'Speed is irrelevant if the test exercises a stub instead of the real code; a fast test that verifies nothing is worse than no test because it implies coverage.', 'Ask what code actually runs when this test executes.'),
      miss('The problem is only that it should mock the database too for full isolation', 'Adding more mocks makes it worse; the core error is mocking the very unit you are supposed to be testing.', 'The bug is what got mocked, not what did not.'),
      miss('It needs more assertions on the mock\'s call count to be valid', 'Counting calls to a mock of the system under test still never runs the real calculation; more assertions on the wrong object do not help.', 'No amount of checking the mock tests the real function.'),
    ],
    lesson: 'You mock collaborators (dependencies), never the unit under test itself. Mocking the function you are testing means the assertion checks the stub you wrote, so the real logic is never exercised and any bug in it is invisible. This is a classic source of high coverage with zero confidence.',
    source,
    generated: true,
  },
  {
    id: 7627007,
    chapter: 'Code, Review, and Quality',
    title: 'Dashboards as hypotheses',
    prompt: 'A team has forty dashboards, and during incidents on-call scrolls past most of them looking for the one that matters. What is the better discipline?',
    correct: 'Treat each dashboard as a hypothesis tied to a question or SLO, and prune ones that no longer answer a real diagnostic question',
    wrong: [
      miss('Add more dashboards so every possible metric is always visible somewhere', 'More dashboards worsen the exact problem — on-call already cannot find the relevant one in forty; volume is the disease, not the cure.', 'The complaint was too many to scan, so what does adding more do?'),
      miss('Combine all forty into one giant dashboard with every chart on it', 'A single wall of charts is even harder to read under stress; consolidation without curation just relocates the noise.', 'Cramming noise onto one screen is still noise.'),
      miss('Auto-refresh all dashboards faster so the data is more current', 'Refresh rate does not help you decide which dashboard answers the question; the issue is relevance, not freshness.', 'Fresher irrelevant charts are still irrelevant.'),
    ],
    lesson: 'A dashboard should answer a specific question ("are we meeting the latency SLO?", "is the queue backing up?"). Dashboards that answer no current question are clutter that slows diagnosis. Curate them like code: each one earns its place by answering something, or it gets pruned.',
    source,
    generated: true,
  },
  {
    id: 7627008,
    chapter: 'Code, Review, and Quality',
    title: 'Worked in staging, broke in prod',
    prompt: 'A change passed every test in staging but failed in production. The likeliest systemic reason, and the right lesson, is which of these?',
    correct: 'Staging did not match production in data volume, traffic shape, or config; trust must come from production-representative signals and gradual rollout, not "staging was green"',
    wrong: [
      miss('Production is fundamentally unpredictable, so pre-release testing cannot reduce this risk and should be cut', 'Tests still catch many bugs; the lesson is to make staging representative and roll out gradually, not to abandon testing because one class of bug slipped through.', 'The fix is closing the staging/prod gap, not giving up on testing.'),
      miss('The staging tests must have been faked or skipped to show green', 'Honest staging tests can pass while prod fails purely because the environments differ in scale and data; assuming fraud misdiagnoses the gap.', 'Environments can differ legitimately without anyone cheating.'),
      miss('Production simply needs the exact same test suite re-run against it after deploy', 'Re-running the same staging tests in prod inherits the same blind spots (data, scale, config) and still misses what staging could not see.', 'Same tests, same gaps — what differs between the environments?'),
    ],
    lesson: '"It worked in staging" is not "it works in production" when the two differ in data volume, real traffic patterns, third-party behavior, or configuration. Reduce the gap (representative data, prod-like config) and rely on gradual rollout with real-user monitoring so the unavoidable surprises are caught small.',
    source,
    generated: true,
  },
  {
    id: 7627009,
    chapter: 'Code, Review, and Quality',
    title: 'Alert on the symptom for this SLO',
    prompt: 'Your checkout SLO is "99.9% of checkout requests succeed." Which alert best protects that SLO without paging on harmless internals?',
    correct: 'Page when the checkout request error rate breaches a burn-rate threshold against the 99.9% target',
    wrong: [
      miss('Page whenever a single backend pod restarts', 'Pods restart routinely under autoscaling and deploys without any user impact; this pages on a cause that is usually harmless, creating fatigue.', 'A restart is an internal event — does it mean a user failed to check out?'),
      miss('Page when database CPU crosses 80%', 'High CPU may be fine if requests still succeed; alerting on a resource cause rather than the user-visible symptom pages without user harm.', 'Tie the alert to whether checkouts actually fail, not how busy a box is.'),
      miss('Page when any single checkout request returns an error', 'A 99.9% SLO explicitly tolerates a small error rate; paging on every single error guarantees noise and contradicts the budget you set.', 'The SLO already budgets for some errors, so what threshold matters?'),
    ],
    lesson: 'Symptom-based alerting fires on what users feel — here, the checkout success rate against its SLO — ideally using a burn-rate that distinguishes a fast catastrophic burn from slow background errors. Paging on causes (pod restarts, CPU) produces fatigue, and paging on every single error ignores the budget the SLO grants.',
    source,
    generated: true,
  },
  {
    id: 7627010,
    chapter: 'Code, Review, and Quality',
    title: 'Idempotency test for a retried event',
    prompt: 'A consumer processes order-placed events from a queue that guarantees at-least-once delivery. What test most directly protects correctness here?',
    correct: 'A test that delivers the same event twice and asserts the side effect (e.g., the order) happens exactly once',
    wrong: [
      miss('A test that asserts the queue never delivers an event more than once', 'At-least-once delivery means duplicates are expected, not a bug; testing for "never duplicated" tests a guarantee the queue does not make.', 'The queue promises at-least-once, so duplicates are normal — what must your code do about them?'),
      miss('A performance test measuring how many events per second the consumer handles', 'Throughput says nothing about whether a redelivered event corrupts state; you can be fast and still double-charge.', 'Speed does not tell you what a duplicate does to your data.'),
      miss('A test that the consumer logs every event it receives', 'Logging receipt does not verify the side effect is deduplicated; you can log twice and still create two orders.', 'Logging a duplicate is not the same as handling it safely.'),
    ],
    lesson: 'At-least-once delivery guarantees a message is not lost, which means it can arrive more than once. The consumer must be idempotent (using an idempotency key or dedupe table), and the test that proves it delivers the same event twice and asserts the effect happened once. Assuming exactly-once delivery is a classic data-corruption trap.',
    source,
    generated: true,
  },
  {
    id: 7627011,
    chapter: 'Code, Review, and Quality',
    title: 'Test pyramid shape gone wrong',
    prompt: 'A team\'s suite is 80% slow end-to-end tests and 20% unit tests, and CI takes 45 minutes and flakes often. What does the "test pyramid" recommend instead?',
    correct: 'Invert the proportions: many fast unit tests at the base, fewer integration tests, and a thin layer of end-to-end tests for critical flows',
    wrong: [
      miss('Keep the shape but run the end-to-end tests on more parallel machines', 'Parallelism hides cost and flakiness temporarily but does not fix the brittleness of an E2E-heavy suite; the wrong shape is the root issue.', 'Throwing machines at slow flaky tests does not make them less flaky.'),
      miss('Remove the unit tests since the end-to-end tests already exercise that code', 'E2E tests touch the code but localize failures poorly and run slowly; deleting fast targeted tests makes diagnosis and feedback worse.', 'You would lose the cheapest, most precise feedback you have.'),
      miss('Convert all tests to end-to-end so coverage is maximally realistic', 'An all-E2E suite is the extreme of the current problem: maximally slow, flaky, and hard to debug, which is exactly the "ice-cream cone" anti-pattern.', 'That doubles down on the slow, flaky layer.'),
    ],
    lesson: 'The test pyramid puts many cheap, fast, deterministic unit tests at the base, fewer integration tests in the middle, and a thin top of end-to-end tests for the few critical user journeys. An E2E-heavy "inverted pyramid" (or ice-cream cone) is slow, flaky, and expensive, and gives slow, imprecise feedback.',
    source,
    generated: true,
  },
  {
    id: 7627012,
    chapter: 'Code, Review, and Quality',
    title: 'Regression vs new failure',
    prompt: 'A feature that worked for two years suddenly fails after an unrelated refactor merged today. In review terms, what is this, and what should it have had?',
    correct: 'A regression — previously-working behavior broke; a guarding test on that behavior would have caught the refactor\'s side effect',
    wrong: [
      miss('A flaky test, since the behavior is intermittent after the refactor', 'Flakiness is nondeterministic pass/fail with no code change; here a specific merge deterministically broke working behavior, which is a regression.', 'The cause is a known change today, not random nondeterminism.'),
      miss('Expected behavior, because refactors are allowed to change outputs', 'A refactor by definition preserves external behavior; if outputs changed, it was not a clean refactor and the broken behavior is a defect.', 'If a "refactor" changed what users see, was it really just a refactor?'),
      miss('A coverage gap that more end-to-end tests at the top of the pyramid would best fill', 'The missing protection is a targeted test on the specific behavior; reaching for slow E2E tests is the wrong, most expensive level for guarding one function\'s contract.', 'What is the cheapest test that would have failed when this behavior changed?'),
    ],
    lesson: 'A regression is when previously-working behavior breaks, often as a side effect of an unrelated change. The defense is a regression test that pins that behavior so any future change that breaks it fails CI. Refactors are supposed to be behavior-preserving, so a behavior change during one is a red flag, not "expected."',
    source,
    generated: true,
  },
  {
    id: 7627013,
    chapter: 'Code, Review, and Quality',
    title: 'Monitoring vs observability',
    prompt: 'Your dashboards cover every failure mode you anticipated, but a novel bug produced a symptom you never thought to graph. What capability do you actually need?',
    correct: 'Observability — the ability to ask new questions of rich telemetry (high-cardinality logs/traces) about failure modes you did not predict in advance',
    wrong: [
      miss('More monitoring — add a predefined dashboard for this specific new failure', 'You can only pre-build dashboards for failures you anticipate; the defining problem here is a failure you did not foresee, which monitoring cannot pre-cover.', 'You cannot pre-graph a symptom you never imagined — what lets you explore after the fact?'),
      miss('A higher alert sensitivity so unexpected failures trip an existing alert', 'Alerts fire on conditions you defined ahead of time; a truly novel symptom has no predefined condition to trip, so sensitivity does not help.', 'An alert can only catch what someone already described.'),
      miss('A longer metrics retention window so old data is available', 'Retention lets you look further back at the same predefined metrics; it does not let you ask a question you never instrumented for.', 'Keeping old aggregates longer does not add new dimensions to explore.'),
    ],
    lesson: 'Monitoring watches for known, predefined failure modes (dashboards and alerts you set up in advance). Observability is being able to interrogate rich, high-cardinality telemetry to debug "unknown unknowns" you never anticipated. Pre-built dashboards handle the failures you imagined; observability handles the ones you did not.',
    source,
    generated: true,
  },

  // ============================================================
  // Chapter 2: Architecture and Tradeoffs
  // (includes Ch.7 Data/State and Ch.8 Security routing)
  // ============================================================
  {
    id: 7627014,
    chapter: 'Architecture and Tradeoffs',
    title: 'A replica is not a backup',
    prompt: 'A team says "we\'re safe from data loss, we have a read replica." An engineer pushes back. Why is a replica not a backup?',
    correct: 'A replica faithfully copies writes including bad ones — an accidental DELETE or corruption replicates instantly, so it cannot restore a prior good state the way a backup can',
    wrong: [
      miss('A replica is a backup as long as it is in a different data center', 'Geographic separation protects against a site outage, not against logical corruption: a bad DELETE still replicates to the other site in seconds.', 'A faithful copy in another city is still a faithful copy of the mistake.'),
      miss('A replica is a stronger form of backup because it is always up to date', 'Being up to date is exactly the problem for recovery: it instantly mirrors the deletion you need to undo, leaving no earlier good state to restore.', 'Always-current means it also currently has the corruption — is that what you want to restore from?'),
      miss('They are equivalent; "backup" and "replica" are just two words for redundancy', 'Replication is for availability/read-scaling; backups are point-in-time copies for recovery. Conflating them is how teams discover they cannot restore.', 'One protects against a node dying; the other against a mistake — are those the same need?'),
    ],
    lesson: 'A replica protects against losing a node and helps scale reads, but it copies every write — including erroneous ones — so it cannot roll back to a prior good state. A backup is a point-in-time copy you can restore from, and it only counts if you have actually tested restoring it. "We have a replica" and "we have a tested backup" are different guarantees.',
    source,
    generated: true,
  },
  {
    id: 7627015,
    chapter: 'Architecture and Tradeoffs',
    title: 'Least privilege for a read job',
    prompt: 'A nightly reporting job only needs to read three tables, but it runs with a database admin credential "because that always works." What does least privilege require, and why does it matter?',
    correct: 'Scope the credential to read-only access on exactly those three tables, so a bug or a leak of that credential cannot drop tables or read unrelated data',
    wrong: [
      miss('Keep admin access but rotate the password monthly to stay safe', 'Rotation limits how long a leaked credential is valid but does nothing to limit what it can do while valid; an over-broad admin token is catastrophic the moment it leaks.', 'Rotation shrinks the time window, not the blast radius.'),
      miss('Give it write access too, in case the report ever needs to cache results', 'Granting capability for a hypothetical future need violates least privilege now and widens the blast radius for no current reason.', 'Provisioning for a "maybe later" need expands risk today.'),
      miss('Admin is fine because the job runs inside the trusted internal network', 'Network position is not authorization; an internal job with admin rights still has full blast radius if compromised, and "internal" traffic is not automatically safe.', 'Being inside the perimeter does not narrow what the credential can do.'),
    ],
    lesson: 'Least privilege means each actor gets exactly the permissions it needs and no more. A read-only job should hold read-only, narrowly-scoped credentials so a bug or a leaked token cannot delete data or reach unrelated systems. Broad credentials maximize blast radius; rotation and network position do not substitute for scoping.',
    source,
    generated: true,
  },
  {
    id: 7627016,
    chapter: 'Architecture and Tradeoffs',
    title: 'Cache invalidation story',
    prompt: 'A team adds a cache in front of the user-profile service to cut latency, but never decides how the cache is updated when a profile changes. What bug should they expect?',
    correct: 'Stale reads — users see old profile data after an update because nothing invalidates or refreshes the cached entry',
    wrong: [
      miss('A cache miss storm that permanently overloads the database', 'Missing an invalidation story causes staleness, not a permanent overload; the cache still serves (wrong) data quickly, which is precisely the problem.', 'Without invalidation the cache keeps answering — with what data?'),
      miss('Data corruption in the source-of-truth database', 'A read cache does not write to the source of truth, so it cannot corrupt it; the source stays correct while the cache serves outdated copies.', 'The database has the right value; what does the cache return?'),
      miss('Nothing — caches expire on their own so staleness is impossible', 'TTL expiry bounds staleness but does not eliminate it; until the entry expires, every read returns the old value, which is exactly the bug.', 'Even with a TTL, what does a read return before it expires?'),
    ],
    lesson: 'A cache without an invalidation strategy serves stale data: after the underlying value changes, cached reads keep returning the old value until something evicts or refreshes it. You must choose an invalidation approach (write-through, explicit invalidation on update, or a tight TTL) and accept the staleness window it implies. "Add a cache" is half a design; the invalidation story is the other half.',
    source,
    generated: true,
  },
  {
    id: 7627017,
    chapter: 'Architecture and Tradeoffs',
    title: 'Two sources of truth, no reconciliation',
    prompt: 'A customer\'s subscription status is stored independently in both the billing service and the auth service, with no sync. Over months they drift apart. What is the root design flaw?',
    correct: 'The same fact has two independent sources of truth with no reconciliation, so they inevitably diverge and neither can be trusted',
    wrong: [
      miss('The services are too tightly coupled and should share even less', 'The problem is duplicated authoritative state, not coupling; the fix is one source of truth others derive from, which actually requires a defined relationship, not less contact.', 'Storing the same fact twice is the issue — is that too much coupling or too little agreement?'),
      miss('A caching problem solvable by adding a TTL to both copies', 'Neither copy is a cache of the other; both are treated as authoritative, so a TTL has nothing to expire toward and the drift persists.', 'A cache needs a source to refresh from — which of these two is the source?'),
      miss('Simply a database performance issue from reading the status twice', 'Reading twice is cheap; the failure is correctness, not speed — the two stored values disagree.', 'The pain is wrong data, not slow data.'),
    ],
    lesson: 'A given fact should have exactly one source of truth; other places may cache or derive from it, but only one is authoritative. Storing the same authoritative fact in two systems with no reconciliation guarantees eventual divergence and an unanswerable "which is right?" The fix is to designate one owner and have the other read from or subscribe to it.',
    source,
    generated: true,
  },
  {
    id: 7627018,
    chapter: 'Architecture and Tradeoffs',
    title: 'Authentication vs authorization',
    prompt: 'A logged-in user requests an admin-only report and the API must reject it. Which status code fits, and what distinction does it encode?',
    correct: '403 Forbidden — the user is authenticated (identity is known) but not authorized for this resource',
    wrong: [
      miss('401 Unauthorized, because they lack permission for the report', '401 signals missing or invalid authentication ("we don\'t know who you are"); here identity is known and only authorization fails, which is 403.', 'They are already logged in — is the problem who they are, or what they may do?'),
      miss('200 OK with an empty body so the client doesn\'t see an error', 'Silently returning empty data hides an access-control failure and can mask bugs or leaks; the request must be explicitly refused.', 'Pretending success on a denied request is worse than a clear refusal.'),
      miss('500 Internal Server Error, since the request could not be fulfilled', '500 means the server failed unexpectedly; a permission denial is an expected, well-defined outcome, not a server fault.', 'The server worked correctly and chose to refuse — is that an error or a decision?'),
    ],
    lesson: 'Authentication answers "who are you?"; authorization answers "are you allowed to do this?". A 401 means authentication is missing or invalid; a 403 means you are authenticated but lack permission. Conflating them confuses clients (re-login won\'t fix a 403), and silently returning 200 hides an access-control decision that should be explicit.',
    source,
    generated: true,
  },
  {
    id: 7627019,
    chapter: 'Architecture and Tradeoffs',
    title: 'Strong vs eventual consistency tradeoff',
    prompt: 'A "like" counter on a social post can lag a second or two without harm, while a bank balance cannot. What does this contrast illustrate about choosing consistency?',
    correct: 'Consistency is a per-feature tradeoff: eventual consistency buys availability and scale where brief staleness is harmless; strong consistency is worth its cost where stale reads cause real harm',
    wrong: [
      miss('Strong consistency is always correct, so the like counter should use it too', 'Strong consistency costs latency and availability; spending that on a counter where a 2-second lag is invisible is over-engineering, not correctness.', 'Is a one-second-stale like count actually a problem worth paying for?'),
      miss('Eventual consistency is just a polite name for a bug and should be avoided', 'Eventual consistency is a deliberate, correct design choice for tolerant workloads, not a defect; it trades immediate agreement for availability and scale on purpose.', 'A delay you chose and bounded is a design, not an accident.'),
      miss('The two cases are identical because both are just numbers in a database', 'The data type is irrelevant; what differs is the cost of a stale read — negligible for likes, serious for a balance — which is what drives the choice.', 'Same data type, very different consequences for being slightly wrong — which matters?'),
    ],
    lesson: 'Strong consistency guarantees every read sees the latest write but costs latency and availability, especially under partitions. Eventual consistency lets replicas converge over time, buying availability and scale. The right choice depends on the cost of a stale read for that specific feature, not on a blanket rule.',
    source,
    generated: true,
  },
  {
    id: 7627020,
    chapter: 'Architecture and Tradeoffs',
    title: 'Cohesion and coupling, named correctly',
    prompt: 'You split a "utils" grab-bag into focused modules, and now each module changes for one reason and they rarely depend on each other\'s internals. Which property pair did you improve?',
    correct: 'You raised cohesion (each module does one related thing) and reduced coupling (modules depend less on each other\'s internals)',
    wrong: [
      miss('You raised coupling and reduced cohesion, which is the goal of modularity', 'That is backwards: good modularity means high cohesion and low coupling; raising coupling and lowering cohesion is the anti-pattern.', 'Which way should each metric move for clean modules?'),
      miss('You improved cohesion but coupling is irrelevant within one codebase', 'Coupling matters everywhere, including in-process; tightly coupled internal modules still propagate changes and break in surprising ways.', 'Do internal modules never affect each other when one changes?'),
      miss('Both terms describe the same thing, so only one actually changed', 'They are distinct axes: cohesion is within a module, coupling is between modules; a change can move both, but they are not synonyms.', 'One is about what is inside a module, the other about ties between modules.'),
    ],
    lesson: 'Cohesion measures how focused a single module is (does it do one well-defined thing?); coupling measures how dependent modules are on each other\'s internals. The durable goal is high cohesion and low coupling: each piece has a clear purpose and you can change one without a cascade through the others.',
    source,
    generated: true,
  },
  {
    id: 7627021,
    chapter: 'Architecture and Tradeoffs',
    title: 'Secure defaults',
    prompt: 'A new internal admin tool ships with authentication turned off by default and a note: "enable auth before production." What principle does this violate?',
    correct: 'Secure-by-default: the safe configuration should be the default, so a forgotten step fails closed (locked) rather than open (exposed)',
    wrong: [
      miss('Nothing — defaulting to open is fine because the deploy checklist covers enabling auth', 'Relying on a human remembering a checklist is exactly the failure mode secure defaults prevent; the one time it is forgotten, the tool ships wide open.', 'What happens the day someone skips that checklist line?'),
      miss('It violates least privilege, since the tool grants too many permissions', 'Least privilege is about scoping what an authenticated actor can do; here the issue is that the unsafe state (no auth at all) is the default.', 'The problem is the default posture, not the breadth of granted rights.'),
      miss('It violates idempotency because enabling auth twice could conflict', 'Idempotency is unrelated to default security posture; the flaw is that the default is insecure, not that an operation repeats.', 'This is about what state ships by default, not repeated operations.'),
    ],
    lesson: 'Secure defaults mean the out-of-the-box configuration is the safe one, so omissions fail closed rather than exposing the system. "Turn on security later" inverts this: it makes the insecure state the path of least resistance, and the one forgotten step becomes a breach. Default to locked; require an explicit, deliberate action to open up.',
    source,
    generated: true,
  },
  {
    id: 7627022,
    chapter: 'Architecture and Tradeoffs',
    title: 'Build vs buy for a commodity',
    prompt: 'A small team is about to build its own email-delivery infrastructure (deliverability, bounce handling, IP warmup) instead of using a vendor. What is the strongest argument to buy?',
    correct: 'Email deliverability is a deep, undifferentiated commodity; building it diverts scarce engineering from the product\'s actual differentiation and incurs ongoing maintenance for no competitive edge',
    wrong: [
      miss('Buying is always cheaper than building, so vendors should be the default for everything', 'Buy is not categorically cheaper; for core differentiating capabilities, building can be right. The argument here is specific to a commodity, not a universal rule.', 'Would you also buy the one thing that makes your product unique?'),
      miss('Building is better because owning the code means zero long-term cost', 'Owned code carries ongoing maintenance, on-call, and security cost; "we built it" is the start of a cost, not the end of one.', 'Does code stop costing anything the moment it ships?'),
      miss('It does not matter because email is trivial to implement correctly', 'Email deliverability (spam reputation, bounces, IP warmup, compliance) is notoriously hard; underestimating it is how teams sink months into a commodity.', 'Is "make sure mail lands in the inbox, not spam" actually easy?'),
    ],
    lesson: 'Build-vs-buy turns on whether the capability differentiates your product. Deep commodity infrastructure (email delivery, payments rails, auth) is usually better bought, freeing your engineers for what makes the product distinct. Build what is core and differentiating; buy the undifferentiated heavy lifting — and remember owned code has a permanent carrying cost.',
    source,
    generated: true,
  },
  {
    id: 7627023,
    chapter: 'Architecture and Tradeoffs',
    title: 'Dependency direction and the stable-abstraction idea',
    prompt: 'A volatile UI module is imported by a stable core-domain module, so every UI tweak forces the core to recompile and re-review. What dependency principle is violated?',
    correct: 'Dependencies should point toward stability: stable code must not depend on volatile code, so the core should not import the frequently-changing UI',
    wrong: [
      miss('Nothing — as long as it compiles, which module imports which is arbitrary', 'Direction is not arbitrary: it determines what change forces what to change; pointing a stable module at a volatile one drags churn into the stable core.', 'If it compiles but every UI edit recompiles the core, is the direction really harmless?'),
      miss('The fix is to merge the two modules so there is no dependency to worry about', 'Merging a stable domain with a volatile UI destroys cohesion and spreads the churn into one big module; the answer is to flip the direction, not erase the boundary.', 'Collapsing the boundary makes the whole thing as volatile as the UI.'),
      miss('The core should import even more UI modules to amortize the recompile cost', 'Adding dependencies on volatile code increases how often the core is disturbed; this worsens the exact problem.', 'More ties to churn means more churn, not less.'),
    ],
    lesson: 'Dependency direction matters: stable, slow-changing code should not depend on volatile, fast-changing code, or the volatility propagates upward. Point dependencies toward stable abstractions (e.g., have the UI depend on the core domain, not the reverse). This keeps the parts that should be steady from being dragged into every cosmetic change.',
    source,
    generated: true,
  },
  {
    id: 7627024,
    chapter: 'Architecture and Tradeoffs',
    title: 'Don\'t log secrets or PII',
    prompt: 'To debug an auth issue, an engineer adds a log line that prints the full request including the bearer token and the user\'s email. Why is this dangerous beyond the immediate bug?',
    correct: 'Logs are widely accessible, retained, and shipped to third-party tools; logging a token or PII leaks credentials and personal data into systems with a far larger blast radius than the request itself',
    wrong: [
      miss('It is fine in production as long as the log level is set to DEBUG', 'Log level controls verbosity, not who can read the logs; DEBUG logs still flow to storage and log vendors where the secret is now exposed.', 'Does setting DEBUG limit who can read what got written?'),
      miss('It only matters if the logs are public; internal logs are a safe place for secrets', 'Internal log stores are broadly accessible, retained for long periods, and often forwarded to external SaaS; "internal" is not a secure vault for credentials or PII.', 'How many people and tools can read your internal logs over the next year?'),
      miss('The real problem is just that it makes the log file larger', 'Size is trivial next to leaking a live credential and personal data into a long-lived, widely-read store; the harm is exposure, not bytes.', 'Compare a few extra bytes against a leaked live token — which is the danger?'),
    ],
    lesson: 'Logs are durable, broadly readable, and frequently exported to third-party platforms, so anything logged should be assumed widely visible for a long time. Logging secrets (tokens, keys) effectively leaks live credentials; logging PII creates privacy and compliance exposure. Redact or omit them, and treat a token in a log as a token that needs rotating.',
    source,
    generated: true,
  },
  {
    id: 7627025,
    chapter: 'Architecture and Tradeoffs',
    title: 'Replica lag read-after-write',
    prompt: 'A user updates their display name, the write goes to the primary, but their next page load reads from a replica that hasn\'t caught up and shows the old name. What is the cleanest fix?',
    correct: 'Route reads that must reflect a user\'s own recent write to the primary (read-your-writes), or wait for the replica to catch up before reading',
    wrong: [
      miss('Disable all replicas and serve every read from the primary forever', 'That eliminates lag but throws away the read-scaling the replicas exist to provide; you only need read-your-writes consistency for the user\'s own recent writes, not globally.', 'Do all reads need the latest write, or just this user\'s view of their own change?'),
      miss('Add a client-side retry that reloads the page a few times until the name updates', 'Blind retries waste requests, give a janky flicker, and still race replica lag with no guarantee of success.', 'Hammering reload gambles on the lag resolving — is that a guarantee?'),
      miss('Cache the old name longer so it stays consistent', 'Caching the stale value entrenches the wrong data; the user wants to see their new name, and a longer cache shows the old one even more reliably.', 'Holding the outdated value longer is the opposite of what the user needs.'),
    ],
    lesson: 'Primary/replica setups have replication lag, so a read from a replica can miss a write that just landed on the primary. The "read-your-writes" pattern routes a user\'s reads of their own recent writes to the primary (or pins to a position the replica has reached), giving that user a consistent view without giving up replica read-scaling for everyone else.',
    source,
    generated: true,
  },
  {
    id: 7627026,
    chapter: 'Architecture and Tradeoffs',
    title: 'Input validation against injection',
    prompt: 'A service builds a SQL query by concatenating a raw search string from the URL. A reviewer flags injection risk. What is the correct fix?',
    correct: 'Use parameterized queries (bound parameters) so user input is always treated as data, never as executable SQL',
    wrong: [
      miss('Blocklist the word "DROP" and a few other dangerous keywords in the input', 'Keyword blocklists are trivially bypassed (encoding, comments, alternate syntax) and never enumerate every dangerous case; parameterization removes the attack class entirely.', 'Can you really list every way to write malicious SQL?'),
      miss('Escape quotes by doubling them up manually before concatenating', 'Hand-rolled escaping is error-prone and misses edge cases across dialects and encodings; parameter binding is the standard, complete defense.', 'Manual escaping is a patch on the wrong approach — what eliminates the concatenation itself?'),
      miss('Run the query as a lower-privileged DB user so injection does less damage', 'Least privilege limits blast radius and is good practice, but it does not stop the injection — an attacker can still read or alter anything that user can.', 'Limiting damage is not the same as preventing the attack.'),
    ],
    lesson: 'Injection happens when untrusted input is interpreted as code (SQL, shell, etc.). The robust fix is to separate code from data: parameterized/bound queries ensure the input is always treated as a value, not executable SQL. Blocklists and manual escaping are leaky; least privilege reduces blast radius but does not prevent the injection.',
    source,
    generated: true,
  },

  // ============================================================
  // Chapter 3: Releases and Migrations
  // (includes Ch.7 Data and Ch.8 Security routing)
  // ============================================================
  {
    id: 7627027,
    chapter: 'Releases and Migrations',
    title: 'Error budget for 99.95%',
    prompt: 'A service targets a 99.95% availability SLO over a 30-day month (43,200 minutes). About how much downtime does the monthly error budget allow?',
    correct: 'About 21.6 minutes — 0.05% of 43,200 minutes',
    wrong: [
      miss('About 43 minutes — 0.1% of the month', 'That is the budget for a 99.9% SLO; 99.95% is half that loss, roughly 21.6 minutes, not ~43.', 'Use 0.05%, not 0.1%, for 99.95%.'),
      miss('About 4.3 minutes — 0.01% of the month', 'That is closer to a 99.99% target; 99.95% allows about five times more, around 21.6 minutes.', 'Match the percentage: 100% − 99.95% = 0.05%, not 0.01%.'),
      miss('Zero — 99.95% rounds up to "always available"', 'An SLO below 100% explicitly budgets downtime; 99.95% permits 0.05% unavailability, which is the whole point of an error budget.', 'Any SLO under 100% allows some downtime — how much is 0.05%?'),
    ],
    lesson: 'Error budget = (100% − SLO) of the period. For 99.95% over a 30-day month (43,200 min), 0.05% × 43,200 ≈ 21.6 minutes of allowed downtime. The budget is a spendable quantity: stay under it and you can keep shipping; burn through it and you slow down to protect reliability.',
    source,
    generated: true,
  },
  {
    id: 7627028,
    chapter: 'Releases and Migrations',
    title: 'Major version zero',
    prompt: 'A library is at 0.8.3 and ships a breaking change to its public API. Under SemVer, what version is acceptable, and why does this differ from a post-1.0 library?',
    correct: '0.9.0 is acceptable — under 0.y.z (initial development), the public API is not considered stable and breaking changes need not bump to 1.0',
    wrong: [
      miss('It must go to 1.0.0 because any breaking change always requires a MAJOR bump', 'That rule applies once you are at 1.0.0+; while in 0.y.z the API is explicitly unstable, so breaking changes do not force a 1.0 release.', 'Does the "MAJOR for breaking" rule apply before the API is declared stable at 1.0?'),
      miss('It stays 0.8.4 because PATCH covers any change during early development', 'PATCH is for backward-compatible fixes even pre-1.0; a breaking change is signaled by the next allowed bump (here the minor digit), not a patch.', 'Even in 0.x, a breaking change should not hide under a patch bump.'),
      miss('Versioning rules simply do not apply until 1.0.0, so any number is fine', 'SemVer does define 0.y.z behavior: 0.x is for rapid change and the API is unstable, but it is still a defined regime, not a free-for-all.', 'The spec has explicit rules for 0.x — it is not unstructured.'),
    ],
    lesson: 'SemVer treats 0.y.z as initial development: the public API is not stable and anything may change at any time. So a breaking change in 0.x can land as a minor bump (0.8.3 → 0.9.0) rather than forcing 1.0.0. The strict "breaking = MAJOR" guarantee begins at 1.0.0, which is when you commit to a stable public API.',
    source,
    generated: true,
  },
  {
    id: 7627029,
    chapter: 'Releases and Migrations',
    title: 'Dual-write divergence',
    prompt: 'During a migration you write each change to both the old and new stores in application code. The new store occasionally ends up with values the old one doesn\'t. What is the fundamental weakness of dual-write?',
    correct: 'The two writes are not atomic — if the process crashes or the second write fails after the first succeeds, the stores diverge with no automatic reconciliation',
    wrong: [
      miss('Dual-write is always safe; the divergence must be a bug in the new database engine', 'Dual-write is a known high-risk pattern precisely because the two writes can partially fail; blaming the engine misses the non-atomicity that is inherent to writing two places from app code.', 'What happens if the app dies between the first write and the second?'),
      miss('The fix is to make the writes faster so there is less time for them to diverge', 'Speed shrinks but never closes the window; even a microsecond gap allows a crash to leave one store updated and the other not.', 'A smaller window is still a window — does faster make it atomic?'),
      miss('The problem is only read performance, not write consistency', 'The symptom is values present in one store and absent in the other — a write-consistency/divergence problem, not a read-latency one.', 'The stores hold different data — is that about speed or correctness?'),
    ],
    lesson: 'Dual-write means the application writes to two stores separately, but those writes are not a single atomic transaction. A crash or partial failure between them leaves the stores inconsistent, and nothing reconciles the gap automatically. Safer patterns use a single source of truth plus change-data-capture or an outbox, with reconciliation/verification, rather than trusting two independent writes to stay in step.',
    source,
    generated: true,
  },
  {
    id: 7627030,
    chapter: 'Releases and Migrations',
    title: 'Adding a NOT NULL column safely',
    prompt: 'You need to add a required (NOT NULL) column to a large, actively-used table with zero downtime. What is the safe ordering?',
    correct: 'Add the column as nullable (or with a default), backfill existing rows, start writing the value in code, then add the NOT NULL constraint once every row is populated',
    wrong: [
      miss('Add the column with NOT NULL and no default in a single migration', 'On a large table this either fails because existing rows have no value, or locks the table while it rewrites every row, causing downtime — exactly what you must avoid.', 'What value do the millions of existing rows get the instant you demand NOT NULL?'),
      miss('Add NOT NULL first, then delete and re-insert every row with the value', 'Mass delete-and-reinsert is destructive, slow, lock-heavy, and risks data loss; the expand/contract approach achieves the same end without rewriting the table at once.', 'Re-inserting every row is a heavier, riskier rewrite than just backfilling.'),
      miss('Make the application validate the field and skip the database constraint entirely', 'App-only validation is bypassable (other writers, scripts, future services) and lets nulls slip in; the constraint is the durable guarantee.', 'If another writer ignores your app logic, what stops a null?'),
    ],
    lesson: 'Adding a NOT NULL column online follows expand/contract: first add it nullable or with a default (a fast metadata change in modern databases), backfill in batches, deploy code that always sets it, then tighten to NOT NULL once no nulls remain. Demanding NOT NULL up front on a populated table either fails or takes a blocking rewrite.',
    source,
    generated: true,
  },
  {
    id: 7627031,
    chapter: 'Releases and Migrations',
    title: 'Backward-compatible deploy ordering',
    prompt: 'You\'re rolling out a change where new code reads a new field. During a rolling deploy, old and new versions run simultaneously. What ordering keeps things working?',
    correct: 'Make the change backward-compatible: deploy the producer/schema change first so the field exists, and ensure new readers tolerate its absence, so old and new instances coexist safely during the rollout',
    wrong: [
      miss('Deploy the readers first so they\'re ready, then add the field afterward', 'New readers expecting a field that does not exist yet will fail for every request during the window before the field is added.', 'What do the new readers see in the gap before the field exists?'),
      miss('Take the whole service down, deploy everything at once, then bring it back up', 'A stop-the-world deploy is downtime, which defeats a rolling deploy and is exactly what compatible ordering lets you avoid.', 'A rolling deploy exists to avoid that outage — why reintroduce it?'),
      miss('It doesn\'t matter because a rolling deploy finishes in seconds', 'Even a brief overlap means old and new run together; a single request hitting the wrong combination fails, so ordering and compatibility still matter.', 'Even seconds of overlap serve real requests — can any of them break?'),
    ],
    lesson: 'During a rolling deploy, old and new code run at the same time, so each step must be backward- and forward-compatible. Expand first (add the field/endpoint so both versions work), migrate readers/writers, then contract (remove the old path) in a later deploy. Changes that require old and new to be incompatible at the same instant cannot be rolled out without downtime.',
    source,
    generated: true,
  },
  {
    id: 7627032,
    chapter: 'Releases and Migrations',
    title: 'Idempotency key for a payment POST',
    prompt: 'A "charge card" POST endpoint must be safe to retry after a client timeout without double-charging. POST isn\'t idempotent by default. What is the standard mechanism?',
    correct: 'Have the client send a unique idempotency key per logical charge; the server records it and returns the original result on any retry with the same key instead of charging again',
    wrong: [
      miss('Switch the endpoint to PUT, since PUT is idempotent', 'PUT is idempotent for replacing a resource at a known identifier, but "create a charge" has no client-chosen target id; an idempotency key, not the method, is what dedupes the create.', 'PUT\'s idempotency assumes you know the resource\'s id up front — do you, when creating a charge?'),
      miss('Tell the client never to retry, so a duplicate is impossible', 'Timeouts hide whether the charge succeeded, so a no-retry rule risks losing legitimate charges; the reliable answer is to make retries safe, not to forbid them.', 'After a timeout the client does not know if it worked — is "never retry" safe?'),
      miss('Add a unique database constraint on the amount and timestamp', 'Amount-plus-timestamp is not a reliable identity for a charge (legitimate identical charges exist; clocks differ), so it both blocks valid charges and misses duplicates.', 'Two separate valid charges can share an amount and near-identical time — is that a safe key?'),
    ],
    lesson: 'POST is not idempotent, so a retried charge can duplicate. The standard fix is an idempotency key: the client generates a unique key per intended operation, the server stores it with the result, and any retry carrying the same key returns the stored result instead of executing again. This makes "create" operations safe to retry across timeouts.',
    source,
    generated: true,
  },
  {
    id: 7627033,
    chapter: 'Releases and Migrations',
    title: 'Migrating reads in expand/contract',
    prompt: 'You\'re mid-way through an expand/contract column migration: new column added, both columns written, old data backfilled. The next step is to switch reads to the new column. Why is this step ordered after backfill but before the drop?',
    correct: 'Reads can only safely move once the new column is fully populated (backfill done), and the old column must remain until nothing reads it, so reads migrate after backfill and before the drop',
    wrong: [
      miss('Reads should switch before backfill so you find missing data faster', 'Switching reads before backfill means the new column is partially empty, so reads return nulls/wrong data for un-backfilled rows — a live correctness bug, not a clever test.', 'If you read the new column before it is filled, what comes back for the old rows?'),
      miss('The order is arbitrary; reads, backfill, and drop can happen in any sequence', 'Each step depends on the previous: you cannot read a column that is not populated, and you cannot drop one still being read. The sequence is a safety invariant, not a preference.', 'Could you safely drop a column that reads still depend on?'),
      miss('Reads and the drop should happen together in one deploy to save a step', 'Dropping the old column the instant you switch reads removes the rollback path; if the new read has a problem you have nothing to fall back to.', 'If the new read misbehaves right after the drop, what do you revert to?'),
    ],
    lesson: 'Expand/contract is a dependency chain: add the new column, dual-write, backfill, then migrate reads (only safe once the new column is complete), and finally drop the old column (only safe once nothing reads it). Keeping the old column briefly after switching reads preserves a rollback path. Each step is independently deployable and reversible, which is the whole point.',
    source,
    generated: true,
  },
  {
    id: 7627034,
    chapter: 'Releases and Migrations',
    title: 'Rotate, don\'t just delete the commit',
    prompt: 'An engineer notices an AWS access key was committed to the repo last week. They delete the line and push a "fix" commit. Why is this insufficient?',
    correct: 'The key still exists in git history (and every clone, CI cache, and possibly a scraper\'s database); the only real remediation is to revoke/rotate the key immediately',
    wrong: [
      miss('It\'s sufficient, because the latest commit no longer contains the key', 'Deleting the line only changes the current tip; the secret remains in the prior commit\'s history forever and in every existing clone, so it is still compromised.', 'Does removing a line from today\'s file erase it from last week\'s commit?'),
      miss('Just make the repository private and the key is safe again', 'The key may already have been cloned or scraped while public, and even private repos have many readers; privacy does not un-leak a credential that already left.', 'Once a secret was exposed, does making the repo private retract it?'),
      miss('Rewrite git history to remove the commit and consider it fully resolved', 'History rewriting helps stop future discovery but cannot recall copies already pulled or scraped; without rotation the key is still live and exploitable.', 'History rewriting cannot reach clones others already have — is the key still valid?'),
    ],
    lesson: 'A committed secret must be treated as compromised the moment it lands, because it persists in git history and in every clone, CI cache, and possibly attackers\' scrapers. The first and essential step is to revoke and rotate the credential; cleaning history is a useful secondary step but never a substitute. Deleting the line alone leaves a live, exposed key.',
    source,
    generated: true,
  },
  {
    id: 7627035,
    chapter: 'Releases and Migrations',
    title: 'Canary that watches the wrong metric',
    prompt: 'A canary sends 2% of traffic to a new build and auto-promotes if "the canary instances stay up." The build is up but silently returns wrong results for that 2%. What did the canary get wrong?',
    correct: 'It gated on liveness instead of the success/quality signals that reflect user impact, so a healthy-but-wrong build passed the canary',
    wrong: [
      miss('The canary percentage was too low; 2% can\'t reveal anything', 'Two percent is a normal, even generous canary slice; the failure is the metric it judged, not the size of the slice — wrong results would show in error/quality metrics at 2%.', 'Would a bigger slice help if you are still only checking that it is up?'),
      miss('Canaries are unreliable in general and the team should use blue-green instead', 'Blue-green would flip 100% of traffic at once with the same blind spot; the problem is the validation signal, not the rollout strategy.', 'Would switching all users at once with the same "is it up?" check be safer?'),
      miss('The build should have been promoted faster to limit canary exposure time', 'Promoting faster spreads the bad build to everyone sooner; the canary is meant to catch this before promotion, which it failed to do by watching the wrong signal.', 'Promoting a wrong build sooner helps whom?'),
    ],
    lesson: 'A canary is only as good as the metrics it evaluates. "Instances stay up" misses builds that run fine but produce wrong or failing responses. Gate canaries on user-impact signals — error rate, latency, and business/quality metrics compared against the baseline — so a healthy-but-incorrect release is caught while only a small slice is exposed.',
    source,
    generated: true,
  },
  {
    id: 7627036,
    chapter: 'Releases and Migrations',
    title: 'Feature flag vs deploy',
    prompt: 'A team wants to turn a risky feature off in seconds if it misbehaves, without redeploying. Which mechanism gives them that, and what is the key distinction from a deploy?',
    correct: 'A feature flag — it toggles behavior at runtime independent of the deployed artifact, so you can disable the feature instantly without shipping new code',
    wrong: [
      miss('A faster CI pipeline, so the redeploy-to-disable takes only a minute', 'Even a fast redeploy is slower and riskier than a runtime toggle, and it couples "turn this off" to a full deploy; the point of a flag is to decouple them.', 'Why redeploy at all if you only need to flip behavior?'),
      miss('A rollback, which is the same thing as a feature flag', 'A rollback reverts the whole deployed artifact (and everything in it); a flag toggles one behavior precisely while the same code stays deployed. They are different tools.', 'Do you want to revert the entire release or switch off one feature?'),
      miss('A canary, since canaries let you turn features off', 'A canary controls how much traffic reaches a new deployment, not whether a specific feature is on; it does not give a per-feature runtime kill switch.', 'Canary is about traffic to a build, not toggling one feature in place.'),
    ],
    lesson: 'A feature flag decouples release (shipping code) from activation (turning a feature on). That lets you deploy code dark and flip a feature on or off at runtime in seconds — a kill switch — without a redeploy. A rollback reverts the whole artifact; a canary controls traffic to a new build. Flags are the precise, instant runtime control.',
    source,
    generated: true,
  },
  {
    id: 7627037,
    chapter: 'Releases and Migrations',
    title: 'Backfill that races live writes',
    prompt: 'A backfill script populates a new column for old rows while the application is live and also writing that column for new activity. Without care, what subtle bug appears?',
    correct: 'The backfill can overwrite a fresher value the live application just wrote, so the script must only fill rows still missing the value (or use conditional writes), never blindly overwrite',
    wrong: [
      miss('There is no risk because the backfill and the app write to different rows', 'They can absolutely touch the same row: a row the backfill is processing may be updated by live traffic in the same moment, and an unconditional backfill clobbers it.', 'Can a live request update a row the backfill is mid-processing?'),
      miss('The only risk is the backfill being slow; correctness is not affected', 'Slowness is an operational concern, but the data bug here is correctness: a blind backfill reverts a newer live value to a stale computed one.', 'Could the backfill replace a value the app just wrote with an older one?'),
      miss('Run the backfill in one big transaction so it wins any conflict', 'A giant transaction holds locks long enough to stall live traffic and still can\'t safely "win" against newer business writes — it would discard legitimate updates.', 'Locking the whole table to win conflicts trades a data bug for an outage.'),
    ],
    lesson: 'An online backfill races live writes for the same rows. If it overwrites unconditionally, it can replace a fresh value the application just wrote with a stale, recomputed one. Make the backfill conditional — only set the value where it is still missing, or use a compare-and-set — and run it in small batches so it does not lock out live traffic.',
    source,
    generated: true,
  },
  {
    id: 7627038,
    chapter: 'Releases and Migrations',
    title: 'Vulnerable dependency triage',
    prompt: 'A scanner flags a CVE with CVSS 9.8 in a library you depend on. Before treating it as a drop-everything emergency, what most determines real urgency?',
    correct: 'Whether the vulnerable code path is actually reachable and exploitable in your usage, and whether it is being exploited in the wild (e.g., on CISA\'s KEV list) — not the CVSS score alone',
    wrong: [
      miss('The CVSS score alone — 9.8 means it is always a top-priority emergency', 'CVSS measures theoretical severity, not whether your application reaches the vulnerable code or whether anyone is exploiting it; a 9.8 in an unreachable path may be low real risk.', 'Does a high severity score know whether your code even calls the vulnerable function?'),
      miss('How many other companies use the same library', 'Popularity of the library does not tell you about your exposure or exploitability; your reachability and active exploitation are what set your urgency.', 'Other companies\' usage is not your attack surface.'),
      miss('The age of the CVE — older CVEs are always safe to ignore', 'Age is not safety: old vulnerabilities are routinely exploited (often more, since exploits are mature). Reachability and active exploitation matter, not how old the advisory is.', 'Do attackers stop using an exploit just because it is old?'),
    ],
    lesson: 'CVSS rates a vulnerability\'s intrinsic severity, but your real urgency depends on reachability (does your code actually exercise the vulnerable path?) and exploitation in the wild (is it on CISA\'s KEV catalog or scoring high on EPSS?). A 9.8 in an unreachable dependency may be low priority; a moderate-scored bug being actively exploited on a reachable path jumps the queue. Triage by exploitability, not score alone.',
    source,
    generated: true,
  },

  // ============================================================
  // Chapter 4: Incidents and Reliability
  // ============================================================
  {
    id: 7627039,
    chapter: 'Incidents and Reliability',
    title: 'Page vs ticket',
    prompt: 'A background nightly report failed and can simply be re-run in the morning with no user impact. Someone set it to page on-call at 3 a.m. What is the right alerting choice?',
    correct: 'Route it to a ticket or low-priority queue, not a page — pages should be reserved for things that need a human right now',
    wrong: [
      miss('Keep it as a page so nothing is ever missed', 'Paging on non-urgent, no-impact failures is how on-call burns out and starts ignoring pages, so the next real page gets missed; "never miss anything" via paging backfires.', 'If everything pages, what happens to attention for the page that matters?'),
      miss('Delete the alert entirely so no one is bothered', 'Removing all signal means a persistent or worsening failure goes unnoticed; the fix is to downgrade the channel, not to go blind.', 'There is a middle ground between waking someone and seeing nothing.'),
      miss('Page two engineers instead of one for redundancy', 'Doubling who is woken for a non-urgent, no-impact failure multiplies fatigue without adding any value, since no immediate action is required.', 'Waking more people for something that can wait helps how?'),
    ],
    lesson: 'A page interrupts a human because action is needed now; a ticket/alert records something to handle during business hours. Reserve pages for user-impacting or time-sensitive conditions. Paging on benign, deferrable failures causes alert fatigue, which erodes response to the pages that actually matter. Match the urgency of the notification to the urgency of the problem.',
    source,
    generated: true,
  },
  {
    id: 7627040,
    chapter: 'Incidents and Reliability',
    title: 'Incident commander\'s real job',
    prompt: 'In a messy SEV1 with five engineers all typing fixes at once, what is the primary role of the incident commander (IC)?',
    correct: 'To coordinate — own the response, assign roles, track actions, and drive decisions — not necessarily to be the one debugging or fixing',
    wrong: [
      miss('To personally find and fix the root cause, since the IC should be the best engineer', 'The IC\'s value is coordination and clear decision ownership; if the most capable engineer is buried in a debugger, no one is steering the overall response.', 'Who is directing the room if the IC is heads-down in the code?'),
      miss('To write the postmortem while the incident is still ongoing', 'The postmortem comes after; during the incident the IC keeps the response organized and moving, capturing a timeline for later, not authoring the analysis now.', 'Is documenting the lessons the urgent need while users are down?'),
      miss('To approve every individual command before anyone runs it', 'Micro-approving each command bottlenecks the response; the IC delegates clear ownership and decisions, not keystroke-level sign-off.', 'Gating every command on one person slows the very response they manage.'),
    ],
    lesson: 'The incident commander coordinates the response: they hold the overall picture, assign clear roles (comms, scribe, ops), prevent duplicated or conflicting actions, and own key decisions like "roll back now." The IC deliberately stays out of the weeds so someone is steering. Mixing the coordinator and the hands-on debugger into one overloaded person is how incidents become chaotic.',
    source,
    generated: true,
  },
  {
    id: 7627041,
    chapter: 'Incidents and Reliability',
    title: 'Mitigation is not root cause',
    prompt: 'Rolling back a deploy stopped the errors and restored service. A manager says "great, incident closed, root cause found." What is the precise correction?',
    correct: 'The rollback mitigated user impact, but the root cause — why the new code failed — is still unknown and must be investigated before re-shipping',
    wrong: [
      miss('The manager is right; if it\'s working again, the root cause is resolved', 'Restoring service via rollback removes the symptom, not the underlying defect; re-deploying the same code would reproduce the incident, so the cause is unresolved.', 'If you redeployed the exact same build now, would it break again?'),
      miss('Root cause and mitigation are the same thing whenever a rollback works', 'They are distinct: mitigation stops the bleeding; root cause explains why it bled. A working rollback proves mitigation, not understanding.', 'Stopping the pain is not the same as knowing what caused it.'),
      miss('The cause must be the rollback target, so the previous version is the root cause', 'The previous version is the known-good state you returned to, not the cause; the defect lives in the new change you rolled back from.', 'You rolled back to the version that works — is that the culprit?'),
    ],
    lesson: 'Mitigation restores service (rollback, failover, shedding load); root cause is the underlying reason it broke. A successful rollback proves you mitigated, but the bug in the new code is still there and will recur if you re-ship without understanding it. "Restore first, explain second" — and the explanation is a separate, necessary step before re-release.',
    source,
    generated: true,
  },
  {
    id: 7627042,
    chapter: 'Incidents and Reliability',
    title: 'Burning the error budget',
    prompt: 'Three weeks into the month, a service has already consumed its entire monthly error budget for its 99.9% SLO. The team wants to ship a risky new feature. What does error-budget policy suggest?',
    correct: 'Freeze risky releases and prioritize reliability work until the budget recovers, since further unreliability would breach the SLO commitment',
    wrong: [
      miss('Ship anyway — the SLO is just an internal target with no teeth', 'The point of an error budget is to convert the SLO into a release policy; ignoring an exhausted budget defeats the mechanism and risks breaching commitments to users.', 'If exhausting the budget changes nothing, what was the budget for?'),
      miss('Raise the SLO to 99.99% so there is more budget to spend', 'Tightening the SLO shrinks the allowed downtime, giving less budget, not more — and you cannot conjure reliability by editing the target.', 'Does a stricter target create slack or remove it?'),
      miss('Reset the budget early because three weeks is "close enough" to a month', 'Resetting the budget on demand removes the consequence that makes it work; the budget is tied to the actual period, not to convenience.', 'If you can reset the budget whenever it is inconvenient, does it constrain anything?'),
    ],
    lesson: 'An error budget turns an SLO into a decision rule: while budget remains, ship freely; when it is exhausted, slow down and invest in reliability until it recovers. That tension is the feature — it balances velocity against stability with a number instead of an argument. Exhausting the budget early is a signal to freeze risky changes, not to redefine the target or reset the clock.',
    source,
    generated: true,
  },
  {
    id: 7627043,
    chapter: 'Incidents and Reliability',
    title: 'Deploy timing as a signal',
    prompt: 'Latency tripled at 14:32. The team has timestamps for every deploy and config change. What is the fastest high-value question to ask first?',
    correct: '"What changed right before 14:32?" — correlate the onset with recent deploys/config changes, since most incidents follow a change',
    wrong: [
      miss('"Which engineer is most likely at fault?" so we know who to talk to', 'Hunting for a person is both blameful and slow; the fast diagnostic signal is what changed near the onset, not who.', 'Are you looking for a name or for the change that lines up with the spike?'),
      miss('"Should we rewrite the service to be faster?" since latency is the symptom', 'A rewrite is a months-long project, not an incident response; during an active incident you correlate and mitigate, you do not re-architect.', 'Is a rewrite a same-incident action?'),
      miss('"Is the monitoring graph definitely accurate?" before doing anything else', 'Second-guessing instrumentation first wastes time while users suffer; assume the signal until proven otherwise and chase the change that aligns with the onset.', 'Doubting the dashboard first stalls the response — what aligns with the spike?'),
    ],
    lesson: 'Most incidents are triggered by a change, so the highest-yield first question is "what changed right before the symptom started?" Deploy and config timestamps let you line up onset with a candidate cause in seconds, often pointing straight at a rollback. It is faster and more reliable than guessing at architecture or assigning blame.',
    source,
    generated: true,
  },
  {
    id: 7627044,
    chapter: 'Incidents and Reliability',
    title: 'SLA breach vs SLO miss',
    prompt: 'A service had a bad month and dipped to 99.0% availability. Its internal SLO is 99.9% and its customer SLA promises 99.5%. What is the situation?',
    correct: 'Both were missed, but only the SLA breach carries contractual consequences (e.g., credits); the SLO miss is an internal warning that fired earlier by design',
    wrong: [
      miss('Only the SLO was missed, since the SLA is just an aspirational internal goal', 'It is reversed: the SLA is the external contract with consequences and the SLO is the internal target; at 99.0% both the 99.9% SLO and the 99.5% SLA were breached.', 'Which number is the customer-facing promise with penalties?'),
      miss('Nothing was breached because 99.0% rounds up to "about 99.9%"', '99.0% is far below both targets; rounding away a 0.9-point gap ignores that 99.0% allows roughly ten times the downtime of 99.9%.', 'Is the gap between 99.0% and 99.9% small in downtime terms?'),
      miss('Only the SLA was missed; the SLO is met because it is stricter', 'A stricter target is harder to meet, so 99.0% misses the 99.9% SLO by more, not less; being met is not implied by strictness.', 'A tougher target is easier or harder to fall short of?'),
    ],
    lesson: 'The SLO (internal target, here 99.9%) is deliberately stricter than the SLA (customer contract, here 99.5%), so the SLO trips first as an early warning before you breach what you promised customers. At 99.0% both are missed, but only the SLA breach triggers contractual remedies. Setting the SLO tighter than the SLA is what gives you headroom to react before penalties hit.',
    source,
    generated: true,
  },
  {
    id: 7627045,
    chapter: 'Incidents and Reliability',
    title: 'Preserving evidence while mitigating',
    prompt: 'Rolling back will fix the outage but will also wipe the logs and state on the bad instances that explain the cause. What is the balanced move?',
    correct: 'Mitigate to restore users first, but capture the needed evidence (snapshot logs/heap/state) before or as you roll back, so root cause is still investigable',
    wrong: [
      miss('Keep the broken instances live to preserve evidence until root cause is found', 'Leaving the outage running makes users pay for the investigation; restoring service comes first, with evidence captured rather than left in production.', 'Who absorbs the cost while you keep the broken version up to study it?'),
      miss('Roll back immediately and don\'t worry about evidence; you can reproduce it later', 'Discarding all forensic data often makes the cause unreproducible; a quick snapshot costs little and preserves the ability to fix it for good.', 'If the state is gone, how do you investigate what you cannot reproduce?'),
      miss('Skip mitigation and write the postmortem from memory afterward', 'The postmortem is after the fact and cannot restore service; memory is a poor substitute for captured evidence, and users stay down meanwhile.', 'Does writing it up later restore service or recover the lost state?'),
    ],
    lesson: 'Restoring users is the priority, but a good responder grabs the evidence needed for root cause before it is destroyed: snapshot logs, capture a heap dump, copy the bad config or state. It is usually a few seconds of work that preserves the ability to truly fix the problem, so you do not have to choose between fast recovery and ever understanding what happened.',
    source,
    generated: true,
  },
  {
    id: 7627046,
    chapter: 'Incidents and Reliability',
    title: 'Toil vs engineering work',
    prompt: 'On-call spends hours each week manually restarting a service that hangs and clearing a queue by hand. SRE practice calls this "toil." What is the prescribed response?',
    correct: 'Treat recurring manual operational work as toil to be automated or designed away, since it scales with load and adds no lasting value',
    wrong: [
      miss('Accept it as the normal cost of running software and hire more on-call staff', 'Adding people to absorb toil scales headcount with load instead of removing the work; SRE explicitly aims to cap and reduce toil, not staff up to endure it.', 'Does hiring to do the manual work make the manual work go away?'),
      miss('Page the on-call faster each time the service hangs so it\'s handled sooner', 'Faster paging just makes the human do the repetitive fix sooner; it does not eliminate the recurring manual work, which is the actual problem.', 'Quicker manual fixes are still manual fixes — what removes them?'),
      miss('It\'s fine as long as the manual steps are well documented in a runbook', 'A runbook makes toil repeatable but still manual; documentation is a stepping stone, and the goal is to automate the toil away, not to enshrine it.', 'A documented repetitive chore is still a repetitive chore.'),
    ],
    lesson: 'Toil is manual, repetitive, automatable operational work that scales with the service and produces no lasting improvement (restarting hung processes, clearing queues by hand). SRE practice caps toil and invests in automating or engineering it away, because absorbing it with more people or faster paging just grows the burden as the system grows.',
    source,
    generated: true,
  },

  // ============================================================
  // Chapter 5: Technical Debt and Team Communication
  // (includes Ch.9 Working Effectively in a Team routing)
  // ============================================================
  {
    id: 7627047,
    chapter: 'Technical Debt and Team Communication',
    title: 'Disagree and commit',
    prompt: 'After a thorough design discussion, the team picks option A. You preferred B but were outvoted on reasonable grounds. What does "disagree and commit" ask of you?',
    correct: 'Voice your concern fully during the decision, then once it is made, support option A\'s execution rather than relitigating it or quietly undermining it',
    wrong: [
      miss('Keep arguing for B in every standup until people reconsider', 'Relitigating a settled, reasonably-made decision blocks progress and erodes the team\'s ability to decide; the time to argue was during the discussion, which already happened.', 'The decision was made on reasonable grounds — what does continuing to fight it cost the team?'),
      miss('Silently implement B anyway because you know it\'s better', 'Quietly subverting the agreed decision is the opposite of committing and breaks trust; it also splits the system between two designs.', 'Is undermining the agreed choice the same as committing to it?'),
      miss('Refuse to work on A since you didn\'t agree with it', 'Withholding effort because you lost a reasonable decision stalls delivery and is not how teams function; disagreement does not exempt you from execution.', 'Does losing a fair decision mean you stop contributing?'),
    ],
    lesson: '"Disagree and commit" lets a team move past genuine disagreement without endless deadlock: you make your case during the decision, and once a reasonable choice is made, you back its execution wholeheartedly even if it was not your pick. It depends on the disagreement actually being heard first — and on not relitigating or sabotaging afterward.',
    source,
    generated: true,
  },
  {
    id: 7627048,
    chapter: 'Technical Debt and Team Communication',
    title: 'Runbook that survives a vacation',
    prompt: 'A runbook for recovering the payments worker says only "restart it and check it\'s healthy." When the expert is away, a teammate still can\'t use it. What is the runbook missing?',
    correct: 'Concrete, testable steps: exact commands, how to verify health, what to check if the restart fails, and when/how to escalate',
    wrong: [
      miss('Nothing — a runbook should stay high-level so it doesn\'t go out of date', 'A runbook too vague to follow under pressure is useless precisely when it is needed; specificity is the point, and you keep it current by testing it, not by leaving it abstract.', 'Can a stranger actually recover the system from "restart it and check it\'s healthy"?'),
      miss('A longer narrative explaining the history of the payments worker', 'Background prose does not tell a stressed responder what command to run next; a runbook is an actionable procedure, not a history lesson.', 'During an outage, does the responder need the backstory or the next exact step?'),
      miss('The name of the original author so people know who to credit', 'Attribution does not help someone recover the system; the missing thing is executable steps, not a byline.', 'Will knowing who wrote it tell you which command to run?'),
    ],
    lesson: 'A runbook earns its keep when someone unfamiliar can follow it under stress: exact commands, expected outputs, how to confirm recovery, what to do when the happy path fails, and a clear escalation point. Vague guidance ("restart and check it\'s healthy") collapses the moment the one expert is unavailable — which is exactly when the runbook is supposed to save you. Test it by having a non-expert run it.',
    source,
    generated: true,
  },
  {
    id: 7627049,
    chapter: 'Technical Debt and Team Communication',
    title: 'Cloud cost is a shared concern',
    prompt: 'An engineer notices a debug feature flag is sending full request bodies to an expensive log vendor, quietly costing thousands a month. What is the right framing?',
    correct: 'Treat runaway cost like a bug: flag it with the data, propose the fix (disable the verbose logging), and own it rather than assuming spend is someone else\'s problem',
    wrong: [
      miss('Ignore it — cloud spend is finance\'s or the platform team\'s problem, not an engineer\'s', 'Cost is a shared engineering concern; the person who can see the waste and fix it is best placed to act, and "not my job" lets it bleed indefinitely.', 'Who else even knows this debug flag is the cause?'),
      miss('Quietly disable the flag in production without telling anyone', 'Silent changes, even good ones, skip review and can hide a reason the verbose logging was on; flag it and fix it transparently, the same as any change.', 'Does an unreviewed silent prod change fit how the team handles changes?'),
      miss('Wait until the monthly bill is reviewed so the right meeting can decide', 'Deferring a known, fixable waste burns thousands more while waiting; you would not sit on a known bug until a meeting, and cost is no different.', 'Would you let a known bug run for a month to wait for a meeting?'),
    ],
    lesson: 'Cost and efficiency are shared engineering responsibilities, not solely finance\'s. An engineer who spots avoidable spend should surface it with evidence, propose the fix, and follow the normal change process — exactly as they would for a bug. Treating cloud spend as "someone else\'s problem" is how small misconfigurations quietly become large recurring bills.',
    source,
    generated: true,
  },
  {
    id: 7627050,
    chapter: 'Technical Debt and Team Communication',
    title: 'Hiding cleanup inside a feature',
    prompt: 'To avoid asking for cleanup time, an engineer buries a large refactor inside an unrelated feature PR. Beyond review difficulty, why is this a bad team practice?',
    correct: 'It evades transparent prioritization and makes the change hard to review or revert, so the team can neither weigh the cleanup nor isolate it if the feature must roll back',
    wrong: [
      miss('It\'s actually smart, since it gets necessary cleanup done without bureaucracy', 'Bypassing prioritization erodes trust and review, and entangles the cleanup with the feature so neither can be reasoned about independently; "smart" here means unaccountable.', 'If the feature must be reverted, what happens to the smuggled refactor?'),
      miss('The only downside is that the PR is slightly larger than necessary', 'Size is the least of it: the deeper harm is bypassed prioritization and an un-isolatable, un-revertable change, not a few extra lines.', 'Is the problem the line count or that two unrelated changes are now fused?'),
      miss('It\'s fine as long as the tests still pass on the combined PR', 'Passing tests do not make the entanglement reviewable or revertable, nor do they restore the team\'s ability to prioritize the cleanup honestly.', 'Green tests do not let you separate the feature from the refactor later.'),
    ],
    lesson: 'Smuggling cleanup into an unrelated feature dodges the honest conversation about whether the cleanup is worth doing now, and it fuses two independent changes so the team cannot review, prioritize, or revert them separately. Make the case for cleanup openly and ship it as its own change; the right answer to "they never give us cleanup time" is to argue it in business terms, not to hide it.',
    source,
    generated: true,
  },
  {
    id: 7627051,
    chapter: 'Technical Debt and Team Communication',
    title: 'Stakeholder update in business terms',
    prompt: 'You must update a non-technical product leader on a fragile auth module. Which one-paragraph update is most useful to them?',
    correct: '"The login system causes about one outage a month and slows every related feature; a two-week fix would cut both. Risk if we wait: more outages and slower delivery."',
    wrong: [
      miss('"We need to refactor the AuthManager class because it has high cyclomatic complexity and tight coupling."', 'Internal code metrics mean nothing to a product leader; they decide by business impact (outages, delivery speed, cost), which this sentence never states.', 'Does "cyclomatic complexity" tell a product leader what it costs the business?'),
      miss('"Trust us, the auth code is bad and we really need to fix it soon."', 'An appeal to trust gives the leader nothing to weigh against competing priorities; they need quantified consequence and a scoped ask.', 'What can the leader compare this against without numbers or impact?'),
      miss('"Everything is basically fine; we\'ll clean up auth whenever we get spare time."', 'Downplaying a module that causes monthly outages misinforms the leader and guarantees the work is never prioritized, letting the risk compound.', 'If you say it\'s fine, when does the recurring outage ever get fixed?'),
    ],
    lesson: 'Stakeholder updates land when framed in the leader\'s terms: user/business impact, a concrete cost, a scoped fix, and the risk of inaction. Quantify the consequence (outages, slowed delivery, spend) rather than reaching for internal code metrics or asking them to take it on faith. The goal is to give a decision-maker something they can actually prioritize against other work.',
    source,
    generated: true,
  },
  {
    id: 7627052,
    chapter: 'Technical Debt and Team Communication',
    title: 'Escalate without blame',
    prompt: 'Your team is blocked because another team\'s API has been broken for two days and informal pings went nowhere. You need to escalate. What framing is most effective?',
    correct: 'Escalate on impact and the ask: "Our launch is blocked by the X API returning errors since Tuesday; we need an owner and an ETA," addressed to the right manager',
    wrong: [
      miss('"Team X is incompetent and keeps breaking things; please make them fix it."', 'Framing escalation as an accusation puts the other team on the defensive and slows resolution; effective escalation describes impact and a concrete ask, not blame.', 'Does calling them incompetent make them want to help you faster?'),
      miss('Keep sending informal pings, since escalating would damage the relationship', 'Two days of ignored pings on a launch-blocking issue is exactly when escalation is warranted; avoiding it lets the block persist and is not "protecting" anyone.', 'How long should a launch-blocking issue sit on ignored pings?'),
      miss('Go straight to the CTO to maximize pressure', 'Jumping several levels over a team-level block is disproportionate and burns goodwill; escalate to the appropriate manager first, with room to go higher if needed.', 'Is the CTO the right first stop for a two-team coordination block?'),
    ],
    lesson: 'Good escalation is blameless and specific: state the business impact, the concrete blocker, what you have already tried, and the exact ask (an owner and an ETA), directed to the appropriate level. Framing it as an accusation or jumping straight to the top makes people defensive and slows the fix. Escalation is a coordination tool, not a weapon.',
    source,
    generated: true,
  },
])
