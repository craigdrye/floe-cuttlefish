import { makeQuestionBank } from './base'
import type { Question } from './types'

const miss = (answer: string, why: string, hint: string): [string, string, string] => [answer, why, hint]
const source = 'Floe alevelComputerScience top-up'

export const alevelComputerScienceHigh103TopUpBQuestions: Question[] = makeQuestionBank('High', [
  // ---------------------------------------------------------------------------
  // Data structures
  // ---------------------------------------------------------------------------
  {
    id: 7909500,
    chapter: 'Data structures',
    title: 'Stack behaviour',
    prompt: 'A stack is implemented as a static array with a top pointer. The values 4, 9, 2 are pushed in that order, then one item is popped and the value 7 is pushed. Reading from the bottom to the top, what does the stack now contain?',
    mentorHint: 'A stack is LIFO, so the last value pushed is the first removed.',
    correct: '4, 9, 7',
    wrong: [
      miss('4, 9, 2, 7', 'This forgets the pop: a pop removes the most recently pushed item (2) before 7 is added.', 'A stack is LIFO, so the last value pushed is the first removed.'),
      miss('7, 9, 4', 'The order is reversed; the first item pushed (4) stays at the bottom, not the top.', 'Pushing adds to the top, so earlier items sit lower.'),
      miss('4, 2, 7', 'The popped item should be 2 (the top), not 9.', 'Pop always removes the item at the top pointer.'),
    ],
    lesson: 'A stack is a Last-In-First-Out (LIFO) structure: push adds to the top and pop removes from the top. After pushing 4, 9, 2 the top is 2; popping removes 2, leaving 4, 9; pushing 7 gives 4, 9, 7 from bottom to top. The top pointer is incremented on push and decremented on pop.',
    source,
    generated: true,
  },
  {
    id: 7909501,
    chapter: 'Data structures',
    title: 'Queue using a circular buffer',
    prompt: 'Why is a circular (ring) buffer often preferred over a simple linear array for implementing a queue?',
    mentorHint: 'Think about what happens to the empty cells left behind after dequeuing.',
    correct: 'It reuses array slots that were freed by dequeue operations, avoiding wasted space at the front',
    wrong: [
      miss('It allows items to be removed from either end in constant time', 'That describes a deque; a standard queue still removes only from the front. The circular buffer\'s benefit is slot reuse, not double-ended access.', 'Think about what happens to the empty cells left behind after dequeuing.'),
      miss('It removes the need to store a front and a rear pointer', 'A circular buffer still needs front and rear pointers; they simply wrap around using modulo arithmetic.', 'How would you know where to enqueue or dequeue without pointers?'),
      miss('It makes the queue unbounded so it can never become full', 'A circular buffer has a fixed capacity and can become full; only its reuse of slots improves.', 'Consider whether the underlying array can grow.'),
    ],
    lesson: 'In a linear array queue, the front pointer advances on each dequeue and the cells it passes are wasted, so the queue can report full even with free space at the start. A circular buffer wraps the rear and front pointers around using modulo the array size, reusing the freed slots. It is typically full when advancing rear would meet front.',
    source,
    generated: true,
  },
  {
    id: 7909502,
    chapter: 'Data structures',
    title: 'Linked list versus array',
    prompt: 'Which operation is generally more efficient in a singly linked list than in a static array?',
    mentorHint: 'Can you jump straight to position 500 in a linked list?',
    correct: 'Inserting a new node at the start of the list',
    wrong: [
      miss('Accessing the element at a known index', 'Arrays give O(1) random access by index; a linked list must be traversed from the head, which is O(n).', 'Can you jump straight to position 500 in a linked list?'),
      miss('Reading the list in reverse order', 'A singly linked list only has forward pointers, so reverse traversal is awkward, whereas an array can be indexed backwards easily.', 'Which structure stores pointers in only one direction?'),
      miss('Finding the maximum value', 'Both must examine every element, so both are O(n); the linked list has no advantage here.', 'Does either structure let you skip elements when searching?'),
    ],
    lesson: 'A linked list stores each element with a pointer to the next node, so inserting at the head just creates a node and updates one pointer in O(1), with no shifting. An array insertion at the start requires shifting every element up by one, which is O(n). Arrays win on random access because elements sit at calculable memory offsets.',
    source,
    generated: true,
  },
  {
    id: 7909503,
    chapter: 'Data structures',
    title: 'Hash table collision',
    prompt: 'A hash table uses a hashing function to map keys to array indices. What is a collision?',
    mentorHint: 'Collisions are about two keys, not about how full the table is.',
    correct: 'When two different keys hash to the same array index',
    wrong: [
      miss('When the hash table becomes completely full', 'A full table is a separate problem (load factor reaching capacity); a collision can occur long before the table is full.', 'Collisions are about two keys, not about how full the table is.'),
      miss('When a key cannot be converted into a number by the hashing function', 'A well-designed hash function always produces an index; failing to hash a key is a function design fault, not a collision.', 'A collision still produces valid indices, just the same one twice.'),
      miss('When the same key is inserted twice', 'Inserting a duplicate key is not a collision; a collision involves two distinct keys mapping to one slot.', 'Focus on different keys landing in the same place.'),
    ],
    lesson: 'A collision occurs when the hash function maps two distinct keys to the same index. Because hashing maps a large key space onto a smaller array, collisions are inevitable. They are resolved by techniques such as separate chaining (storing a list at each slot) or open addressing (probing to the next free slot).',
    source,
    generated: true,
  },
  {
    id: 7909504,
    chapter: 'Data structures',
    title: 'Binary tree traversal order',
    prompt: 'An in-order traversal is performed on a binary search tree. In what order are the node values output?',
    mentorHint: 'A BST keeps smaller values on the left of each node.',
    correct: 'In ascending sorted order',
    wrong: [
      miss('In the order the values were inserted', 'In-order traversal depends on the tree structure, not insertion history; for a BST it produces sorted output regardless of insertion order.', 'A BST keeps smaller values on the left of each node.'),
      miss('Root first, then all left descendants, then all right descendants', 'That describes a pre-order traversal (root, left, right), not in-order.', 'In-order visits the left subtree before the root.'),
      miss('In descending sorted order', 'In-order on a standard BST gives ascending order; descending would require visiting right subtrees before left.', 'Which subtree holds the smaller values?'),
    ],
    lesson: 'In-order traversal visits the left subtree, then the node, then the right subtree, recursively. Because a binary search tree stores smaller values to the left and larger to the right, this visits values in ascending order. Pre-order (root, left, right) and post-order (left, right, root) produce different sequences useful for copying or deleting trees.',
    source,
    generated: true,
  },
  {
    id: 7909505,
    chapter: 'Data structures',
    title: 'Graph representation',
    prompt: 'A sparse graph has many vertices but relatively few edges. Which representation is generally more memory-efficient for it?',
    mentorHint: 'Which structure only stores edges that actually exist?',
    correct: 'An adjacency list',
    wrong: [
      miss('An adjacency matrix', 'An adjacency matrix uses O(V squared) space whether or not edges exist, wasting memory when the graph is sparse.', 'Which structure only stores edges that actually exist?'),
      miss('A single one-dimensional array of edge weights', 'A flat array cannot record which pairs of vertices an edge connects, so it cannot represent a graph on its own.', 'You need to know both endpoints of each edge.'),
      miss('A stack of vertices', 'A stack stores an ordering of vertices but does not record the connections between them.', 'A graph representation must store the edges, not just the vertices.'),
    ],
    lesson: 'An adjacency matrix is a V by V grid, using O(V squared) memory regardless of how many edges exist, which is wasteful for sparse graphs. An adjacency list stores, for each vertex, only the vertices it connects to, using O(V + E) memory. Matrices are preferred for dense graphs or when fast edge-existence lookups are needed.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Algorithms and complexity
  // ---------------------------------------------------------------------------
  {
    id: 7909506,
    chapter: 'Algorithms and complexity',
    title: 'Binary search precondition',
    prompt: 'What condition must hold for a binary search to work correctly on a list?',
    mentorHint: 'The algorithm decides which half to discard based on order.',
    correct: 'The list must be sorted',
    wrong: [
      miss('The list must contain only unique values', 'Binary search works fine on lists with duplicates; it just may return the index of any matching element. Sorting, not uniqueness, is the requirement.', 'The algorithm decides which half to discard based on order.'),
      miss('The list length must be a power of two', 'Binary search handles any length by computing the midpoint each time; it does not require a power-of-two size.', 'How does the algorithm pick the middle of an odd-length list?'),
      miss('The list must be stored as a linked list', 'Binary search needs constant-time random access to the midpoint, which a linked list cannot provide; an array is required.', 'Think about how the algorithm jumps to the middle element.'),
    ],
    lesson: 'Binary search repeatedly compares the target with the middle element and discards the half it cannot be in. This logic relies on the list being sorted, so that being greater or smaller than the midpoint reliably tells you which half to keep. It runs in O(log n) time but requires sorted data with random access, which is why arrays, not linked lists, are used.',
    source,
    generated: true,
  },
  {
    id: 7909507,
    chapter: 'Algorithms and complexity',
    title: 'Big-O of merge sort',
    prompt: 'What is the time complexity of merge sort in the worst case?',
    mentorHint: 'Merge sort splits the list in half each time.',
    correct: 'O(n log n)',
    wrong: [
      miss('O(n squared)', 'O(n squared) is the worst case for simple sorts like bubble or insertion sort; merge sort\'s divide-and-conquer keeps it at O(n log n) even in the worst case.', 'Merge sort splits the list in half each time.'),
      miss('O(log n)', 'O(log n) describes algorithms like binary search that discard data; merge sort must touch every element during merging, so it cannot be sub-linear.', 'You still have to merge all n items at each level.'),
      miss('O(n)', 'Merging is O(n) per level, but there are log n levels of splitting, so the total is O(n log n), not O(n).', 'Count how many levels of recursion there are.'),
    ],
    lesson: 'Merge sort recursively splits the list into halves (log n levels of division) and merges them back together, with each full merge across a level costing O(n). Multiplying gives O(n log n) in the best, average and worst cases, making it more reliable than quicksort\'s O(n squared) worst case. Its drawback is needing O(n) extra space for merging.',
    source,
    generated: true,
  },
  {
    id: 7909508,
    chapter: 'Algorithms and complexity',
    title: 'Dijkstra\'s algorithm purpose',
    prompt: 'What does Dijkstra\'s algorithm compute?',
    mentorHint: 'A shortest path tree is not the same as a minimum-weight tree linking everything.',
    correct: 'The shortest path from a single source vertex to all other vertices in a weighted graph',
    wrong: [
      miss('The minimum spanning tree connecting all vertices', 'That is the goal of Prim\'s or Kruskal\'s algorithm; Dijkstra finds shortest paths from one source, which is a different objective.', 'A shortest path tree is not the same as a minimum-weight tree linking everything.'),
      miss('Whether a graph contains a cycle', 'Cycle detection is done by traversal algorithms like depth-first search; Dijkstra assumes it can compute path costs, not cycle existence.', 'Dijkstra outputs distances, not a yes/no about cycles.'),
      miss('A traversal that visits every vertex exactly once in any order', 'That is a general graph traversal; Dijkstra specifically minimises path cost rather than just visiting nodes.', 'The defining feature is shortest distance, not visiting order.'),
    ],
    lesson: 'Dijkstra\'s algorithm finds the lowest-cost path from one source vertex to every other vertex in a graph with non-negative edge weights. It maintains a set of tentative distances, repeatedly selecting the closest unvisited vertex and relaxing its neighbours\' distances. It will not work correctly with negative edge weights, which require the Bellman-Ford algorithm instead.',
    source,
    generated: true,
  },
  {
    id: 7909509,
    chapter: 'Algorithms and complexity',
    title: 'A* heuristic role',
    prompt: 'In the A* search algorithm, what is the purpose of the heuristic function?',
    mentorHint: 'A* uses f(n) = g(n) + h(n); which part looks ahead?',
    correct: 'To estimate the remaining cost from a node to the goal, guiding the search towards the target',
    wrong: [
      miss('To record the exact cost already travelled from the start node', 'That is the g(n) component of A*, not the heuristic; the heuristic h(n) estimates the cost still to come.', 'A* uses f(n) = g(n) + h(n); which part looks ahead?'),
      miss('To guarantee the algorithm never revisits any node', 'A* may revisit and update nodes; the heuristic guides direction, it does not control revisiting.', 'The heuristic is about estimating distance to the goal.'),
      miss('To randomly select which neighbour to expand next', 'A* expands nodes by lowest estimated total cost, not randomly; randomness would defeat its efficiency.', 'The heuristic makes the search informed, the opposite of random.'),
    ],
    lesson: 'A* search selects the node with the lowest value of f(n) = g(n) + h(n), where g(n) is the actual cost from the start and h(n) is the heuristic estimate of the cost to the goal. A good admissible heuristic (one that never overestimates) lets A* find the optimal path while exploring far fewer nodes than uninformed search. It extends Dijkstra by adding goal-directed guidance.',
    source,
    generated: true,
  },
  {
    id: 7909510,
    chapter: 'Algorithms and complexity',
    title: 'Intractable problems',
    prompt: 'In computational complexity, what is meant by an "intractable" problem?',
    mentorHint: 'Intractable means slow, not impossible.',
    correct: 'A problem for which no known algorithm can solve it in polynomial time as input size grows',
    wrong: [
      miss('A problem that no computer can ever solve, even given unlimited time', 'That describes a non-computable (undecidable) problem; intractable problems are solvable in principle but too slow in practice.', 'Intractable means slow, not impossible.'),
      miss('A problem whose answer depends on hardware speed', 'Complexity is measured by how the running time scales with input size, independent of the particular machine\'s speed.', 'Buying a faster computer does not change the complexity class.'),
      miss('A problem that produces a different answer each time it runs', 'That describes non-determinism in output, not intractability; intractable problems still have well-defined answers.', 'Intractability is about time, not about answer consistency.'),
    ],
    lesson: 'A tractable problem has an algorithm that runs in polynomial time, such as O(n) or O(n squared). An intractable problem has solutions only in worse-than-polynomial time, like O(2^n), so it becomes impractical as input grows even though it is solvable in principle. This differs from non-computable problems, which have no algorithm at all.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Theory of computation
  // ---------------------------------------------------------------------------
  {
    id: 7909511,
    chapter: 'Theory of computation',
    title: 'Halting problem',
    prompt: 'What did Alan Turing prove about the halting problem?',
    mentorHint: 'How long would you wait before deciding it never halts?',
    correct: 'It is undecidable: no algorithm can determine for every program and input whether the program will halt',
    wrong: [
      miss('It can always be solved by simply running the program and waiting', 'If the program loops forever you would wait forever, so running it does not give a guaranteed answer; that is precisely why it is undecidable.', 'How long would you wait before deciding it never halts?'),
      miss('It can be solved but only for programs without loops', 'Programs without loops always halt trivially; the difficulty and undecidability arise exactly because general programs can loop.', 'The hard cases are the ones that might loop.'),
      miss('It can be solved on a quantum computer in polynomial time', 'Undecidability is not about speed; no model of computation, including quantum, can decide the general halting problem.', 'This is a limit on what is computable, not on how fast.'),
    ],
    lesson: 'Turing proved by contradiction that no general algorithm can decide, for every possible program and input, whether the program halts or runs forever. Assuming such a decider exists leads to a self-referential contradiction. The halting problem is the classic example of an undecidable problem, showing there are well-defined questions no computer can answer.',
    source,
    generated: true,
  },
  {
    id: 7909512,
    chapter: 'Theory of computation',
    title: 'Finite state machine',
    prompt: 'A finite state machine has states and transitions triggered by inputs. What is its key defining limitation compared with a Turing machine?',
    mentorHint: 'Think about what the machine can store, not how much input it reads.',
    correct: 'It has no external memory or tape, so it can only remember which state it is in',
    wrong: [
      miss('It can only accept inputs that are exactly one character long', 'An FSM processes a sequence of inputs of any length, one symbol at a time; the limitation is memory, not input length.', 'Think about what the machine can store, not how much input it reads.'),
      miss('It must always have exactly two states', 'An FSM can have any finite number of states; two is not a requirement.', 'The "finite" refers to the count being limited, not fixed at two.'),
      miss('It cannot produce any output, only accept or reject', 'Some FSMs (Mealy and Moore machines) do produce outputs; the fundamental limit is the lack of unbounded memory.', 'Consider what makes a Turing machine more powerful.'),
    ],
    lesson: 'A finite state machine has only a finite set of states and no auxiliary storage, so its entire memory is the current state. This means it cannot, for example, count arbitrarily high or match unlimited nested brackets. A Turing machine adds an infinite tape for read/write memory, which makes it strictly more powerful and able to recognise languages an FSM cannot.',
    source,
    generated: true,
  },
  {
    id: 7909513,
    chapter: 'Theory of computation',
    title: 'Regular expression matching',
    prompt: 'Which strings are matched by the regular expression ab*c (where * means zero or more of the preceding item)?',
    mentorHint: 'What does zero or more of b allow?',
    correct: 'ac, abc, abbc, abbbc and so on',
    wrong: [
      miss('Only the single string abc', 'The * means zero or more b characters, so abc is just one match among many, including ac and abbbc.', 'What does zero or more of b allow?'),
      miss('aabbcc and any string with equal numbers of each letter', 'The expression fixes a single a then any number of b then a single c; it does not allow repeated a or c.', 'Only the b is governed by the star.'),
      miss('Any string containing the letters a, b and c in any order', 'A regular expression matches a specific pattern in order, not an arbitrary arrangement of letters.', 'Order matters: a, then bs, then c.'),
    ],
    lesson: 'In the expression ab*c, the * applies only to the b immediately before it, meaning zero or more b characters. So a single a must come first, then any number of b including none, then a single c. Matches therefore include ac (zero bs), abc, abbc and so on. The star quantifier is one of the core operators of regular expressions.',
    source,
    generated: true,
  },
  {
    id: 7909514,
    chapter: 'Theory of computation',
    title: 'Backus-Naur Form',
    prompt: 'What is Backus-Naur Form (BNF) used to define?',
    mentorHint: 'BNF tells you if code is well-formed, not what it computes.',
    correct: 'The syntax (grammar) of a programming language',
    wrong: [
      miss('The meaning of each instruction at run time', 'The meaning, or semantics, is separate; BNF describes only the allowed structure of valid statements, not what they do.', 'BNF tells you if code is well-formed, not what it computes.'),
      miss('The machine code produced by a compiler', 'Generated machine code is an output of compilation; BNF specifies the rules a source program must follow before that stage.', 'BNF is about source-language structure, not the binary output.'),
      miss('The amount of memory a program will use', 'Memory usage is a run-time property; BNF is purely a notation for grammar rules.', 'Think about what makes a line of code grammatically valid.'),
    ],
    lesson: 'Backus-Naur Form is a formal metalanguage for defining the syntax of programming languages using production rules with terminals and non-terminals. For example, a rule might state that a digit is one of 0 to 9 and a number is one or more digits. BNF can express some constructs (like nested brackets) that regular expressions cannot, because grammars are more powerful than regular languages.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Networking
  // ---------------------------------------------------------------------------
  {
    id: 7909515,
    chapter: 'Networking',
    title: 'TCP/IP layering',
    prompt: 'In the four-layer TCP/IP model, at which layer does the IP protocol operate?',
    mentorHint: 'Which layer is concerned with addressing and routing across networks?',
    correct: 'The network (internet) layer',
    wrong: [
      miss('The application layer', 'The application layer holds protocols like HTTP and SMTP that programs use directly; IP works lower down to route packets between networks.', 'Which layer is concerned with addressing and routing across networks?'),
      miss('The transport layer', 'The transport layer is where TCP and UDP operate, handling end-to-end delivery; IP sits below them handling routing.', 'TCP and IP are different layers despite the model\'s name.'),
      miss('The link (network interface) layer', 'The link layer deals with the physical local connection and MAC addressing; IP provides logical addressing across networks above it.', 'IP addresses span networks; MAC addresses are local.'),
    ],
    lesson: 'The TCP/IP model has four layers: application, transport, network (internet), and link. The Internet Protocol operates at the network layer, providing logical IP addressing and routing packets between networks. TCP and UDP sit above it at the transport layer, while protocols such as HTTP belong to the application layer.',
    source,
    generated: true,
  },
  {
    id: 7909516,
    chapter: 'Networking',
    title: 'TCP versus UDP',
    prompt: 'Why might a live video-conferencing application choose UDP rather than TCP?',
    mentorHint: 'Which protocol is the reliable, connection-oriented one?',
    correct: 'UDP has lower latency because it does not retransmit lost packets or wait for acknowledgements',
    wrong: [
      miss('UDP guarantees every packet arrives in the correct order', 'That is a property of TCP, not UDP; UDP makes no delivery or ordering guarantees, which is exactly why it is faster.', 'Which protocol is the reliable, connection-oriented one?'),
      miss('UDP encrypts the data automatically', 'Neither TCP nor UDP provides encryption by itself; security is added by higher-layer protocols like TLS.', 'Encryption is a separate concern from the transport protocol.'),
      miss('UDP uses fewer IP addresses than TCP', 'Both protocols use the same IP addressing; the difference is in reliability and overhead, not address usage.', 'Think about acknowledgements and retransmission overhead.'),
    ],
    lesson: 'TCP is reliable and connection-oriented: it numbers packets, retransmits lost ones, and ensures correct ordering, but this adds latency. UDP is connectionless and does none of this, so it is faster and lighter. For real-time media a slightly dropped packet is preferable to the delay of waiting for a retransmission, so latency-sensitive applications favour UDP.',
    source,
    generated: true,
  },
  {
    id: 7909517,
    chapter: 'Networking',
    title: 'DNS function',
    prompt: 'What is the primary purpose of the Domain Name System (DNS)?',
    mentorHint: 'DNS is about looking up addresses, not protecting data.',
    correct: 'To translate human-readable domain names into IP addresses',
    wrong: [
      miss('To encrypt traffic between a browser and a web server', 'Encryption is provided by HTTPS/TLS; DNS performs name resolution and does not itself secure traffic.', 'DNS is about looking up addresses, not protecting data.'),
      miss('To assign a unique IP address to every new device on a network', 'Automatic IP assignment is the job of DHCP, not DNS; DNS maps names to existing addresses.', 'Which protocol hands out addresses when a device joins?'),
      miss('To store the actual web page content for a website', 'Web content is served by web servers; DNS only resolves the name to the server\'s address.', 'DNS is a directory, not a content store.'),
    ],
    lesson: 'DNS acts like a distributed directory that converts domain names such as example.com into the numeric IP addresses computers use to route traffic. When you enter a URL, your device queries DNS servers to find the matching IP before connecting. This separates memorable names from the underlying addresses, which can change without affecting users.',
    source,
    generated: true,
  },
  {
    id: 7909518,
    chapter: 'Networking',
    title: 'Packet switching',
    prompt: 'In a packet-switched network, how is a large message transmitted?',
    mentorHint: 'Does the internet reserve a private line for each transfer?',
    correct: 'It is split into packets that may travel independently along different routes and are reassembled at the destination',
    wrong: [
      miss('A dedicated physical circuit is reserved for the whole message until it completes', 'That describes circuit switching, used in traditional telephone networks; packet switching shares links and does not reserve a circuit.', 'Does the internet reserve a private line for each transfer?'),
      miss('The whole message is sent as one indivisible unit along a single fixed path', 'Packet switching deliberately divides the message and allows different packets to take different routes for resilience and efficiency.', 'The word "packet" hints that the message is broken up.'),
      miss('The message is broadcast to every device on the internet until the recipient accepts it', 'Packets are routed towards their destination address, not broadcast to all devices, which would be hugely wasteful.', 'Routers forward packets based on destination addresses.'),
    ],
    lesson: 'Packet switching breaks a message into packets, each carrying source and destination addressing and a sequence number. Routers forward packets independently, so different packets may take different routes depending on network conditions. The receiver uses the sequence numbers to reassemble them in order, requesting retransmission of any lost packets. This makes efficient, resilient use of shared links.',
    source,
    generated: true,
  },
  {
    id: 7909519,
    chapter: 'Networking',
    title: 'Subnet mask purpose',
    prompt: 'What does a subnet mask determine for an IP address?',
    mentorHint: 'The mask splits the address into two functional parts.',
    correct: 'Which part of the address identifies the network and which part identifies the host',
    wrong: [
      miss('The geographic country in which the device is located', 'IP addresses are not assigned strictly by geography, and a subnet mask is a local network/host divider, not a location code.', 'The mask splits the address into two functional parts.'),
      miss('The maximum data transfer speed of the connection', 'Speed depends on the physical link and protocols, not on the subnet mask, which only structures the address.', 'A mask is about addressing, not bandwidth.'),
      miss('The MAC address of the network interface card', 'The MAC address is a separate hardware identifier; the subnet mask works on IP addresses, not MAC addresses.', 'Subnet masks pair with IP addresses, not hardware addresses.'),
    ],
    lesson: 'A subnet mask is a 32-bit value (for IPv4) where the 1 bits mark the network portion and the 0 bits mark the host portion of an IP address. Performing a bitwise AND of an IP address with its mask yields the network address. This lets routers decide whether a destination is on the local network or must be sent to a gateway.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Databases
  // ---------------------------------------------------------------------------
  {
    id: 7909520,
    chapter: 'Databases',
    title: 'Primary key role',
    prompt: 'What is the role of a primary key in a relational database table?',
    mentorHint: 'Which key points outward to a different table?',
    correct: 'It uniquely identifies each record in the table',
    wrong: [
      miss('It links the table to a record in another table', 'That is the role of a foreign key; a primary key uniquely identifies rows within its own table.', 'Which key points outward to a different table?'),
      miss('It stores the most important attribute of each record', 'A primary key need not be the most meaningful field; it just has to uniquely identify rows, and is often a generated ID.', 'Importance of data is not the same as uniqueness.'),
      miss('It sorts the records into alphabetical order automatically', 'A primary key enforces uniqueness; physical sorting is a separate indexing concern.', 'Think about identifying rows, not ordering them.'),
    ],
    lesson: 'A primary key is one or more attributes whose value is unique for every record, allowing each row to be referenced unambiguously. It cannot be null. A foreign key, by contrast, is an attribute in one table that refers to the primary key of another, which is how relationships between tables are formed.',
    source,
    generated: true,
  },
  {
    id: 7909521,
    chapter: 'Databases',
    title: 'Third normal form',
    prompt: 'A table in second normal form still has an attribute that depends on another non-key attribute rather than on the primary key. Which normalisation step removes this?',
    mentorHint: 'Which form specifically removes non-key to non-key dependencies?',
    correct: 'Converting it to third normal form (3NF)',
    wrong: [
      miss('Converting it to first normal form (1NF)', '1NF only removes repeating groups and ensures atomic values; it does not address transitive dependencies on non-key attributes.', 'Which form specifically removes non-key to non-key dependencies?'),
      miss('Converting it to second normal form (2NF)', 'The table is already in 2NF; 2NF removes partial dependencies on part of a composite key, not transitive dependencies.', 'The problem here is a dependency between two non-key fields.'),
      miss('Denormalising the table', 'Denormalisation deliberately reintroduces redundancy for performance; it would not remove the dependency, it would tolerate it.', 'You want to remove the dependency, not embrace it.'),
    ],
    lesson: 'Third normal form requires that every non-key attribute depends only on the primary key, with no transitive dependencies (where a non-key attribute determines another non-key attribute). Reaching 3NF removes such dependencies by splitting the data into separate tables linked by foreign keys. This reduces redundancy and update anomalies while keeping the data consistent.',
    source,
    generated: true,
  },
  {
    id: 7909522,
    chapter: 'Databases',
    title: 'SQL SELECT with WHERE',
    prompt: 'Which SQL statement returns the names of all students in the Students table whose age is greater than 17?',
    mentorHint: 'You need a comparison that means "greater than".',
    correct: 'SELECT Name FROM Students WHERE Age > 17;',
    wrong: [
      miss('SELECT Name FROM Students WHERE Age = 17;', 'The = operator returns only students aged exactly 17, not those older than 17.', 'You need a comparison that means "greater than".'),
      miss('SELECT Age FROM Students WHERE Name > 17;', 'This selects the Age column, not Name, and nonsensically compares Name to 17.', 'Check which column you are filtering on and which you are returning.'),
      miss('INSERT Name FROM Students WHERE Age > 17;', 'INSERT adds new records; retrieving existing data requires SELECT.', 'Which keyword reads data rather than writing it?'),
    ],
    lesson: 'A SELECT statement retrieves columns from a table, and the WHERE clause filters which rows are returned. To list names of students older than 17, you select the Name column and filter on Age > 17. Using > rather than = ensures the boundary value 17 itself is excluded, returning only those strictly older.',
    source,
    generated: true,
  },
  {
    id: 7909523,
    chapter: 'Databases',
    title: 'Referential integrity',
    prompt: 'What does referential integrity in a relational database ensure?',
    mentorHint: 'Referential integrity is about links between tables.',
    correct: 'That every foreign key value matches an existing primary key value in the referenced table',
    wrong: [
      miss('That no two records in a table are completely identical', 'Avoiding duplicate rows relates to entity integrity and keys, not referential integrity, which is specifically about valid references between tables.', 'Referential integrity is about links between tables.'),
      miss('That all data is stored in third normal form', 'Normalisation is a separate design process; referential integrity is a constraint enforced on relationships at run time.', 'Think about foreign keys pointing to real records.'),
      miss('That the database is automatically backed up after each change', 'Backups are an operational concern, unrelated to the consistency rule of referential integrity.', 'This is a data-consistency rule, not a recovery feature.'),
    ],
    lesson: 'Referential integrity guarantees that relationships between tables stay consistent: a foreign key must either be null or refer to an existing primary key in the related table. This prevents orphaned records, such as an order linked to a customer who does not exist. Database management systems enforce it by rejecting invalid inserts or restricting deletes that would break a reference.',
    source,
    generated: true,
  },
  {
    id: 7909524,
    chapter: 'Databases',
    title: 'ACID transactions',
    prompt: 'In the ACID properties of database transactions, what does "Atomicity" guarantee?',
    mentorHint: 'Which letter deals with separating concurrent transactions?',
    correct: 'A transaction either completes entirely or has no effect at all',
    wrong: [
      miss('Concurrent transactions cannot interfere with one another', 'That is Isolation; atomicity is specifically about all-or-nothing completion of a single transaction.', 'Which letter deals with separating concurrent transactions?'),
      miss('Committed data survives a subsequent system crash', 'That is Durability; atomicity concerns whether a transaction is applied fully, not whether results persist after a crash.', 'Atomic means indivisible, not permanent.'),
      miss('Every transaction leaves the database in a valid state', 'That is Consistency; while related, atomicity is narrowly about the transaction running fully or not at all.', 'Focus on the all-or-nothing idea.'),
    ],
    lesson: 'The ACID properties keep transactions reliable: Atomicity means a transaction is treated as a single indivisible unit, so a partial update is rolled back. Consistency keeps the database in a valid state, Isolation stops concurrent transactions interfering, and Durability ensures committed changes survive crashes. Atomicity prevents, for example, money leaving one account without arriving in another.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Boolean algebra and logic
  // ---------------------------------------------------------------------------
  {
    id: 7909525,
    chapter: 'Boolean algebra and logic',
    title: 'De Morgan\'s law',
    prompt: 'According to De Morgan\'s laws, the expression NOT (A AND B) is equivalent to which of the following?',
    mentorHint: 'When you push NOT inside, the AND must become OR.',
    correct: '(NOT A) OR (NOT B)',
    wrong: [
      miss('(NOT A) AND (NOT B)', 'This swaps the operator incorrectly; De Morgan\'s law flips AND to OR when the negation is distributed, so the result uses OR, not AND.', 'When you push NOT inside, the AND must become OR.'),
      miss('A OR B', 'Dropping the negations entirely changes the meaning; the law negates each variable and changes the connective.', 'Each variable must also be negated.'),
      miss('NOT (A OR B)', 'NOT (A OR B) equals (NOT A) AND (NOT B), which is the other De Morgan identity, not the equivalent of NOT (A AND B).', 'Match the original operator before negating.'),
    ],
    lesson: 'De Morgan\'s laws state that NOT (A AND B) equals (NOT A) OR (NOT B), and NOT (A OR B) equals (NOT A) AND (NOT B). When you distribute a negation across a bracket, each variable is negated and the connective flips between AND and OR. These laws are essential for simplifying logic circuits and rearranging Boolean expressions.',
    source,
    generated: true,
  },
  {
    id: 7909526,
    chapter: 'Boolean algebra and logic',
    title: 'XOR gate output',
    prompt: 'A two-input XOR (exclusive OR) gate outputs 1 in which case?',
    mentorHint: 'XOR means the inputs must differ.',
    correct: 'When exactly one of its two inputs is 1',
    wrong: [
      miss('When both inputs are 1', 'That is the behaviour of an AND gate; XOR outputs 0 when both inputs are 1.', 'XOR means the inputs must differ.'),
      miss('When at least one input is 1', 'That describes an OR gate, which also outputs 1 when both are 1; XOR outputs 0 in that case.', 'Compare what XOR does when both inputs are 1 versus OR.'),
      miss('When both inputs are 0', 'XOR outputs 0 when both inputs are equal, including when both are 0; it requires the inputs to differ.', 'Equal inputs give an output of 0 for XOR.'),
    ],
    lesson: 'An exclusive OR (XOR) gate outputs 1 only when its inputs differ, that is, when exactly one input is 1. If both inputs are the same (both 0 or both 1) it outputs 0. XOR is widely used in adders, parity checking and simple encryption, because it detects difference between bits.',
    source,
    generated: true,
  },
  {
    id: 7909527,
    chapter: 'Boolean algebra and logic',
    title: 'Half adder outputs',
    prompt: 'A half adder takes two single-bit inputs. Which two outputs does it produce?',
    mentorHint: 'Adding bits can overflow into a carry, not a borrow.',
    correct: 'A sum bit and a carry bit',
    wrong: [
      miss('A sum bit and a borrow bit', 'A borrow is produced by a subtractor, not an adder; addition generates a carry when the result exceeds one bit.', 'Adding bits can overflow into a carry, not a borrow.'),
      miss('Two sum bits', 'A half adder produces a single sum bit plus a carry, not two sum bits; the carry handles the overflow.', 'Where does the overflow from 1 + 1 go?'),
      miss('A carry bit and a parity bit', 'Parity is unrelated to a half adder; the second output is the sum bit, alongside the carry.', 'The main job of an adder is to produce the sum.'),
    ],
    lesson: 'A half adder adds two single bits using an XOR gate for the sum and an AND gate for the carry. Adding 1 + 1 gives a sum bit of 0 and a carry bit of 1. It is "half" because it cannot accept a carry-in from a previous stage; a full adder adds that third input so multiple full adders can be chained for multi-bit addition.',
    source,
    generated: true,
  },
  {
    id: 7909528,
    chapter: 'Boolean algebra and logic',
    title: 'Karnaugh map purpose',
    prompt: 'What is the main purpose of a Karnaugh map?',
    mentorHint: 'K-maps deal with logic, not arithmetic conversion.',
    correct: 'To simplify a Boolean expression by grouping adjacent terms',
    wrong: [
      miss('To convert a denary number into binary', 'Number base conversion is unrelated; a Karnaugh map operates on Boolean logic terms, not number representations.', 'K-maps deal with logic, not arithmetic conversion.'),
      miss('To measure the propagation delay of a logic circuit', 'Timing analysis is a separate hardware concern; a Karnaugh map is a paper technique for logic minimisation.', 'K-maps simplify expressions, they do not time signals.'),
      miss('To generate a truth table from a circuit diagram', 'A truth table is usually the input to a Karnaugh map, not its output; the map\'s job is to minimise the resulting expression.', 'The K-map takes the truth table and simplifies it.'),
    ],
    lesson: 'A Karnaugh map arranges the outputs of a truth table in a grid ordered using Gray code, so that adjacent cells differ by only one variable. Grouping adjacent 1s in powers of two (1, 2, 4, 8) lets you cancel variables and read off a simplified sum-of-products expression. This produces logic circuits with fewer gates than the unsimplified form.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Programming paradigms
  // ---------------------------------------------------------------------------
  {
    id: 7909529,
    chapter: 'Programming paradigms',
    title: 'Encapsulation in OOP',
    prompt: 'In object-oriented programming, what does encapsulation describe?',
    mentorHint: 'Inheritance is about reusing a parent class.',
    correct: 'Bundling data and the methods that operate on it together, while restricting direct external access to the data',
    wrong: [
      miss('Creating a new class that inherits attributes and methods from an existing class', 'That is inheritance, a different OOP principle; encapsulation is about bundling and protecting an object\'s internal state.', 'Inheritance is about reusing a parent class.'),
      miss('Allowing a method to behave differently depending on the object that calls it', 'That is polymorphism; encapsulation concerns hiding and grouping data with its methods.', 'Different behaviour from the same call is a separate concept.'),
      miss('Splitting a large program into separate source-code files', 'Splitting files is modular organisation; encapsulation is a language-level grouping of data and methods inside objects.', 'Encapsulation is about access control within objects, not file structure.'),
    ],
    lesson: 'Encapsulation packages an object\'s attributes (data) together with the methods that act on them, and uses access modifiers such as private to hide the internal state. External code interacts through public methods rather than touching the data directly, which protects the object\'s integrity and lets the internal implementation change safely. It is one of the core pillars of OOP alongside inheritance and polymorphism.',
    source,
    generated: true,
  },
  {
    id: 7909530,
    chapter: 'Programming paradigms',
    title: 'Polymorphism',
    prompt: 'A program has a base class Shape with a method area(), overridden in subclasses Circle and Square. Calling area() on a list of Shape objects runs the correct version for each. Which OOP principle is this?',
    mentorHint: 'Which principle is about a single interface, many implementations?',
    correct: 'Polymorphism',
    wrong: [
      miss('Encapsulation', 'Encapsulation is about hiding internal data; here the key idea is one method call producing different behaviour per object type.', 'Which principle is about a single interface, many implementations?'),
      miss('Abstraction', 'Abstraction is about exposing only essential features and hiding complexity; the described behaviour is specifically method overriding selecting an implementation.', 'The defining feature here is the same call behaving differently.'),
      miss('Aggregation', 'Aggregation is a "has-a" relationship between objects; it does not describe one method call dispatching to different overridden versions.', 'This is about overriding methods, not composing objects.'),
    ],
    lesson: 'Polymorphism lets a single method call behave differently depending on the actual object type at run time. Here area() is declared in Shape but overridden in each subclass, so iterating over a list of Shapes calls each object\'s specific version. This allows code to be written against the general type while objects supply their own behaviour, improving extensibility.',
    source,
    generated: true,
  },
  {
    id: 7909531,
    chapter: 'Programming paradigms',
    title: 'Functional programming first-class functions',
    prompt: 'In functional programming, what does it mean to say functions are "first-class objects"?',
    mentorHint: 'Think about whether a function can be used like a piece of data.',
    correct: 'Functions can be passed as arguments, returned from other functions, and assigned to variables',
    wrong: [
      miss('Functions are always executed before any other code in the program', 'Execution order is not what "first-class" means; it refers to functions being treated as ordinary values that can be passed around.', 'Think about whether a function can be used like a piece of data.'),
      miss('Functions can only be defined at the top level of a program', 'First-class functions can actually be nested and defined anywhere; the term means the opposite of being restricted.', 'First-class implies more freedom, not less.'),
      miss('Functions automatically run faster than equivalent loops', 'Performance is not implied by being first-class; the term is about how functions can be manipulated as values.', 'It is about treating functions as data, not speed.'),
    ],
    lesson: 'Treating functions as first-class objects means they are values like integers or strings: you can store them in variables, pass them as arguments, and return them from other functions. This enables higher-order functions such as map, filter and fold, which take or return functions. It is a defining feature of the functional paradigm and supports concise, composable code.',
    source,
    generated: true,
  },
  {
    id: 7909532,
    chapter: 'Programming paradigms',
    title: 'Higher-order function map',
    prompt: 'A functional language applies map(f, [1, 2, 3]) where f doubles its argument. What is the result?',
    mentorHint: 'map keeps the list shape, returning one result per element.',
    correct: '[2, 4, 6]',
    wrong: [
      miss('12', 'map applies f to each element and returns a new list; it does not combine results into a single sum. Summing the doubled values would be a fold, not a map.', 'map keeps the list shape, returning one result per element.'),
      miss('[1, 2, 3, 2, 4, 6]', 'map replaces each element with f applied to it, returning a list of the same length; it does not append the new values to the original.', 'The output list has the same number of elements as the input.'),
      miss('[2, 3, 4]', 'This adds 1 to each element rather than doubling; the function f doubles, so each value should be multiplied by 2.', 'Apply the doubling function, not an increment.'),
    ],
    lesson: 'The higher-order function map applies a given function to every element of a list and returns a new list of the results, preserving the original length. Doubling 1, 2 and 3 yields 2, 4 and 6. This contrasts with fold (or reduce), which combines all elements into a single value, and filter, which keeps only elements satisfying a condition.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Recursion
  // ---------------------------------------------------------------------------
  {
    id: 7909533,
    chapter: 'Recursion',
    title: 'Base case necessity',
    prompt: 'Why must a recursive function include a base case?',
    mentorHint: 'The base case is about stopping, not speed.',
    correct: 'To provide a stopping condition that prevents infinite recursion',
    wrong: [
      miss('To make the function run faster than an equivalent loop', 'A base case does not improve speed; it ensures the recursion terminates. Recursion is often slower than iteration due to call overhead.', 'The base case is about stopping, not speed.'),
      miss('To allow the function to return more than one value', 'The base case is unrelated to the number of return values; it is the condition under which the function stops calling itself.', 'Focus on what ends the recursion.'),
      miss('To reserve memory on the heap for the recursive calls', 'Recursive calls use the call stack, and the base case does not allocate memory; it simply terminates the recursion.', 'Without it, the calls would never stop.'),
    ],
    lesson: 'A recursive function calls itself on a smaller version of the problem. The base case defines a simple input that is solved directly without further recursion, so the chain of calls eventually stops. Without a reachable base case, the function calls itself indefinitely until the call stack overflows. Every recursive case must make progress towards a base case.',
    source,
    generated: true,
  },
  {
    id: 7909534,
    chapter: 'Recursion',
    title: 'Call stack and recursion',
    prompt: 'What can happen if a recursive function recurses too deeply without reaching a base case?',
    mentorHint: 'Each pending call must be stored somewhere finite.',
    correct: 'A stack overflow occurs because the call stack runs out of memory',
    wrong: [
      miss('The program runs faster as the calls accumulate', 'Accumulating unfinished calls consumes memory and eventually crashes; it does not speed the program up.', 'Each pending call must be stored somewhere finite.'),
      miss('The results are automatically merged into a single value', 'There is no automatic merging; the calls simply pile up on the stack until it overflows.', 'Think about where each unfinished call is kept.'),
      miss('The function switches to using iteration instead', 'Programs do not automatically convert recursion to iteration; runaway recursion just exhausts the stack.', 'Nothing rescues unbounded recursion automatically.'),
    ],
    lesson: 'Each recursive call adds a new stack frame holding its parameters, local variables and return address. These frames remain on the call stack until the call returns. If recursion never reaches a base case (or goes extremely deep), the stack exhausts its allocated memory and a stack overflow error occurs. This is a key practical limit on recursion depth.',
    source,
    generated: true,
  },
  {
    id: 7909535,
    chapter: 'Recursion',
    title: 'Converting recursion to iteration',
    prompt: 'Any recursive algorithm can be rewritten iteratively. Which data structure is typically used to mimic the behaviour of recursion in an iterative version?',
    mentorHint: 'Recursion returns to the last call first.',
    correct: 'A stack',
    wrong: [
      miss('A queue', 'A queue is FIFO, but recursion resumes the most recently suspended call first, which is LIFO behaviour matching a stack.', 'Recursion returns to the last call first.'),
      miss('A hash table', 'A hash table stores key-value pairs for fast lookup; it does not model the order in which suspended calls resume.', 'You need to mimic call return order, not key lookups.'),
      miss('A binary search tree', 'A BST organises data for ordered search, not for tracking pending function calls in last-in-first-out order.', 'Think about which structure naturally reverses order.'),
    ],
    lesson: 'Recursion relies on the call stack, which resumes the most recently suspended call first (LIFO). To convert recursion to iteration manually, you use an explicit stack to push pending work and pop it to process, replicating the runtime\'s behaviour. This is useful when recursion depth would risk a stack overflow or when explicit control is needed.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Compression, encryption and hashing
  // ---------------------------------------------------------------------------
  {
    id: 7909536,
    chapter: 'Compression, encryption and hashing',
    title: 'Lossless versus lossy compression',
    prompt: 'Why must compression of a text document use a lossless rather than a lossy method?',
    mentorHint: 'Which method preserves the original data exactly?',
    correct: 'Lossy compression discards data, which would corrupt or change the exact characters of the text',
    wrong: [
      miss('Lossless compression always produces smaller files than lossy compression', 'Lossy methods usually achieve greater size reduction; the reason for choosing lossless on text is fidelity, not file size.', 'Which method preserves the original data exactly?'),
      miss('Lossy compression cannot be applied to files smaller than one megabyte', 'There is no such size restriction; lossy methods can be applied to any file but would damage exact text.', 'The issue is data loss, not file size limits.'),
      miss('Text files cannot be compressed by lossy algorithms at all', 'Lossy algorithms could technically be run on text, but they would discard characters; the point is the result would be unacceptable, not impossible.', 'Consider what "discarding data" does to letters and symbols.'),
    ],
    lesson: 'Lossless compression reduces file size while allowing the exact original data to be perfectly reconstructed, using techniques like run-length encoding or dictionary methods. Lossy compression achieves smaller files by permanently discarding data the human senses are less likely to notice, which suits images and audio. For text, every character matters, so any loss is unacceptable and lossless compression is required.',
    source,
    generated: true,
  },
  {
    id: 7909537,
    chapter: 'Compression, encryption and hashing',
    title: 'Symmetric versus asymmetric encryption',
    prompt: 'What is the key difference between symmetric and asymmetric encryption?',
    mentorHint: 'Focus on how many keys each uses and how.',
    correct: 'Symmetric uses the same key to encrypt and decrypt, while asymmetric uses a public/private key pair',
    wrong: [
      miss('Symmetric encryption cannot be broken, while asymmetric can', 'Both can be broken given enough resources or weak keys; the actual distinction is how keys are used, not unbreakability.', 'Focus on how many keys each uses and how.'),
      miss('Symmetric encryption only works on text and asymmetric only on images', 'Both methods work on any binary data; the difference lies in the key arrangement, not the data type.', 'Encryption operates on bytes regardless of content.'),
      miss('Asymmetric encryption is always faster than symmetric encryption', 'Asymmetric encryption is generally slower due to its mathematics; it is often used just to exchange a symmetric key.', 'Consider which is more computationally expensive.'),
    ],
    lesson: 'Symmetric encryption uses a single shared secret key for both encryption and decryption, which is fast but requires securely distributing that key. Asymmetric encryption uses a mathematically linked public key (to encrypt) and private key (to decrypt), solving key distribution but running more slowly. In practice, asymmetric encryption is often used to exchange a symmetric session key.',
    source,
    generated: true,
  },
  {
    id: 7909538,
    chapter: 'Compression, encryption and hashing',
    title: 'Run-length encoding',
    prompt: 'Run-length encoding compresses the string "AAAAABBB" most effectively because the data has which property?',
    mentorHint: 'RLE rewards repetition, not variety.',
    correct: 'Long runs of repeated identical symbols',
    wrong: [
      miss('A wide variety of different symbols with no repetition', 'RLE replaces runs of identical symbols with a count; data with no repetition would gain little and could even grow.', 'RLE rewards repetition, not variety.'),
      miss('Symbols arranged in alphabetical order', 'Ordering does not help RLE; only consecutive repeats of the same symbol are compressed.', 'It is repetition of the same symbol in a row that matters.'),
      miss('An even number of total characters', 'The character count being even or odd is irrelevant; RLE depends on consecutive runs of the same value.', 'Think about consecutive identical characters.'),
    ],
    lesson: 'Run-length encoding replaces consecutive runs of the same symbol with a single symbol and a count, so AAAAABBB might become A5B3. It is most effective when data contains long runs of identical values, such as simple images with large blocks of one colour. On data with little repetition it offers little or no saving and may even increase size.',
    source,
    generated: true,
  },
  {
    id: 7909539,
    chapter: 'Compression, encryption and hashing',
    title: 'One-way property of hashing',
    prompt: 'Why is a cryptographic hash function suitable for storing passwords?',
    mentorHint: 'If it could be decrypted, it would not protect the password.',
    correct: 'It is computationally infeasible to reverse the hash to recover the original password',
    wrong: [
      miss('The hash can be easily decrypted back into the password when the user logs in', 'A secure hash is one-way and is not decrypted; at login the entered password is hashed and the hashes are compared.', 'If it could be decrypted, it would not protect the password.'),
      miss('Hashing compresses the password to save storage space', 'Saving space is not the purpose; hashing protects the password by being irreversible, regardless of length.', 'Security, not size, is the reason here.'),
      miss('Every password produces a unique hash with no possibility of collision', 'Collisions are theoretically possible because hashes map a huge input space to fixed-length output; the security comes from being one-way and collision-resistant, not collision-free.', 'No hash function is fully collision-free.'),
    ],
    lesson: 'A cryptographic hash function maps input of any size to a fixed-length value, and it is one-way: deriving the input from the hash is computationally infeasible. Storing the hash rather than the plaintext means a breach does not directly reveal passwords. At login the entered password is hashed and compared with the stored hash; salting is added to defend against precomputed lookup attacks.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Computational thinking and problem solving
  // ---------------------------------------------------------------------------
  {
    id: 7909540,
    chapter: 'Computational thinking',
    title: 'Abstraction in problem solving',
    prompt: 'In computational thinking, what is abstraction?',
    mentorHint: 'Which technique simplifies by hiding detail rather than dividing?',
    correct: 'Removing or hiding unnecessary detail to focus on the essential features of a problem',
    wrong: [
      miss('Breaking a large problem into smaller, more manageable sub-problems', 'That is decomposition, a related but distinct technique; abstraction is about ignoring irrelevant detail rather than splitting the problem.', 'Which technique simplifies by hiding detail rather than dividing?'),
      miss('Identifying patterns and similarities between different problems', 'That is pattern recognition; abstraction specifically concerns reducing complexity by hiding detail.', 'Abstraction strips away detail rather than spotting repetition.'),
      miss('Writing the step-by-step instructions to solve a problem', 'That is algorithmic thinking; abstraction comes earlier, deciding what detail to ignore.', 'Abstraction is about what to leave out, not the steps themselves.'),
    ],
    lesson: 'Abstraction is the process of hiding or removing unnecessary detail so that only the relevant features of a problem remain, making it easier to reason about. For example, a map abstracts a city to roads and landmarks. It works alongside decomposition (breaking problems into parts), pattern recognition (spotting similarities) and algorithm design as the four pillars of computational thinking.',
    source,
    generated: true,
  },
  {
    id: 7909541,
    chapter: 'Computational thinking',
    title: 'Decomposition',
    prompt: 'A student designs a chess program by separately tackling move generation, board display, and checking for checkmate. Which computational thinking technique is this?',
    mentorHint: 'The focus is on splitting the problem into parts.',
    correct: 'Decomposition',
    wrong: [
      miss('Abstraction', 'Abstraction hides detail; here the work is being divided into independent sub-tasks, which is decomposition.', 'The focus is on splitting the problem into parts.'),
      miss('Pattern recognition', 'Pattern recognition spots similarities across problems; splitting one problem into modules is decomposition.', 'Dividing into sub-problems is the key activity here.'),
      miss('Iteration', 'Iteration is repeating a process; the described activity is breaking the problem into manageable components.', 'Look for the idea of dividing, not repeating.'),
    ],
    lesson: 'Decomposition breaks a complex problem into smaller, more manageable sub-problems that can be solved independently and then combined. Tackling move generation, display and checkmate detection separately makes the chess program easier to design, test and maintain. It is a core computational thinking skill and underpins modular program design.',
    source,
    generated: true,
  },
  {
    id: 7909542,
    chapter: 'Computational thinking',
    title: 'Backtracking',
    prompt: 'An algorithm tries one option, and if it leads to a dead end, it returns to the previous decision point and tries another. What is this technique called?',
    mentorHint: 'The defining feature here is returning to a previous decision.',
    correct: 'Backtracking',
    wrong: [
      miss('Brute force', 'Brute force tries every possible combination exhaustively; backtracking abandons partial solutions early once they cannot succeed.', 'Backtracking prunes dead ends rather than testing everything blindly.'),
      miss('Greedy selection', 'A greedy algorithm always commits to the locally best choice and never reconsiders; backtracking deliberately undoes choices.', 'The defining feature here is returning to a previous decision.'),
      miss('Divide and conquer', 'Divide and conquer splits a problem into independent sub-problems; backtracking explores a search space, undoing choices that fail.', 'Think about reversing a step, not splitting a problem.'),
    ],
    lesson: 'Backtracking is a problem-solving strategy that builds a solution incrementally and abandons a partial solution (backtracks) as soon as it determines that the partial solution cannot be completed validly. It is used in puzzles like Sudoku, the n-queens problem and maze solving. By pruning dead ends early it is often far more efficient than pure brute force.',
    source,
    generated: true,
  },
  {
    id: 7909543,
    chapter: 'Computational thinking',
    title: 'Heuristic approach',
    prompt: 'When is a heuristic approach typically used to solve a problem?',
    mentorHint: 'Heuristics earn their place on hard, large problems.',
    correct: 'When finding the exact optimal solution is impractical, so a good-enough solution found quickly is acceptable',
    wrong: [
      miss('When the problem is so simple that any method works instantly', 'Trivial problems do not need heuristics; heuristics are valuable precisely when exact methods are too slow.', 'Heuristics earn their place on hard, large problems.'),
      miss('When the problem has no possible solution at all', 'If no solution exists, a heuristic cannot conjure one; heuristics approximate solutions to solvable but hard problems.', 'Heuristics find good answers, not answers where none exist.'),
      miss('When you need a guaranteed mathematically proven best answer', 'Guaranteeing the optimal answer rules out heuristics, which trade optimality for speed; you would use an exact algorithm instead.', 'Heuristics sacrifice guaranteed optimality for speed.'),
    ],
    lesson: 'A heuristic is a practical, rule-of-thumb method that finds a good (but not guaranteed optimal) solution quickly. Heuristics are used for intractable or very large problems, such as route planning or game-playing, where an exact algorithm would take impractically long. They trade guaranteed optimality for acceptable performance, which is often the right engineering compromise.',
    source,
    generated: true,
  },

  // ---------------------------------------------------------------------------
  // Big data and the consequences of computing
  // ---------------------------------------------------------------------------
  {
    id: 7909544,
    chapter: 'Big data',
    title: 'Functional programming and big data',
    prompt: 'Why are functional programming techniques well suited to processing big data across many machines?',
    mentorHint: 'Think about why side-effect-free code parallelises well.',
    correct: 'Functions with no side effects and immutable data make computations easy to run in parallel',
    wrong: [
      miss('Functional programs always use less memory than other paradigms', 'Memory usage is not the reason; functional code can use plenty of memory. Its value for big data is safe parallelism through statelessness.', 'Think about why side-effect-free code parallelises well.'),
      miss('Functional languages can only run on supercomputers', 'Functional languages run on ordinary hardware; the point is they distribute well, not that they need special machines.', 'The benefit is about parallelism, not exotic hardware.'),
      miss('Functional programming removes the need to store any data', 'Big data processing obviously still stores data; functional style manages how data is transformed, not whether it is stored.', 'Data is still stored; it is the computation style that helps.'),
    ],
    lesson: 'Functional programming avoids mutable state and side effects, so a function\'s output depends only on its inputs. This referential transparency means computations can be split across many processors or machines without the risk of one task corrupting another\'s shared state. Models like MapReduce exploit this, distributing map and reduce operations over huge datasets reliably.',
    source,
    generated: true,
  },
  {
    id: 7909545,
    chapter: 'Big data',
    title: 'Characteristics of big data',
    prompt: 'The "volume, velocity and variety" of big data refer to which respective characteristics?',
    mentorHint: 'Recall what each V word literally means.',
    correct: 'The amount of data, the speed it is generated, and the range of different data formats',
    wrong: [
      miss('The cost of storage, the number of users, and the encryption strength', 'These are not the standard 3 Vs; volume, velocity and variety describe size, speed of arrival and format diversity.', 'Recall what each V word literally means.'),
      miss('The accuracy of data, its legal status, and its monetary value', 'Accuracy relates to veracity (a sometimes-added V), but the core three are volume, velocity and variety.', 'Two of these are not part of the original three Vs.'),
      miss('The number of databases, servers, and programmers involved', 'These describe infrastructure, not the data itself; the 3 Vs characterise properties of the data.', 'The Vs describe the data, not the systems.'),
    ],
    lesson: 'Big data is commonly characterised by the three Vs: volume (the sheer quantity of data), velocity (the speed at which it is generated and must be processed), and variety (the many structured and unstructured formats). These properties make big data hard to handle with traditional databases, motivating distributed storage and processing techniques. Some models add veracity (data quality) as a fourth V.',
    source,
    generated: true,
  },
  {
    id: 7909546,
    chapter: 'Consequences of computing',
    title: 'Computer Misuse Act',
    prompt: 'Under the UK Computer Misuse Act, which action is the most clearly defined offence?',
    mentorHint: 'Which law deals specifically with unauthorised access to systems?',
    correct: 'Gaining unauthorised access to computer material',
    wrong: [
      miss('Selling a second-hand laptop without wiping personal data', 'Failing to wipe data may breach data protection duties, but the offence under the Computer Misuse Act centres on unauthorised access, not resale.', 'Which law deals specifically with unauthorised access to systems?'),
      miss('Copying a music file you have legally purchased for personal backup', 'Copyright matters fall under copyright law, not the Computer Misuse Act, which targets unauthorised access and modification.', 'This is a copyright question, not a misuse-of-computers one.'),
      miss('Storing customer data without their explicit consent', 'Handling personal data is governed by data protection legislation; the Computer Misuse Act addresses hacking-type offences.', 'Consent for personal data is a different Act entirely.'),
    ],
    lesson: 'The Computer Misuse Act criminalises unauthorised access to computer material, unauthorised access with intent to commit further offences, and unauthorised modification of computer material (including spreading malware). It targets hacking and related activities. Other concerns such as personal data handling fall under data protection law, and copying media falls under copyright law, illustrating how different legislation covers different issues.',
    source,
    generated: true,
  },
  {
    id: 7909547,
    chapter: 'Consequences of computing',
    title: 'Open source software',
    prompt: 'Which statement best describes open source software?',
    mentorHint: 'Open source is about source-code freedom, not necessarily zero cost.',
    correct: 'Its source code is made freely available so users may study, modify and redistribute it',
    wrong: [
      miss('It is always completely free of any charge and can never be sold', 'Open source refers to access to the source code and freedoms to modify and share; software can still be sold or have paid support.', 'Open source is about source-code freedom, not necessarily zero cost.'),
      miss('Its source code is hidden but it can be downloaded without payment', 'Hidden source describes proprietary freeware; open source specifically means the source code is available.', 'The defining feature is access to the source code.'),
      miss('It can only be run on the Linux operating system', 'Open source software runs on many platforms; it is defined by its licensing, not a single operating system.', 'Open source is about licensing, not platform.'),
    ],
    lesson: 'Open source software is distributed with its source code under a licence that lets users freely study, modify and redistribute it. This contrasts with proprietary software, where the source is closed and usage is restricted. Open source can still be sold and supported commercially; its defining feature is the legal freedom around the source code, which can encourage collaboration and transparency.',
    source,
    generated: true,
  },
  {
    id: 7909548,
    chapter: 'Consequences of computing',
    title: 'Algorithmic bias',
    prompt: 'A recruitment machine-learning model trained mostly on past male hires starts rejecting qualified female applicants. What does this most clearly illustrate?',
    mentorHint: 'Look at what the model was trained on, not the machine.',
    correct: 'Algorithmic bias arising from unrepresentative training data',
    wrong: [
      miss('A hardware fault in the computer running the model', 'The outcome stems from the data the model learned from, not a physical hardware malfunction.', 'Look at what the model was trained on, not the machine.'),
      miss('A computer virus altering the recruitment results', 'No malware is implied; the skewed results follow directly from biased training data the model generalised from.', 'The cause is the data, not an infection.'),
      miss('Encryption failing to protect the applicant data', 'Encryption concerns confidentiality, not whether the model\'s decisions are fair; this is about biased outcomes.', 'This is a fairness issue, not a data-security one.'),
    ],
    lesson: 'Machine-learning models learn patterns from their training data, so if that data reflects historical imbalances the model can reproduce or amplify them. Training mainly on past male hires teaches the model to favour features associated with men, producing biased decisions. Addressing algorithmic bias requires representative data, careful feature selection, and ongoing evaluation of outcomes across groups.',
    source,
    generated: true,
  },
  {
    id: 7909549,
    chapter: 'Consequences of computing',
    title: 'Digital divide',
    prompt: 'What does the term "digital divide" refer to?',
    mentorHint: 'The digital divide is a societal concern, not an architecture one.',
    correct: 'The gap between those who have access to digital technology and the internet and those who do not',
    wrong: [
      miss('The split between hardware and software components of a computer', 'That is a technical distinction within a computer, not a social issue about access to technology.', 'The digital divide is a societal concern, not an architecture one.'),
      miss('The difference in processing speed between old and new processors', 'Processor performance is a hardware metric; the digital divide concerns unequal access to technology among people.', 'Think about who can use technology, not how fast chips are.'),
      miss('The separation of a network into smaller subnets', 'Dividing a network into subnets is a networking technique, unrelated to the social concept of unequal technology access.', 'The term describes a social gap, not a network configuration.'),
    ],
    lesson: 'The digital divide describes the inequality between individuals, communities or countries that have ready access to computers and the internet and those that do not, often due to cost, infrastructure, skills or geography. It matters because so many services, jobs and educational opportunities now depend on digital access. Bridging it is a significant ethical and social challenge of computing.',
    source,
    generated: true,
  },
])
